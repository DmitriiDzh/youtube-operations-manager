# BACKLOG.md — Tracked Backlog Items

Discrete, in-flight tasks derived from `docs/roadmap/FUTURE_PHASES.md`. This is neither the
strategic backlog (that's `FUTURE_PHASES.md`) nor the execution log (that's
`docs/ROADMAP_STATUS.md`) — it is the tracked middle state between "this capability is on the
roadmap" and "this is done and recorded in ROADMAP_STATUS.md". A row here reaching `done` should
also produce (or point at) a `ROADMAP_STATUS.md` entry; `ROADMAP_STATUS.md` remains the sole
record of what actually happened.

**No status here is an authorization.** Per `AGENTS.md` §C, a phase begins only with the project
owner's own explicit assignment — a row marked `assigned` records that an assignment already
happened (and must say where/when), it never substitutes for one.

See `.claude/skills/roadmap-backlog/SKILL.md` for the full format, status definitions, and
workflow this file follows.

## Items

| ID | Title | Source | Status | Opened | Notes |
|---|---|---|---|---|---|
| BL-001 | Phase 7: draft the API/MCP error-shape and versioning contract for Codex-facing endpoints | FUTURE_PHASES.md §3 (Phase 7) | done | 2026-09-20 | Assigned 2026-09-19 (Telegram msg 60). Delivered as `docs/roadmap/plans/PHASE_7_PLAN.md` — a plan, not implementation; no code written. See `docs/ROADMAP_STATUS.md`'s Phase 7 planning row. |
| BL-002 | Phase 7: design the isolated Codex operations workspace boundary (permissions model, read-only data access, zero dev-repo access) | FUTURE_PHASES.md §3 (Phase 7) | done | 2026-09-20 | Assigned 2026-09-19 (Telegram msg 60). Same delivery as BL-001 — `docs/roadmap/plans/PHASE_7_PLAN.md` §4/§5. Actual workspace implementation remains unassigned. |
| BL-003 | Phase 8: research YouTube Analytics API scope and design a minimal scheduled-sync + metric-definition model | FUTURE_PHASES.md §4 (Phase 8) | done | 2026-09-20 | Assigned 2026-09-19 (Telegram msg 60). Delivered as `docs/roadmap/plans/PHASE_8_PLAN.md` — plan only; no Analytics API call, no OAuth, no schema change made. |
| BL-004 | Phase 9: design the research-watchlist data model (source attribution, freshness/confidence fields, no fixed competitor-count limit) | FUTURE_PHASES.md §5 (Phase 9) | done | 2026-09-20 | Assigned 2026-09-19 (Telegram msg 60). Delivered as `docs/roadmap/plans/PHASE_9_PLAN.md` — plan only; no table, code, or public API call made. Discovery logic itself remains a later, separate slice requiring its own assignment. |
| BL-005 | Phase 10: design the hypothesis/experiment/decision data model, kept explicitly distinct from the existing Change Set entities | FUTURE_PHASES.md §6 (Phase 10) | done | 2026-09-20 | Assigned 2026-09-19 (Telegram msg 60). Delivered as `docs/roadmap/plans/PHASE_10_PLAN.md` — plan only; no table or code written. Explicitly notes this phase depends on Phase 8/9 actually existing to have real evidence to reason about. |
| BL-006 | Investigate application-managed concurrent multi-device sync as a Syncthing-handoff replacement/extension | FUTURE_PHASES.md §7 (future directions) | done | 2026-09-20 | Assigned 2026-09-19 (Telegram msg 60). Delivered as `docs/roadmap/plans/FUTURE_DIRECTIONS_RESEARCH.md` — concludes a foundational architecture decision (conflict resolution vs. networked DB) is needed before any vertical slice can be planned; not advanced to a plan document. |
| BL-007 | Feasibility research for automated media production (audio/video generation, rendering, publishing, livestream management) | FUTURE_PHASES.md §7 (future directions) | done | 2026-09-20 | Assigned 2026-09-19 (Telegram msg 60). Delivered as `docs/roadmap/plans/FUTURE_DIRECTIONS_RESEARCH.md` — concludes this is 4 unrelated sub-capabilities needing an owner choice of which one (if any) to pursue before a plan is possible; not advanced to a plan document. |
| BL-008 | Implement `channel_sync`/`channel_list`/`channel_video_list` MCP tools, closing the remainder of RISK-04's MCP portion | `docs/TECHNICAL_DEBT.md` RISK-04 (not part of `PHASE_7_PLAN.md`'s own slice list — a gap found while updating that risk entry after Phase 7 slice 1) | done | 2026-09-20 | Self-initiated during Phase 7 slice 1 implementation, per the project owner's Telegram msg 67 ("При необходимости по результатам анализа создавай новые задачи в бэклог и реализуй их"). Delivered: 3 MCP tools + 5 tests, `channel_sync` gated (local-mutating), `channel_list`/`channel_video_list` ungated (read-only). `docs/TECHNICAL_DEBT.md` RISK-04 updated. |
| BL-009 | Rework the CLI's `ParsedArgs["command"]` flat string union and `READ_ONLY_CLI_COMMANDS`/`AUTH_SESSION_EXEMPT_CLI_COMMANDS` gate Sets to key on the `(namespace, command)` pair instead of a bare command string | `src/cli/video-metadata.ts` — found by independent review (`/code-review high`, review cycle 1, 2026-09-20) during the CLI-parity task | proposed | 2026-09-20 | Currently fail-safe by construction (an un-listed command defaults to gated, never the reverse), so no live bug — but a future namespace reusing "list"/"get" for a genuinely mutating command would silently skip the device-availability gate purely because that string happens to already be in the read-only Set from an unrelated namespace. MCP's own equivalent gate is immune to this (keyed per fully-qualified tool handler). Deliberately not fixed inline during review cycle 1 — a real refactor of the namespace/command type design, not a one-line fix. |
| BL-010 | Extract a shared "get batch with its ledger rows" helper (`requireBatchForChannel` + `listLedgerRows` + `{batch, ledgerRows}`) instead of the same 3-line composition duplicated in the Web API route, the MCP `batch_get` tool, and the CLI `batch get` command | `src/lib/batches/services.ts` (natural home) — found by independent review (`/code-review high`, review cycle 1, 2026-09-20) during the CLI-parity task | proposed | 2026-09-20 | Not a live bug (all three call sites are currently identical and correct), but `AGENTS.md` §D's "no parallel implementation" spirit is stretched by three independent copies of the same composition — a future change to this read (extra field, ledger pagination) must be applied three times by hand. Deliberately not fixed inline during review cycle 1 (small refactor, better scoped as its own task with its own test). |
| BL-011 | Studio-parity: research real YouTube Studio's Home/Content/Analytics/Languages tabs and design an implementation plan | `FUTURE_PHASES.md` §7 (future directions — "YouTube Studio UI parity") | done | 2026-09-20 | Assigned 2026-09-20 (Telegram msg 122: "разберать какая информация представлена... и сделать подробный план с тасками"). Delivered as `docs/roadmap/plans/STUDIO_PARITY_PLAN.md` — live research against the real "Tropico Jazz" Studio session (via claude-in-chrome) plus a per-tab dependency table and proposed slices (BL-012..BL-015 below). Plan only; no UI/schema/API code written. |
| BL-012 | Studio-parity S1: sync video view/comment/like counts (extend `channel-sync`'s `videos.list` call with the `statistics` part, additive columns on `videos`) | `docs/roadmap/plans/STUDIO_PARITY_PLAN.md` §4 (Slice S1) | done | 2026-09-20 | Bookkeeping correction 2026-09-23: found already implemented (`videos.viewCount`/`commentCount`/`likeCount` columns exist and are populated) while researching BL-072 — this row was never updated when it shipped. Implementing commit not identified here; see `docs/ROADMAP_STATUS.md`'s "Languages tab merge" row, which already flagged this exact bookkeeping gap. |
| BL-013 | Studio-parity S2: Content tab — Studio-style videos table (thumbnail+title+description, Access/visibility, Date, Views, Comments columns, filter bar, pagination) | `docs/roadmap/plans/STUDIO_PARITY_PLAN.md` §4 (Slice S2) | done | 2026-09-20 | Bookkeeping correction 2026-09-23 (same as BL-012): `content-manager.tsx` already has this table shape with real Views/Comments columns. |
| BL-014 | Studio-parity S3: Languages tab — restyle existing Localizations tab to Studio's table/menu shape, fold "AI Localization" into it (no longer a separate top-level tab) | `docs/roadmap/plans/STUDIO_PARITY_PLAN.md` §4 (Slice S3) | done | 2026-09-20 | Assigned 2026-09-20 (Telegram msg 137, "Да, реализуй то что мы обсуждали", after all of `LANGUAGES_TAB_MERGE_PLAN.md`'s open questions were resolved in msgs 125/128/131/135). Delivered via BL-018/BL-019/BL-020 below, merged into `dev` — see `docs/ROADMAP_STATUS.md`'s "Languages tab merge" row. |
| BL-016 | Studio-parity S3 sub-plan: design merging "AI Localization" into a Studio-styled "Languages" tab | `docs/roadmap/plans/STUDIO_PARITY_PLAN.md` §4 (Slice S3) / owner's explicit follow-up request | done | 2026-09-20 | Assigned 2026-09-20 (Telegram msg 125: "Это переделка требует отдельного планирования и проработки"). Delivered as `docs/roadmap/plans/LANGUAGES_TAB_MERGE_PLAN.md` — confirms Change Set review is already fully shared/unified across both sources (no duplicate logic to reconcile), finds an existing display bug (Change Set source never shown, AI-generated sets mislabeled "XLSX import"), proposes a 5-slice breakdown (L1..L5) and flags 4 open questions (sub-tab semantics vs. our no-live-write reality, bulk-vs-per-video AI generation UI placement, editorial-profile placement, nav-item removal). Plan only; no code written. |
| BL-017 | Studio-parity S6-stub: placeholder Analytics nav tab (correct sidebar position/icon, "coming soon" message, no real data) | `docs/roadmap/plans/STUDIO_PARITY_PLAN.md` §4 (Slice S6-stub) | proposed | 2026-09-20 | Resolved 2026-09-20 (Telegram msg 125): build only a stub for now, revisit at Phase 8's own stage (`docs/roadmap/plans/PHASE_8_PLAN.md`, BL-003) for real data. No open questions, no new OAuth scope — immediately assignable independent of everything else in this plan. Not yet assigned. |
| BL-018 | Languages merge L1: remove redundant per-tab channel selectors (Localizations/AI Localization) and fix the Change Set source mislabeling bug | `docs/roadmap/plans/LANGUAGES_TAB_MERGE_PLAN.md` §5 (Slice L1) | done | 2026-09-20 | Assigned 2026-09-20 (Telegram msg 137). Channel-selector removal shipped earlier (commit `9aa5f58`, dropdown-removal batch); the source-mislabeling bug (`ChangeSetReview` always showed "XLSX import" regardless of `ChangeSet.source`) fixed in this pass — `src/components/change-set-review.tsx` now reads `changeSet.source`. Merged into `dev` — see `docs/ROADMAP_STATUS.md`. |
| BL-019 | Languages merge L4: AI generation as the primary per-video action (manual edit = review/correct the agent's output), "Import from XLSX" demoted to a secondary bulk action, "AI Localization" nav item removed outright | `docs/roadmap/plans/LANGUAGES_TAB_MERGE_PLAN.md` §5 (Slice L4) | done | 2026-09-20 | Assigned 2026-09-20 (Telegram msg 137, framing per msg 128). Delivered in `src/components/languages-manager.tsx` — "Generate with AI" is the primary, top-of-tab section (absorbed from the deleted `ai-localization-panel.tsx`) with a per-row "Generate with AI for this video" shortcut; XLSX import/export demoted to a secondary, collapsed-by-default section; "AI Localization" nav tab and icon removed from `dashboard/page.tsx`/`icons.tsx`; "Localizations" renamed "Languages". Merged into `dev` — see `docs/ROADMAP_STATUS.md`. |
| BL-020 | Languages merge L2/L3: Studio-shaped video table, sub-tab filtering | `docs/roadmap/plans/LANGUAGES_TAB_MERGE_PLAN.md` §5 (Slices L2/L3) | done | 2026-09-20 | Assigned 2026-09-20 (Telegram msg 137, semantics per msg 131). Delivered: Studio-shaped table (Video/Languages count/Last modified, new `LocalizationOverviewRow.lastSyncedAt` field) in `src/components/languages-manager.tsx`; sub-tabs Все/В процессе/Одобрено filter the Change Set list by `ChangeSet.status` (a deliberate mapping onto the approval unit, not a nonexistent per-video draft state — documented in `src/components/languages-manager.tsx`'s header comment and `docs/ARCHITECTURE.md` §9). (The plan's former L5, editorial-profile relocation, is retired — moved to `BL-030` on the Home tab instead, per Telegram msg 135.) Merged into `dev` — see `docs/ROADMAP_STATUS.md`. |
| BL-021 | Tab-refresh T1: remove the Sync tab's own channel dropdown (single active channel implicit everywhere) | `docs/roadmap/plans/TAB_REFRESH_AND_CHANNEL_UI_PLAN.md` §5 (Slice T1) | proposed | 2026-09-20 | No open questions. Complements BL-018 (same removal for Localizations/AI Localization). Not yet assigned. |
| BL-022 | Tab-refresh T2: Rules tab refetches on its own tab activation instead of only once at dashboard mount | `docs/roadmap/plans/TAB_REFRESH_AND_CHANNEL_UI_PLAN.md` §5 (Slice T2) | dropped | 2026-09-20 | Dropped 2026-09-20 — the Rules tab itself (and Manual) was removed entirely by explicit project-owner decision ("давай удалим их, т.к. пока не вижу им применения"), see `docs/ROADMAP_STATUS.md`. There is no longer a Rules tab to refresh. |
| BL-023 | Tab-refresh T3: Sync tab live-resync trigger policy — staleness-gated auto-resync (~15-30 min) + manual "Sync now" | `docs/roadmap/plans/TAB_REFRESH_AND_CHANNEL_UI_PLAN.md` §4/§5 (Slice T3) | proposed | 2026-09-20 | Resolved 2026-09-20 (Telegram msg 131, "принимается"): staleness-gated option (b) confirmed. No open questions. Not yet assigned. |
| BL-024 | Device-handoff D1: graceful-shutdown export hook (no such mechanism exists today) | `docs/roadmap/plans/DEVICE_HANDOFF_AUTO_SYNC_PLAN.md` §4 (Slice D1) | dropped | 2026-09-20 | Superseded 2026-09-22 by BL-068 (whole-DB Device-Handoff mechanism deleted entirely once the sync gateway covers everything it used to carry) — a shutdown export hook for a mechanism that no longer exists has nothing left to protect. |
| BL-025 | Device-handoff D2+D3: detect a newer snapshot in the shared folder while running, surface via a new notification-bell UI | `docs/roadmap/plans/DEVICE_HANDOFF_AUTO_SYNC_PLAN.md` §4 (Slices D2/D3) | dropped | 2026-09-20 | Superseded 2026-09-22 by BL-068 — there is no whole-DB "snapshot" left to detect once the sync gateway (BL-065/066/067) propagates changes continuously instead. |
| BL-026 | Device-handoff D4: safe-case auto-import at startup (never overriding the existing divergent-lineage-requires-a-human check) | `docs/roadmap/plans/DEVICE_HANDOFF_AUTO_SYNC_PLAN.md` §4 (Slice D4) | dropped | 2026-09-20 | Superseded 2026-09-22 by BL-068, same reasoning as BL-025 — the sync gateway's continuous propagation makes a startup "import a snapshot" step meaningless once there is no snapshot mechanism left. |
| BL-027 | Device-handoff D5: periodic/debounced auto-export while running (in addition to shutdown export) | `docs/roadmap/plans/DEVICE_HANDOFF_AUTO_SYNC_PLAN.md` §4 (Slice D5) | dropped | 2026-09-20 | Superseded 2026-09-22 by BL-068. Originally expected to become fully moot; BL-067's now-final scope (all four Category D tables stay in SQL, `docs/decisions/0009-defer-write-pipeline-sync-gateway-migration.md`) reopened whether it's fully moot (BL-068 option a) or partially re-applies to a smaller 4-table allowlist (option b) -- still dropped either way, since even option (b) is a materially different, much smaller mechanism than what this row originally proposed. |
| BL-028 | Design plan: auto-refresh tab data on switch + remove redundant channel dropdowns | `FUTURE_PHASES.md` §7 (future directions) | done | 2026-09-20 | Assigned 2026-09-20 (Telegram msg 128). Delivered as `docs/roadmap/plans/TAB_REFRESH_AND_CHANNEL_UI_PLAN.md` (BL-021..BL-023). Plan only; no code written. |
| BL-029 | Design plan: automatic device-handoff export/import + update-available notification | `FUTURE_PHASES.md` §7 (future directions) | done | 2026-09-20 | Assigned 2026-09-20 (Telegram msg 128). Delivered as `docs/roadmap/plans/DEVICE_HANDOFF_AUTO_SYNC_PLAN.md` (BL-024..BL-027) — read the actual export/import code first to ground the design in real cost/safety constraints (exclusive lock + full-DB `VACUUM` on export; divergent-lineage-requires-a-human on import; no graceful-shutdown hook exists today). Explicitly scoped to never imply this solves concurrent multi-device editing safety (`BL-006` remains separate). Plan only; no code written. |
| BL-030 | Studio-parity S4 (Home tab): relocate the Editorial Profile editor into a collapsible Home-tab panel (open/edit/save/close) | `docs/roadmap/plans/STUDIO_PARITY_PLAN.md` §4 (Slice S4, editorial-profile addition) | done | 2026-09-20 | Bookkeeping correction 2026-09-23 (same class as BL-012): `EditorialProfilePanel` already lives on the Home tab. |
| BL-015 | Studio-parity S4: Home tab, Data-API-v3 portion (recent videos w/ stats card) | `docs/roadmap/plans/STUDIO_PARITY_PLAN.md` §4 (Slice S4) | done | 2026-09-23 | Delivered as part of BL-072 (`home-dashboard-panel.tsx`: latest-video performance, published-videos list, channel-analytics summary) — see that row. The comments (`commentThreads.list`, never integrated) and recent-subscribers feed halves remain explicitly split out, still needing their own feasibility research (OAuth scope, whether a subscriber list is obtainable via public API at all) — not assigned. |
| BL-031 | Content tab: per-video "Details" open/edit/save screen, wired to the already-built `src/lib/video-details/` module | `docs/roadmap/plans/STUDIO_PARITY_PLAN.md` §2 (Content tab parity) / owner's real-write-module request, Telegram 2026-09-20 | done | 2026-09-20 | Assigned 2026-09-20 (Telegram: "возвращаемся к тому что через контент можно открывать как в студио отдельные видео и менять их по необходимости"). Delivered: click-to-open row in Content (`src/components/content-manager.tsx`) expands into `src/components/video-details-panel.tsx` -- basic fields always visible (title/description/privacy/category/tags), rest behind "Show more"; "Preview changes" (dry-run diff) then a separately-gated "Save to YouTube" that only enables once a preview has run for the EXACT current patch (re-editing after preview re-disables it). See `docs/ROADMAP_STATUS.md` for the full record; `docs/TECHNICAL_DEBT.md` RISK-40 updated (Gap 1 closed). |
| BL-032 | Languages tab UX redesign: analysis + proposal document | Owner's explicit request, Telegram 2026-09-20 ("Сейчас там 2 таблицы, нужно преобразовать в одну... сделай анализ и подготовь предложение по редизайну") | done | 2026-09-20 | Delivered as `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` -- identifies the two overlapping video lists (the "Generate with AI" checklist and the main table), three independent/unlinked selection sets, and the "Generate with AI for this video" button's own context-losing scroll-jump; proposes consolidating to one table with a contextual bulk-action bar and per-video AI generate/review moved inline into the expanded row. Plan only; no code written. Four open questions left for the owner (language-badge display, a new "pending change set" column, popover vs. panel for bulk generation, and slice sequencing). |
| BL-033 | Languages tab UX redesign: implementation (slices per `LANGUAGES_UX_REDESIGN_PLAN.md` §7.6, superseding the original §6.4 list) | `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` | proposed | 2026-09-20 | Not yet assigned. §7.6's slice table (E1-E6) is the current one to work from, not §6.4 -- E1-E4 (sortable columns, per-language ✓/— grid, bulk "add missing translation," contextual bulk bar + inline generate) have no open product questions and are immediately assignable together; E5 (add/remove tracked language columns) and E6 (AI-recommended languages) are each blocked on one named owner decision (§7.7) before being assigned. |
| BL-034 | Languages tab: six concrete follow-up requirements — analysis + risk write-up | Owner's explicit request, Telegram 2026-09-20 ("Проведи анализ этих предложений и предложи план по имплементации, оцени риски и запиши их") | done | 2026-09-20 | Delivered as `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` §7 (addendum) -- analyzes per-language ✓/— columns (supersedes the original plan's own "compact count" recommendation), add/remove tracked-language columns (new, additive `channels.target_languages_json` + endpoints; flags and asks the owner to confirm "remove = display-only, never deletes translation data," per AGENTS.md §F), client-side sortable columns (no backend change, publish-date default already correct), AI-recommended languages (flags that a real audience-based recommendation needs the not-yet-integrated YouTube Analytics API / Phase 8, and that a content-based heuristic must be clearly labeled as such to avoid being mistaken for real audience data), and bulk "add missing translation" per language (lowest-risk, reuses the existing bulk-generate flow entirely). Plan only; no code written. Three open questions left for the owner (§7.7). Superseded in part by BL-035 below, same day -- the owner's actual answer to the deletion question was "yes, real deletion, with confirmation + a 30-day restore window," not the display-only reading this row's own analysis had proposed. |
| BL-035 | Languages tab: owner's answers to BL-034's open questions — revised §7.2 (real deletion) and §7.4 (placeholder), risk re-analysis | Owner's Telegram answers, 2026-09-20 | done | 2026-09-20 | Delivered as a revision to `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` §7.2/§7.4/§7.6/§7.7. §7.4 (AI-recommended languages) resolved simply: empty placeholder now, real functionality after/as part of Phase 8 -- moved into the low-risk E1-E4b lane, no longer blocked. §7.2 grew substantially: the owner confirmed real deletion (including from YouTube itself) is wanted, gated behind multi-step confirmation and a 30-day (default) UI-visible restore window -- this **reverses `docs/PROJECT_SPEC.md` §8's documented "prefer deferring deletion" stance** (flagged explicitly, not silently implemented) and requires a **second live-write path outside `src/lib/batches/`**, since Batches' `WriteExecutor` is unconditionally barrier-disabled pending Gate B and would make a delete button non-functional if routed through it -- the only way to ship a working delete is a standalone path architecturally sibling to `src/lib/video-details/`, reusing `write-context`/`src/lib/backup/`'s pattern but for `localizations`. Also established that "restore" is itself a full write (not a free local undo) and that the 30-day window governs only what the UI offers as restorable, never an actual purge of the (already-immutable, never-deleted) backup files. E5 (now covering all of this) is marked high-risk and NOT assignable until three concrete questions are answered (§7.7: spec-reversal handling, explicit acceptance of the second write path, restore-mechanics confirmation). Plan only; no code written. |

| BL-036 | Localization deletion: backend proposal path only, routed through the existing (Gate-B-blocked) Batches pipeline | `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` §7.2/E5; owner instruction, Telegram 2026-09-21 ("откладываем до реальных боевых тестов Gate B, пока можешь просто сделать просто функционал который упирает в gate B") | done | 2026-09-21 | Delivered on `feature/localization-deletion-backend`, merged to `dev`. `changesets.proposeLocalizationDeletion` + `ChangeType: "delete"` + `ChangeSetSource: "deletion"` + `buildSafeLocalizationsPayload` delete-wins/defaultLanguage-guard merge logic; 10 new tests. Explicitly backend-only: no UI, no restore mechanism (see `docs/ROADMAP_STATUS.md`'s row for the full writeup and the exact deferral reasoning). Does NOT close BL-035's Open Question 2 (the second-live-write-path decision) -- that decision is deferred, not made, per the owner's own instruction. The rest of E5 (UI to trigger a deletion, the 30-day restore mechanism) remains unassigned. |

| BL-037 | Languages tab redesign: E1-E4b (sortable columns, per-language ✓/— grid, bulk "add missing" per language, contextual bulk bar + inline per-row generate, empty AI-recommendation placeholder) | `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` §7.6; owner instruction, Telegram 2026-09-21 ("Да, начинай") | done | 2026-09-21 | Delivered on `feature/languages-ux-e1-e4b`, merged to `dev`. Full rewrite of `src/components/languages-manager.tsx`'s table/generation UI; no backend/API changes. Live browser-verified (sorting, both bulk and per-row generate-to-Change-Set paths, the `+N` bulk-add-per-language shortcut) against the real "Tropico Jazz" channel. See `docs/ROADMAP_STATUS.md` for the full writeup, including the known narrow-viewport popover limitation and the deliberate decision not to add a "remove language" affordance (that stays E5's, still unassigned). **Follow-up, same day:** run under the new `autonomous-dev-loop` skill's independent-review cycle (§4) — 7 rounds against this same file, findings per round 7/5/6/3/5/2/6; round 6 introduced a real regression (destroyed operator edits on a checkbox misclick) that round 7 caught and round 7's own fix reverted the same day (`docs/TECHNICAL_DEBT.md` RISK-45, RESOLVED). Cycle stopped after the revert (not at "0 findings" — rounds 5-7 were re-reviewing the review cycle's own fixes, not new E1-E4b surface). Four rounds independently flagged RISK-42 (no component test coverage anywhere in `src/components/`) against this exact file — see `docs/ROADMAP_STATUS.md` for the full writeup and the decision this surfaces for the owner. |

| BL-038 | Shared video-detail popup module (`video-detail-modal.tsx`), replacing row-expansion in Content and Languages | Owner instruction, Telegram 2026-09-21 ("может использоваться как в контенте, так и в языках и возможно будет нужна где-то ещё — поэтому можно делать модулем... не расширением строки, а... поп ап, почти на весь экран") | done | 2026-09-21 | Delivered on `feature/video-detail-modal`, merged to `dev`. New pure-presentation `src/components/video-detail-modal.tsx` (overlay shell, no domain logic); `content-manager.tsx` and `languages-manager.tsx` migrated their click-to-expand row behavior to open this shared modal instead, each rendering its own tab-specific content inside (`VideoDetailsPanel` for Content; original/locales/inline-generate for Languages) exactly as the owner asked ("наполнение... будет отличаться в зависимости от того в какой категории мы сейчас находимся"). No other tab uses it yet. Live browser-verified both tabs (open, correct content, close via backdrop click and Escape), zero console errors. `npm test` 594 unchanged (pure UI), lint/tsc/build clean. |

| BL-039 | E5a — tracked-language columns (add/remove as a display preference, no real deletion) | Owner instruction, Telegram 2026-09-21 ("Давай пока добавим возможность добавить столбцы для новых языков... И возможность удалять колонки"), scoped by the advisor's explicit "split it, ship Slice A alone first" review before implementation | done | 2026-09-21 | Delivered on `feature/tracked-languages`, merged to `dev`. Additive `channels.target_languages_json` (SCHEMA_MIGRATIONS v6), `addTrackedLanguage`/`removeTrackedLanguage` in `src/lib/localization/services.ts`, 2 new API routes, UI in `languages-manager.tsx`. `languages` is now `trackedLanguages ∪ languages-with-real-data` -- solves the chicken-and-egg gap (couldn't propose a translation into a language with no column). Removing a tracked language never deletes real data -- if real translations exist, the column stays via the union and the operator is told so explicitly. 6 new spec-driven tests; live browser-verified (add, remove-with-no-data, remove-with-real-data-stays-visible). Found and fixed an unrelated test fixture bug along the way: `src/lib/snapshot/services.test.ts`'s RISK-29 column-reordering test hardcoded the old `channels` column list, breaking migration of its own staged test-copy DB (not production code) once the new column existed. Real deletion (E5b) deliberately NOT included -- see BL-035/E5b's own entry when assigned. |
| BL-040 | E5b — real localization-deletion proposal (warning UI + unified bulk backend `proposeLocalizationDeletion({channelId, language, videoIds?})`), routed through the existing Gate-B-blocked Batches pipeline | Owner instruction, Telegram 2026-09-21 ("Продолжай с E5b"), following BL-039/E5a; design reviewed with the advisor first (unify BL-036's single-video form with the new whole-column form; never untrack on real deletion) | done | 2026-09-21 | Delivered on `feature/localization-deletion-e5b`, merged to `dev`. See `docs/ROADMAP_STATUS.md`'s BL-040 row for full detail: unified backend signature, `defaultLanguage` exclusion computed independent of the `existingLocalizations` union (the advisor-flagged "undercount trap"), new `POST .../localizations/languages/[language]/propose-deletion` route, explicit pre-confirm warning in `languages-manager.tsx` before calling it, column deliberately stays tracked/visible on a real-deletion proposal. 5 spec-driven tests (`600` total, up from `599`); live-verified against the real "Tropico Jazz" channel (found and correctly handled the case where every "es"-localized video also has "es" as its own `defaultLanguage`, returning `changeSet: null` rather than creating an empty/wrong Change Set). Restore UI/mechanism for the 30-day window explicitly out of scope until Gate B clears. |
| BL-041 | Languages tab: "Sync now" button (parity with Content) + real-language-list combobox for "Add language column" | Owner instruction, Telegram 2026-09-21 ("1. Нужна кнопка Sync now... 2. Меню добавления колонок должно быть основано на списке языков которые реально можно добавить... En - English... Поиск через написания сразу слова") | done | 2026-09-21 | See `docs/ROADMAP_STATUS.md`'s BL-041 row and `docs/SYSTEM_MAP.md` §2.9's new bullet for detail: `listSupportedLanguages` (`src/lib/youtube.ts`, real `i18nLanguages.list` call), new global `GET /api/youtube/languages` route (cached, not channel-scoped), combobox UI filtering by code/name as you type. Suggestion list, not a hard allowlist. |
| BL-042 | Languages tab: turn the "Add language column" suggestion list into a hard allowlist (researched, hardcoded, both client and server enforce it) + write-time rejection fallback | Owner instruction, Telegram 2026-09-21 ("Проведи исследования в сети... Пользователь не может добавить язык, которого не будет в этом списке... fall off логику... на случай отказа YouTube"), refined after a follow-up round on scope (owner: hardcode the raw i18nLanguages.list, no extra real-data merge) | done | 2026-09-21 | See `docs/ROADMAP_STATUS.md`'s BL-042 row and `docs/SYSTEM_MAP.md` §2.9 for detail: `src/lib/youtube-supported-languages.ts` (83 hardcoded codes, captured live from `i18nLanguages.list`), enforced server-side in `addTrackedLanguage`, client UI blocks Add/Enter for any non-exact-match. Documented known gap (`en-US`, real on this channel, absent from the list) instead of hiding it. Fallback need turned out to be already covered by existing `classifyYoutubeWriteError` + `batch-manager.tsx`'s per-row error table -- no new code required. |
| BL-043 | Replace `window.confirm` in `languages-manager.tsx` with a shared in-app `ConfirmDialog`; establish a standing rule that no app-designed popup/notice ever uses a native browser dialog | Owner instruction, Telegram 2026-09-21 ("это тоже должен быть элемент нашего UI... любые контекстные окна, поп апы, сообщения об ошибках и тд... должны отрисовываться через наш UI в едином стиле") | done | 2026-09-21 | See `docs/ROADMAP_STATUS.md`'s BL-043 row, `docs/SYSTEM_MAP.md` §2.9e, and `docs/DEVELOPMENT_PLAYBOOK.md` §6.9 item 9 (the new standing rule). New `src/components/confirm-dialog.tsx`, adopted by both `languages-manager.tsx`'s two remove-tracked-language confirmations and `video-detail-modal.tsx`'s discard-changes confirmation (one implementation, AGENTS.md §D). |
| BL-044 | Languages tab: two-column redesign of the per-video review popup (original vs. one selected translation, wider modal, own scroll per text box) | Owner instruction, Telegram 2026-09-21 ("этот поп ап нужно сделать шире... слева оригинальный язык... справа один из переводов... в каждом окне описаний должен быть свой скрол... заголовки над описанием как отдельное окно"); the "bind dropdown to already-added columns?" question resolved by agreed recommendation (bind to `overview.languages`, with an empty-state generate shortcut for missing languages) | done | 2026-09-21 | See `docs/ROADMAP_STATUS.md`'s BL-044 row and `docs/SYSTEM_MAP.md` §2.9e for detail: `VideoDetailModal` gained an optional `widthClassName`, `renderVideoLocalizationDetail` rewritten as a two-column layout with independent scroll per text box and a language dropdown sourced from the table's own columns. |
| BL-045 | Gate B "live writes" toggle (Settings tab, session-default-off) + persisted MCP-restricted-mode toggle + the actual Batches "Execute" action this unlocks | Owner instruction, Telegram 2026-09-21 ("Хорошо, давай добавим такой рубильник... вынесем его в настройки... По дефолту при запуске сессии он выключен... добавь рубильник так же на mcp_restricted_mode"), following a multi-message discussion clarifying what Gate B actually is and confirming scope (toggle + the Apply/Execute action) before implementation | done | 2026-09-21 | See `docs/ROADMAP_STATUS.md`'s BL-045 row, `docs/SYSTEM_MAP.md` §2.9f, and `docs/TECHNICAL_DEBT.md` RISK-09's new progress note for full detail. Two-layer barrier preserved (not collapsed into one setting check, per the advisor's explicit review); does NOT close Gate B -- no live validation was run against a real channel. |
| BL-046 | Single YouTube write gateway (`src/lib/youtube-write-gateway/`) — the only module allowed to call a mutating YouTube API method, mechanically enforced; `assertLiveWritesAuthorized` generalized to every write path (`apply`, `playlist_*`), not just Batches | Owner instruction, Telegram 2026-09-21 ("Делаем новый модуль, который будет отвечать за отправку какой-либо информации на YouTube... единственным путем... Никакие будущие модули не имеют права записывать / изменять / отправлять данные на YouTube в обход этого модуля"), following the BL-045 discussion that surfaced `apply`/`playlist_*` had no live-write barrier at all | done | 2026-09-21 | See `docs/ROADMAP_STATUS.md`'s BL-046 row and `docs/decisions/0005-youtube-write-gateway.md` for full detail. Closes the gap BL-045 didn't cover: `apply`/`playlist_*` now also refuse with `live_writes_disabled` while Live writes is off. |
| BL-047 | Rename "MCP restricted mode" to "MCP connection"; invert default so MCP/agent starts fully disconnected (zero tools) until explicitly enabled in Settings | Owner instruction, Telegram 2026-09-21 ("По началу MCP / агент от всего отключен и получит доступ только если я зайду в настройки и переключу этот тумблер... Все взаимодействия MCP / агента должны идти через это переключение"), confirmed the toggle persists across restarts (not reset per-session like Live writes) via a clarifying question | done | 2026-09-21 | See `docs/ROADMAP_STATUS.md`'s BL-047 row, `docs/TECHNICAL_DEBT.md` RISK-04's progress note, and `docs/interfaces.md`. |
| BL-048 | Remove `start.sh`/`start.bat`'s own `git pull`; fix the build-staleness bug this uncovered (manual `.next` deletion needed on Windows to pick up a new version) | Owner instruction, Telegram 2026-09-21 ("Ранее мы пытались дать ему разрешение на гит пул - это лишние... давай уберем эту часть функционала" + "На Windows чтобы запустить актуальную версию мне все время приходится удалять .next перед запуском старта") | done | 2026-09-21 | See `docs/ROADMAP_STATUS.md`'s BL-048 row and `docs/FIRST_LOCAL_TEST_BUILD.md` §3/§4. Root cause: the only rebuild trigger was "`.next` missing", true only once; fixed with a git-commit marker file instead. |
| BL-049 | Migration plan for CRDT-backed (Automerge) concurrent multi-device sync of the draft/change-set layer — BL-006's follow-up, now that the owner made the architecture decision | Owner instruction, Telegram 2026-09-21 ("Создай план перехода на automerge"), following a dedicated research pass this session comparing CRDT engines against Postgres-backed sync platforms | done | 2026-09-21 | Plan only, no code written. See `docs/decisions/0006-automerge-for-draft-layer.md` and `docs/roadmap/plans/AUTOMERGE_MIGRATION_PLAN.md` (slices, acceptance criteria, required owner decisions on transport/rollout/priority before any slice may be assigned). |
| BL-050 | Single YouTube read gateway (`src/lib/youtube-read-gateway/`), mirroring BL-046's write gateway — umbrella barrel + category-specific children, mechanically enforced | Owner instruction, Telegram 2026-09-22 ("Все запросы на получения данных должны идти через него и никак иначе... для разных групп API нужны разные модули... общий шлюз, а дальше расходится на дочерние шлюзы") | done | 2026-09-22 | See `docs/ROADMAP_STATUS.md`'s BL-050 row and `docs/decisions/0007-youtube-read-gateway.md` for full detail. Merged to `dev` in `feature/youtube-read-gateway`. |
| BL-055 | Phase 8 slice 2: `video_metrics_daily` table + tests | FUTURE_PHASES.md §4 (Phase 8) / `docs/roadmap/plans/PHASE_8_PLAN.md` §6 slice 2 | done | 2026-09-22 | Assigned 2026-09-22, Telegram msg 349. Branch `feature/phase-8-intelligence-foundation` (created from `dev` tip, not merged — owner instruction #4 in msg 349). See `docs/ARCHITECTURE.md` §14 / `docs/SYSTEM_MAP.md` §2.9i and `docs/ROADMAP_STATUS.md`'s matching row for the 3-round independent-review history (2/2/0 findings). Split into BL-056..BL-059 for the remaining slices per msg 356's answers, rather than carrying the whole phase in one row. **Renumbered from the original BL-050..BL-054 (2026-09-22) to resolve an ID collision found while merging into `dev`, where BL-050 was independently assigned to the read gateway (`docs/decisions/0007-youtube-read-gateway.md`) the same day -- no content changed, only the ID.** |
| BL-056 | Phase 8 slice 1: YouTube Analytics OAuth scope (`yt-analytics.readonly`) + re-consent flow | `docs/roadmap/plans/PHASE_8_PLAN.md` §6 slice 1 / §10 item 1 | done | 2026-09-22 | Owner approved 2026-09-22, Telegram msg 356 ("Да, разрешаю"). Delivered: `YOUTUBE_ANALYTICS_READ_SCOPE` added to `src/lib/auth.ts`'s `YOUTUBE_SCOPES` -- no separate re-consent mechanism needed, every sign-in path already forces full consent (`docs/SYSTEM_MAP.md` §2.1). Real re-authorization performed by the owner 2026-09-22 (see BL-057's row). (Renumbered from BL-051, see BL-055's note.) |
| BL-057 | Phase 8 slice 3: Analytics adapter — every metric `yt-analytics.readonly` covers (not `views` alone) | `docs/roadmap/plans/PHASE_8_PLAN.md` §6 slice 3 / §10 item 2 | done | 2026-09-22 | Owner instruction 2026-09-22, Telegram msg 356; real re-consent + GCP "YouTube Analytics API" enablement performed by the owner 2026-09-22. Live-verified against the real "Rural Japan Music" channel via BL-058's route: 28/28 videos succeeded (zero skipped), 2912 real metric rows written, and all 28 `ANALYTICS_METRIC_NAMES` confirmed present in the real response (checked directly against `video_metrics_daily`) -- the metric list needed no correction. `PHASE_8_PLAN.md` §10 item 5's bulk-query question is now definitively answered: a temporary diagnostic route added on this branch (never committed, removed immediately after use), reusing the app's own real session-based credential resolution (`resolveGoogleCredentials` -- no raw token ever handled directly, after an earlier direct-token probe script was correctly blocked by the environment's own credential-handling safety check and was not worked around), issued a real `dimensions=video,day` query against the live channel with no `filters=video==...`. The API rejected it outright: `"The query is not supported. Check the documentation at https://developers.google.com/youtube/analytics/v2/available_reports for a list of supported queries."` This confirms the one-query-per-video design is not a simplification opportunity left on the table -- it is the only shape the API actually supports for this report; no further design change is needed or possible here. (Renumbered from BL-052, see BL-055's note.) |
| BL-058 | Phase 8 slice 4: manual "collect now" trigger (API route + Web UI display) | `docs/roadmap/plans/PHASE_8_PLAN.md` §6 slice 4 | done | 2026-09-22 | Assigned as part of the original "приступить к полной реализации фазы 8" (msg 349). Delivered: `POST/GET /api/channels/[channelId]/analytics(/collect)`, `src/components/analytics-manager.tsx` (replaces the BL-017 stub). Live-verified against the real "Tropico Jazz" channel -- correctly failed with `AUTH_SCOPE_INSUFFICIENT` pending the owner's re-consent, then re-exercised successfully after it (see BL-057's row). (Renumbered from BL-053, see BL-055's note.) |
| BL-059 | Phase 8: daily staleness-based auto-collection on dashboard load + configurable local sync-time/timezone setting | `docs/roadmap/plans/PHASE_8_PLAN.md` §10 items 3-4 | done | 2026-09-22 | Owner instruction 2026-09-22, Telegram msg 356 (items 3-6) / "Ок" confirming to proceed. Delivered: `src/lib/analytics/staleness.ts` (pure, IANA-timezone-aware wall-clock boundary check, DST-verified by test), `channels.analyticsLastAutoCollectedAt` (v9 migration, mirrors `lastSyncedAt`, deliberately not derived from `MAX(collected_at)`), `runAutoCollectionIfStale` (mark-then-run, concurrency-safe), `app_settings` keys for local time/timezone (validated at the `/api/settings` write boundary), `POST .../analytics/auto-collect` (real mutation, gated by `src/proxy.ts` normally), dashboard-mount trigger in `page.tsx`, `analytics-sync-settings.tsx` (Settings tab). Live-verified against the real "Tropico Jazz" channel -- fires once, reaches `AUTH_SCOPE_INSUFFICIENT` correctly (first run), and the resulting real timestamp write was reset to NULL afterward so the owner's first post-re-consent load wasn't skipped. Depends on BL-057/BL-058 (done). (Renumbered from BL-054, see BL-055's note.) |
| BL-060 | Per-category "reads enabled" toggles for the read gateway (Data API v3 + Analytics), plus fold `youtube-analytics.ts` into `youtube-read-gateway/analytics-api.ts` as its second child | Owner instruction, Telegram 2026-09-22 ("выведи такие же тумблеры в настройки по запросам API (теперь входящим). Делаем отдельный тумблер на каждый модуль / шлюз API чтения") | done | 2026-09-22 | See `docs/ROADMAP_STATUS.md`'s BL-060 row and `docs/decisions/0007-youtube-read-gateway.md`'s "Follow-up completed" note for full detail. Live-verified in the browser: each toggle, switched off, correctly fails a real read with a clear error, and recovers once switched back on. |
| BL-061 | Rolling 24h gateway traffic counters (attempts/succeeded per category) + daily freshness gate for Analytics collection | Owner instruction, Telegram 2026-09-22 ("Сколько было попыток пройти через шлюз за последние сутки... сколько попыток... увенчались успехом"; separately, "Данные... обновляются раз в сутки... шлюзы не должны позволять повторный вызовы") | done | 2026-09-22 | Merged to `dev` in `4be1f29` (owner: "ок, можно мерджить в дев"). See `docs/ROADMAP_STATUS.md`'s BL-061/062/063 row and `docs/SYSTEM_MAP.md` §2.9k. |
| BL-062 | Cloud connection slice 1: device-persistent Google Cloud OAuth grant (connect/disconnect only, no Quotas/Monitoring API call yet) | Owner instruction, Telegram 2026-09-22 ("Делаем полную интеграцию с реальными квотами Google... право получать эту информацию не должно отзываться при смене аккаунта / логина") | done | 2026-09-22 | Merged to `dev` in `4be1f29`, same branch as BL-061. See `docs/decisions/0008-cloud-connection.md` and `docs/SYSTEM_MAP.md` §2.9j. |
| BL-063 | Real Google Cloud quota limit/usage numbers, with a reusable progress-bar UI, under the Data API reads / Live writes / Analytics reads toggles | Owner instruction, Telegram 2026-09-22 ("сколько максимальная квота... сколько из неё уже использовано... Прогресс бар... как отдельный 'модуль / ассет'") | done | 2026-09-22 | Merged to `dev` in `4be1f29`, same branch as BL-061/BL-062. See `docs/ROADMAP_STATUS.md`'s BL-061/062/063 row and `docs/SYSTEM_MAP.md` §2.9l. |
| BL-064 | Drop `rules` from `SNAPSHOT_TRANSFERRED_TABLES` (dead table, feature already removed 2026-09-20) | Owner instruction, Telegram 2026-09-22 ("Правила авто-добавления в плейлисты — можно удалить") | done | 2026-09-22 | `FUTURE_PHASES.md`'s Sync-Gateway bullet; full reasoning in `docs/roadmap/plans/FULL_DEVICE_HANDOFF_MIGRATION_PLAN.md` §5/M0. Delivered on `feature/sync-gateway`, merged to `dev` 2026-09-23 (`5fff0fa`). See `docs/ROADMAP_STATUS.md`'s BL-064/065 row. Actually dropping the empty `CREATE TABLE` itself remains a separate, optional, lower-priority follow-up needing its own small ADR per `docs/decisions/0001`. |
| BL-065 | Stand up `src/lib/sync-gateway/`: move `change-drafts`/`change-drafts-sync` into it unchanged in behavior, extract a `TransportAdapter` interface, re-home Syncthing logic as its first adapter | Owner instruction, Telegram 2026-09-22 ("используем также правило 1 модуля и шлюза. Объединяем весь этот функционал в отдельный модуль... Транспортом пока занимается syncthing") | done | 2026-09-22 | Plan's slice M1, `docs/roadmap/plans/FULL_DEVICE_HANDOFF_MIGRATION_PLAN.md` §4/§5. Delivered on `feature/sync-gateway`, merged to `dev` 2026-09-23 (`5fff0fa`). Pure refactor -- CD1-CD7's existing test suite passed unchanged. See `docs/ROADMAP_STATUS.md`'s BL-064/065 row. |
| BL-066 | Catalog `channel_editorial_profiles` + `ai_connections` (config fields) through the sync gateway; drop `channels`/`videos` from cross-device transfer entirely (no CRDT needed, pure external caches); fold `ai_localization_generation_provenance` into the existing per-channel draft document | Owner instruction, Telegram 2026-09-22 ("Ок" on the category grouping) | done | 2026-09-22 | Plan's Categories A-C / slices M2-M4, `docs/roadmap/plans/FULL_DEVICE_HANDOFF_MIGRATION_PLAN.md` §2/§5. Delivered on `feature/sync-gateway`, merged to `dev` 2026-09-23 (`5fff0fa`). See `docs/ROADMAP_STATUS.md`'s BL-066 M2/M3 and M4 rows for full detail, including two real bugs found and fixed (Automerge merge mutation-order; peer-deletion orphan-row projection). |
| BL-067 | Category D (write pipeline) sync-gateway migration -- dropped in full, all four tables stay relational SQLite (second correction, 2026-09-22) | Owner instruction, Telegram 2026-09-22 ("Если нет конфликтов то не страшно, но думаю нам надо все перевести на 1 систему"), reversed by a safety finding before implementation began | dropped | 2026-09-22 | Plan's Category D / slice M5 (CANCELLED), `docs/roadmap/plans/FULL_DEVICE_HANDOFF_MIGRATION_PLAN.md` §2/§5; see `docs/decisions/0009-defer-write-pipeline-sync-gateway-migration.md` for the full reasoning. First correction (same day) had narrowed this row to "`audit_events` only," reasoning the other three tables rely on SQL compare-and-set/UNIQUE-constraint primitives with no Automerge equivalent (Phase 5 AC-CONCURRENCY-01/02/03/AC-RESUME-01). A second research pass, done before writing any M5 code, found `audit_events` blocked too, for an unrelated reason: its ordering guarantee (AC-AUDIT-01/04) is derived from the SQLite `AUTOINCREMENT` rowid, which has no CRDT equivalent -- a peer's merged-in events would silently corrupt reconstructed order. All four tables (`batches`/`batch_ledger_rows`/`batch_attempts`/`audit_events`) remain unmigrated; ADR 0006's original write-pipeline exclusion is reinstated in full. The audit-ordering problem alone could in principle be redesigned and revisited later (ADR 0009's "Named follow-up") -- that would be a new, separately-scoped/assigned backlog item, not a reopening of this one. |
| BL-068 | Scope down `src/lib/device-handoff/`/`src/lib/snapshot/` to just the 4 Category D tables (owner chose option (b), not full deletion) | Owner instruction, Telegram 2026-09-23, after a web-research pass on industry patterns for this exact problem ("'CRDT для драфтов/настроек + явная передача владения для конвейера записи' ок, тогда так и делай") | done | 2026-09-22 | Plan's slice M6, `docs/roadmap/plans/FULL_DEVICE_HANDOFF_MIGRATION_PLAN.md` §3/§5. Delivered on `feature/sync-gateway`, merged to `dev` 2026-09-23 (`5fff0fa`): `SNAPSHOT_TRANSFERRED_TABLES` narrowed to `schema_meta` + `batches`/`batch_ledger_rows`/`batch_attempts`/`audit_events`; `ai_connections`' upsert-by-id special case removed from `applySnapshotToDatabase`; tests updated. See `docs/ROADMAP_STATUS.md`'s M6 row. |
| BL-069 | Split Settings into 4 sub-tabs (API / AI Agent / Sync / About) | Owner instruction, Telegram 2026-09-23 ("давай в настройках сделаем 4 категории закладок") | done | 2026-09-23 | Delivered on `feature/settings-tabs`, merged to `dev` 2026-09-23 (`318b859`). MCP connection split out of `LiveWritesSettings` into its own `mcp-connection-settings.tsx` (grouped with AI provider connections under "AI Agent" per the owner's explicit choice, despite being unrelated mechanisms); Syncthing folder path moved out of the Merge tab into a new `sync-folder-settings.tsx` under "Sync". Three live-testing follow-up fixes included (pill width, sub-tabs staying mounted on switch, Settings preloading from dashboard load). See `docs/ROADMAP_STATUS.md`. |
| BL-070 | Redesign the Merge tab: persistent sync status, conflicts/divergent-peer parity across all 3 sync-gateway families, Device handoff as its own card | Owner instruction, Telegram 2026-09-23 ("давай распланируем что должно быть показано на закладке merge... проведи исследование и предложи оптимальное решение") | done | 2026-09-23 | Delivered on `feature/merge-tab-redesign`, merged to `dev` 2026-09-23 (`864c0a7`, owner approval given over Telegram). Generalized `adoptDivergentPeer` into `automerge-core/sync-runner.ts` (`AGENTS.md` §M) and added `resolveConflict` to `editorial-profile`/`ai-connections-catalog` services, closing a real gap (conflict UI previously existed only for `change-drafts` despite identical server-side capability existing for all three). New `sync_family_status` table (SCHEMA_MIGRATIONS v12) for a persistent last-sync-outcome per family. Live-verified with real demo conflicts (owner-authorized), resolved through the real UI, then cleaned up through real UI actions. Independent-review cycle: 5 rounds, findings 1/1/1/1/0. See `docs/ROADMAP_STATUS.md` and `docs/SYSTEM_MAP.md` §2.9m. |
| BL-072 | Studio-parity S6b: Analytics "Overview" (channel-level cards/chart/deltas, live Analytics API read, no new schema) + completing BL-015's Home stats cards | Owner instruction, Telegram 2026-09-23 ("Давай поработаем над окнами Home и Analytics. Проверь живой Dashboard YT Studio и сделай весь такой же функционал отображения статистики у нас."), following a live YT Studio research pass this session | done | 2026-09-23 | `docs/roadmap/plans/STUDIO_PARITY_PLAN.md` §4 Slice S6b, now unblocked since S6a (Phase 8) is done — see commits `4a0a47d`/`9a4441a` on `feature/studio-parity-home-analytics-overview` for full detail. Live-confirmed the channel-level Analytics API read works and that thumbnail impressions/CTR are genuinely unavailable via the public API (`docs/ARCHITECTURE.md` §14.8). Comments feed, Recent-subscribers feed, and the Realtime panel deliberately out of scope (unconfirmed public-API feasibility, same as BL-015). Three independent-review rounds (pre-commit + 2 fresh-subagent) found and fixed 5 real bugs total, then found nothing further. `npm test` 929/929, `lint`/`build` clean; live-verified in the browser against two real channels. Owner approved the merge in Telegram ("Ок"), 2026-09-23; merged `--no-ff` into `dev` in `111adf7`. |
| BL-073 | Phase 8 follow-up, slice 1 of 4: `analytics_list`/`analytics_overview` MCP tools + `analytics`/`list`/`overview` CLI namespace (machine-readable analytics for operational agents) | Owner instruction, Telegram 2026-09-23, after being asked which Phase 8 capabilities from `FUTURE_PHASES.md` §4 are still missing ("Ок, доделай эти функции") | done | 2026-09-23 | Both tools reuse `analyticsCore.listMetrics`/`getChannelOverview` unchanged (no parallel implementation). `analytics_list` gained optional `startDate`/`endDate`/`videoId`/`metricNames` filters (additive schema change) so an agent's response isn't always every collected row (3,700+ on a real channel). Both are read-only/ungated, matching `playlist_list`'s own live-YouTube-read classification; `collectMetrics`/auto-collection deliberately excluded (real quota-spending local mutations). CLI parity included from the start (`analytics list`/`analytics overview`), avoiding a new RISK-04-style gap. Fixed a stale claim in `docs/SYSTEM_MAP.md` §2.13 ("no CLI parity for anything on this list" — wrong since 2026-09-20). **Independent review (fresh subagent) found and fixed 1 real bug**: `analytics_list`'s date-range filters had no validation, so an inverted/calendar-invalid range silently returned an empty "success" instead of `validation_failed` (same bug class already fixed once in `getChannelOverview` — extracted into shared `period.ts` helpers). `npm test` 944/944, `lint`/`build` clean. Owner approved the merge in Telegram ("Ок, мердж"), 2026-09-23; merged `--no-ff` into `dev` in `7da72fa`. |
| BL-074 | Phase 8 follow-up, slice 2 of 4: data-quality/missing-data diagnostics (`getDataQualityReport`, `analytics_collection_runs` table, MCP/CLI/API exposure) | Owner instruction, Telegram 2026-09-23 ("Ок, доделай эти функции") | done | 2026-09-23 | New additive `analytics_collection_runs` table (SCHEMA_MIGRATIONS v13) -- append-only log of each `collectMetrics` run, needed because the Analytics API live-verified to silently omit zero-activity days from its response, making an absent `video_metrics_daily` row ambiguous between "never collected" and "zero activity" on its own. `computeDataQualityReport` (`data-quality.ts`, pure) classifies each requested date as covered/uncovered/too-recent and aggregates per-video skips; a "covered" fallback also checks for a real pre-existing metric row (handles data collected before this table existed). Exposed via `GET .../analytics/data-quality`, MCP `analytics_data_quality`, CLI `analytics data-quality`, and a small warning card in `channel-overview-panel.tsx`. Live-verified against the real "Tropico Jazz" channel: correctly found only 6 of 28 requested days actually covered by local data -- a genuine, previously-invisible gap. **Independent review found and fixed 3 real bugs**: a zero-video collection run (e.g. firing before channel sync ever populated `videos`) permanently marked every date in its range "covered" with no evidence anything went wrong -- fixed by requiring `videoCount > 0` for a run to count as coverage; a video skipped once stayed flagged forever even after a later successful re-collection -- fixed so only the chronologically latest overlapping run's own skip list determines current status (self-healing), with historical skip count kept for context; the Web UI's `fetchDataQuality` never reset its own state on a failed request, so a stale warning banner could persist after a channel/period switch. **Round 2 of the same review confirmed all 3 fixes correct** and found only two accepted, documented, non-blocking edge cases (an exact `ranAt` tie between two runs, and a period-switch UI flicker) -- neither a correctness bug. See `docs/ARCHITECTURE.md` §14.9 / `docs/SYSTEM_MAP.md` §2.9o for full detail. `npm test` 972/972, `lint`/`build` clean. |
| BL-071 | Persistent, re-activatable channel connections: a new Settings "Channels" section to connect any number of channels once and switch between them without re-consenting to Google each time | Owner instruction, Telegram 2026-09-23, following a research answer on the current channel-auth model ("используй подход модульности... Сделать эту фичу с хранилищем каналов... как отдельный модуль так же. Составь план и приступай к его реализации в самодостаточном режиме") | done | 2026-09-23 | `docs/decisions/0010-persistent-channel-connections.md`. Implemented and independently reviewed on `feature/persistent-channel-connections`. No new encrypted store needed -- `users`/`channels` already persist every connected identity's tokens indefinitely; the actual gap was a way to reactivate an already-known identity's session without a fresh Google consent screen, closed via a new NextAuth Credentials provider. Also extracts the duplicated AES-256-GCM logic in `ai-connections/crypto.ts`/`cloud-connection/crypto.ts` into a new shared `src/lib/shared-crypto/` module per the owner's explicit modularity instruction this session. Independent-review cycle: 3 rounds, findings 3/5/0 (round 2's medium finding was a documented, deliberate scope decision -- RISK-49 -- not a code defect). Live-verified against the owner's real Tropico Jazz / Rural Japan Music channels: switching without a Google consent screen, graceful handling of a channel whose stored refresh token had genuinely gone stale with Google, and Disconnect (confirmed via direct DB read: tokens cleared, `connectedUserId` unlinked, channel row preserved). Post-review, owner live-testing found a real bug (a freshly-connected channel never appeared in the list -- fixed by having the list-fetch also trigger `channel-sync`'s "mine" resolution, the missing link) and then asked for a same-day follow-up: reworking the topbar's "Switch channel" button into a quick-switch dropdown over this same list, reusing the identical fetch/activate logic via a new shared `use-connected-channels.ts` hook -- no new mechanism, per the owner's own explicit "maximize reuse" instruction. `npm test` 905/905, `tsc`/`lint`/`build` clean. Merged to `dev` in `370a81c` after a final comprehensive owner-requested review (0 findings). See `docs/ROADMAP_STATUS.md`. |
| BL-075 | Replace XLSX as the localization Change Set interface with the same JSON/MCP-based pattern used by the new Analytics diagnostics tools, since translation is meant to be agent-driven rather than human-edited via spreadsheet | `docs/roadmap/FUTURE_PHASES.md` §7 (future directions — replace XLSX localization interface, recorded 2026-09-23) | proposed | 2026-09-23 | Owner instruction, Telegram 2026-09-23: "запиши в бэклог что эксель нам тоже не нужен и надо на такую же систему для локализации завезти. т.к. перевод делать должен агент, а не человек." **Research done 2026-09-23 (full `AGENTS.md` §A read), before any implementation, per this row's own requirement.** Found the framing doesn't match the actual architecture: XLSX was never the agent's authoring interface -- it is a separate, optional path for a human editing a spreadsheet by hand. "Generate with AI" (`src/lib/ai-localization/`) already bypasses XLSX entirely and creates the same Change Set. The real, narrower gap -- no MCP/CLI tool could reach `generateProposals`/`createChangeSetFromGeneration` -- was scoped out and delivered as **BL-078** (owner approved that narrower scope, Telegram "ок"). **This row stays `proposed`, not `done`**: the original, broader idea (deprioritizing/removing XLSX import as an interface, and its own open question about whether XLSX still has value for a human reviewer) was deliberately NOT decided or acted on by BL-078 and remains open for a future, separate assignment. |
| BL-078 | MCP/CLI tools for AI Localization's existing generate + create-Change-Set workflow (`ai_localization_generate`/`ai_localization_create_change_set`), the concrete gap BL-075's own research identified | BL-075's research finding (see that row); owner approval to proceed with this narrower scope, Telegram 2026-09-23 ("ок") | done | 2026-09-23 | Branch `feature/ai-localization-mcp-cli-tools`. Purely additive: both MCP tools and their CLI equivalents (`ai-localization generate`/`ai-localization create-change-set`) call the identical, already-tested `generateProposals`/`createChangeSetFromGeneration` functions the Web UI's own `POST .../ai-localization/{generate,change-sets}` routes already call -- zero new validation, persistence, or approval logic. `generate` persists nothing (ungated, mirrors `localization_import_preview`); `create-change-set` persists a new Change Set, `source: "ai_localization"` (gated like `changeset_create_from_import`), but every resulting Change still starts `pending` -- no code path anywhere can mark an AI-authored proposal already-approved (`AGENTS.md` §G untouched). Neither `generateProposals` nor `createChangeSetFromGeneration` check active-channel scoping internally (the Web UI's own routes do it at the route boundary) -- MCP/CLI replicate that same explicit `assertActiveChannel` check, mirroring `changeset_list`/`changeset_get`'s own established pattern in this codebase. `getEditorialProfile`/`saveEditorialProfile`/`getGenerationProvenance` and any approve/reject/apply path were explicitly left out of scope. XLSX import/export untouched. `docs/TECHNICAL_DEBT.md` RISK-04 updated (a second, previously-undocumented Change-Set-creation gap, now closed the same way as the XLSX path). **Independent review: 1 round, 0 findings** (confirmed via direct diff that `src/lib/ai-localization/**`/the Web UI routes are byte-identical to `dev`; verified the approval-bypass invariant, channel-scoping at all 4 call sites, and gating classification with real operation-lock/CHANNEL_NOT_ACTIVE tests, not just documentation trust) -- one non-blocking nitpick noted: the CLI's `generate` command has no `--editorialBrief` flag (MCP's tool exposes it since it reuses the schema directly; the field is optional, so this is a feature-parity gap, not a bug). `npm test` 1049/1049, `lint`/`build` clean. |
| BL-079 | Phase 7 (expanded), slice A of J: Agent Operations Interface contracts + capability/version discovery (`get_capabilities`) | `docs/roadmap/FUTURE_PHASES.md` §3 (Phase 7, status update 2026-09-23) | done | 2026-09-23 | Branch `feature/phase-7-agent-operations-interface` (one long-lived branch for the whole expanded Phase 7 -- owner instruction: only the FINAL merge to `dev` needs approval, not per-slice). New module `src/lib/agent-operations/` (contracts/schemas/services/index). `getSystemCapabilities()` returns productVersion, an independently-versioned `agentApiVersion` (`0.1.0` at the time this slice shipped -- since bumped, see BL-081's row for the current value and why), the literal list of capabilities actually reachable at the time (just `system.get_capabilities` itself -- since grown considerably, see BL-080/BL-081), the full 4-class permission vocabulary (READ/DRAFT/APPROVE/EXECUTE) vs. what's actually GRANTED (hardcoded `["READ","DRAFT"]`, never APPROVE/EXECUTE), named future extension points (Phase 9/10, not implemented), and the local schema version. One implementation, three transports (`AGENTS.md` §D): MCP `agent_get_capabilities`, CLI `agent capabilities`, `GET /api/agent-operations/capabilities`. New structured error codes (`CAPABILITY_NOT_AVAILABLE` etc.) added to the shared `DomainErrorCode` union and HTTP-status mapper, not a separate enum. Full design in new `docs/AGENT_OPERATIONS_INTERFACE.md`. `npm test` 1062/1062, `lint`/`build` clean. **Independent-review cycle (batched with BL-080/slice B): 4 rounds, findings 1/4/2/0** -- round 1 found stale slice-status wording in `docs/ARCHITECTURE.md` §17; rounds 2-3 found that fix incomplete plus further doc-drift (`docs/SYSTEM_MAP.md`, `docs/AGENT_OPERATIONS_INTERFACE.md` §7) and a missing wrong-channel negative test for `agent_get_video_context`, plus a self-introduced `Co-Authored-By` trailer on 2 local commits violating `AGENTS.md` §J (fixed via owner-approved history rewrite of this unpushed branch); round 4 confirmed all fixes and found nothing further. Merged to `dev` in `cc6a431` ("Интегрировать feature/phase-7-agent-operations-interface в dev"), 2026-09-25 -- see `docs/ROADMAP_STATUS.md` for the phase-level summary row. |
| BL-076 | Phase 8 follow-up, slice 3 of 4: comparing videos at comparable ages (`getComparableAgeComparison`, days-since-publish alignment) | Owner instruction, Telegram 2026-09-23, after being asked which Phase 8 capabilities from `FUTURE_PHASES.md` §4 are still missing ("Ок, доделай эти функции") | done | 2026-09-23 | Branch `feature/comparable-age-comparison`, created from `dev`'s tip after BL-074 merged, merged to `dev` in `f251e66` (owner approval, Telegram, 2026-09-23). Aligns 2-10 videos' already-collected `video_metrics_daily` rows by days-since-publish (Pacific-Time day 0, matching the Analytics API's own day-dimension convention) rather than calendar date, so videos published on different dates can be compared at the same point in their lifecycle. Only additive metrics accepted (explicit allowlist, `src/lib/analytics/comparable-age.ts`); a missing day is "unknown," never a fabricated zero -- cumulative total stops at the first gap, never zero-filled or skipped-over. Deliberately does NOT reuse `analytics_collection_runs` for per-video "known zero" inference (documented, same tradeoff class `data-quality.ts` already accepted for its own narrower scope -- see `docs/ARCHITECTURE.md` §14.10). Real-data caveat confirmed by directly querying two real channels before designing: videos older than ~1-2 weeks before regular collection started typically have no data at low day-offsets, since auto-collection only covers a rolling ~7-day window, not "the video's first 7 days" -- no backfill mechanism added (out of scope, would require touching the owner's own once-a-day freshness gate). No ranking/"outperforming" language, per `FUTURE_PHASES.md` §4's facts-only constraint. Exposed via `GET .../analytics/comparable-age`, MCP `analytics_comparable_age`, CLI `analytics comparable-age`. **Independent-review cycle: 2 rounds, findings 5/0** -- round 1 found 3 documentation-accuracy issues (a false "in dev" claim, an overstated data-coverage caveat conflating `points` with `cumulativePoints`, `analytics_overview` wrongly grouped with the local/ungated reads) plus a low-severity latent error-mapping gap (an unparseable `publishedAt` could surface as a misleading 401 instead of 400) and some missing boundary tests; all fixed and independently re-verified by round 2, which found nothing further. `npm test` 1004/1004, `lint`/`build` clean. |
| BL-077 | Phase 8 follow-up, slice 4 of 4 (last slice): weekly analytical reports (`runWeeklyReportIfDue`/`listWeeklyReports`/`getWeeklyReport`) | Owner instruction, Telegram 2026-09-23, after being asked which Phase 8 capabilities from `FUTURE_PHASES.md` §4 are still missing ("Ок, доделай эти функции"); trigger design per follow-up owner answers ("Давай завяжемся на то же время что мы выбираем в настройках -- 12-05 сейчас по понедельникам") | done | 2026-09-23 | Branch `feature/analytics-weekly-reports`, created from `dev`'s tip after BL-076 merged, merged to `dev` in `8ca0ab2` (owner approval, Telegram, 2026-09-23). Frozen, reproducible weekly (Mon-Sun) snapshot per channel, computed entirely from already-collected local `video_metrics_daily` -- never a live YouTube call. Full design (Pacific-Time-vs-local-trigger-time skew, final/provisional status, percentChange gated on both weeks being fully covered, `syncedVideoTotals` naming/caveats, provenance) in `docs/ARCHITECTURE.md` §14.11. New `analytics_weekly_reports` table (SCHEMA_MIGRATIONS v14). Exposed via `GET .../analytics/weekly-reports(/[weekStartDate])`, `POST .../weekly-reports/generate-if-due` (dashboard-mount trigger, chained after auto-collect), MCP `analytics_weekly_reports_list`/`analytics_weekly_report_get`, CLI `analytics weekly-reports`/`weekly-report-get` -- no generate-on-demand MCP/CLI tool, same exclusion as `collectMetrics`. Known tracked duplication: `docs/TECHNICAL_DEBT.md` RISK-50 (top-content ranking exists twice, client-side Overview tab + this slice's server-side version). **Independent-review cycle: 2 rounds, findings 2/2 (both round-2 findings low/non-blocking)** -- round 1 found and fixed a real HIGH-severity race (a "final" report could be silently regressed to "provisional" by two near-simultaneous dashboard-mount triggers, e.g. two open tabs; fixed via a DB-layer conditional `ON CONFLICT ... WHERE status != 'final'`, not just the service's own read-then-write check) plus a minor code-duplication nitpick (`formatZonedDateAndTime`, deduplicated into `staleness.ts`). Round 2 confirmed both fixes and found two new LOW items, left as-is: the generate-if-due response can (rarely, under the exact race the fix targets) describe content that doesn't match what was actually persisted -- accepted since the only caller ignores the response body and no MCP/CLI surface exposes this path; and this BACKLOG row's own test count was stale before this update. `npm test` 1038/1038, `lint`/`build` clean. |
| BL-080 | Phase 7 (expanded), slice B of J: read-only channel/video context (`get_channel_context`/`get_video_context`) | `docs/roadmap/FUTURE_PHASES.md` §3 (Phase 7, status update 2026-09-23); same standing authorization as BL-079 (owner spec, Telegram 2026-09-23 -- implement without per-slice approval) | done | 2026-09-24 | Same branch as BL-079 (`feature/phase-7-agent-operations-interface`). Adds `getChannelContext`/`getVideoContext` to `src/lib/agent-operations/`, both channel-scoped (explicit `assertActiveChannel` at the MCP/CLI layer, mirroring the `ai-localization` convention, not checked inside the service itself). MCP `agent_get_channel_context`/`agent_get_video_context`, CLI `agent channel-context`/`agent video-context` -- no HTTP route yet. Full design: `docs/AGENT_OPERATIONS_INTERFACE.md` §4a. `npm test` 1081/1081, `lint`/`build` clean. **Independent-review cycle (batched with BL-079/slice A): 4 rounds, findings 1/4/2/0, cycle closed 2026-09-24** -- see BL-079's Notes for the finding summary. Merged to `dev` in `cc6a431` ("Интегрировать feature/phase-7-agent-operations-interface в dev"), 2026-09-25 -- see `docs/ROADMAP_STATUS.md` for the phase-level summary row. |
| BL-081 | Phase 7 (expanded), slice C of J: analytics interface (`query_channel_analytics`/`query_video_analytics`) | `docs/roadmap/FUTURE_PHASES.md` §3 (Phase 7, status update 2026-09-23); same standing authorization as BL-079/BL-080 | done | 2026-09-24 | Same branch. Thin wrappers over the existing `analyticsCore.getChannelOverview`/`listMetrics` (no new metric/table/YouTube call), adding metric definitions, period/filters, and data-freshness to each response per owner spec §9. `credentialRef` is required in this module's own schema for these two (unlike slice B), resolved by the MCP/CLI layer before calling in -- mirrors `analytics_list`/`analytics_overview`'s own convention, verified against the REAL `analyticsCore` via a new index.test.ts wiring test. Also fixed two gaps an independent advisor review found in slice B: `get_capabilities` now registers the pre-existing `channel_list`/`channel_video_list`/`ai_localization_*`/remaining `analytics_*` tools it previously omitted, and `AGENT_API_VERSION` (stuck at `0.1.0` despite slice B adding capabilities) is corrected to `0.3.0`. MCP `agent_query_channel_analytics`/`agent_query_video_analytics`, CLI `agent channel-analytics`/`agent video-analytics` -- no HTTP route yet. Full design: `docs/AGENT_OPERATIONS_INTERFACE.md` §4b. `npm test` 1096/1096, `lint`/`build` clean. **Independent-review cycle: 6 rounds, findings 4/2/1/5/2/1** -- rounds 1-4 found real doc-drift (stale version/count claims, a false "never before" claim, missing test symmetry); round 4's rising count was traced to fix commits adding narrative history into comments/docs, which itself kept drifting -- an advisor consultation recommended stripping that history (it already lives in commit messages) and consolidating restated facts to one location each; rounds 5-6 did that cleanup, closed two real owner-spec §9/§23 gaps (added a `filters` field to `ChannelAnalyticsContext`; documented, not fixed, `queryVideoAnalytics`'s inherited lack of pagination), and mopped up two remaining narration comments. Merged to `dev` in `cc6a431` ("Интегрировать feature/phase-7-agent-operations-interface в dev"), 2026-09-25 -- see `docs/ROADMAP_STATUS.md` for the phase-level summary row. |
| BL-082 | Phase 7 (expanded), slice D of J: creative asset catalog (`list_assets`/`get_asset_context`, new `src/lib/asset-catalog/` module + `creative_assets` table, SCHEMA_MIGRATIONS v15) | `docs/roadmap/FUTURE_PHASES.md` §3 (Phase 7, status update 2026-09-23); same standing authorization as BL-079/080/081 | done | 2026-09-24 | Same branch. Genuinely new subsystem (nothing to reuse). Populated only via the operator-facing `asset register` CLI command, never an agent capability (owner spec §25 lists only the two READ capabilities for this domain). Channel-scoping mirrors slice B (no `credentialRef`, MCP/CLI does `assertActiveChannel`). Designed with the `advisor` tool before implementation (scope clarification: `register_external_artifact` belongs to slice G, not this one; avoided shipping a read-only capability with no working writer). `creative_assets` does not travel with a device snapshot/handoff yet (`docs/TECHNICAL_DEBT.md` RISK-52). MCP `agent_list_assets`/`agent_get_asset_context`, CLI `agent list-assets`/`agent get-asset-context`/`asset register` -- no HTTP route yet. Full design: `docs/AGENT_OPERATIONS_INTERFACE.md` §4c. `npm test` 1126/1126, `lint`/`build` clean. **Independent-review cycle: 4 rounds, findings 0/2/3/0, closed 2026-09-24** -- rounds 2-3 found and fixed a `agent-operations`/`asset-catalog` schema-duplication chain (an enum, then the surrounding object schemas -- fixed by importing/re-exporting directly instead of a second copy) plus a reintroduced review-narration test comment; round 4 confirmed all fixes and found nothing further. An analogous, out-of-scope duplication in slice A's own code was tracked (not fixed) as `docs/TECHNICAL_DEBT.md` RISK-53. Merged to `dev` in `cc6a431` ("Интегрировать feature/phase-7-agent-operations-interface в dev"), 2026-09-25 -- see `docs/ROADMAP_STATUS.md` for the phase-level summary row. |
| BL-083 | Phase 7 (expanded), slice E of J: agent draft/proposal provenance (`get_generation_provenance`) | `docs/roadmap/FUTURE_PHASES.md` §3 (Phase 7, status update 2026-09-23); same standing authorization as BL-079/080/081/082 | done | 2026-09-24 | Same branch. Delegates to the already-existing `ai-localization` provenance mechanism unchanged (no new storage) -- closes a previously-scoped-out gap (RISK-04/BL-078 history: this read had a working HTTP route but no MCP/CLI tool). Additively widened `GenerationProvenance` into a new `StoredGenerationProvenance` type (adds `changeSetId`/`channelId`/`createdAt`, verified via `advisor` consultation to be the real creation moment, not a later device's own projection time) rather than duplicating the type. Channel-scoping mirrors slice B. **Marked PARTIAL, not IMPLEMENTED**: owner spec §22 asks for server-stamped agent/client identity and product/API version on every agent-created object -- not added here (would mean a schema change to the CRDT `DraftProvenance` shape, out of proportion to this slice's own scope), tracked as `docs/TECHNICAL_DEBT.md` RISK-54. MCP `agent_get_generation_provenance`, CLI `agent get-generation-provenance` -- no new HTTP route (reuses the pre-existing one). Full design: `docs/AGENT_OPERATIONS_INTERFACE.md` §4d. `npm test` 1137/1137, `lint`/`build` clean. **Independent-review cycle: 2 rounds, findings 0/0, closed 2026-09-24** -- the cleanest cycle in this phase so far; round 2 deliberately re-checked for the schema-duplication defect class slices C/D each hit in their own round 2, found none. Merged to `dev` in `cc6a431` ("Интегрировать feature/phase-7-agent-operations-interface в dev"), 2026-09-25 -- see `docs/ROADMAP_STATUS.md` for the phase-level summary row. |
| BL-084 | Phase 7 (expanded), slice F of J: bulk localization integration -- evidence, rationale, and identity stamping on the existing `ai_localization_*` Change Set creation path | `docs/roadmap/FUTURE_PHASES.md` §3 (Phase 7, status update 2026-09-23); same standing authorization as BL-079-083 | done | 2026-09-24 | Same branch. Widens `createChangeSetFromGeneration` (MCP/CLI/Web, no new tool) with optional `evidence`/`rationale` and a required, server-stamped `callOrigin` (`createdVia`/`agentApiVersion`), closing RISK-54. New additive columns, SCHEMA_MIGRATIONS v16, plus an `onConflictDoNothing` → `onConflictDoUpdate` projection fix. **PARTIAL**: RISK-55 (granularity/missing fields), RISK-56 (non-atomic write), RISK-57 (owner spec §22's "operation type" still unrecorded -- slice E's own status also downgraded back to PARTIAL for this reason). Full design: `docs/AGENT_OPERATIONS_INTERFACE.md` §4d/§4e. `npm test` 1145/1145, `lint`/`build` clean. **Independent-review cycle: 4 rounds, findings 3/6/2/0, closed 2026-09-24.** Merged to `dev` in `cc6a431` ("Интегрировать feature/phase-7-agent-operations-interface в dev"), 2026-09-25 -- see `docs/ROADMAP_STATUS.md` for the phase-level summary row. |
| BL-085 | Phase 7 (expanded), slice G1 of J: Content Proposal record (create/get/list) | `docs/roadmap/FUTURE_PHASES.md` §3 (Phase 7, status update 2026-09-23); same standing authorization as BL-079-084 | done | 2026-09-24 | Same branch. New `src/lib/content-proposals/` module + `content_proposals` table (SCHEMA_MIGRATIONS v17), write-once (no update/approval, per owner spec's own silence on a review workflow for this domain). Ahead of this slice: extracted `CreatedVia`/`EvidenceReference` into new `src/lib/shared-provenance/` (AGENTS.md §M) and fixed RISK-53 (a real capability-discovery bug this slice's own new data domain exposed). MCP `agent_create_content_proposal`/`agent_get_content_proposal`/`agent_list_content_proposals`, CLI equivalents. `AGENT_API_VERSION` → `0.6.0`. Full design: `docs/AGENT_OPERATIONS_INTERFACE.md` §4f. `npm test` 1188/1188, `lint`/`build` clean. **Independent-review cycle: 4 rounds, findings 3/4/1/0, closed 2026-09-24** -- round 3's finding was its own regression test still not discriminating `createdAt`-based ordering from `rowid`-based ordering (production code was correct throughout; only the test needed a third rewrite, using random UUIDs and a createdAt permutation matching neither insertion order nor its reverse). Owner spec §19's artifact-return path is the separate follow-on, BL-086. Merged to `dev` in `cc6a431` ("Интегрировать feature/phase-7-agent-operations-interface в dev"), 2026-09-25 -- see `docs/ROADMAP_STATUS.md` for the phase-level summary row. |
| BL-086 | Phase 7 (expanded), slice G2 of J: external-artifact registration against a Content Proposal (owner spec §19) | `docs/roadmap/FUTURE_PHASES.md` §3 (Phase 7, status update 2026-09-23); same standing authorization as BL-079-085 | done | 2026-09-24 | Same branch, follow-on to BL-085. New `content_proposal_artifacts` link table (SCHEMA_MIGRATIONS v18); asset row created via `asset-catalog`'s own `registerAsset` (AGENTS.md §D); agent-callable `referenceKind` restricted to `url`/`external_artifact_id`, never `local_path` (owner spec §17). MCP `agent_register_external_artifact`/`agent_list_proposal_artifacts`, CLI equivalents. `AGENT_API_VERSION` → `0.7.0`. Full design: `docs/AGENT_OPERATIONS_INTERFACE.md` §4f. **Independent-review cycle: 6 rounds, findings 4/1/2/4/1/0, closed 2026-09-24** -- rounds 4-5 found and fixed RISK-58 (`docs/TECHNICAL_DEBT.md`): the `url` label wasn't structurally validated against its content, and the first fix checked the parsed form while persisting the raw string. Round 6 adversarially re-attacked the fix and found nothing further. Detail in commits `bef2d30..739fd7f`. `npm test` 1226/1226, `tsc`/`lint`/`build` clean. Merged to `dev` in `cc6a431` ("Интегрировать feature/phase-7-agent-operations-interface в dev"), 2026-09-25 -- see `docs/ROADMAP_STATUS.md` for the phase-level summary row. |
| BL-087 | Phase 7 (expanded), slice I of J: Codex operations-workspace path surfacing (owner spec §3/§30) | `docs/roadmap/FUTURE_PHASES.md` §3 (Phase 7, status update 2026-09-23); scope narrowed by owner, Telegram 2026-09-24, to a path-configuration/surfacing mechanism only | done | 2026-09-24 | Same branch. New `src/lib/operations-instructions/` module; `operationsWorkspacePath` setting (existing `appSettings` table, no migration), settable ONLY via `POST /api/settings`, never an agent-callable MCP tool/CLI command (owner spec §17's `local_path` self-authorization logic). Set-time AND every-read-time validation rejects a path overlapping this app's own app-data directory (RISK-07). MCP `agent_list_operations_files`/`agent_get_operations_file`, CLI equivalents, both READ, not channel-scoped. `AGENT_API_VERSION` → `0.8.0`. Verified live in browser and via CLI. Full design: `docs/AGENT_OPERATIONS_INTERFACE.md` §4j. **Independent-review cycle: 4 rounds, findings 3/1/1/0, closed 2026-09-24** -- rounds 1-3 each found a real (non-escaping, no content ever leaked outside the configured directory) list/get consistency gap around symlinked dotfiles/extensions/nested-dotted-ancestors; after round 3, root-caused all three to one cause (`walk()`/`getOperationsFile` each kept a separately-evolving copy of the same admissibility logic) and refactored to a single shared `classifyEntry` predicate both call, plus an ancestor-chain symlink-cycle guard and a comprehensive invariant test. Round 4 found nothing after an adversarial pass on the refactor itself. Detail in commits `26bbc2d..e5c6b4a`. `npm test` 1276/1276, `tsc`/`lint`/`build` clean. Merged to `dev` in `cc6a431` ("Интегрировать feature/phase-7-agent-operations-interface в dev"), 2026-09-25 -- see `docs/ROADMAP_STATUS.md` for the phase-level summary row. |
| BL-088 | Phase 7 (expanded), slice K of J: comparable-content context (`find_comparable_videos`, owner spec §10) | `docs/roadmap/FUTURE_PHASES.md` §3 (Phase 7); found 2026-09-24 when the full owner spec text was recovered from this session's own pre-compaction transcript to independently verify slice H -- never previously implemented or tracked anywhere in `docs/AGENT_OPERATIONS_INTERFACE.md`/`docs/TECHNICAL_DEBT.md`. **Assigned by owner, Telegram 2026-09-24: "Да, такие находки как BL 88 и 89 тоже включай в список тасков текущей 7 фазы."** | done | 2026-09-24 | Same branch. Two commits: K0 (`videos.durationSeconds` sync, schema v19) and K1 (`src/lib/comparable-content/`, MCP `agent_find_comparable_videos`, CLI `agent find-comparable-videos`, `AGENT_API_VERSION` → `0.9.0`). Explicitly excludes content-family/audience/metadata-pattern matching and embeddings (no data source / owner spec §10). Full design `docs/AGENT_OPERATIONS_INTERFACE.md` §4g. **Independent-review cycle: 4 rounds, findings 3/4/4/0, closed 2026-09-25.** Round 1: age-alignment derived from wall-clock instead of the anchor's own data; `durationProximity` missing an anchor-duration precondition; ASCII-only tokenizer. Round 2: a missing test, a doc-drift gap, 2 wording nits. Round 3: `limit` above max was rejected instead of silently clamped (AC-CMP-07 mismatch); a malformed-`publishedAt` robustness gap; recurring doc-drift; a wording nit -- also judged an "anchor predates collection" scenario as correct-as-is (absolute threshold, never poisoned by a null anchor value). Round 4 found nothing. Detail in commits `79c3697..c66936f`. `npm test` 1326/1326 (311/311 in the affected suites at round 4), `tsc`/`lint`/`build` clean. Status stays `in_progress` (not `done` until the phase's own final merge to `dev`, per this branch's established convention, e.g. BL-087). Merged to `dev` in `cc6a431` ("Интегрировать feature/phase-7-agent-operations-interface в dev"), 2026-09-25 -- see `docs/ROADMAP_STATUS.md` for the phase-level summary row. |
| BL-089 | Phase 7 (expanded), slice L of J: performance ↔ asset linkage (owner spec §16) | `docs/roadmap/FUTURE_PHASES.md` §3 (Phase 7); found the same way and same date as BL-088. Assigned alongside BL-088, same owner instruction. | done | 2026-09-24 | Same branch. New `src/lib/asset-performance/` module joins `creative_assets.linkedVideoId` against each linked video's lifetime counters and an optional caller-day-aligned performance value (never derived from `now`, never rejecting an oversized `limit` -- slice K's own round-1/round-3 lessons applied from the start). MCP `agent_list_asset_performance`, CLI `agent list-asset-performance`, `AGENT_API_VERSION` → `0.10.0`. Full design `docs/AGENT_OPERATIONS_INTERFACE.md` §4h. **Independent-review cycle: 4 rounds, findings 5/1/1/0, closed 2026-09-25** -- rounds 1-3 found only documentation staleness (an acceptance-doc amendment gap, then two recurring copies of a stale "review not started" claim); round 4's fresh functional + repo-wide staleness sweep found nothing. Detail in commits `8894168..ba4ee2c`. `npm test` 1360/1360, `tsc`/`lint`/`build` clean. Status stays `in_progress` (not `done`) until the phase's own final merge to `dev`, per this branch's established convention (e.g. BL-087/BL-088). Merged to `dev` in `cc6a431` ("Интегрировать feature/phase-7-agent-operations-interface в dev"), 2026-09-25 -- see `docs/ROADMAP_STATUS.md` for the phase-level summary row. |
| BL-091 | Multi-agent responsibility zones -- per-capability exclusive assignment across simultaneously-connected agent clients (e.g. Claude + Codex), shared READ context preserved | `docs/roadmap/FUTURE_PHASES.md` §7 (recorded 2026-09-25) | in_progress | 2026-09-25 | **Assigned by owner, Telegram 2026-09-25** (quoted in `docs/roadmap/plans/AGENT_ZONES_PLAN.md`). All 3 slices (data model, enforcement at 6 owner-approved actions, Settings UI) done on `feature/agent-connections`, not yet merged. Fail-closed keyed on ENABLED connections; an unassigned capability is rejected for EVERYONE once 1+ are enabled, with no exception for exactly one connection (owner's exclusivity rule). Owner resolved the `asset register` CLI question (Telegram 2026-09-25) -- not a gap, a human operator is not meant to be constrained by agent zoning. **Independent-review cycle: 13 rounds, findings 3/7/4/2/2/1/1/1/1/3/6/3/1 (each number is that round's own count of the reviewer's enumerated findings). Stopped by explicit owner instruction (Telegram 2026-09-25) after round 13, NOT because a round found zero issues -- round 14 was cancelled mid-run, its output discarded unread.** 0 correctness bugs found in the exclusivity/fail-closed policy logic itself in any of the 13 completed rounds; 3 real code bugs found and fixed in peripheral spots (traffic-counter, API 500-vs-400, UI warning); the rest was documentation/comment precision. **Policy corrected after the cycle stopped, per the owner: unassigned is always blocked once 1+ connections are enabled (no more "exactly one = implicit grant"); commit `0692c54` was independently reviewed once (targeted, not a full cycle) -- found 2 stale doc comments describing the old policy, both fixed in `3bf97e3`; zero issues in the logic/tests/UI itself.** `npm test` 1422/1422, `tsc`/`lint`/`build` clean. Full detail: `docs/roadmap/plans/AGENT_ZONES_PLAN.md`, `docs/SYSTEM_MAP.md` §2.9s. **Remaining**: owner's "yes, merge". |
| BL-090 | Phase 7, slice J: final independent security/integration review of the whole phase, owner spec §28 | `docs/roadmap/FUTURE_PHASES.md` §3 (Phase 7); owner spec §29's own slice order names J as the final slice, always implied by the phase assignment, not a separate owner instruction | done | 2026-09-25 | Same branch. Closed the owner spec §28 acceptance-contract gap retroactively (`docs/AGENT_OPERATIONS_INTERFACE.md` §4i, `docs/acceptance/PHASE_7_ACCEPTANCE.md` §8-§11): quoted §28 verbatim, mapped its 20 scenario categories to existing coverage (backfill for A-I, explicitly marked as weaker-standing than a pre-implementation criterion), and ran fresh cross-cutting checks not covered by any single slice's own review -- channel isolation (no gaps, one deliberate 3-way identity-trust-model asymmetry noted), capability parity re-run with K/L included (24 entries, zero drift), secrets (pass), DB access (pass for K/L, one pre-existing slice-B exception noted, not new), dev-repo dependency (pass), approval separation/write safety (pass, 5 sub-checks). Resolved `RISK-59` (MCP `tools/list` rendering of a ZodEffects schema) empirically by building a real MCP server instance and calling the SDK's own rendering path -- no fix needed. `npm test` 1360/1360, `tsc`/`lint`/`build` clean. Live end-to-end verification against a real synced channel deliberately not attempted (no OAuth login available in this environment; not seeding fabricated data into the local dev DB to simulate one). Remaining: owner's explicit "yes, merge" before `feature/phase-7-agent-operations-interface` → `dev` (`AGENTS.md` §K.2). Merged to `dev` in `cc6a431` ("Интегрировать feature/phase-7-agent-operations-interface в dev"), 2026-09-25 -- see `docs/ROADMAP_STATUS.md` for the phase-level summary row. |

Studio-parity's Analytics-tab portion (plan §4 Slice S6) is the same work already tracked as
BL-003/`docs/roadmap/plans/PHASE_8_PLAN.md` (S6a, done) and BL-072 (S6b's first slice — Overview
only, done). S6c (Content-analytics sub-tab: retention curve, CTR/impressions funnel -- the latter
now confirmed unavailable via the public API per BL-072's notes) and S6d (Audience/Trends parity:
demographics, geography, device, traffic sources) remain undesigned in detail and unassigned, per
the plan's own §4 reasoning -- each needs its own scoping pass once picked up.

Rows were seeded on 2026-09-20 when the `roadmap-backlog` skill was created, as a first pass at
turning `FUTURE_PHASES.md`'s Phase 7-10 and future-directions sections into trackable slices.
All seven were assigned the same day per the project owner's explicit Telegram instruction (msg
60, 2026-09-19: "Приступай к разбиению future phases на таски backlog. После чего приступай к их
выполнению. Автоматический режим работы.") — each item's own Notes column records this. Every
item's *current* scope is a design/research artifact (a planning document under
`docs/roadmap/plans/`), never a live YouTube write, paid AI call, OAuth login, or deployment —
those remain separately gated by `AGENTS.md` §G/§K regardless of this assignment. Should any of
these design efforts surface a need for actual feature implementation, that implementation is its
own new backlog item requiring its own assignment — this one covers the design/research slice
only, per `AGENTS.md` §C's "smallest safe implementation phase" principle.
