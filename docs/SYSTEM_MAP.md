# SYSTEM_MAP.md

Быстрая карта репозитория для коддинг-агентов (Claude Code), заходящих в проект впервые. Описывает систему **как она есть** после завершения Phase 4 (read-only синхронизация каналов/видео + Localization Manager read-only UI/XLSX export + XLSX import, draft state, change sets, diff/approval UI). Не описывает нереализованные фичи как существующие — см. раздел 4 про статус компонентов.

Это карта **девелоперской стороны** проекта (см. раздел B «Development / operations separation» в `AGENTS.md`). Будущий операционный агент (Codex) не читает этот документ и не обращается к этому репозиторию — он работает только с выпущенным релизом через MCP/API.

Каждый подраздел ниже помечен статусом: **IMPLEMENTED** (работает и покрыто тестами), **PLANNED** (осознанно запланировано, но не начато), **DEFERRED** (намеренно отложено с задокументированной причиной).

Смежные документы:

- `docs/PROJECT_SPEC.md` — продуктовый roadmap, модель безопасности записи, фазы.
- `docs/ROADMAP_STATUS.md` — журнал выполнения: какая фаза реально завершена, дата, коммит, следующее задание, открытые блокеры.
- `docs/ARCHITECTURE.md` — подробное описание архитектуры и её ограничений.
- `docs/DEVELOPMENT_PLAYBOOK.md` — практическое руководство «как расширять» эту архитектуру.
- `docs/TECHNICAL_DEBT.md` — реестр рисков и release-gates.
- `docs/decisions/` — ADR для значимых архитектурных решений.
- `docs/UPSTREAM_ANALYSIS.md` — анализ архитектуры унаследованного от TubeMaster кода (Phase 0).
- `docs/UPSTREAM_BASELINE.md` — верификация baseline (тесты/lint/build), состояние npm audit.

---

## 1. Общая схема потока данных

```text
User / Agent
    ↓
Web UI (dashboard) / CLI (video-metadata.ts) / MCP (server.ts) / API route handlers
    ↓
Domain Services (video-metadata, playlist-management, channel-sync)
    ↓
Auth (NextAuth / cli-auth) + Write-Context guardrail (только для write-путей)
    ↓
YouTube Read Gateway (src/lib/youtube-read-gateway/, googleapis) + Write Gateway (src/lib/youtube-write-gateway/)         Database (src/lib/db.ts, SQLite/libSQL)
```

Все четыре интерфейса (Web/CLI/MCP/API) вызывают одни и те же core-фабрики (`createVideoMetadataCore()`, `createPlaylistManagementCore()`, `createChannelSyncCore()`) — ни один интерфейс не реализует свою собственную логику работы с YouTube API или свои проверки безопасности записи.

---

## 2. Подсистемы

### 2.1 Authentication / OAuth — **IMPLEMENTED**

- **Ответственность:** Google OAuth (веб-сессия через NextAuth + CLI/MCP через PKCE-loopback или device flow).
- **Файлы:** `src/lib/auth.ts` (`authOptions`, `YOUTUBE_SCOPES`, `buildGoogleLoopbackAuthUrl`, `startGoogleDeviceAuthorization`, `pollGoogleDeviceAuthorizationToken`, `exchangeGoogleAuthCode`, `revokeGoogleToken`), `src/app/api/auth/[...nextauth]/route.ts`.
- **Точки входа:** веб — `getServerSession(authOptions)` в каждом API route handler; CLI — `npm run cli:video-metadata -- auth login [--device]`.
- **Зависимости:** `src/lib/db.ts` (сохранение токенов в `users`).
- **Read/Write:** сама по себе не пишет в YouTube; получает/обновляет OAuth-токены.
- **Важные ограничения безопасности:** access/refresh токены никогда не логируются и не уходят в браузерный JS; хранение — только на сервере/локально.
- **Phase 8, BL-056 (2026-09-22, owner approval, Telegram msg 356):** `YOUTUBE_SCOPES` расширен новым `YOUTUBE_ANALYTICS_READ_SCOPE` (`yt-analytics.readonly`) — read-only доступ к YouTube Analytics API, нужен для Phase 8's Analytics-адаптера (BL-057, ещё не реализован). Не включает `yt-analytics-monetary.readonly` (доходные метрики) — отдельный, не запрошенный и не одобренный scope. **Re-consent не потребовал отдельного механизма**: все три существующих пути входа (`buildGoogleLoopbackAuthUrl`, NextAuth `authOptions`, `startGoogleDeviceAuthorization`) уже безусловно используют `prompt: "select_account consent"`/полный consent на каждом входе — новый scope просто появится на экране согласия при следующем входе любого пользователя. Уже сохранённый токен существующей сессии не получает новый scope автоматически (нужен повторный вход) — это и есть fail-closed поведение: `resolveGoogleCredentials`'s `requiredScopes`-проверка (уже существующая, ничего нового) отклонит вызов Analytics-адаптера с `AUTH_SCOPE_INSUFFICIENT`, пока пользователь не переавторизуется. Реальная переавторизация настоящего Google-аккаунта не выполнялась агентом (не может быть выполнена автоматически) — владелец должен провести её сам.

### 2.2 Credential resolution — **IMPLEMENTED**

- **Ответственность:** превращение `CredentialRef` (`{ userId }` или явные токены) в `ResolvedCredentials` с проверкой достаточности OAuth-скоупов и автообновлением истёкшего access token.
- **Файлы:** `src/lib/video-metadata/adapters/google-auth.ts` (`resolveGoogleCredentials`, используется всеми доменными модулями, включая `channel-sync`), `src/lib/cli-auth/*` (для CLI/MCP: `service.ts`, `storage.ts` — файл `data/auth-context.json` с активным локальным пользователем, `errors.ts` — типизированные коды ошибок `AUTH_*`).
- **Точки входа:** `authResolver.resolve({ credentialRef, requiredScopes })` — вызывается из `services.ts` каждого доменного модуля.
- **Зависимости:** `src/lib/db.ts` (чтение/обновление токенов), `src/lib/auth.ts` (OAuth2-клиент).
- **Read/Write:** может обновлять (refresh) и персистить токены; не пишет в YouTube.
- **Важные ограничения безопасности:** несоответствие требуемых скоупов → `AUTH_SCOPE_INSUFFICIENT`; истёкший токен без refresh token → `AUTH_REFRESH_TOKEN_MISSING`; отсутствие локального пользователя (CLI/MCP) → `AUTH_USER_NOT_FOUND`.

### 2.3 Channel identity / write-context guardrails — **IMPLEMENTED**

- **Ответственность:** единственная реализация проверки `expectedChannelId` — блокирует write-операции, если активный OAuth-канал не совпадает с ожидаемым (fail-closed).
- **Файлы:** `src/lib/write-context/contracts.ts`, `service.ts` (`assertWriteChannel`, `getWriteChannelContext`, `listKnownChannels`, `selectWriteChannel`), `adapters/youtube-api.ts`.
- **Точки входа:** `writeContext.assertWriteChannel({ credentialRef, credentials, expectedChannelId })` — вызывается из `video-metadata/services.ts` (`applyMetadata`) и `playlist-management/services.ts` (create/update/delete).
- **Зависимости:** `src/lib/db.ts` (`getSelectedChannelId`/`setSelectedChannelId` — персистентный выбор канала).
- **Read/Write:** сам guardrail не пишет в YouTube; используется исключительно перед write-вызовами.
- **Важные ограничения безопасности:** несовпадение → `WRITE_CHANNEL_MISMATCH`; невозможность определить активный канал → `WRITE_CHANNEL_UNRESOLVED`; отсутствие `expectedChannelId` → `WRITE_CHANNEL_REQUIRED`. **`channel-sync` (Phase 2) этот guardrail не использует — синхронизация read-only и не нуждается в проверке канала записи** (см. `docs/ARCHITECTURE.md` §4.3).

### 2.4 YouTube API client layer — **IMPLEMENTED**

- **Ответственность:** единственная низкоуровневая обёртка над `googleapis` (`youtube_v3`) для READ-вызовов. **Переехало в `src/lib/youtube-read-gateway/` (2026-09-22, `docs/decisions/0007-youtube-read-gateway.md`, owner instruction по Telegram — "Все запросы на получения данных должны идти через него и никак иначе"), бывший `src/lib/youtube.ts`.**
- **Файлы:** `src/lib/youtube-read-gateway/index.ts` — тонкий барьер-реэкспорт (единственный разрешённый путь импорта для остального кода); `data-api.ts` (переехавший `youtube.ts`, без изменения поведения) — общие функции (`createYoutubeClient`, `getAuthenticatedYoutube`, `getMyChannelId`, `listVideosByChannel`, `getVideoById`, `getVideoMetadataContext`, плейлист-функции) **плюс новые для Phase 2**: `getChannelForSync`, `listUploadsPlaylistVideoIds`, `getVideosMetadataContextBatch` (батчинг по ≤50 id за вызов `videos.list`); `analytics-api.ts` — второй дочерний модуль (см. §2.9i). Write-функции (`applyVideoMetadataUpdate` и другие) живут отдельно в `src/lib/youtube-write-gateway/` (см. §2.9g) — этот слой теперь read-only.
- **Точки входа:** используется через тонкие адаптеры каждого доменного модуля (`video-metadata/adapters/youtube-api.ts`, `playlist-management/adapters/youtube-api.ts`, `channel-sync/adapters/youtube-api.ts`, `write-context/adapters/youtube-api.ts`), которые импортируют исключительно из барьера `@/lib/youtube-read-gateway`, никогда напрямую из `data-api.ts` (зафиксировано `read-gateway-inventory.test.ts`) — не вызывается напрямую из сервисов.
- **Тумблер "Data API reads" (2026-09-22, Settings tab):** `createYoutubeClient` вызывает `assertDataApiReadsAuthorized()` (проверяет `getDataApiReadsEnabled`, по умолчанию `true`, не сбрасывается при рестарте) перед созданием клиента — единственная точка входа для ЛЮБОГО read- или write-вызова Data API v3 (включая построение write-клиента Batches и identity-проверку `write-context` перед записью — намеренная связь, не баг: без чтения write-путь всё равно не может безопасно продолжить). Живьём проверено в браузере: выключение тумблера корректно ломает Content-таб с понятной ошибкой, включение обратно — восстанавливает.
- **Классификация ошибки превышения квоты (добавлено 2026-09-22, owner instruction, Telegram: «Сделай правильную обработку этого события... чтобы агенту это тоже было транслировано корректно»):** новый `error-classification.ts` — `classifyYoutubeReadError` (проверка `403 quotaExceeded`/`dailyLimitExceeded` → `DomainError` с кодом `youtube_quota_exceeded`, 429; любая другая ошибка пробрасывается БЕЗ ИЗМЕНЕНИЙ). **Применяется один раз в самом конструкторе клиента** (`createYoutubeClient`/`createYoutubeAnalyticsClient`), а не в каждой из ~15 точек вызова по отдельности — по прямому уточнению владельца («у нас же один шлюз который взаимодействует с API, он и может и обрабатывать / переводить это сообщение»). `wrapYoutubeClientForQuotaClassification` строит НОВЫЙ объект (через `Object.defineProperty`), а не `new Proxy(client, {...})` напрямую — живой проверкой найдено, что реальный клиент `googleapis` определяет свойства ресурсов (`.channels`, `.videos` и т.д.) как non-configurable/non-writable, что нарушает инвариант `Proxy`'s `get`-ловушки и роняет реальный Content sync ошибкой `TypeError`; тест на это добавлен (фикстура с `Object.defineProperty`, воспроизводящая именно эту форму). Намеренно НЕ вычисляет момент сброса квоты (только текст «сбрасывается в полночь по Тихоокеанскому времени») — граница сброса для Analytics API отдельно не проверена, неверный machine-readable timestamp хуже отсутствующего. Отдельный от `classifyYoutubeWriteError` (write-путь, AC-QUOTA-02, `FAILED/permanent/systemic`, останавливает весь batch). Проверено: MCP (`toolErrorResult`) и CLI уже передают `{code, message, details}` для любого `DomainError` без дополнительных изменений.
- **Зависимости:** `src/lib/auth.ts` (создание OAuth2-клиента).
- **Read/Write:** содержит и read-, и write-методы (`applyVideoMetadataUpdate`, playlist create/update/delete/insert/delete-item). Методы, используемые `channel-sync`, — только read (`channels.list`, `playlistItems.list`, `videos.list`).
- **Важные ограничения безопасности:** не должен дублироваться — новый код обязан расширять этот файл, а не создавать параллельный клиент (`docs/PROJECT_SPEC.md` §3).

### 2.5 Persistence / database — **IMPLEMENTED**

- **Ответственность:** локальное хранилище на SQLite (libSQL), файл `data/playlist-manager.db`.
- **Файлы:** `src/lib/db.ts` — вся схема (`users`, `rules`, `channels`, `videos`) и CRUD-функции.
- **Точки входа:** прямой импорт функций из `db.ts` внутри адаптеров каждого модуля (`channel-sync/adapters/store.ts`, `write-context`'s `channelSelectionStore` и т.д.).
- **Зависимости:** нет (нижний уровень).
- **Read/Write:** и то, и другое (это и есть хранилище).
- **Важные ограничения безопасности:** OAuth-токены хранятся в открытом виде — приемлемо только для локального однопользовательского инструмента (см. `docs/UPSTREAM_ANALYSIS.md` §9, риск №1). Схема создаётся идемпотентно при старте (`CREATE TABLE IF NOT EXISTS` + `ALTER TABLE` в try/catch) — **это осознанное решение, не миграционный инструмент**; решение и условие его пересмотра задокументированы в `docs/ARCHITECTURE.md` §6.2.

### 2.6 Channel synchronization (Phase 2) — **IMPLEMENTED**

- **Ответственность:** получение и локальное сохранение сведений о канале (`title`, `thumbnailUrl`, `uploadsPlaylistId`) при синхронизации.
- **Файлы:** `src/lib/channel-sync/{contracts,schemas,services,index}.ts`, `adapters/{youtube-api,store,logger}.ts`.
- **Точки входа:** `core.syncChannel({ credentialRef, channelId? })`, `core.listChannels(...)` — через `createChannelSyncCore()`.
- **Зависимости:** credential resolution (`resolveGoogleCredentials`), YouTube API layer (`getChannelForSync`), persistence (`upsertChannel`, `markChannelSynced`, `listStoredChannels`, `getStoredChannel`).
- **Read/Write:** **только чтение** YouTube (`channels.list`); запись — только в локальную БД.
- **Важные ограничения безопасности:** read-only scope (`YOUTUBE_READ_SCOPE`); guardrail write-context не применяется (не нужен для read-операции). **С 2026-09-20:** `listChannels`/`listSyncedVideos` фильтруют результат по активному каналу вызывающего (`src/lib/channel-access`, `docs/decisions/0004-active-channel-read-scoping.md`) — закрывает RISK-02. `syncChannel` без явного `channelId` ("sync my own channel") — единственное место, где активный канал автоматически проставляется; с явным `channelId` синк по-прежнему не проверяет принадлежность аккаунту (RISK-39, отдельный открытый пункт).

### 2.7 Video synchronization (Phase 2) — **IMPLEMENTED**

- **Ответственность:** перечисление всех видео канала через uploads playlist и батч-получение полных метаданных (`title`, `description`, `publishedAt`, `privacyStatus`, `defaultLanguage`, `defaultAudioLanguage`, `thumbnails`, `existingLocalizations`, `etag`) без one-request-per-video. **С 2026-09-20** (`docs/roadmap/plans/STUDIO_PARITY_PLAN.md` Slice S1): также `viewCount`/`commentCount`/`likeCount` из `statistics`-части `videos.list` — `NULL`, если YouTube не вернул значение (комментарии/лайки скрыты), никогда не подменяется на `0`. Аддитивная миграция схемы (`schema_meta` версия 4, `src/lib/db.ts`'s `SCHEMA_MIGRATIONS`).
- **Файлы:** та же директория `src/lib/channel-sync/` (единый модуль с channel sync); низкоуровневая логика — `src/lib/youtube-read-gateway/data-api.ts` (`listUploadsPlaylistVideoIds`, `getVideosMetadataContextBatch`), доступная только через барьер `@/lib/youtube-read-gateway`.
- **Точки входа:** `core.syncChannel(...)` (та же функция, что и для канала — один вызов синхронизирует и канал, и все его видео), `core.listSyncedVideos({ credentialRef, channelId })`.
- **Зависимости:** channel synchronization (нужен `uploadsPlaylistId`), YouTube API layer, persistence (`upsertVideos`, `listStoredVideosByChannel`).
- **Read/Write:** только чтение YouTube; upsert в таблицу `videos`.
- **Важные ограничения безопасности:** батчинг ≤50 id за вызов `videos.list` — обязательное требование (проверено тестами на реальной форме клиента `googleapis`, не только на моках уровня сервиса).

### 2.8 Localization-related read model (Phase 3) — **IMPLEMENTED**

- **Статус: реализовано как отдельный read-only доменный модуль `src/lib/localization/`.** Никаких новых таблиц БД и никаких новых вызовов YouTube API — это чистый read-model поверх того, что уже синхронизировал `channel-sync` (таблица `videos`).
- **Ответственность:** сводная таблица локализаций по каналу (какие языки есть/отсутствуют у каждого видео), детальный просмотр одного видео (оригинал + все существующие remote-локали), экспорт в XLSX (две вкладки: Videos, Localizations).
- **Файлы:** `src/lib/localization/{contracts,schemas,services,index,languages}.ts`, `adapters/{store,xlsx}.ts` (использует `exceljs`); UI — изначально `src/components/localization-manager.tsx`, **с 2026-09-20 объединён с AI Localization в `src/components/languages-manager.tsx`** (см. §2.9b и `docs/roadmap/plans/LANGUAGES_TAB_MERGE_PLAN.md`); `LocalizationOverviewRow` дополнен полем `lastSyncedAt` (из уже существующего `StoredVideoRecord.lastSyncedAt`) для Studio-стиля колонки «Last modified».
- **Точки входа:** `core.getLocalizationOverview({credentialRef, channelId})`, `core.getVideoLocalizationDetail({credentialRef, channelId, videoId})`, `core.exportLocalizations({credentialRef, channelId, videoIds?})` — через `createLocalizationCore()`.
- **Зависимости:** persistence (`getStoredChannel`, `listStoredVideosByChannel` из `src/lib/db.ts` — те же функции, что использует `channel-sync`); `exceljs` для генерации файла.
- **Read/Write:** **только чтение** (ни одного вызова YouTube API вообще — нет даже `authResolver`, только проверка сессии на уровне API route); экспорт — генерация локального файла, не запись куда-либо.
- **Важные ограничения безопасности:** язык — производится как объединение (union) всех `existingLocalizations` по каналу, никогда не хардкодится (`docs/PROJECT_SPEC.md` §13); `video_id` — единственный canonical идентификатор в экспорте, никогда title.
- **Чего нет (после Phase 3):** draft/remote-состояний, change set, diff/approval UI, XLSX **import**, записи локализаций, конфигурации целевых языков канала. **Реализовано в Phase 4** — см. 2.9 ниже. По-прежнему нет: записи локализаций в YouTube (это Phase 5) — см. раздел 4.

### 2.9 Change sets / XLSX import / diff & approval (Phase 4) — **IMPLEMENTED** (local-only; real YouTube write remains **PLANNED**, Phase 5)

- **Статус: реализовано как отдельный доменный модуль `src/lib/changesets/`.** Новые таблицы БД (`change_sets`, `changes`), новый парсер XLSX-импорта, чистый (без React/DB) diff-движок, доменные сервисы, API-роуты, UI. **Ни одного вызова `videos.update` или любого другого write-метода YouTube API нигде в модуле** — approve/reject остаются исключительно локальной операцией, YouTube не затрагивается.
- **Cutover на Automerge (2026-09-21, AUTOMERGE_MIGRATION_PLAN.md §6 CD2, `docs/decisions/0006-*.md`):** источником истины для записи теперь является `src/lib/sync-gateway/change-drafts/` (Automerge-документ на канал), а не сама таблица SQLite напрямую — `change_sets`/`changes` становятся регенерируемой read-проекцией этого документа, перестраиваемой после каждой **успешной** записи. Смена реализована исключительно на уровне адаптера (`src/lib/changesets/adapters/change-drafts-store.ts`, подключён в `src/lib/changesets/index.ts`) — `services.ts`, API-роуты, MCP-инструменты и CLI-команды не изменились и продолжают работать через тот же интерфейс стора, что и раньше. Чтение по-прежнему идёт напрямую из SQL. **Проекция НЕ гарантированно синхронна**: `sync-gateway/change-drafts/services.ts`'s `saveDocument` намеренно изолирует ошибку проекции (try/catch, без повторного throw) — если проекция падает, Automerge-документ (источник истины) уже сохранён корректно, но SQL временно отстаёт до следующей успешной записи; в этом окне чтение через `services.ts` может вернуть устаревшие данные, а `updateChange`/`approveChange` для ещё непроецированной записи получит `not_found` (поиск `channelId` по `changeSetId`/`changeId` идёт через ту же SQL-проекцию). Внешнее поведение не изменилось: по-прежнему ни одного вызова YouTube API.
- **Ответственность:** парсинг и валидация загруженного XLSX (совместим с форматом экспорта Phase 3 — `remote_title`/`remote_description` уже были в экспорте с самого начала Phase 3 и служат baseline для conflict detection; Phase 4 лишь добавил необязательный лист `Meta` со `schema_version`/`exported_at`/`channel_id`), сопоставление `video_id`+`language` с синхронизированными данными канала, построение персистентного Change Set из валидных/невалидных строк (unchanged-и-валидные строки не сохраняются, только учитываются в сводке), field-level diff (ADD/MODIFY/UNCHANGED + conflictStatus), детерминированный жизненный цикл Change Set (`in_review`/`approved`/`partially_approved`/`rejected`, вычисляется чистой функцией `computeChangeSetStatus`), локальные approve/reject (одиночные и массовые), ревалидация конфликтов и инвалидация устаревшего approval при каждом чтении/действии.
- **Файлы:** `src/lib/changesets/{contracts,schemas,diff,import,services,index}.ts`, `adapters/store.ts`; UI — `src/components/change-set-review.tsx` (с 2026-09-20 принимает опциональный `onStatusChange`-колбэк, чтобы родитель мог обновить свой собственный, отдельно закэшированный список Change Sets после approve/reject — нужно для статус-based сабтабов в `languages-manager.tsx`) + блок импорта/экспорта в `src/components/languages-manager.tsx` (ранее — `localization-manager.tsx`).
- **Точки входа:** `core.previewImport(...)`, `core.createChangeSetFromImport(...)`, `core.listChangeSets(...)`, `core.getChangeSet(...)`, `core.approveChange(...)`, `core.rejectChange(...)`, `core.approveAllValid(...)`, `core.rejectAllPending(...)` — через `createChangeSetCore()`.
- **Зависимости:** persistence (`getStoredChannel`, `listStoredVideosByChannel` — те же функции, что использует `channel-sync`/`localization`, для получения *текущего* синхронизированного remote-значения), новые CRUD-функции в `src/lib/db.ts` для `change_sets`/`changes`, `exceljs` для парсинга (без вычисления формул — только `cell.result`/текст, импортируемый контент никогда не исполняется).
- **Read/Write:** **ноль вызовов YouTube API** (ни read, ни write) — весь модуль работает поверх уже синхронизированных локальных данных; запись — только в локальные таблицы `change_sets`/`changes`.
- **Важные ограничения безопасности:** blank-ячейка = нет изменения (`docs/PROJECT_SPEC.md` §16); `video_id` — единственный canonical идентификатор, строка с несуществующим/чужим `video_id` отклоняется как невалидная, а лист `Meta.channel_id`, не совпадающий с целевым каналом, блокирует **весь** импорт целиком (защита от импорта книги, экспортированной для другого канала); conflict detection сравнивает baseline экспорта с *последним синхронизированным* remote-значением — **не свежий вызов YouTube API** (задокументированное ограничение, см. `docs/ARCHITECTURE.md` §6.6); approve возможен только для `validationStatus: valid` + `conflictStatus: none`, а ранее выданный approval автоматически инвалидируется, если после ре-синхронизации remote-значение разошлось с baseline (`docs/ARCHITECTURE.md` §6.7). **RISK-47, исправлено 2026-09-21:** approve/approve-all дополнительно отказывают, если на этом `changeId` есть открытый CRDT-level конфликт (`FieldConflict` из `src/lib/sync-gateway/change-drafts/`, отдельная концепция от `conflictStatus` выше) — новая зависимость `crdtConflicts.listConflictedChangeIds(channelId)` в `changesets/services.ts`, реализованная через `change-drafts`'s `listConflicts`; `approveChange` кидает `crdt_conflict_open` (409), `approveAllValid` молча исключает такое изменение из пакета, не проваливая весь батч.
- **Deletion-предложения, добавлено 2026-09-21, backend + UI (`docs/PROJECT_SPEC.md` §16, обновление 2026-09-20 — удаление никогда не мгновенно/безвозвратно).** Новый `ChangeType` `"delete"` и `ChangeSetSource` `"deletion"`; `changesets.proposeLocalizationDeletion({channelId, language, videoIds?})` создаёт Change Set из `delete`-изменений (title+description на видео) для одного языка, проходящий тот же review/approval/conflict-пайплайн, что и любой другой Change Set — это только первый из нескольких обязательных этапов подтверждения, а не само удаление. Опущенный `videoIds` означает «каждое видео канала, у которого есть реальная локализация на этот язык» (bulk/whole-column форма — добавлена E5b поверх исходной single-видео версии); переданный `videoIds` ограничивает предложение конкретным подмножеством. **Жёстко исключает** (не ошибка, а `skippedDefaultLanguageVideoIds` в ответе) любое видео, у которого запрошенный язык — это его собственный `defaultLanguage`: этот язык живёт в `snippet.title`/`description`, а не в удаляемой `localizations`-записи, и `buildSafeLocalizationsPayload` (`src/lib/batches/merge.ts`) в таком случае обнулил бы реальные title/description видео вместо удаления локализации. Это исключение считается напрямую по `video.defaultLanguage`, а не через `existingLocalizations`-объединение (`collectChannelLanguages`) — независимая от последнего проверка обязательна, потому что видео может иметь язык как `defaultLanguage`, не внося вклад в `existingLocalizations` вообще («undercount trap», найдено advisor-ревью). Если после фильтрации не осталось ни одного затронутого видео, функция возвращает `{changeSet: null, ...}` — это не ошибка, а легитимный результат («предлагать нечего»). `buildSafeLocalizationsPayload` дополнен тем же самым guard'ом как defense-in-depth и delete-wins мерж-логикой (удаление применяется после всех остальных изменений батча, поэтому побеждает над одновременным modify той же локали независимо от порядка). **UI (E5b):** кнопка «✕» у языкового заголовка в `languages-manager.tsx`, если у языка есть хотя бы одно реальное видео — вместо тихого untrack показывает явное предупреждение (что произойдёт, что backup уже делается существующим batch-пайплайном подготовки, что ничего не применяется немедленно) и по подтверждению вызывает `POST .../localizations/languages/[language]/propose-deletion` (bulk-форма, без `videoIds`); колонка **намеренно не снимается с отслеживания** при реальном удалении — она и так остаётся видимой через объединение (E5a), пока реальные данные не исчезнут после настоящей записи+ресинка. **Явно НЕ реализовано:** restore-механизм (30-дневное окно локального восстановления из §16) — отложен до реальных live-валидационных тестов Gate B, поскольку до снятия барьера `assertLiveWritesAuthorized()` восстанавливать пока нечего (см. §2.9a); `docs/TECHNICAL_DEBT.md` RISK-41 отслеживает backup-retention/purge на будущее. Файлы: `src/lib/changesets/{contracts,schemas,services}.ts`, `src/lib/batches/{merge,contracts,services,adapters/store}.ts`, `src/lib/db.ts`, `src/lib/video-metadata/contracts.ts` (+`error-status.ts`), новый роут `src/app/api/channels/[channelId]/localizations/languages/[language]/propose-deletion/route.ts`, `src/components/languages-manager.tsx`.
- **Чего нет:** реальной записи локализаций в YouTube, immutable backup/audit log для write-операций, batch executor, CLI/MCP-инструментов для change sets, restore-UI для deletion-предложений (см. выше). Per-item execution ledger частично появился в Phase 5 Slice 1 — см. 2.9a.

### 2.9a Batch execution ledger + safety preparation + recovery/audit + write adapter + Web UI/API (Phase 5, Slices 1-5 + completion task) — **PARTIALLY IMPLEMENTED**

- **Phase 5 completion task, добавлено 2026-09-18 (после отчёта независимого ревью).** Закрыты оставшиеся два авто-тестируемых пробела: `AC-CONCURRENCY-03` (детерминированный real-SQLite тест — два разных батча претендуют на одно и то же видео; проигравший корректно отклоняется с `video_locked`, а не гонится) и `AC-E2E-01` (единый собранный сценарий: dry-run батч → diff/сохранность локалей → live-батч (мок) → SUCCESS → полный audit → повторный запуск того же батча — ноль дублирующих записей). **При построении теста AC-CONCURRENCY-03 найдены и исправлены два реальных бага в `executeBatch`:** (1) inline-подготовка `PENDING`-строки при возобновлении не была обёрнута в try/catch — конфликт лока между двумя батчами приводил к необработанному исключению, роняющему весь вызов `executeBatch`, а не к одной корректной `FAILED`-строке; (2) оба catch-блока (этот и уже существовавший в `prepareBatchExecution`) только пушили `FAILED` в память, никогда не сохраняя `transitionLedgerStatus` — строка в БД молча оставалась `PENDING` навсегда, и возобновлённый батч бесконечно ретраил бы видео, залоченное другим батчем. RISK-11 расширен на legacy single-item write-путь (общий whitelist теперь живёт в `src/lib/youtube.ts`). Добавлен минимальный Web UI/API для Batches (см. §2.10/§2.11 ниже) — единственный способ инициировать что-либо, связанное с Batch, из живого интерфейса, и он полностью dry-run-only (нет кнопки/роута live-исполнения). **Два открытых вопроса из этой задачи закрыты follow-up задачей 2026-09-18 (с явным одобрением владельца проекта):** RISK-12 — default `dryRun` у legacy `applyMetadata`/MCP `apply`-инструмента изменён с `false` на `true` (осознанное breaking behavioral change); при повторной проверке всех write-путей найден и исправлен второй, независимый небезопасный дефолт в CLI-команде `apply` (пропуск `--dryRun` отправлял явное `dryRun: false`, в обход дефолта схемы); все четыре точки входа (Web UI, `/api/video-metadata/apply`, MCP `apply`, CLI `apply`) перепроверены fail-closed. RISK-13 — `AC-QUOTA-01` разбит на `AC-QUOTA-01a`/`AC-QUOTA-01b` (узкая редакционная правка акцептанс-контракта, без изменения кода/тестов и без ослабления требования безопасности). **RISK-12 CLOSED, RISK-13 RESOLVED.** См. `docs/TECHNICAL_DEBT.md` RISK-09/12/13 для полного текста.
- **Slice 5 (INDEPENDENT VERIFICATION AND COMPLETION), 2026-09-18.** Закрыты все три пробела, найденные при Slice 4: (1) `AC-CONCURRENCY-01` — реализована реальная ограниченная параллельность (`runWithConcurrencyLimit`, cursor-based worker pool, уважает `batch.concurrency` 1-5); systemic-сбой останавливает только ещё не начатые строки, уже выполняющиеся завершаются штатно. (2) `AC-QUOTA-01` — `fetchPreliminaryBatchContext` подключён в `prepareBatchExecution` как разовый информационный проход по всем видео батча, структурно не способный повлиять на per-video конфликт/merge/backup решение; обязательный fresh per-video fetch перед каждой записью остался неизменным (RISK-03) — честно задокументировано, что это НЕ снижает общее число API-вызовов живого батча, только добавляет один дешёвый обзорный проход. (3) `AC-RESUME-01` — точный сценарий из акцептанс-контракта (100 видео, 43 успеха, 5 классов состояний) реализован и пройден на реальном SQLite с имитацией перезапуска процесса. **При этом найден и исправлен реальный баг** в `executeBatch`: строки, оставшиеся `PENDING` при возобновлении уже запущенного (не `PENDING` на уровне батча) батча, никогда не проходили подготовку и молча оставались бы `PENDING` навсегда — `executeBatch` теперь вызывает `prepareLedgerRow` inline для любой встреченной `PENDING`-строки. RISK-11 (эхо read-only полей `snippet`) закрыт для этого модуля через явный whitelist (`pickWritableSnippetFields`) вместо проверки безопасности эха вживую. Добавлен автоматический инвентарный тест (`write-path-inventory.test.ts`), доказывающий отсутствие скрытых путей к записи по всему репозиторию (API/MCP/CLI), а не только точечный grep. **Открытые вопросы Slice 5 не выполнялись и намеренно оставлены** (см. `docs/TECHNICAL_DEBT.md` RISK-09): `AC-CONCURRENCY-03`'s полный сценарий гонки двух батчей через реальную оркестрацию (только базовый lock-примитив проверен), `AC-E2E-01` как единый собранный тест на 23 шага (все составляющие поведения покрыты по отдельности), и вся `LIVE VALIDATION` часть акцептанс-контракта.
- **Slice 4 (WRITE INTEGRATION), добавлено 2026-09-18.** Реальный `WriteExecutor` (`src/lib/batches/adapters/write-executor.youtube.ts`) реализован поверх уже существующего `applyVideoMetadataUpdate` (`src/lib/youtube.ts`) — один YouTube-клиент, не создан параллельный write-путь. Классификация ошибок (`classifyYoutubeWriteError`) построена на официальной документации YouTube Data API v3 (проверено 2026-09-18): 400/403(non-quota)/404 → `permanent`; 403 `quotaExceeded` → `permanent` + `systemic` (`AC-QUOTA-02`); 408/429/5xx → `transient` (совпадает с `AC-RETRY-01/03`); отсутствие HTTP-ответа (timeout/обрыв соединения) → `UNKNOWN`, никогда напрямую не ретраится (`DEC-OQ-6`/`§0.F`). **Двухуровневый барьер против реальной записи:** (1) `src/lib/batches/index.ts` по-прежнему не конструирует `WriteExecutor` — ни один API-route/MCP-инструмент не ссылается на модуль `batches` (проверено grep, `AC-SCOPE-01` выполняется); (2) сам `attemptWrite` вызывает жёстко закодированный, не зависящий ни от каких параметров/env/dryRun `assertLiveWritesAuthorized()`, который безусловно бросает `live_writes_disabled` до обращения к клиенту — снять этот барьер можно только правкой исходного кода, что является отдельной, будущей, явно авторизуемой процедурой после Gate B. 12 новых тестов покрывают классификацию ошибок, точную форму запроса к `videos.update` (сохранение id/snippet/localizations) и сам барьер (проверено, что он срабатывает до конструирования клиента).
- **Пробелы, найденные при повторном инспектировании Slices 1-3 во время Slice 4 — все три закрыты в Slice 5 (см. выше):** `AC-CONCURRENCY-01`'s "configured `K > 1`", `AC-QUOTA-01`'s подключение батчевого preliminary-fetch, `AC-RESUME-01`'s точный 100-видео сценарий.
- **Slice 3 (RECOVERY AND AUDIT), добавлено 2026-09-17.** Явная модель состояний ledger дополнена статусом `AWAITING_EXECUTION` (подготовлено, live-запись ещё не начата — заменяет прежнее использование `APPLYING` как «заглушки», раскритикованное владельцем проекта: `APPLYING` теперь означает исключительно «attempt реально выполняется»). Новый модуль `src/lib/audit/` — durable append-only audit trail (`PREPARATION`/`ATTEMPT`/`RESULT`/`CONFLICT`/`VERIFICATION`/`DRY_RUN`/`RECONCILIATION`), таблица `audit_events` (упорядочена по rowid). Реализован §0.F reconciliation (два bounded-чтения, никогда не авторизует retry сам по себе), §0.E bounded retry (4 попытки, classification transient/permanent от исполнителя), обязательная post-write verification перед любым `SUCCESS` (`AC-VERIFY-01/02`, `AC-CONFLICT-02`), причинно-осознанный audit (`AC-AUDIT-05`: `ownResponseObserved` отличает подтверждённый собственным ответом `SUCCESS` от подтверждённого через reconciliation). `executeBatch`/`executeWithRetry` — новая оркестрация, гоняющая `AWAITING_EXECUTION`-строки через обязательную повторную safety-проверку непосредственно перед отправкой (переиспользует ту же `runSafetyPipeline`, что и Slice 2, — общий guardrail, не дублирование). Item-level isolation + systemic abort (включая quota) + downloadable error report (`getBatchErrorReport`). Crash recovery — `recoverBatch`/`recoverLedgerRow`, идемпотентны при повторных перезапусках, никогда не освобождают лок только по таймауту, никогда не изобретают неподтверждённый API-вызов.
- **Найденные и исправленные в ходе Slice 3 баги Foundation-слоя:** несколько функций в `src/lib/db.ts` (`transitionLedgerRowStatus`, `markBatchTerminal`, `listStoredLedgerRowsByBatch`, `listStoredAttemptsByBatch`) никогда не принимали параметр `database` и продолжали писать/читать singleton вместо переданной изолированной БД — баг не проявлялся в тестах на fake-store (Slice 1/2) и обнаружился только при добавлении реального SQLite-теста восстановления после сбоя. Также обнаружено расхождение: `src/lib/db.ts` держит собственную копию типа `LedgerStatus`, не синхронизированную с `src/lib/batches/contracts.ts` (не содержала `DRY_RUN_COMPLETE`/`AWAITING_EXECUTION`) — исправлено вручную, отмечено как известный source-of-truth риск (см. `docs/TECHNICAL_DEBT.md`).

- **Статус: Slices 1-2 из утверждённого пятислойного implementation plan Phase 5** (`docs/acceptance/PHASE_5_ACCEPTANCE.md`, статус APPROVED). Slice 1: сущность `Batch` с неизменяемым составом (`AC-BATCH-01/02`), per-video execution ledger (`AC-LEDGER-01`), durable двухфазная (`INTENDED` → результат) запись attempt-намерения (`AC-ATTEMPT-02/03`), явная модель состояний и atomic-локи (`AC-CONCURRENCY-01/02/03`) — включая исправление от 2026-09-17 по итогам Foundation safety verification: слот `active_attempt_id` на ledger row (не только UNIQUE-констрейнт) гарантирует не более одного активного attempt на строку под реальной конкурентностью.
- **Slice 2 (SAFETY PREPARATION), добавлено 2026-09-17:** переиспользование `write-context.assertWriteChannel` перед любой работой batch (`AC-GUARD-01`); re-check approval/validation/conflict каждого `Change` как при создании batch, так и непосредственно перед подготовкой записи, одной и той же функцией (`AC-MERGE-04`, `AC-BATCH-03`); `src/lib/batches/merge.ts` — чистая логика defaultLanguage-проверки (`AC-DEFAULTLANG-01/02`), pre-write conflict detection против **свежего** fetch (`AC-CONFLICT-01`/`AC-LEDGER-04`, закрывает `RISK-03` для пайплайна подготовки), safe merge с сохранением нетронутых locale/snippet-полей (`AC-MERGE-01..03`, `AC-MULTI-01`); новый модуль `src/lib/backup/` — immutable backup с разделением item-level/infrastructure-wide отказа, никогда не перезаписывается (`AC-BACKUP-01..04`); dry-run проходит весь pipeline (identity → fetch → merge → backup) и не делает ни одного write-вызова, ledger row переходит в отдельный терминальный статус `DRY_RUN_COMPLETE`, никогда не `SUCCESS` (`AC-DRYRUN-01/02/03`). Явно разделены batched preliminary fetch (`fetchPreliminaryBatchContext`) и обязательный fresh single-video fetch (`fetchFreshVideoContext`) — merge/conflict/backup всегда используют только второй.
- **Файлы:** `src/lib/batches/{contracts,schemas,services,merge,ledger-state,index}.ts`, `adapters/{store.ts,write-executor.fake.ts,write-executor.youtube.ts,youtube-api.ts}`; ключевые новые тесты Slice 5: `resume-100-video.acceptance.test.ts`, `concurrency-execution.integration.test.ts`, `write-path-inventory.test.ts`; `src/lib/backup/{contracts,services,index}.ts`, `adapters/filesystem-store.ts`; новые таблицы в `src/lib/db.ts` (`batches`, `batch_ledger_rows` — с `active_attempt_id`, `batch_attempts`, `video_execution_locks`), схема инициализации остаётся аддитивной (`docs/decisions/0001-additive-idempotent-schema-strategy.md`).
- **Чего явно нет после завершающей задачи** (см. отчёты о реализации, не путать с «готово»): CLI-команд и MCP-инструментов для этого модуля по-прежнему не существует (`DEC-OQ-5`, планируется отдельной будущей фазой) — только минимальный dry-run-only Web UI/API (§2.10/§2.11). `AC-CONCURRENCY-03`'s полный сценарий закрыт частично (lock-примитив под гонкой двух батчей проверен; полный two-batch orchestration race через `executeBatch` — да, теперь тоже проверен, см. выше). Live-валидация (реальный канал, реальный OAuth) не проводилась ни для одного сценария. **Ни один код-путь в репозитории не может достичь реального вызова `videos.update`** — реальный `WriteExecutor` существует (`adapters/write-executor.youtube.ts`), но не сконструирован нигде в production-коде (проверено автоматическим инвентарным тестом, включающим и новые API-роуты, не только вручную), а его собственный `attemptWrite` безусловно отказывает через `assertLiveWritesAuthorized()` независимо от того, кто и как его вызовет.

### 2.9b AI Localization (Phase 6, Slice 1) — **IMPLEMENTED for the mock-provider vertical slice**

- **Ответственность:** генерация localization-предложений через заменяемый `LocalizationProvider` (docs/PROJECT_SPEC.md §32), валидация вывода теми же правилами, что и XLSX-импорт, human-редактирование предложений и создание Change Set (`source: "ai_localization"`) через существующий, не изменённый путь создания Change Set из Phase 4 — approve/reject, конфликт-детекция и Batch/dry-run переиспользуются полностью без изменений (AGENTS.md §D). Реального AI-провайдера нет — только детерминированный mock; см. `docs/acceptance/PHASE_6_ACCEPTANCE.md`. **UI, с 2026-09-20:** отдельная вкладка «AI Localization» удалена; генерация стала основным способом добавить язык внутри объединённой вкладки «Languages» (`src/components/languages-manager.tsx`, см. §2.8/§2.9 и `docs/roadmap/plans/LANGUAGES_TAB_MERGE_PLAN.md`) — по явному решению владельца проекта («мы сразу подразумеваем, что наш инструмент использует локализацию с помощью агента; ручная правка — только чтобы проверить/подправить»). Домен `src/lib/ai-localization/` и его API-роуты не изменились, изменилось только то, какой React-компонент их вызывает.
- **Файлы:** `src/lib/ai-localization/{contracts,schemas,provider-registry,services,index}.ts`, `src/lib/ai-localization/adapters/mock-provider.ts`; тесты: `services.test.ts`, `provider-registry.test.ts`, `integration.acceptance.test.ts`, `write-path-inventory.test.ts`. Изменения в существующих файлах: `src/lib/changesets/contracts.ts` (`ChangeSetSource` расширен до `"xlsx_import" | "ai_localization"`), `src/lib/changesets/services.ts` (новая экспортируемая `createChangeSetFromProposals` — общий персистентный путь, извлечённый из `createChangeSetFromImport`, чтобы не дублировать создание ChangeSet). UI изначально — `src/components/ai-localization-panel.tsx` (удалён 2026-09-20), теперь — секция «Generate with AI» внутри `src/components/languages-manager.tsx`.
- **Точки входа:** `ai.generateProposals(...)` (preview, ничего не пишет), `ai.createChangeSetFromGeneration(...)` (персистентность через переиспользуемый `changesets.createChangeSetFromProposals`); Web UI/API — см. §2.10/§2.11 ниже.
- **Зависимости:** тот же `channelStore` (локальная синхронизированная копия видео), что и `changesets`; `resolveLocalizationProvider` (только `"mock"` разрешим — любое другое имя падает с `provider_not_configured`, реальный провайдер не выбран и не будет выбран без отдельного решения владельца проекта); `src/lib/changesets/diff.ts` (переиспользованы `classifyFieldChange`/`isValidLanguageCode`/`YOUTUBE_TITLE_MAX_LENGTH`/`YOUTUBE_DESCRIPTION_MAX_LENGTH` — один источник правды, не задублированные константы).
- **Read/Write:** **ноль вызовов к реальному AI-провайдеру и ноль вызовов YouTube API** — ни в сервисах, ни в роутах, ни в UI этой фичи; запись — только в те же локальные таблицы `change_sets`/`changes`, что и у XLSX-импорта. Автоматический инвентарный тест (`write-path-inventory.test.ts`, зеркалит паттерн `src/lib/batches/write-path-inventory.test.ts`) проверяет отсутствие импортов реальных AI SDK/сетевых клиентов и отсутствие ссылок на любой live-write-способный символ Batch-модуля.
- **Важные ограничения безопасности:** каждый персистентный `Change` создаётся с `approvalStatus: "pending"` — ни один код-путь в этом модуле не может выставить `approved` (AGENTS.md §G, INV-6.1); пропуск поля в отредактированном предложении означает «нет изменения», никогда не удаление существующей локализации (INV-6.3/§8); `batches/**` не содержит ни одной ветки по `ChangeSet.source` — одобренные AI-изменения проходят Batch/dry-run пайплайн Phase 5 совершенно неизменённым.
- **Channel Editorial Profiles, добавлено 2026-09-19 (Part B of `docs/ai-localization/CHANNEL_CONTEXT_PROPOSAL.md`, явно авторизовано владельцем проекта).** Постоянный, per-channel редакционный профиль (targetAudience/toneNotes/terminologyNotes/titleConstraints/descriptionConstraints), одна запись на канал, версия увеличивается при каждом сохранении. Поддерживает произвольное число каналов; **никаких захардкоженных инструкций для конкретных реальных каналов нигде нет**. Новые таблицы `channel_editorial_profiles` и `ai_localization_generation_provenance` (`src/lib/db.ts`, чисто аддитивно, ADR 0001) — второй хранит неизменяемую запись о том, какая версия профиля/эффективный контекст реально использовались для конкретного Change Set, независимо от последующих правок профиля (reproducibility). Комбинирование: per-request `editorialBrief`-поле побеждает поле профиля независимо по каждому из пяти полей; если ни одно не задано — поле просто отсутствует. Ничего в этой функциональности не устанавливает `approvalStatus` и не создаёт Change/ChangeSet как побочный эффект сохранения профиля. Новые роуты (`GET/PUT .../ai-localization/profile`, `GET .../ai-localization/change-sets/[changeSetId]/provenance`) следуют тому же паттерну auth + channel-scoping, что и остальные роуты. Минимальный UI-редактор профиля изначально был добавлен в существующую вкладку «AI Localization» (не новая вкладка); **с 2026-09-20 переехал во вкладку «Home»** (`docs/roadmap/plans/STUDIO_PARITY_PLAN.md` Slice S4, `src/components/editorial-profile-panel.tsx`) — по решению владельца проекта («звучит как что-то фундаментальное... давай вынесем это в закладку Home»), сам редактор/API-роуты не менялись, только расположение. 10 новых тестов (`profiles.test.ts`): channel isolation, missing/empty profile, editing/persistence, version/snapshot preservation после правки профиля, применение при генерации, взаимодействие с per-request editorialBrief, malformed data, отсутствие авто-одобрения.
- **Чего нет:** реального AI-провайдера (OpenAI/Anthropic/DeepL/etc. — явно отложенное решение владельца проекта); канало-специфичных editorial/SEO-инструкций (запрещено AGENTS.md §B постоянно, не только на этой фазе); CLI/MCP-инструментов для `getEditorialProfile`/`saveEditorialProfile`/`getGenerationProvenance` или для approve/reject/apply (см. следующий bullet — generate/create-Change-Set теперь есть, остальное всё ещё RISK-04-класса пробел).
- **MCP/CLI-инструменты для generate + create-Change-Set, добавлено 2026-09-23 (BL-075/BL-078, owner instruction по Telegram: «эксель нам тоже не нужен... перевод делать должен агент, а не человек»).** Исследование перед реализацией показало: XLSX никогда не был интерфейсом агента — это отдельный, опциональный путь для человека, правящего таблицу руками; путь «Generate with AI» уже полностью минует XLSX и создаёт тот же Change Set. Реальный пробел был уже, чем формулировка задачи: не было ни одного MCP/CLI-инструмента для `generateProposals`/`createChangeSetFromGeneration` — агент извне мог создать Change Set только через `changeset_create_from_import` (загрузка XLSX-байт). Закрыто: MCP `ai_localization_generate`/`ai_localization_create_change_set`, CLI `ai-localization generate`/`ai-localization create-change-set` — оба вызывают ровно те же, уже протестированные функции, что и веб-роуты, **без единой новой строчки кода валидации/персистентности**. `generate` не пишет ничего (ungated, как `localization_import_preview`); `create-change-set` пишет новый Change Set (гейтится как `changeset_create_from_import`), но результат всегда стартует `pending` — approve/reject/Batch/dry-run/live-write barrier совершенно не тронуты (AGENTS.md §G). XLSX-импорт/экспорт не менялись, не депрекейтились, не скрывались — открытый вопрос из BL-075 (нужна ли XLSX для человека-ревьюера) сознательно не решён этим слайсом.
- **UX-редизайн, слайсы E1-E4b, добавлено 2026-09-21 (`docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` §7.6, `docs/roadmap/BACKLOG.md` BL-037).** Единая таблица `languages-manager.tsx` больше не показывает языки как один count-столбец — вернулась колонка-грид «✓/—» по каждому реально встречающемуся в канале языку (`overview.languages`), с сортировкой по клику на заголовок (по умолчанию — дата публикации, новые сверху; для языковой колонки — сначала отсутствующие). Прежняя всегда открытая секция «Generate with AI» вверху вкладки (со своим отдельным чек-листом видео) удалена целиком: одно общее `selectedIds` теперь управляет и генерацией, и XLSX-экспортом (было три независимых набора выбора); при отметке ≥1 строки над таблицей появляется контекстная панель («N selected — Generate with AI / Export to XLSX»), а внутри попапа видео (§2.9e; изначально было — разворачивающаяся строка, заменено тем же днём) — свой инлайн-генератор для одного видео (никакого скролла к отдельной секции, как раньше делала кнопка «Generate with AI for this video»). Обе точки входа используют один и тот же рендер-путь генерации/ревью/создания Change Set (`generateScope: {kind:"bulk"} | {kind:"row", videoId}` внутри компонента), а не два отдельных. Под каждым языковым заголовком — маленькая кнопка `+N`, которая одним кликом выбирает все видео без этого языка (по всему каналу) и открывает попап генерации с уже подставленным языком. Добавлена статичная заглушка «Recommended languages — coming with Analytics integration (Phase 8)» (пустая, без вызовов). Live-проверено в браузере (`claude-in-chrome`, реальный канал «Tropico Jazz»): сортировка, оба пути генерации (bulk и per-row) до реального создания Change Set включительно, `+N`-кнопка. **Известное ограничение:** попап bulk-генерации — `position: absolute; right-4` фиксированной ширины и может частично уезжать за левый край на очень узком окне; на обычной desktop-ширине не проявляется, отдельно не чинилось в этом срезе. Никакой кнопки «удалить язык» в новых заголовках нет — это остаётся частью E5, которая по-прежнему не назначена (Open Question 2, §7.7 плана).
- **Tracked languages (E5a), добавлено 2026-09-21 (`docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` §7.2, owner instruction по Telegram: «добавим возможность добавить столбцы для новых языков... И возможность удалять колонки»).** Новый аддитивный столбец `channels.target_languages_json` (SCHEMA_MIGRATIONS версия 6, JSON-массив кодов языков) + два новых сервисных метода в `src/lib/localization/services.ts` (`addTrackedLanguage`/`removeTrackedLanguage`) + два новых роута (`POST`/`DELETE .../localizations/tracked-languages[/[language]]`). `LocalizationOverview.languages` теперь — объединение `trackedLanguages` (явно отслеживаемые, могут не иметь ни одного реального перевода) и языков с реальными данными (`existingLocalizations`) — решает проблему «курицы и яйца»: раньше нельзя было предложить перевод на язык, у которого ещё нет ни одной реальной локализации, потому что колонка для него просто не появлялась. UI: поле «Add language column» в тулбаре таблицы; маленькая кнопка «✕» у каждого языкового заголовка. **Удаление колонки — только локальная настройка отображения, никогда не реальное удаление**: если у языка нет реальных переводов — колонка пропадает; если реальные переводы есть хотя бы у одного видео — язык остаётся в `languages` (через объединение) несмотря на снятие с отслеживания, и оператору показывается явное уведомление об этом, а не тишина. Реальное удаление перевода (E5b) — отдельный, ещё не реализованный срез, сознательно отделённый от этого (см. `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` §7.6 после консультации с advisor: «ship Slice A alone first»). 6 новых spec-driven тестов (`src/lib/localization/services.test.ts`). Live-проверено в браузере на реальном канале: добавление нового языка (появляется колонка с нулевыми реальными данными), удаление языка без реальных данных (колонка пропадает), удаление языка с реальными данными (колонка остаётся + уведомление). Найден и исправлен попутный тест-баг: `src/lib/snapshot/services.test.ts`'s RISK-29-тест хардкодил старый список колонок таблицы `channels` — не включал новую колонку, что ломало миграцию промежуточной копии БД в тесте (не в реальном коде) — добавлена колонка в тестовый CREATE TABLE.
- **Languages-тулбар: Sync now + реальный список языков для «Add language column», добавлено 2026-09-21 (owner instruction по Telegram, BL-041).** Кнопка «Sync now» (как в Content) дергает тот же `POST /api/channels/sync`, но по завершении обновляет и таблицу, и список Change Sets (ре-синк может сдвинуть remote baseline и инвалидировать approval/породить конфликт, `docs/ARCHITECTURE.md` §6.7) — без Content-овского авто-ре-синка по staleness, это чисто ручная кнопка. Поле добавления языка стало комбобоксом на основе `listSupportedLanguages` (`src/lib/youtube.ts`, реальный вызов `youtube.i18nLanguages.list`, использован один раз 2026-09-21 для сбора данных, не вызывается в runtime). **Обновлено тем же днём (owner instruction, BL-042): список стал жёстким allowlist, не подсказкой.** Захваченные 83 языка захардкожены в `src/lib/youtube-supported-languages.ts` (`SUPPORTED_YOUTUBE_LANGUAGES`/`isSupportedYoutubeLanguageCode`) — `GET /api/youtube/languages` отдаёт их без обращения к YouTube API вообще (owner: «не дергать по этому поводу API лишний раз»); `localization/services.ts`'s `addTrackedLanguage` реально отклоняет любой код не из этого списка (не просто подсказка на фронтенде — настоящий серверный гейт), заменив прежний более широкий `isValidLanguageCode`. **Задокументированный компромисс:** это список языков ИНТЕРФЕЙСА YouTube, а не официально описанный список для `localizations`; эмпирически подтверждённый пробел — реальный код "en-US" (используется на 2 видео канала Tropico Jazz) отсутствует в этом списке. Уже реальные языки остаются видны как колонка через объединение `trackedLanguages ∪ real-data` (E5a) независимо от этого гейта — гейт ограничивает только ручное добавление НОВОГО, ещё не использованного кода. **Fallback на случай отказа YouTube от уже одобренного языка при реальной записи (owner request) — не новый код, а уже существующая инфраструктура:** `classifyYoutubeWriteError` (`src/lib/batches/adapters/write-executor.youtube.ts`) классифицирует любой `400`-ответ `videos.update` (включая невалидный язык) как `permanent`-FAILED с текстом ошибки YouTube, `batch-manager.tsx` уже показывает такие ошибки построчно в таблице — ничего дополнительно строить не потребовалось, поскольку это тот же путь, что и любая другая ошибка записи. 5 новых тестов всего (`src/lib/youtube.test.ts` + `src/lib/youtube-supported-languages.test.ts`). Live-проверено в браузере на канале Tropico Jazz: ввод "en-US" не даёт добавить (Add задизейблена, Enter не срабатывает, подсказок нет); ввод "fr" (реально есть в списке) добавляется штатно.

### 2.9c AI Connections (Phase 6, "Provider-Agnostic AI Connections", 2026-09-19) — **IMPLEMENTED for the OpenAI-compatible protocol adapter**

- **Ответственность:** provider-agnostic сущность «connection» (id, display name, adapter type, base URL, model id, опциональный зашифрованный credential, enabled, status, capability metadata, task assignment) — управляется через Settings UI. Ни один конкретный вендор/модель не захардкожены; вместо адаптера на модель — маленький protocol-adapter registry (`mock` и `openai_compatible`).
- **Файлы:** `src/lib/ai-connections/{contracts,schemas,crypto,endpoint-security,services,index}.ts`, `adapters/{registry,mock-adapter,openai-compatible,store}.ts`; тесты: `crypto.test.ts`, `endpoint-security.test.ts`, `services.test.ts`, `adapters/openai-compatible.test.ts`, `integration.acceptance.test.ts`, `write-path-inventory.test.ts` (37 тестов; +2 после независимого security-ревью: redirect-based SSRF bypass, IPv6-фикс). UI: `src/components/ai-connections-manager.tsx` (вкладка «Settings»); интеграция в «Generate with AI» секцию `languages-manager.tsx` (ранее — `ai-localization-panel.tsx`) через выпадающий список connection (по умолчанию — mock, без сети).
- **Хранение credential — архитектурное решение (см. `docs/acceptance/PHASE_6_AI_CONNECTIONS_ACCEPTANCE.md` §2):** НЕ OS-keychain (отложено — нативная зависимость, кроссплатформенный риск), НЕ plaintext (запрещено явно). Выбрано: **AES-256-GCM, ключ из `AI_CONNECTIONS_ENCRYPTION_KEY`** (env-переменная, никогда не в репозитории — base64, 32 байта), шифротекст в отдельной таблице `ai_connection_credentials` (никогда не в одной таблице/запросе с публичными полями connection). Без настроенного ключа сохранение connection с credential падает с `encryption_key_not_configured` — **plaintext-фолбэка нет**.
- **Endpoint security (SSRF):** `endpoint-security.ts`'s `validateEndpointUrl` — HTTPS обязателен для удалённых endpoint'ов; IP-литералы и DNS-резолвленные адреса проверяются против списка приватных/loopback/link-local/metadata (включая `169.254.169.254`)/reserved диапазонов (IPv4 и IPv6); local-inference mode — отдельный, явно включаемый флаг connection, разрешающий localhost/приватную сеть. Проверка выполняется и при сохранении, и непосредственно перед каждым реальным исходящим вызовом (defense-in-depth). **Задокументированное остаточное ограничение:** проверка не «пиннит» сокет к провалидированному адресу — узкое TOCTOU-окно для DNS rebinding остаётся (см. `docs/TECHNICAL_DEBT.md`), приемлемо для текущей модели «доверенный оператор на своей машине» (Gate D).
- **Точки входа:** `GET/POST /api/ai-connections`, `GET/PUT/DELETE /api/ai-connections/[connectionId]`, `POST /api/ai-connections/[connectionId]/test` (единственный способ вызвать `testConnection` — явное действие пользователя, никогда не автоматически); `ai-localization.generateProposals` принимает опциональный `connectionId`, который резолвится в `LocalizationProvider` через `resolveConnectionProvider` — домен `ai-localization` при этом не меняется (только одно новое опциональное поле зависимостей).
- **Read/Write:** реальный сетевой вызов возможен только через `openai_compatible`-адаптер, и только по явному действию (generate с выбранным connection, или Test connection). Ни модуль, ни его тесты никогда не вызывают `fetch` напрямую — только через инжектируемый `fetchImpl` (структурно проверено инвентарным тестом), поэтому «ноль реальных сетевых вызовов в автотестах» — не обещание, а проверяемое свойство.
- **Cost controls:** отдельный, более низкий лимит целей на вызов через реальный connection (50, против гораздо более высокого лимита mock-режима), timeout (по умолчанию 30с) + ограниченный retry (2 попытки) на transient-ошибки, token usage из ответа провайдера (если есть) прокидывается вверх, неизвестное usage/pricing репортится как `null`, никогда как ноль.
- **Важные ограничения безопасности:** ни один код-путь не возвращает сохранённый credential клиенту (`hasCredential: boolean` — единственное публичное поле); каждый новый роут — та же сессионная проверка, что и везде в кодовой базе; capability `"none"` (или необъявленная) — явная ошибка `capability_not_supported` до любого сетевого вызова, никогда не best-effort попытка; вывод реального provider'а валидируется теми же полевыми правилами, что и mock (переиспользуется `classifyAndValidateField`, не дублируется).
- **Чего нет:** OS-keychain; универсального agent-execution фреймворка; Codex как провайдера; выбранного платного вендора/модели.

### 2.9d Video Details (Studio-parity "Details" edit) — **IMPLEMENTED, module + API + Content-tab UI**

- **Статус:** первая в приложении настоящая (не dry-run-only) запись в YouTube из веб-интерфейса. Назначено владельцем проекта через Telegram 2026-09-20, после явного обсуждения рисков (существующий legacy write-путь `video-metadata.apply` молча трогает `localizations` при любой правке — нарушение AGENTS.md §F — и не имеет backup/diff/approval/audit) и явного выбора «Вариант 3: просмотр + реальное сохранение сразу в YouTube». Сначала (по запросу «модуль, который мы свободно сможем использовать позже, включая запись батчем... не перезаписывая всё остальное») реализован как переиспользуемый модуль с partial-patch семантикой без UI; **UI добавлен тем же днём** (BL-031, после «возвращаемся к тому что через контент можно открывать как в студио отдельные видео и менять их по необходимости») — клик по строке видео в Content открывает `src/components/video-details-panel.tsx`. **С 2026-09-21:** открывается не разворачиванием строки таблицы, а в общем модальном окне `src/components/video-detail-modal.tsx` (см. §2.9e) — по требованию владельца проекта («модуль, который может использоваться как в контенте, так и в языках»).
- **Ответственность:** partial-patch редактирование «родных» (не per-language) полей видео — `snippet.{title,description,tags,categoryId,defaultLanguage}`, `status.{privacyStatus,publishAt,license,embeddable,publicStatsViewable,selfDeclaredMadeForKids,containsSyntheticMedia}`, `recordingDetails.recordingDate`. Список полей подтверждён вживую по официальной документации YouTube Data API v3 (не по памяти, проверено 2026-09-20) — `defaultAudioLanguage`, `madeForKids`, весь `contentDetails.*` (включая возрастное/гео-ограничение) читаемы, но не входят в официальный список «settable properties» и намеренно исключены. **Никогда не трогает `localizations`** — структурно (адаптер даже не запрашивает и не может отправить эту часть), не только по соглашению (AGENTS.md §F).
- **Файлы:** `src/lib/video-details/{contracts,schemas,services,index}.ts`, `adapters/{youtube-api,store}.ts`; новые функции добавлены в тогдашний `src/lib/youtube.ts` — read-часть (`getVideoDetailsContext`) с 2026-09-22 живёт в `src/lib/youtube-read-gateway/data-api.ts`, write-часть (`applyVideoDetailsUpdate`, `pickWritableStatusFields`, `pickWritableRecordingDetailsFields`) — в `src/lib/youtube-write-gateway/index.ts` (см. §2.9g); UI — `src/components/video-details-panel.tsx`, интеграция в `content-manager.tsx`; тесты — `data-api.test.ts` (бывший `youtube.test.ts`, расширен), `video-details/{schemas,services}.test.ts` (22 теста в самом модуле, spec-driven по AGENTS.md §L, ожидания посчитаны вручную из официальной документации API, не скопированы с вывода реализации). `npm test`: 554 → 584 суммарно за обе части этой фичи (модуль+API, затем UI+conflict detection).
- **Точки входа:** `GET /api/channels/[channelId]/videos/[videoId]/details` (чистое чтение снапшота, без патча — то, что панель загружает при открытии, без audit-записи), `POST .../details/preview` (read-only diff по патчу, exempt от `src/proxy.ts`'s operation-lock gate, как и другие preview-роуты), `POST .../details/apply` (реальная запись, НЕ exempt — обычный гейт устройства). Все три — через `createVideoDetailsCore()`.
- **Conflict detection (добавлено вместе с UI, 2026-09-20):** `applyFieldsUpdate` принимает опциональный `expectedEtag` — если видео изменилось на YouTube между preview и apply (другой etag), запрос отклоняется с `video_details_conflict` вместо тихого применения патча поверх диффа, который оператор на самом деле не видел. UI всегда передаёт `expectedEtag`, полученный при последнем preview.
- **Partial-patch семантика (владелец: «поменять что-то одно... не перезаписывая всё остальное»):** патч — Zod-объект, где присутствуют только реально изменяемые поля; сервис читает СВЕЖЕЕ текущее состояние видео непосредственно перед записью, мёржит на него только присутствующие в патче поля, и отправляет в `videos.update` только те `part` (`snippet`/`status`/`recordingDetails`), которые патч реально затронул — патч, трогающий только `title`, никогда не отправляет `status` вообще (не может случайно сбросить его гонкой с другим писателем). YouTube API не поддерживает true field-level PATCH — каждая отправленная часть всегда полная (это и обеспечивает «не перезаписывая остальное» на уровне мёржа, не на уровне API).
- **Safety-модель (AGENTS.md §G, минимум для нового write-пути — identity/validation/backup/diff/approval/dry-run/audit/verification), как это реализовано здесь:**
  - identity check — переиспользован `write-context.assertWriteChannel` (не новый guardrail);
  - validation — Zod-схема, включая cross-field правило (`publishAt` требует `privacyStatus: "private"` в том же патче — иначе YouTube отклоняет запрос) и проверку «видео уже опубликовано» (сервисный уровень, по факту `before.publishAt`), а также точный лимит `description` в БАЙТАХ UTF-8 (не JS-символах — эмодзи/кириллица бьют лимит раньше 5000 символов) и бюджет `tags` с учётом запятых/кавычек;
  - backup — переиспользован (не задублирован) `src/lib/backup/` (см. ниже про генерализацию);
  - diff — `previewFieldsUpdate` возвращает before/proposed по каждому полю патча;
  - approval — отдельная явная кнопка «Save to YouTube» в `video-details-panel.tsx`, заблокированная, пока diff не просмотрен для ТОЧНО текущего патча (правка любого поля после preview снова блокирует кнопку — сравнение патчей по значению, не по факту клика);
  - dry-run — `previewFieldsUpdate`, полностью read-only, никогда не вызывает `videos.update`;
  - audit — **новая, отдельная таблица** `video_edit_audit_events` (см. ниже, почему не переиспользован `audit_events`);
  - verification — после записи выполняется НЕЗАВИСИМОЕ повторное чтение видео и сверка каждого патченного поля с тем, что было отправлено; расхождение — `update_failed`, а не тихий успех.
- **Обобщение `src/lib/backup/` (2026-09-20, документированная причина по AGENTS.md §D):** `captureBackup`'s параметр переименован `batchId` → `operationId` (чистое переименование, поведение не изменилось — это был не Batches-специфичный концепт, просто назывался по единственному на тот момент вызывающему); `BackupSnapshot` расширен до discriminated union (`{kind:"localization",...}` — прежнее поведение Batches, `{kind:"video_fields",...}` — новый вариант). Файловый layout (`<root>/<channelId>/<operationId>/<videoId>.metadata_before.json`, атомарный `wx`-флаг против перезаписи) не изменился.
- **Почему audit — ОТДЕЛЬНАЯ новая таблица, а не переиспользование `audit_events`:** `audit_events.batch_id`/`ledger_row_id` — `NOT NULL` FK на `batches`/`batch_ledger_rows`; одиночная правка видео вне Batch не имеет ни того, ни другого id, а ослабление этих `NOT NULL FK` — ровно тот «первый неаддитивный schema change», для которого `docs/decisions/0001-additive-idempotent-schema-strategy.md` требует отдельного ADR, и трогать проверенный Phase 5 audit trail ради этого не нужно. `video_edit_audit_events` (SCHEMA_MIGRATIONS версия 5) — без единого foreign key (осознанно, чтобы append-only лог переживал строку, которую описывает, и не добавлял новых edge для RISK-33's `foreign_keys=ON`), с обычными `channel_id`/`video_id` TEXT-колонками.
- **«Не ломая синхронизацию» (владелец, дословно):** после успешной записи `src/lib/video-details/adapters/store.ts`'s `refreshVideoFields` обновляет ТОЛЬКО те поля локального кэша `videos`, что реально существуют в этой таблице И были затронуты патчем (`title`/`description`/`privacyStatus`/`defaultLanguage`/`etag`) — переиспользуя `upsertVideos` (не новый write-путь), но сначала читая текущую строку через новый `getStoredVideo(channelId, videoId)`, чтобы не обнулить остальные колонки (`upsertVideos` всегда перезаписывает весь ряд целиком). Осознанно **не продвигает** `lastSyncedAt` — это поле означает «случился полный ресинк канала», чем точечная правка одного поля не является; продвижение сломало бы `content-manager.tsx`'s `AUTO_RESYNC_STALENESS_MS`-логику для полей, которые эта правка не трогала (`viewCount`/`commentCount`/`likeCount`, `thumbnails`, `existingLocalizations`).
- **Read/Write:** `previewFieldsUpdate` — только чтение (`YOUTUBE_READ_SCOPE`), ноль вызовов `videos.update`. `applyFieldsUpdate` — единственный настоящий write-путь этого модуля, `YOUTUBE_WRITE_SCOPE`, ровно один вызов `videos.update` на патч, всегда только с реально затронутыми `part`.
- **UI:** `src/components/video-details-panel.tsx`, открывается в общем модальном окне `src/components/video-detail-modal.tsx` (§2.9e) при клике по видео в `content-manager.tsx` (было — разворачивающаяся строка таблицы, заменено 2026-09-21). Основные поля (title/description/privacy/category/tags) видны всегда, остальные — за «Show more». «Preview changes» → diff → «Save to YouTube» становится активной, только если текущий патч байт-в-байт совпадает с тем, что был в последнем preview; любая правка формы после preview снова блокирует кнопку с подсказкой «Preview again before saving».
- **Live-верификация (2026-09-20):** и `preview`/`GET details`-роуты, и вся UI-логика блокировки кнопки «Save to YouTube» проверены вживую в браузере против реального видео канала «Tropico Jazz» — корректный diff, корректные ошибки валидации, корректное блокирование/разблокирование кнопки при правке поля до/после preview. **`apply` ни разу не был вызван ни агентом, ни через UI-клик по «Save to YouTube»** — по явной договорённости с владельцем проекта, первую настоящую запись инициирует только он сам.
- **Чего нет:** CLI/MCP-инструментов (модуль спроектирован для переиспользования, но провода пока нет — тот же RISK-04, что и у остальных доменов); batch-режима (владелец просил «включая запись батчем» — сервисная функция уже per-video и композируема в цикле, но интеграция с Batches' ledger/UI для множественного выбора видео не сделана в этом проходе); поддержки кастомных thumbnail/captions (отдельные YouTube API endpoint'ы, не `videos.update` — не в скоупе этой задачи); `paidProductPlacementDetails.hasPaidProductPlacement` (документация API неоднозначна насчёт возможности записи — не реализовано, требует эмпирической проверки перед добавлением).

### 2.9e Video detail popup (общий UI-модуль) — **IMPLEMENTED**

- **Добавлено 2026-09-21**, по прямому требованию владельца проекта (Telegram): «Первая правка функционала, может использоваться как в контенте, так и в языках и возможно будет нужна где-то ещё — поэтому можно делать модулем. При нажатии на видео, нам раскрывать информацию о нём не расширением строки, а выводить отдельный поп ап, почти на весь экран... Функционал/наполнение этого поп апа будет отличаться в зависимости от того в какой категории мы сейчас находимся.»
- **Ответственность:** `src/components/video-detail-modal.tsx` — чисто презентационная общая «оболочка» попапа (затемнение фона, near-fullscreen карточка, шапка с миниатюрой/заголовком/кнопкой закрытия, закрытие по клику на фон и по Escape). Не содержит никакой доменной логики и не знает, что именно отображает — контент передаётся вызывающей стороной через `children`, поэтому наполнение осознанно различается по вкладке (в Content — `VideoDetailsPanel`, в Languages — оригинал/локали/инлайн-генерация), как и просил владелец.
- **Используется:** `content-manager.tsx` (заменил разворачивающуюся строку `<tr>`) и `languages-manager.tsx` (аналогично, включая существующий инлайн-генератор AI-переводов, который переехал внутрь попапа без изменения своей внутренней логики). Оба компонента ищут открытое видео по полному (не отфильтрованному/не постраничному) списку, чтобы попап оставался корректным, даже если оператор поменяет поиск/сортировку/страницу, пока он открыт.
- **Точки входа:** нет собственного API — чисто клиентский UI-компонент.
- **Read/Write:** не меняет ничего в контракте данных — под капотом остались те же самые вызовы (`GET .../details`, `GET .../localizations/[videoId]`), что и раньше при разворачивании строки.
- **Правки после первого пользовательского тестирования (тот же день, Telegram):** (1) клик-и-протяжка (выделение текста) внутри попапа, отпущенная за его пределами, раньше засчитывалась как клик по фону и закрывала попап — исправлено: закрытие по клику на фон теперь требует, чтобы И `mousedown`, И `mouseup` произошли непосредственно на самом фоне, а не только `mouseup` (событие `click` в браузере срабатывает по месту `mouseup`, независимо от того, где началось выделение). (2) Если внутри попапа есть несохранённые изменения, попытка закрыть (фон/крестик/Escape) больше не закрывает его сразу — сначала показывается подтверждение «Discard unsaved changes?» (новый проп `hasUnsavedChanges`, `content-manager.tsx` получает его из нового `onDirtyChange`-колбэка `VideoDetailsPanel`, `languages-manager.tsx` — из наличия ещё не отправленных AI-предложений в открытой генерации для этого видео). (3) В Content поле Description увеличено (растягивается на всё оставшееся по высоте место, с собственным скроллом), Tags стало трёхстрочным textarea вместо однострочного поля.
- **Live-верификация (2026-09-21, оба захода):** обе вкладки проверены вживую в браузере против реального канала «Tropico Jazz» — открытие попапа, корректное содержимое (включая генерацию AI-перевода внутри попапа Languages), закрытие кликом по фону и клавишей Escape, устойчивость к клику-и-протяжке за пределы попапа, появление и работа диалога подтверждения при несохранённых изменениях в обеих вкладках. Ноль ошибок в консоли.
- **Чего нет:** ни один другой таб пока не использует этот модуль (Batches, Home и т.д.) — расширение переиспользования оставлено на будущее, когда/если появится такая потребность, как и предвидел владелец («возможно будет нужна где-то ещё»).
- **Общий `ConfirmDialog`, добавлено 2026-09-21 (постоянное правило владельца, `docs/DEVELOPMENT_PLAYBOOK.md` §6.9 п.9): никакие спроектированные нами попапы/предупреждения не должны рендериться нативными диалогами браузера (`window.confirm`/`alert`/`prompt`) — только через собственный UI в едином стиле.** Поводом послужил `window.confirm` в `languages-manager.tsx` (`handleRemoveTrackedLanguage`), который не только не соответствовал стилю приложения, но и (обнаружено в этот же день) блокировал CDP-управляемую браузерную автоматизацию, используемую для живой проверки. Новый `src/components/confirm-dialog.tsx` — единственная реализация (AGENTS.md §D), `fixed inset-0 z-[60]`, работает и как самостоятельный оверлей (Languages), и вложенным в уже открытый `VideoDetailModal` (Content/Languages discard-confirm). Оба существующих `window.confirm` в `languages-manager.tsx` и инлайновый discard-диалог в `video-detail-modal.tsx` переведены на этот компонент; в кодовой базе `grep` не находит больше ни одного `window.confirm/alert/prompt`. Live-проверено в браузере: обе ветки удаления языковой колонки (без реальных данных / с реальными данными) показывают стилизованный диалог вместо системного, without-real-data ветка отработала end-to-end (Cancel/Remove), with-real-data ветка показала полный текст предупреждения и корректно отменяется по Cancel.
- **Languages: двухколоночный редизайн попапа рассмотрения видео, добавлено 2026-09-21 (owner instruction, BL-044).** `VideoDetailModal` получил опциональный `widthClassName` (по умолчанию `max-w-5xl`, Content не затронут) — Languages передаёт `max-w-7xl`. Внутри (`renderVideoLocalizationDetail`): слева — оригинал (defaultLanguage видео, заголовок и описание каждый в своём боксе со своим `overflow-y-auto`); справа — дропдаун выбора языка перевода поверх такого же заголовок+описание бокса. Дропдаун сознательно построен на `overview.languages` (та же уния отслеживаемых/реальных языков, что и колонки таблицы) minus `video.defaultLanguage`, а не на `detail.locales` — значит можно выбрать язык, которого у видео ещё нет, и увидеть пустое состояние с кнопкой «Generate with AI for this language», которая открывает существующую генерацию с уже подставленным целевым языком (`startRowGenerate` + `setTargetLanguages`). Выбор языка сохраняется при переключении между видео (если он ещё валиден для нового видео), сбрасывается на первый доступный иначе. Live-проверено в браузере на канале Tropico Jazz: пустое состояние для "en"/"en-US" (на этом канале ни у одного видео нет кросс-языкового перевода — единственная реальная локализация видео всегда совпадает с его собственным defaultLanguage), заполненное состояние проверено временной подменой `fetch` на синтетические данные (без изменения реальной БД) — оба текстовых блока корректно скроллятся независимо; кнопка генерации по конкретному языку корректно предзаполняет форму.

### 2.9f Live-writes / Gate B toggle + MCP connection toggle (Settings tab) — **IMPLEMENTED**, does NOT close Gate B

- **Ответственность:** owner-controlled, persisted app-wide flags: «Live writes» (Gate B, owner instruction 2026-09-21, BL-045) и «MCP connection» (owner instruction 2026-09-21, BL-047 — переименован и инвертирован из прежнего «MCP restricted mode»: *«По началу MCP / агент от всего отключен и получит доступ только если я зайду в настройки и переключу этот тумблер»*).
- **Файлы:** `src/lib/db.ts` (`appSettings` таблица, SCHEMA_MIGRATIONS версия 7, `getLiveWritesEnabled`/`setLiveWritesEnabled`/`getMcpRestrictedModeEnabled`/`setMcpRestrictedModeEnabled`), `GET/POST /api/settings`, `src/components/live-writes-settings.tsx` (Settings tab), `src/lib/batches/adapters/write-executor.ts` (Layer 1), `src/lib/batches/adapters/write-executor.youtube.ts`'s `assertLiveWritesAuthorized` (Layer 2, теперь `async`, перечитывает настройку при каждом вызове вместо безусловного throw), новый роут `POST .../batches/[batchId]/execute`.
- **Двухслойный барьер сохранён, не заменён одним чтением:** слой 1 — `createLiveWriteExecutorIfEnabled` конструирует реальный `WriteExecutor` только если настройка включена (иначе `null`, роут отказывает до вызова `executeBatch`); слой 2 — сам executor независимо перечитывает ту же настройку прямо перед сетевым вызовом. Оба слоя проверены тестами (`write-executor.youtube.test.ts` — есть и off-, и on-case; `write-path-inventory.test.ts` обновлён с явным, единственным разрешённым исключением на новый роут, а не ослаблен целиком).
- **«Выключено по умолчанию каждую сессию»:** `initializeDatabase()` безусловно пишет `live_writes_enabled = false` при каждой инициализации БД (веб-сервер, MCP-сервер или CLI — что бы ни импортировало `db.ts` первым), поверх того, что было сохранено раньше. `POST /api/channels/[channelId]/batches` создаёт `dryRun: false` batch только если настройка включена в момент создания; иначе игнорирует запрошенное значение и всё равно создаёт dry-run (fail closed).
- **MCP connection (BL-047, 2026-09-21):** по умолчанию выключен — в этом состоянии `createMcpServer` не регистрирует НИ ОДНОГО инструмента вообще, включая read-only (`whoami`, `list`, `changeset_list`, `channel_sync` и т.д.), не только write/identity-инструменты, как было раньше у «restricted mode». Единая точка проверки — один булев параметр `connectionEnabled`, читаемый внутри `registerTool` для каждого инструмента, вместо прежнего списка-исключения на 8 конкретных write-инструментов. В отличие от Live writes, **сохраняется между перезапусками** — не сбрасывается на каждый boot (явное решение владельца проекта, разовая настройка). Env-переменная `MCP_RESTRICTED_MODE` удалена полностью — единственный способ включить это теперь тумблер в Settings. `startMcpServer()` читает настройку один раз при старте процесса и передаёт как явную опцию в `createMcpServer`. **Известное и явно задокументированное ограничение (не решённое, а озвученное):** уже открытое MCP-соединение не подхватывает изменение налету — набор инструментов MCP-сервера фиксируется при конструировании; новое значение применяется только при следующем переподключении/старте клиента.
- **UI:** `ConfirmDialog` перед включением Live writes (пояснение, что это слой 1 барьера, не сама запись); в Batches — чекбокс «Create as a real, live batch» при создании (виден только если тумблер включён) и кнопка «Execute (real write)» на уже созданном live-batch, тоже за `ConfirmDialog` с явным текстом (канал, число видео, число полей, напоминание что это реальная запись).
- **Не закрывает Gate B:** ничего из требуемой в acceptance-контракте живой валидации (реальный тестовый канал, реальный OAuth, сценарии AC-MERGE-01/05/GUARD-01/CONFLICT-01/RESUME-01/E2E-01) этим не выполнено — существование тумблера не является доказательством, что она проводилась. `docs/TECHNICAL_DEBT.md` RISK-09 остаётся `OPEN`, с явной пометкой «owner-gated, not unconditional» вместо «unconditional».
- **Live-проверено в браузере** на канале Tropico Jazz: включение тумблера показывает `ConfirmDialog`, после Save/Apply в Batches появляется чекбокс live-batch; создание batch с ним снятым (обычный dry-run путь) не регрессировало; кнопка Execute корректно не показывается для dry-run batch. Реальная отправка (клик Execute) сознательно НЕ протестирована в рамках этой задачи — это отдельное решение о канале/видео для теста, ещё не принятое владельцем.
- **605 → 607 тестов** (`write-executor.youtube.test.ts`); `npx tsc --noEmit`, `npm run lint`, `npm run build` чисты.

### 2.9g Единый шлюз записи в YouTube (`src/lib/youtube-write-gateway/`) — **IMPLEMENTED**

- **Ответственность:** owner instruction, 2026-09-21 (`docs/decisions/0005-youtube-write-gateway.md`) — «единственный путь как информация может попасть в 'релиз'». Триггер: разбор §2.9f выше показал, что тумблер Gate B защищал только Batches — у `apply`/`playlist_*` не было вообще никакого live-write барьера (только identity guardrail), хотя оба пути в итоге бьют в тот же `videos.update`.
- **Файлы:** `src/lib/youtube-write-gateway/{contracts,index}.ts` + `index.test.ts` + `gateway-inventory.test.ts`. Сюда переехали (без изменения поведения) все write-функции, ранее жившие в `src/lib/youtube.ts`: `applyVideoMetadataUpdate`, `applyVideoDetailsUpdate`, `pickWritable{Snippet,Status,RecordingDetails}Fields`, `createPlaylistForAuthenticated`, `updatePlaylistForAuthenticated`, `addVideoToPlaylistForAuthenticated`, `deletePlaylistItemById`, плюс новая `deletePlaylistForAuthenticated` (раньше — инлайн `youtube.playlists.delete` прямо в `playlist-management/adapters/youtube-api.ts`). `src/lib/youtube.ts` оставлен только для read-операций. Заодно удалена мёртвая, никем не вызываемая `updateVideoMetadataSafe` (апстримный код, попавший в проект тем же коммитом, что и `applyVideoMetadataUpdate`, — писала запрещённое §F поле `defaultLanguage`, но никогда не вызывалась).
- **Механическое, а не декларативное закрытие:** `gateway-inventory.test.ts` грепает весь `src/**` (кроме самого шлюза) на любой вызов вида `.<ресурс>.<мутирующий_метод>(` по широкому, не привязанному к сегодняшним семи вызовам списку ресурсов/методов YouTube Data API v3 — падает, если где-то ещё появится прямой вызов записи.
- **`assertLiveWritesAuthorized`** — единственная, общая реализация Gate B-проверки, переехавшая сюда из `write-executor.youtube.ts`. Сырые write-примитивы шлюза её НЕ вызывают сами (иначе сломались бы существующие тесты `performYoutubeWrite`, которые сознательно вызывают его напрямую без барьера) — вызывающая сторона обязана вызвать `assertLiveWritesAuthorized()` сама, непосредственно перед своим вызовом в шлюз. `write-executor.youtube.ts` (Batches, слой 2) теперь просто импортирует эту функцию вместо своей копии — поведение и тесты не изменились. Три ранее незащищённых адаптера (`video-metadata/adapters/youtube-api.ts`, `video-details/adapters/youtube-api.ts`, `playlist-management/adapters/youtube-api.ts`) теперь вызывают её сами — это и есть фактическое закрытие дыры.
- **Разделение ответственности сохранено:** резолюция credentials/`YOUTUBE_WRITE_SCOPE` осталась на уровне `services.ts` каждого доменного модуля (это отдельная забота от «какой файл делает сетевой вызов»); identity guardrail (`assertWriteChannel`) не тронут.
- **Поведенческое изменение (осознанное, это и есть цель задачи):** `apply` и `playlist_create/update/delete/add_videos/remove_videos` теперь тоже отказывают с `live_writes_disabled`, если тумблер «Live writes» выключен — раньше проверяли только identity канала.
- **617 тестов** (`npm test`); `npx tsc --noEmit`, `npm run lint`, `npm run build` чисты.

### 2.9h Automerge draft layer + multi-device sync (`src/lib/sync-gateway/change-drafts/`, `src/lib/sync-gateway/change-drafts-sync/`, CD1–CD7) — **IMPLEMENTED for the scope below**

- **Перенесено под единый модуль `src/lib/sync-gateway/` 2026-09-22** (owner instruction: "используем также правило 1 модуля и шлюза" — тот же single-gateway-принцип, что и `youtube-read-gateway`/`youtube-write-gateway`, `docs/decisions/0005`/`0007`). Это чистый перенос директорий (`change-drafts` и `change-drafts-sync` стали дочерними модулями `sync-gateway/`, ни одна строка их логики не менялась), M1 из `docs/roadmap/plans/FULL_DEVICE_HANDOFF_MIGRATION_PLAN.md`. Каждый внешний вызывающий код теперь импортирует только барель `@/lib/sync-gateway`, никогда дочерний модуль напрямую — механически проверяется `sync-gateway-inventory.test.ts`. Весь текст ниже описывает работу CD1–CD7 как она была построена (пути в прозе обновлены на новое расположение, поведение не изменилось).
- **Ответственность:** `src/lib/sync-gateway/change-drafts/` — один Automerge-документ на канал (`AUTOMERGE_MIGRATION_PLAN.md` §5), являющийся источником истины для `change_sets`/`changes` (см. §2.9's cutover-заметку); `src/lib/sync-gateway/change-drafts-sync/` (CD5, 2026-09-21) — файловый цикл синхронизации этого документа между устройствами через операторский Syncthing-каталог, переиспользующий существующий `src/lib/bootstrap-config/` (`deviceId`, `syncthingRootPath`) вместо создания второй концепции идентичности устройства.
- **Файлы `sync-gateway/change-drafts/`:** `{contracts,schemas,services,index}.ts`, `adapters/{automerge-store,sql-source,sql-projection,logger}.ts`. Ключевые операции сервиса: `createChangeSet`/`addChange`/`createChangeSetWithChanges` (batched, одна `Automerge.change`+`saveDocument` на всю пачку — атомарность, эквивалентная старому `db.transaction`), `updateProposedValue`/`setApprovalStatus`/`setChangeSetStatus`/`patchChange`/`bulkPatchChanges` (тоже batched), `mergeIncoming`, `listConflicts`, `exportBytes`, `migrateFromSql` (одноразовая, CD4). `adapters/automerge-store.ts`'s `saveDocumentBytes` пишет через temp-файл + атомарный `rename`, никогда напрямую — крах посреди записи не может оставить повреждённый, незагружаемый `.automerge`-файл.
- **`mergeIncoming`'s две эмпирически найденные и исправленные защиты от тихой порчи данных (обе — до появления любого production-кода, использующего множественные устройства):** (1) если у устройства ещё нет локального документа для канала, входящие байты пира принимаются напрямую (`Automerge.load`), а не мержатся в свежесозданный `emptyDocument()` — иначе `Automerge.merge()` между двумя независимо созданными документами терял ВЕСЬ документ пира в 55% прогонов (измерено эмпирически, не редкий краевой случай); (2) если у устройства УЖЕ есть документ, но он не имеет общей истории с входящим (например, оба независимо бутстрапнулись через `migrateFromSql` без предварительной синхронизации), `mergeIncoming` явно отказывает кодом `divergent_document_lineage` (сравнение `genesisChangeHash` — хеша самого первого change документа) вместо тихой потери данных — при таком сценарии потеря была ДЕТЕРМИНИРОВАННОЙ (100/100), а не вероятностной. Оба случая — постоянные регрессионные тесты в `services.test.ts`, не одноразовые скрипты. **RISK-46, частично исправлено 2026-09-22:** новый `discardLocalAndAdoptPeer({channelId, incomingBytes})` — явное, инициируемое человеком (никогда не автоматическое) "отбросить локальную копию, принять версию пира", с обязательным бэкапом отбрасываемого документа перед перезаписью (`adapters/discarded-backup-store.ts`, отдельный от `src/lib/backup/` из-за несовместимой per-video формы последнего). `sync-gateway/change-drafts-sync/services.ts`'s `adoptDivergentPeer({channelId, peerDeviceId})` заново читает АКТУАЛЬНЫЙ файл пира из папки синка в момент разрешения (а не кэшированные байты из старого цикла) и взаимно исключается с `runSyncCycle` (каждый дожидается in-flight вызова другого, не коалесируясь с ним). UI: кнопка "Discard my local copy, adopt this device's version" появляется только для `peersSkipped`-записей с `reason === "divergent_document_lineage"` активного канала, за `ConfirmDialog`. **Живьём найденный и исправленный баг:** `saveDocument`'s проекция только апсертит текущие строки документа, никогда не удаляет строку для changeSet/change, существовавшего ТОЛЬКО в отброшенном документе — без фикса такая строка навсегда оставалась видимой через `listChangeSets`, но кидала `not_found` при любом действии над ней. Исправлено новыми `deleteStoredChangeSet`/`deleteStoredChange` (`db.ts`) и `deleteChangeSet`/`deleteChange` (`SqlProjectionAdapter`) — `discardLocalAndAdoptPeer` считает разницу ключей отброшенного и принятого документов и удаляет только то, что реально исчезло. **Не исправлено:** вариант (a) — структурное предотвращение самого расхождения (это только уборка ПОСЛЕ того как оно уже произошло).
- **Файлы `sync-gateway/change-drafts-sync/`:** `{contracts,services,index}.ts`, `adapters/filesystem-transport.ts`. Каждое устройство пишет ТОЛЬКО свой собственный файл — `<syncthingRootPath ?? локальный fallback>/change-drafts/<channelId>/<deviceId>.automerge` (санитизированные имена), никогда общее на несколько устройств имя, поэтому generic same-filename-конфликт Syncthing для этих данных структурно не возникает; `listPeerFiles` строго фильтрует имена файлов (`^[a-zA-Z0-9_-]+\.automerge$`), исключая как собственный файл устройства, так и артефакты самого Syncthing (`.sync-conflict-*`, `.syncthing.*.tmp`) — ни один из них никогда не передаётся в `Automerge.load`. Один цикл синхронизации (`runSyncCycle`) для каждого известного локально канала: (a) экспортирует и пишет локальный документ (если он есть — канал без него просто пропускает push, это не ошибка), (b) читает все файлы пиров, кроме своего, (c) мержит каждый по отдельности, изолируя одну плохую/повреждённую/разошедшуюся запись от остальных (одна ошибка пира не прерывает синк остальных каналов/пиров — `peersSkipped`, отдельно от обычных `FieldConflict`). **Push происходит ДО pull-мержа** — смёрженное на этом цикле состояние публикуется пирам только на следующем цикле (сходится за 2 цикла, не за 1 — учитывать в UI CD6, кнопка "Sync now" не публикует мгновенно оба направления). **Single-flight guard**: `runSyncCycle()` — не более одного цикла одновременно на один экземпляр ядра; поэтому `createChangeDraftsSyncCoreForProduction()` в `index.ts` **мемоизирован** (единственный экземпляр на процесс), в отличие от `sync-gateway/change-drafts/index.ts`'s собственного stateless `createChangeDraftsCoreForProduction()` — иначе гвард был бы no-op при типичном для этого проекта паттерне «фабрика ядра внутри каждого обработчика роута».
- **CD6 (первый слайс, 2026-09-21) — проводка + минимальный UI.** Три новых API-роута: `POST /api/change-drafts/sync` (device-wide, запускает `runSyncCycle()` — гейтится `src/proxy.ts`'s device-availability lock как любая другая мутация, поскольку не в списке исключений), `GET /api/change-drafts/conflicts-summary` (device-wide, дешёвый — суммирует `listConflicts` по каждому локально известному каналу, БЕЗ записи), `GET /api/channels/[channelId]/change-drafts/conflicts` (per-channel, с `channelAccess.assertActiveChannel`, для детального списка конфликтов активного канала). Оба device-wide роута не берут `channelId` от клиента (список каналов — `listStoredChannels()`, серверная сторона), поэтому §6.6(b)'s active-channel проверка к ним не применяется — по аналогии с `src/app/api/device-handoff/**`. Вкладка "Device" переименована в **"Merge"** (`src/app/dashboard/page.tsx`); `src/components/device-handoff-panel.tsx` получил новую секцию "Change drafts sync" (кнопка "Sync now" + список текущих `FieldConflict` для активного канала, N-колоночный дифф по `valuesByActor`, аналог `change-set-review.tsx`'s 2-колоночного паттерна). Опрос с фронтенда (`page.tsx`) — два независимых интервала: дешёвый `conflicts-summary` каждые 20с (для бейджа в сайдбаре — badge живёт на самом nav-элементе "Merge" через новое поле `NavItem.badge`, не в отдельной header-иконке, которой пока нет ни для чего другого) и настоящий sync-цикл (`POST .../sync`) каждые 60с — оба независимы от того, какая вкладка открыта, обеспечивая AC-CRDT-07 (фоновая синхронизация без необходимости открывать Merge). **Явно НЕ реализовано в этом слайсе:** сама возможность "разрешить" конфликт (выбрать одну из конкурирующих версий) — UI только показывает конфликты, разрешение — следующий подслайс. **Живая проверка** (реальный канал Tropico Jazz, `claude-in-chrome`): "Sync now" реально экспортировал и записал `.automerge`-файл в настоящую Syncthing-папку на внешнем диске (`/Volumes/Samsung T9/YouTube/YT Manager Data/change-drafts/<channelId>/<deviceId>.automerge`), подтверждён валидный загружаемый Automerge-документ; бейдж в сайдбаре корректно показал "3" при временно застабленном ответе и корректно вернулся к пустому состоянию после перезагрузки; ноль ошибок в консоли. Настоящий CRDT-конфликт через UI живьём не демонстрировался (создание искусственного конкурентного редактирования того же документа оказалось не тривиальным без порчи реальных данных) — рендеринг блока конфликтов проверен только на пути "конфликтов нет"; сам механизм обнаружения конфликтов уже покрыт исчерпывающими модульными тестами (`sync-gateway/change-drafts/services.test.ts`'s AC-CRDT-02 и смежные).
- **Read/Write:** ноль вызовов YouTube API во всех трёх модулях; вся запись — в локальный `.automerge`-файл устройства и в файлы обмена в Syncthing-каталоге (если он настроен) либо в локальный fallback-каталог (`changeDraftsSyncFallbackDir`, `src/lib/platform-paths/`).
- **Важное ограничение безопасности:** `channelId` внутри Automerge-документа — удобство партиционирования данных, никогда не решение об авторизации; `write-context.assertWriteChannel` остаётся единственным источником истины для того, на какой канал разрешена запись (AC-CRDT-06, `AUTOMERGE_MIGRATION_PLAN.md` §5).
- **CD6 (второй подслайс, 2026-09-21) — разрешение конфликтов.** Новый метод `sync-gateway/change-drafts/services.ts`'s `resolveConflict({channelId, changeId, field, winningActorId})`: принимает `winningActorId`, НЕ сырое значение — реальное побеждающее значение переизвлекается сервером через `Automerge.getConflicts`, поэтому в документ никогда не может попасть значение, которое не было одним из уже легитимно конфликтующих (записанных через собственные валидированные пути этого модуля). Ограничен полями `proposedValue`/`approvalStatus`/`approvedValue`/`conflictStatus` — единственными, на которых два устройства реалистично могут конфликтовать (`baselineValue`/`changeType`/`validationStatus` выставляются один раз при создании и никогда не редактируются повторно через API этого модуля). Эмпирически подтверждено (пробный скрипт, не из документации Automerge): свежая запись `Automerge.change` поверх конфликтующего поля полностью снимает конфликт (`getConflicts` возвращает `undefined`) и переживает границу save/load. `POST /api/channels/[channelId]/change-drafts/conflicts` — новый обработчик в том же файле, что и `GET` для списка конфликтов; реальная мутация, гейтится `src/proxy.ts` как любая другая запись. UI: кнопка "Use this version" под каждым конкурирующим значением в `device-handoff-panel.tsx`, подтверждение через `ConfirmDialog` (BL-043's стандартное правило для необратимых действий), после подтверждения — обновление списка конфликтов. Поле, не входящее в разрешаемый набор, по-прежнему отображается (никогда не скрывается), но без кнопки действия и с явной пометкой "can't be resolved from this screen yet". **Живьём проверено против реального Tropico Jazz** (advisor: "near-free" раз сервер всё равно поднимался ради проверки исправлений push-изоляции): создан одноразовый тестовый Change Set, затем сконструирован НАСТОЯЩИЙ конкурентный конфликт (форк реального документа в поддельный peer-файл + независимое редактирование того же поля через настоящий production `change-drafts` core) — "Sync now" корректно показал "1 new conflict(s)", карточка конфликта отрисовала оба значения, клик "Use this version" по значению локального устройства реально записал выбранное значение (подтверждено прямым чтением файла на диске после клика), список конфликтов и бейдж в сайдбаре корректно очистились после следующего опроса. Ноль ошибок в консоли. Тестовые артефакты (поддельный peer-файл, тестовый Change Set) удалены/отклонены после проверки.
- **CD7 (2026-09-22) — подтверждено, весь план CD1–CD7 закрыт.** Полный аудит всех вызовов `db.ts`'s `createChangeSetWithChanges`/`updateStoredChangeSetStatus`/`updateStoredChange`/`bulkUpdateStoredChanges` подтвердил: единственный реальный writer этих таблиц — `sync-gateway/change-drafts/adapters/sql-projection.ts` (через отдельную пару `upsertStoredChangeSet`/`upsertStoredChange`); `src/lib/batches/`'s собственный `changeSetStore`-адаптер — только чтение (`getStoredChangeById`, для AC-BATCH-03). Найден и удалён мёртвый код в двух местах: `src/lib/changesets/adapters/store.ts`'s старый pre-cutover прямой SQL-адаптер `createChangeSetStoreAdapter()` (ни одного вызывающего места нигде в `src/`), и, как следствие (advisor-ревью поймал это до коммита), сами `db.ts`'s `updateStoredChangeSetStatus`/`updateStoredChange`/`bulkUpdateStoredChanges` — они стали недостижимы после первого удаления. `createChangeSetWithChanges` оставлена — её по-прежнему легитимно вызывает `sql-source.test.ts` для настоящего SQLite в read-тесте. `npm test` не изменился (690/690) после обоих раундов удаления.
- **Чего нет:** отдельная header-иконка для бейджа (пока живёт на nav-элементе сайдбара); структурное предотвращение независимого бутстрапа двух устройств (RISK-46's вариант (a) — только уборка после факта реализована, не предотвращение).

### 2.9i Phase 8 (Intelligence Foundation) — **IMPLEMENTED**, merged to `dev`, live re-consent done

- **Статус:** назначено 2026-09-22 (Telegram, дословно: «Приступить к полной реализации фазы 8»), реализовано на ветке `feature/phase-8-intelligence-foundation`, смёржено в `dev` в `6f75ccf` (одобрение владельца по `AGENTS.md` §K.2). Владелец ответил на оба требуемых §8 вопроса 2026-09-22 (Telegram msg 356, дословно в §10 плана): OAuth-скоуп одобрен, набор метрик расширен до всего, что покрывает `yt-analytics.readonly`. **Все 5 слайсов теперь реализованы:** 1 (OAuth-скоуп, BL-056), 2 (таблица, BL-055), 3 (Analytics-адаптер + домен-модуль, BL-057), 4 (ручной триггер + UI, BL-058), и ежедневный автосбор (BL-059, принятой тем же днём, отменяет изначальное ограничение плана «без scheduling»). **Проверено вживую на реальном канале "Tropico Jazz"** (`claude-in-chrome`, 2026-09-22, два захода): вкладка рендерится, ручной триггер и автотриггер на входе в дашборд оба корректно проходят всю цепочку до самой Analytics API и падают с ожидаемым `AUTH_SCOPE_INSUFFICIENT` (реальный сохранённый токен ещё не прошёл re-consent). Автотриггер при этом реально проставил `analyticsLastAutoCollectedAt` на реальном канале (задокументированный mark-then-run компромисс, не баг) -- после проверки сброшен обратно в NULL (одноразовый скрипт, не закоммичен), чтобы первая реальная загрузка владельца после re-consent не оказалась пропущена. Ноль ошибок в консоли в обоих проходах.
- **Файлы:** `src/lib/db.ts` (`videoMetricsDaily`, SCHEMA_MIGRATIONS версии 8-9, `channels.analyticsLastAutoCollectedAt`, `upsertVideoMetric`/`listVideoMetricsByVideo`/`listVideoMetricsByChannel`/`markAnalyticsAutoCollected`/`getAnalyticsSyncSettings`/`setAnalyticsSyncSettings`), `src/lib/youtube-read-gateway/analytics-api.ts` (низкоуровневый `google.youtubeAnalytics`-клиент, второй дочерний модуль read-шлюза, с 2026-09-22 — `assertAnalyticsReadsAuthorized`, тумблер в Settings), `src/lib/analytics/` (полный домен-модуль по паттерну §6.2 — `collectMetrics`/`listMetrics`/`runAutoCollectionIfStale`, `staleness.ts` пурая логика, см. `docs/ARCHITECTURE.md` §14.4/14.5/14.6), API-роуты `POST/GET /api/channels/[channelId]/analytics(/collect|/auto-collect)`, UI `src/components/analytics-manager.tsx` (заменил заглушку Studio-parity S6-stub, BL-017) и `src/components/analytics-sync-settings.tsx` (Settings tab).
- **Схема (аддитивно):** `channel_id`, `video_id`, `metric_date` (ISO-дата), `metric_name`, `metric_value` (**REAL**, изменено с `INTEGER` 2026-09-22 до мержа в `dev` — набор метрик теперь включает дробные, например `averageViewPercentage`; см. `docs/ARCHITECTURE.md` §14.7), `collected_at`; составной PRIMARY KEY `(video_id, metric_date, metric_name)`, индекс на `channel_id`. **`video_id` — foreign key на `videos.id`**, точно как в собственной DDL плана (`PHASE_8_PLAN.md` §5). Черновая версия этой таблицы ошибочно опускала FK, ссылаясь на прецедент `video_edit_audit_events` (§2.9d выше) и утверждая, что FK добавит новое ограничение порядка для `applySnapshotToDatabase`/`scrubDatabaseCopy` (`src/lib/snapshot/`) под дефолтным `foreign_keys=ON` (RISK-33) — независимое ревью показало, что это не так: обе функции уже оборачивают ВЕСЬ свой drop/replace-проход в `PRAGMA foreign_keys = OFF` ... `ON`, независимо от каких-либо связей, так что FK здесь не добавляет никакого нового ограничения порядка. В отличие от `video_edit_audit_events` (аудит-лог, который должен реально переживать описываемую строку), у этой таблицы такой необходимости нет — исправлено в `src/lib/db.ts` и здесь. `channelId` остаётся простой колонкой без FK (удобство фильтрации, не граница идентичности/авторизации). **Сознательно НЕ добавлена в `SNAPSHOT_TRANSFERRED_TABLES`** (`src/lib/snapshot/contracts.ts`) — собранные метрики остаются device-local и не переезжают при device handoff/snapshot import; принятое ограничение, параллельное собственному кейсу `rules.user_id` из RISK-33 (см. `docs/ARCHITECTURE.md` §14.7 для полного объяснения и явно задокументированного компромисса).
- **Read/Write:** чтение — только YouTube Analytics API (через `queryVideoAnalyticsReport`, `yt-analytics.readonly`), ни одного вызова Data API v3 изнутри этого модуля; запись — `upsertVideoMetric` в `video_metrics_daily` и `markAnalyticsAutoCollected` в `channels`, через два реальных триггера (`POST .../analytics/collect` вручную, `POST .../analytics/auto-collect` при входе в дашборд) — оба гейтятся `src/proxy.ts` как любая другая мутация.
- **Обновлено 2026-09-22 (после мерджа в `dev`):** владелец выполнил реальную переавторизацию и включил "YouTube Analytics API" в Google Cloud Console (второй, отдельный блокер, найденный только при первой реальной попытке). Реальный сбор отработал успешно: 28/28 видео, 2912 строк метрик, все 28 заявленных метрик подтверждены. Вопрос "один bulk-запрос на все видео вместо 28 отдельных" закрыт живой проверкой — API отклоняет такой запрос («The query is not supported»), нынешний дизайн (один вызов на видео) единственно возможный. `src/lib/youtube-analytics.ts` перенесён в `src/lib/youtube-read-gateway/analytics-api.ts` как второй дочерний модуль read-шлюза, с собственным тумблером "Analytics reads" (Settings tab), живьём проверенным.
- **Чего явно нет:** участие в snapshot/device handoff (см. выше, осознанно). См. `docs/ARCHITECTURE.md` §14 и `docs/roadmap/BACKLOG.md` для истории BL-055..BL-059.

### 2.9j Cloud connection (`src/lib/cloud-connection/`) — **IMPLEMENTED**, все 3 среза, в `dev`

- **Статус:** срез 1 из 3 (`docs/decisions/0008-cloud-connection.md`, owner instruction, 2026-09-22, Telegram: «Продолжаем тут же. Это нужно для работы полноценного счетчика»). Устанавливает единственный device-persistent Google Cloud OAuth grant, полностью независимый от per-channel YouTube логина (`users`) — цель: реальные числа по лимитам/использованию Google Cloud Quotas/Monitoring API поверх уже существующих счётчиков трафика шлюзов (§2.9k ниже). Этот модуль сам по себе (подключение/статус) не вызывает Cloud Quotas/Monitoring API — реальные вызовы делает §2.9l (срез 3, та же ветка, тоже уже смёржена). Все 3 среза смёржены в `dev` в `4be1f29` (см. `docs/ROADMAP_STATUS.md` BL-061/062/063).
- **Файлы:** `src/lib/cloud-connection/{contracts,schemas,crypto,services,redirect-uri,index}.ts`, `adapters/store.ts`; таблица `cloud_connection` в `src/lib/db.ts` (SCHEMA_MIGRATIONS версия 11, singleton-строка, `getStoredCloudConnection`/`upsertStoredCloudConnection`/`clearStoredCloudConnection`); API-роуты `GET /api/cloud-connection/{start,callback,status}`, `POST /api/cloud-connection/disconnect`; UI `src/components/cloud-connection-settings.tsx` (Settings tab, рядом с `ReadGatewaySettings`).
- **Скоуп:** `https://www.googleapis.com/auth/monitoring.read` **плюс `openid`/`email`** — нужны, чтобы `fetchGoogleIdentity` мог определить email подключённого аккаунта. Найдено живой проверкой: первая реальная попытка подключения падала с "Unable to fetch user identity from Google" до добавления этих двух скоупов, а callback-роут ошибочно пробрасывал такие ошибки дальше (голая страница 500) вместо аккуратного редиректа — исправлено вместе. **Скоуп сужен 2026-09-22** (владелец: «Если он нам действительно не нужен, то зачем нам его оставлять?») с изначального широкого `cloud-platform` (был нужен только для `quotaInfos.list`) до `monitoring.read`, когда §2.9l показал, что Cloud Quotas API вообще не требуется. Сужение уже выданного разрешения требует переподключения аккаунта — Google не понижает scope автоматически.
- **Шифрование:** access/refresh токен хранятся одним AES-256-GCM blob'ом (`ciphertext`/`iv`/`authTag`), ключ — `CLOUD_CONNECTION_ENCRYPTION_KEY`, **намеренно отдельный** от `AI_CONNECTIONS_ENCRYPTION_KEY` (`AGENTS.md` §M — независимость фичевых модулей). В отличие от `users` (RISK-07, plaintext), здесь plaintext сочтён неприемлемым из-за широты запрошенного скоупа — см. RISK-48.
- **Сознательно НЕ добавлена в `SNAPSHOT_TRANSFERRED_TABLES`** — device-local, та же логика, что и `users`/`ai_connection_credentials`: переустанавливается на каждом устройстве через собственный Connect-флоу, никогда не переезжает со snapshot/handoff.
- **Независимость от канала:** подключение/отключение здесь никогда не влияет на активный YouTube-канал, и наоборот — смена канала никогда не отзывает этот grant (собственно требование владельца).
- **Чего явно нет в этом срезе:** ротации/бэкапа ключа шифрования (RISK-48, тот же паттерн, что RISK-15). Реальные числа лимитов/использования — см. §2.9l ниже (срез 3, тоже уже реализован на этой ветке).

### 2.9k Счётчики трафика шлюзов (24ч) + суточный гейт свежести Analytics — **IMPLEMENTED**, в `dev`

- **Статус:** owner instruction, 2026-09-22, Telegram («Сколько было попыток пройти через шлюз за последние сутки... сколько попыток... увенчались успехом»). Реализовано на ветке `feature/gateway-traffic-counters` — по явному решению владельца объединена в одну фичу вместе с §2.9j (Cloud connection) и §2.9l, мердж всех трёх состоялся вместе в `4be1f29`.
- **Файлы:** `src/lib/db.ts` (`gatewayCallEvents`, SCHEMA_MIGRATIONS версия 10, скользящее окно 24ч через `recordGatewayCallOutcome`/`getGatewayTrafficLast24h`, ретеншен 7 дней); вызовы записи из `assertDataApiReadsAuthorized`/`assertAnalyticsReadsAuthorized` (`youtube-read-gateway`), `assertLiveWritesAuthorized` (`youtube-write-gateway`), и обёртки `registerTool()` в `src/mcp/server.ts` (только `allowed`, для MCP нет понятия «заблокировано» — незарегистрированный инструмент просто отсутствует). UI: `src/components/gateway-traffic-stats.tsx`, встроен в `ReadGatewaySettings`/`LiveWritesSettings`.
- **Суточный гейт свежести Analytics** (тот же owner instruction, тот же день): `collectMetrics` (`src/lib/analytics/services.ts`) отказывает в повторном сборе за тот же «день» (граница — `analyticsSyncLocalTime` из настроек) кодом `analytics_data_current`, без единого реального вызова API; UI показывает тикающий обратный отсчёт до следующего доступного обновления вместо ошибки. Гейт применяется **только** к Analytics — Data API v3 (`Sync now`) им не ограничен, для него — только счётчики трафика выше. Помечает канал собранным только ПОСЛЕ успешного `authResolver.resolve()` (не до) — иначе сбой авторизации блокировал бы кнопку на сутки без единого реального запроса (найдено независимым ревью, исправлено до мерджа).

### 2.9l Cloud Quotas — реальные числа лимита/использования (`src/lib/cloud-quotas/`) — **IMPLEMENTED**, в `dev`

- **Статус:** срез 3 из 3 плана §2.9j (owner instruction, 2026-09-22, Telegram: «сколько максимальная квота... сколько из неё уже использовано»). Зависит от §2.9j (Cloud connection) для credentials — `resolveCloudCredentials()`.
- **Ключевое открытие живой разведкой (2026-09-22):** Cloud Quotas API (`quotaInfos.list`) оказался НЕ НУЖЕН вообще (в проекте он выключен, 403 SERVICE_DISABLED) — уже работающий Cloud Monitoring API сам по себе отдаёт и лимит (`serviceruntime.googleapis.com/quota/limit`, `limit_name="defaultPerDayPerProject"`, GAUGE), и использование (`serviceruntime.googleapis.com/quota/rate/net_usage`, DELTA, суммируется за 24ч). Реальные подтверждённые цифры на момент разведки: Data API v3 (youtube.googleapis.com) — лимит 10 000/сутки; YouTube Analytics API (youtubeanalytics.googleapis.com) — лимит 100 000/сутки, отдельный пул (те же выводы, что в §2.9k про разделение шлюзов).
- **Файлы:** `src/lib/cloud-quotas/{contracts,services,index}.ts`, `adapters/monitoring-client.ts` (raw REST через `fetch`, **не** `googleapis`-клиент — тот же паттерн, что `auth.ts`'s `revokeGoogleToken`/`fetchGoogleIdentity`, поэтому `read-gateway-inventory.test.ts` этот модуль не затрагивает вообще). Google Cloud Project Number извлекается из уже существующего `GOOGLE_CLIENT_ID` (`deriveGoogleCloudProjectNumber`, формат `{project_number}-{random}.apps.googleusercontent.com` — по прямому указанию владельца: «он уже зашит в GOOGLE_CLIENT_ID»), отдельный проектный ID/номер в конфиге не требуется. Встроено в `GET /api/settings`'s `cloudQuotaStatus` (не в отдельный роут — те же компоненты уже читают `/api/settings`). UI: новый переиспользуемый `src/components/progress-bar.tsx` (генерик, без доменной логики) + тонкая обёртка `cloud-quota-progress.tsx`, встроены под тумблерами `ReadGatewaySettings` (Data API reads и Analytics reads по отдельности) и `LiveWritesSettings` (Live writes — тот же счётчик, что Data API reads, по инструкции владельца).
- **Найденный и исправленный баг (до коммита):** первая живая проверка показала `analytics: null` для Analytics API, хотя разведка ранее нашла реальные данные. Причина — `quota/limit` не является постоянным heartbeat-метриком: Google пишет свежую точку только при реальном трафике к сервису, а Analytics API вызывается редко; окно поиска в 1 час часто оказывалось пустым. Исправлено расширением окна до 25 часов (`fetchDailyQuotaLimit`).
- **Деградация:** любая ошибка одного сервиса (или отсутствие Cloud-подключения) даёт `null` для этого сервиса, не бросает исключение и не показывает выдуманный 0 — Settings-таб никогда не падает из-за внешнего API.
- **Свой счётчик трафика (добавлено 2026-09-22, owner instruction: «на него нам нужно повесить такие же счетчики, как на другие API. Он сделан по такой же схеме модуля / шлюза?»):** пятая категория `cloud_monitoring_reads` в `GatewayTrafficCategory` (`src/lib/db.ts`) — каждый реальный вызов из `adapters/monitoring-client.ts`'s единственной choke-point функции `callMonitoring` пишет событие (только `allowed`, тумблера включения/выключения для этой категории нет). UI — тот же `GatewayTrafficStats`, встроен в карточку "Google Cloud connection". **Механически зафиксировано** новым `cloud-quotas-inventory.test.ts` (строковая проверка на URL `https://monitoring.googleapis.com` вне `monitoring-client.ts`, не на голое имя хоста — `contracts.ts`/`services.ts` легитимно используют `"monitoring.googleapis.com"` как значение параметра/типа; `googleapis`-импорта тут вообще нет, поэтому `read-gateway-inventory.test.ts`'s собственная проверка сюда не применима).
- **Третья карточка квоты, поминутная, не суточная (добавлено 2026-09-22, owner: «Не вижу прогресс бара у Google Cloud connection»):** `CloudQuotaStatus` получил третье поле `monitoring` — тип `PerMinuteQuotaStatus` (`{limit, usedLastMinute}`), не `ServiceQuotaStatus`. Живой проверкой найдено: у Cloud Monitoring API в этом проекте нет дневного лимита вообще, только поминутные (`DefaultRequestsPerMinutePerUser` — фактически безлимитный; `QueryRequestsPerMinutePerProject` — реальные 6000/мин, подтверждено). `fetchPerMinuteQuotaLimit`/`fetchLatestMinuteUsage` — отдельные функции (не переиспользуют дневные), берут САМУЮ ПОСЛЕДНЮЮ 1-минутную DELTA-точку (не сумму за 24ч — иначе сравнение с поминутным лимитом всегда показывало бы "превышение"). UI — отдельный компонент `CloudQuotaProgressPerMinute` (не общий `CloudQuotaProgress`), с меткой "per minute" вместо "24h" и фиолетовым (indigo) акцентом вместо красного — тот же цвет, что у кнопки "Connect Google Cloud"/"Save / Apply" (owner instruction).
- **Чего явно нет:** отдельного роута/кэширования (реальный вызов Monitoring API идёт на каждую загрузку Settings-таба — 4 запроса, не оптимизировано); Cloud Quotas API вообще не используется (сознательно, не нужен).

### 2.9m Sync Gateway generalization: `automerge-core`, editorial-profile + ai-connections-catalog (M0-M4, M6) — **IMPLEMENTED**, в `dev`

- **Статус:** `docs/roadmap/plans/FULL_DEVICE_HANDOFF_MIGRATION_PLAN.md`, слайсы M0-M4 и M6 — все DONE, смержены в `dev` 2026-09-23 (`docs/ROADMAP_STATUS.md`, `docs/roadmap/BACKLOG.md` BL-064..BL-068). M5 (Category D: `batches`/`batch_ledger_rows`/`batch_attempts`/`audit_events`) отменён целиком, см. `docs/decisions/0009-defer-write-pipeline-sync-gateway-migration.md`.
- **Общий движок `src/lib/sync-gateway/automerge-core/`** (`{store,engine,transport,sync-runner,index}.ts`) — вынесенная из §2.9h's `change-drafts` document-shape-agnostic часть проверенной CD1-CD7 merge-логики, переиспользуемая новыми каталогами БЕЗ изменения уже отгруженного `change-drafts/`. Ключевой найденный и исправленный баг: `Automerge.merge(before, incoming)` мутирует свой первый аргумент на месте — сканирование "before"-состояния ПОСЛЕ вызова `merge` тихо теряло реальные конфликты; `mergeIncoming` теперь принимает callback `scanConflicts` и всегда сканирует "before" до вызова `Automerge.merge`.
- **`src/lib/sync-gateway/editorial-profile/` + `editorial-profile-sync/` (M3, часть 1)** — реальный per-channel документ (плоский объект, `version: number`, `0` = "никогда не сохранялся" — sentinel для bootstrap из существующей SQL-строки при первом сохранении, без потери данных). Cutover: `src/lib/ai-localization/adapters/profile-store.ts`.
- **`src/lib/sync-gateway/ai-connections-catalog/` + `ai-connections-catalog-sync/` (M3, часть 2)** — ЕДИНЫЙ глобальный документ (не per-channel — у `ai_connections` нет `channel_id`), ключ — константа `GLOBAL_DOCUMENT_KEY = "global"`, переиспользующий тот же generic per-channel `sync-runner` через одноэлементный `listChannelIds: async () => ["global"]`. Cutover: `src/lib/ai-connections/adapters/store.ts`. **Живьём найденный и дважды исправленный orphan-row баг** (тот же класс, что RISK-46 в `change-drafts`): при удалении соединения пиром "touched ids" для SQL-проекции нужно считать объединением ключей ДО и ПОСЛЕ merge, а не только "после" — иначе `deleteConnection` для удалённого пиром соединения никогда не вызывается. Исправлено в `mergeIncoming` И в `discardLocalAndAdoptPeer` независимо (оба места ранее имели один и тот же пробел).
- **M4 — `ai_localization_generation_provenance` внутри `change-drafts`.** Аддитивное опциональное поле `provenance?: Record<string, DraftProvenance>` на существующем per-channel документе (§2.9h) — документ, сохранённый до M4, просто не имеет этого ключа; каждое чтение защищено (`if (!draft.provenance) draft.provenance = {}`). **Найденный и дважды исправленный (независимым ревью, затем повторной проверкой через advisor-инструмент) баг в `discardLocalAndAdoptPeer`:** `ai_localization_generation_provenance.change_set_id` — реальный `NOT NULL UNIQUE REFERENCES change_sets(id)` без `ON DELETE`, при `foreign_keys=ON`; удаление отброшенного change set с provenance-строкой падало на constraint, и это тихо прерывало очистку ВСЕХ остальных строк того же discard целиком (один `try/catch` на оба цикла). Первый фикс scoped очистку provenance по родительскому change set — тоже неполно (change set может пережить discard у peer'а без provenance для него). Финально: provenance диффится по СОБСТВЕННОМУ набору ключей, как `changes`, независимо от судьбы родительского change set; каждая отдельная попытка удаления — в своём `try/catch`.
- **M6 (2026-09-23) — сужение старого whole-DB `snapshot`/`device-handoff`, а не удаление, см. `docs/ARCHITECTURE.md` §13.5.** `SNAPSHOT_TRANSFERRED_TABLES` теперь = `schema_meta` + 4 таблицы Category D; `change_sets`/`changes`/`channel_editorial_profiles`/`ai_localization_generation_provenance`/`ai_connections` больше не путешествуют этим механизмом.
- **Редизайн вкладки Merge (2026-09-23, owner instruction) — conflicts/divergent-peer parity для всех 3 семейств + постоянный статус синка. `feature/merge-tab-redesign`, смержено в `dev` (`864c0a7`).** До этого `resolveConflict`/`discardLocalAndAdoptPeer`-UI существовал только для `change-drafts`, хотя серверная логика была одинаковой у всех трёх семейств.
  - `automerge-core/sync-runner.ts` получил обобщённый `adoptDivergentPeer` (перенесён из `change-drafts-sync`'s собственной bespoke реализации, `AGENTS.md` §M) — `editorial-profile`/`ai-connections-catalog` получили его автоматически через общий `createSyncRunner`; `change-drafts-sync` оставлен нетронутым (`AGENTS.md` §D).
  - `editorial-profile/services.ts` и `ai-connections-catalog/services.ts` получили `resolveConflict`, зеркалирующий `change-drafts`'s собственную реализацию (`winningActorId`, значение переизвлекается из `Automerge.getConflicts`, никогда не берётся сырым от клиента).
  - Новые роуты: `GET/POST .../channels/[channelId]/editorial-profile/conflicts`, `POST .../editorial-profile/adopt-peer`, `GET/POST /api/ai-connections/conflicts`, `POST /api/ai-connections/adopt-peer` (последние два — device-wide, без `channelId`).
  - Новая таблица `sync_family_status` (`db.ts`, SCHEMA_MIGRATIONS v12) — постоянный last-sync-outcome (ok/error/когда) на каждое из 3 семейств, в отличие от одноразового абзаца, пропадающего при перезагрузке страницы. `GET /api/change-drafts/sync-status` (read-only); `POST /api/change-drafts/sync` пишет в эту таблицу после каждого из 3 циклов независимо (падение одного семейства не мешает записать статус остальных).
  - `device-handoff-panel.tsx` (вкладка Merge) переписан: постоянная секция "Sync status" (по строке на семейство), единый список "Conflicts" по всем 3 семействам (resolve уходит на правильный роут в зависимости от семейства), единые peer-skipped/divergent-lineage предупреждения, "Device handoff" — отдельная визуально выделенная карточка (владелец: "отдельная карточка") для явной передачи владения 4 таблицами конвейера записи.
  - Живьём проверено (владелец: "делаем можешь для демонстрации сделать тестовые записи") — реальные конфликты созданы напрямую через production core (Automerge fork+merge в памяти, без файлов-подделок на диске), оба показаны и разрешены через реальный UI, тестовые данные убраны через реальные UI-действия сразу после проверки.

### 2.9n Studio-Parity S6b (Analytics "Overview") + Home dashboard cards — **IMPLEMENTED**, в `dev`

- **Статус:** owner instruction, Telegram 2026-09-23 («Давай поработаем над окнами Home и Analytics. Проверь живой Dashboard YT Studio и сделай весь такой же функционал отображения статистики у нас»), `docs/roadmap/BACKLOG.md` BL-072. Смержено в `dev` в `111adf7` (owner approval, Telegram «Ок»). Живой research против реального YT Studio (`claude-in-chrome`, канал "Rural Japan Music") плюс живой probe против реального Analytics API (одноразовый диагностический роут, не закоммичен, тот же приём что BL-057).
- **Ключевая находка:** канальный (без фильтра по видео) `dimensions=day`-запрос ПРИНИМАЕТСЯ реальным API (подтверждено на "Tropico Jazz") — новая функция `queryChannelAnalyticsReport` (`youtube-read-gateway/analytics-api.ts`, второй экспорт рядом с `queryVideoAnalyticsReport`, общий парсер вынесен в `parseDayDimensionReport`). Отдельно живьём подтверждено, что `impressions`/`impressionClickThroughRate` (Studio's "Показы значков"/CTR) API ОТКЛОНЯЕТ как неизвестные идентификаторы — это не `annotation*`/`card*` legacy-метрики (те везде нули с 2019), а отдельная, реально недоступная через публичный API вещь. Ни один виджет CTR/impressions нигде не строится.
- **Файлы:** `src/lib/analytics/period.ts` (пурая арифметика периода — `computePreviousPeriod`/`computePercentChange`, свои тесты), `getChannelOverview` (`services.ts` — два вызова `queryChannelAnalyticsReport`, текущий период + предыдущий той же длины, суммирование в `currentTotals`/`previousTotals`, никакой новой таблицы/схемы), `GET .../analytics/overview` роут. `channel-info` роут аддитивно возвращает `subscriberCount` (то же `channels.list` вызов, что уже был). UI: `channel-overview-panel.tsx` (карточки/график/топ-контент), `analytics-tab.tsx` (оборачивает уже существующий `analytics-manager.tsx` нетронутым, за раскрывашкой "Show raw collected data"), `home-dashboard-panel.tsx` (Latest video performance / Published videos / Channel analytics — из уже синхронизированных `videos`/собранных `video_metrics_daily`, без единого нового API-вызова к YouTube), общий `metric-delta.tsx` (`AGENTS.md` §M — один компонент дельты, не по копии на Home и Analytics), `analytics-line-chart.tsx` (inline SVG, без новой npm-зависимости — в `package.json` не было готовой chart-библиотеки).
- **Осознанно НЕ построено:** Comments-фид и Recent-subscribers-фид (та же нерешённая feasibility-задача, что уже стояла в `STUDIO_PARITY_PLAN.md` §5 п.4 — публичный Data API v3 много лет не отдаёт список подписчиков по приватности), Realtime 48-часовая панель (публичный API не даёт часовой грануляности, только дни).
- **`npm test` 918/918, `lint`/`build` чисто.** Живая проверка в браузере (переключатель периода, hover-тултип графика, раскрывашка raw-данных, переход Home → Content) — ноль ошибок в консоли.

### 2.9o Phase 8 follow-up: MCP/CLI аналитика для агентов + диагностика пробелов в данных — **IMPLEMENTED**, в `dev`

- **Статус:** owner instruction, Telegram 2026-09-23 («Ок, доделай эти функции», после списка недостающих возможностей Phase 8 из `docs/roadmap/FUTURE_PHASES.md` §4). `docs/roadmap/BACKLOG.md` BL-073 (слайс 1, machine-readable analytics for agents — смержено в `dev` в `7da72fa`) и BL-074 (слайс 2, data-quality diagnostics — смержено в `dev` после owner approval «ок, если по слайсу 2 все работы выполнены»).
- **BL-073 — MCP/CLI чтение аналитики:** `analytics_list`/`analytics_overview` (MCP) и `analytics list`/`analytics overview` (CLI), переиспользующие `analyticsCore.listMetrics`/`getChannelOverview` без изменений. `listMetrics` получил опциональные `startDate`/`endDate`/`videoId`/`metricNames` фильтры (аддитивно), чтобы ответ агенту не всегда содержал каждую собранную строку.
- **BL-074 — диагностика пробелов, `getDataQualityReport`:** новая таблица `analytics_collection_runs` (SCHEMA_MIGRATIONS версия 13) — append-only лог каждого запуска `collectMetrics` (запрошенный диапазон, число видео, `skippedVideoIds`). Нужна, потому что живьём подтверждено: Analytics API молча ОПУСКАЕТ день с нулевой активностью из ответа (не отдаёт как строку со значением 0) — отсутствие строки в `video_metrics_daily` само по себе не отличает "никогда не собирали" от "собрали, активности не было". Полный технический разбор — `docs/ARCHITECTURE.md` §14.9. Экспонировано как `GET .../analytics/data-quality`, MCP-инструмент `analytics_data_quality`, CLI `analytics data-quality` — все read-only, не гейтятся. Небольшая UI-аннотация в `channel-overview-panel.tsx` (жёлтая карточка "Data quality", показывается только когда есть реальные пробелы/сбои).
- **Независимое ревью (2 раунда для BL-073, 2 раунда для BL-074) нашло и исправило реальные баги** — детали в `docs/roadmap/BACKLOG.md` BL-073/BL-074.

### 2.9p Phase 8 follow-up, слайс 3/4: сравнение видео по сопоставимому возрасту (`getComparableAgeComparison`) — **IMPLEMENTED**, в `dev`

- **Статус:** та же ветка задания, что §2.9o («Ок, доделай эти функции»), `docs/roadmap/BACKLOG.md` BL-076. Продолжение того же списка недостающих возможностей Phase 8 из `docs/roadmap/FUTURE_PHASES.md` §4 ("comparing videos at comparable ages").
- **Что делает:** для 2-10 `videoIds` одного канала выравнивает уже собранные строки `video_metrics_daily` по числу дней с момента публикации (а не по календарной дате), так что видео с разными датами публикации можно сравнивать в одной и той же точке их собственного жизненного цикла — как «Compare videos» в реальной YouTube Studio.
- **Pacific-Time выравнивание дня 0:** `metricDate` — это Pacific-Time календарный день (собственная конвенция Analytics API), а `publishedAt` — UTC-момент из Data API v3. День 0 вычисляется как Pacific-Time календарная дата `publishedAt` (`toPacificCalendarDate`, `src/lib/analytics/comparable-age.ts`, чистый модуль без I/O), а не UTC-дата — иначе видео, опубликованное вскоре после полуночи UTC, сместилось бы на целый день относительно остальных сравниваемых видео. Полный разбор — `docs/ARCHITECTURE.md` §14.10.
- **Только аддитивные метрики:** явный allowlist `CUMULATIVE_COMPARISON_METRIC_NAMES`, исключающий every ratio/average-метрику из `ANALYTICS_METRIC_NAMES` (`averageViewDuration` и т.п.) — суммировать долю/среднее по дням бессмысленно.
- **Пропущенный день — это "неизвестно", а не выдуманный ноль.** Сознательно НЕ переиспользует `analytics_collection_runs` для вывода "известного нуля" на уровне отдельного видео (та же нерешённая проблема "было ли это видео реально запрошено в конкретном прошлом запуске", что уже описана в §2.9o/`docs/ARCHITECTURE.md` §14.9/§14.10) — реальная строка `video_metrics_daily` это единственный факт "известно"; кумулятивная сумма останавливается на последнем непрерывном известном дне от дня 0, никогда не перескакивает пробел.
- **Реальное ограничение покрытия данных (подтверждено прямым запросом к БД двух реальных каналов перед проектированием):** суточный автосбор покрывает только последние ~7 календарных дней за запуск, а не «первые 7 дней жизни видео» — видео старше ~1-2 недель до начала регулярного сбора обычно не имеет данных за дни 0-7. Механизм бэкфилла сознательно НЕ добавлен (расширение окна автосбора или бэкфилл — вне рамок этого слайса, ежесуточный gate свежести — явное правило владельца).
- **Никаких рейтингов/"outperforming"-формулировок/итогового вердикта** — только сырые факты (`docs/roadmap/FUTURE_PHASES.md` §4).
- **Файлы:** `src/lib/analytics/comparable-age.ts` (пурая арифметика + allowlist, свои тесты), `getComparableAgeComparison` (`services.ts`), `GET .../analytics/comparable-age`, MCP-инструмент `analytics_comparable_age`, CLI `analytics comparable-age` — все read-only, не гейтятся, тот же паттерн что `analytics_data_quality`.

### 2.9q Phase 8 follow-up, слайс 4/4: еженедельные аналитические отчёты (`runWeeklyReportIfDue`/`listWeeklyReports`/`getWeeklyReport`) — **IMPLEMENTED**, в `dev`

- **Статус:** та же ветка задания, что §2.9o/§2.9p («Ок, доделай эти функции»), `docs/roadmap/BACKLOG.md` BL-077. Последний из 4 слайсов Phase 8 follow-up, `docs/roadmap/FUTURE_PHASES.md` §4 ("analytical reports and weekly channel reviews").
- **Что делает:** замороженный, воспроизводимый снапшот по каналу за каждую неделю (понедельник-воскресенье), полностью посчитанный из уже собранных локальных `video_metrics_daily` — никогда живой запрос к YouTube.
- **Триггер:** owner instruction, Telegram 2026-09-23 («Давай завяжемся на то же время что мы выбираем в настройках — 12-05 сейчас по понедельникам») — переиспользует ту же пару `localTime`/`timezone` из Settings, что уже использует суточный автосбор (§14.6), отдельной настройки для отчётов нет. `computeDueReportWeek` (`src/lib/analytics/weekly-report.ts`, чистая функция) определяет единственную наиболее недавно завершившуюся неделю, чей граничный момент (понедельник, `localTime`, в локальном часовом поясе оператора) уже прошёл — тем же приёмом сравнения зонированных строк, что `isAnalyticsCollectionStale`. Пропущенные недели никогда не досчитываются задним числом (та же философия "no backfill", что уже в §2.9p).
- **`status: "final"` vs `"provisional"` (совет советника, 2026-09-23):** триггер по локальному времени оператора не совпадает ни с лагом отчётности Analytics API (1-2 дня), ни с Pacific-Time нумерацией дней `video_metrics_daily` — для оператора вне Pacific Time последний день только что завершившейся недели может быть ещё не собран в момент срабатывания. Отчёт помечается `"provisional"`, если хоть одна дата ЕГО СОБСТВЕННОЙ недели ещё не покрыта/слишком свежая (`computeDataQualityReport` за эту неделю); следующая загрузка дашборда перегенерирует (заменяет) provisional-отчёт за ту же неделю, как только данные дособерутся, но `"final"`-отчёт больше никогда не трогается.
- **`percentChange` — `null` целиком, если ЛЮБАЯ из двух недель не покрыта полностью** — иначе сравнение измеряло бы полноту сбора данных, а не реальное изменение (`FUTURE_PHASES.md` §4 "avoid unsupported conclusions from small samples") — именно та ситуация, в которой сейчас реально находится канал "Tropico Jazz" (§2.9o).
- **`syncedVideoTotals`, а не «тотал по каналу»:** явно поименовано и задокументировано (`SYNCED_VIDEO_TOTALS_METRIC_DEFINITIONS`, встроено в каждый сохранённый отчёт) как сумма по уже синхронизированным видео — то же самое ограничение недосчёта, что уже задокументировано в `docs/ARCHITECTURE.md` §14.8.
- **Provenance:** каждый сохранённый отчёт несёт `reportFormatVersion`, `generatedAt`, `source`, `metricDefinitions`, и при каждом ЧТЕНИИ (не только записи) заново проверяется строгой zod-схемой — повреждённая строка в БД падает громко (`validation_failed`), а не отдаёт частичный отчёт.
- **Схема:** `analytics_weekly_reports` (SCHEMA_MIGRATIONS версия 14), один ряд на `(channel_id, week_start_date)` (UNIQUE-индекс), `report_json` хранит весь сериализованный отчёт. Сознательно не в `SNAPSHOT_TRANSFERRED_TABLES` (производные, пересчитываемые данные).
- **Триггер-роут:** `POST .../analytics/weekly-reports/generate-if-due`, вызывается раз на монтирование дашборда, ПОСЛЕ уже существующего auto-collect-триггера (через `.finally()`), гейтится `src/proxy.ts` как любая мутирующая POST.
- **Чтение (read-only, не гейтится, БЕЗ MCP/CLI-инструмента для генерации по запросу — та же логика исключения, что у `collectMetrics`):** `GET .../analytics/weekly-reports`, `GET .../analytics/weekly-reports/[weekStartDate]`, MCP `analytics_weekly_reports_list`/`analytics_weekly_report_get`, CLI `analytics weekly-reports`/`analytics weekly-report-get`.
- **Известный, отслеженный дубль (`docs/TECHNICAL_DEBT.md` RISK-50):** ранжирование `topContent` — вторая реализация той же агрегации, что уже делает клиентский `fetchTopContent` в `channel-overview-panel.tsx` — не объединено в этом слайсе.

### 2.9r Agent Operations Interface (Phase 7, расширенный owner-спекой 2026-09-23) — **CODE-COMPLETE, смержено в `dev`** (`cc6a431`, 2026-09-25; актуальный статус по слайсам — см. `docs/AGENT_OPERATIONS_INTERFACE.md` §7, не здесь в заголовке)

- **Статус:** owner instruction, Telegram 2026-09-23 (полная 34-секционная спецификация "Phase 7 — Agent Operations Interface for Codex"), явно санкционирован дизайн и поэтапная реализация без промежуточных согласований — только финальный мердж в `dev` требует явного "ок". Полный технический дизайн и статус по слайсам — `docs/AGENT_OPERATIONS_INTERFACE.md` (не дублируется здесь). Работа ведётся на одной ветке `feature/phase-7-agent-operations-interface`, накапливая слайсы A→J; отдельного мерджа в dev на каждый слайс не будет.
- **Слайс A (contracts + capability/version discovery) — IMPLEMENTED.** Новый модуль `src/lib/agent-operations/` (contracts/schemas/services/index — паттерн §6.2). MCP `agent_get_capabilities`, CLI `agent capabilities`, `GET /api/agent-operations/capabilities`. Новые коды ошибок в едином `DomainErrorCode`/HTTP-статус-маппере (§6 дизайн-документа). Полный дизайн — `docs/AGENT_OPERATIONS_INTERFACE.md` §4.
- **Слайс B (read-only channel/video context) — IMPLEMENTED.** `getChannelContext`/`getVideoContext`, читают только уже синхронизированные локальные данные; channel-scoping на уровне MCP/CLI (сервис сам ничего не проверяет, как и `ai-localization`/`changesets`). MCP `agent_get_channel_context`/`agent_get_video_context`, CLI `agent channel-context`/`agent video-context`. Полный дизайн — `docs/AGENT_OPERATIONS_INTERFACE.md` §4a.
- **Слайс C (analytics interface) — IMPLEMENTED.** `queryChannelAnalytics`/`queryVideoAnalytics` — тонкие обёртки над уже существующими `analyticsCore.getChannelOverview`/`listMetrics`, дополненные metric definitions/period/filters/freshness. `credentialRef` резолвится на уровне MCP/CLI (как `analytics_list`/`analytics_overview`, не как слайс B). MCP `agent_query_channel_analytics`/`agent_query_video_analytics`, CLI `agent channel-analytics`/`agent video-analytics`. Полный дизайн — `docs/AGENT_OPERATIONS_INTERFACE.md` §4b.
- **Слайс D (creative asset catalog) — IMPLEMENTED.** Новый модуль `src/lib/asset-catalog/` (contracts/schemas/services/adapters/index — паттерн §6.2), новая таблица `creative_assets` (SCHEMA_MIGRATIONS версия 15) — портативный каталог метаданных для уже существующих production-файлов (owner-спека §15), никогда не читающий сам `referenceValue`. Напрямую заполняется только через операторскую CLI-команду `asset register` (сам `asset register` не agent-capability -- owner-спека §25 называет только `list_assets`/`get_asset_context` READ-возможностями этого домена); слайс G2 ниже добавляет косвенный, agent-callable способ заполнения (через Content Proposal, ограниченный `url`/`external_artifact_id`). Channel-scoping — как у слайса B (без `credentialRef`, проверка на уровне MCP/CLI). Не путешествует со snapshot/device handoff (`docs/TECHNICAL_DEBT.md` RISK-52). Полный дизайн — `docs/AGENT_OPERATIONS_INTERFACE.md` §4c.
- **Слайс E (agent draft/proposal provenance) — PARTIAL.** `getGenerationProvenance` делегирует уже существующему механизму `ai-localization` (без нового хранилища); добавляет ранее отсутствовавший MCP/CLI-доступ к уже работавшему HTTP-роуту. MCP `agent_get_generation_provenance`, CLI `agent get-generation-provenance`. "Originating task" (owner-спека §22) всё ещё не записывается (RISK-57 — "operation type" из той же owner-спеки уже структурно закрыт слайсом G, у которого своя отдельная таблица). Полный дизайн — `docs/AGENT_OPERATIONS_INTERFACE.md` §4d.
- **Слайс F (bulk localization integration — evidence/rationale/identity) — PARTIAL.** Расширяет существующий `ai_localization_create_change_set` (MCP/CLI/Web; `ai_localization_generate` не тронут; без нового MCP-tool/agent-capability): опциональные `evidence`/`rationale` в теле запроса (owner-спека §12/§13), плюс серверное проставление `createdVia`/`agentApiVersion` на каждой из трёх точек вызова — закрывает RISK-54. `evidence`/`rationale` записываются один раз на Change Set, а не на каждое предложение (RISK-55, известное ограничение). Попутно исправлен баг самопроецирования (`onConflictDoNothing` → `onConflictDoUpdate` в `setStoredGenerationProvenanceRow`). Полный дизайн — `docs/AGENT_OPERATIONS_INTERFACE.md` §4e.
- **Слайс G (Content Proposal / external artifact registration) — CLOSED.** Новый модуль `src/lib/content-proposals/` (contracts/schemas/services/adapters/index — паттерн §6.2), новая таблица `content_proposals` (SCHEMA_MIGRATIONS версия 17). Create/get/list реализованы (write-once, без approval-механизма — owner-спека его не описывает). Попутно вынесены `CreatedVia`/`EvidenceReference` в новый `src/lib/shared-provenance/` (AGENTS.md §M — общая лексика для несвязанных доменных модулей) и исправлен RISK-53 (agent-operations/schemas.ts дублировал domain/dataDomains-перечисления вместо импорта из contracts.ts, `docs/TECHNICAL_DEBT.md`). MCP `agent_create_content_proposal`/`agent_get_content_proposal`/`agent_list_content_proposals`, CLI `agent create-content-proposal`/`agent get-content-proposal`/`agent list-content-proposals`. **G2 (owner-спека §19, возврат внешне произведённых артефактов) — реализован**: новая таблица-связка `content_proposal_artifacts` (SCHEMA_MIGRATIONS версия 18, FK на `content_proposals`/`creative_assets`), принадлежит `content-proposals`, а не `asset-catalog` (AGENTS.md §M); сам asset-ряд создаётся через уже существующий `assetCatalogCore.registerAsset` (AGENTS.md §D, без параллельного пути вставки). Агентский `referenceKind` ограничен `url`/`external_artifact_id` — никогда `local_path` (owner-спека §17: агент должен получать только явно каталогизированные/авторизованные ассеты; `local_path` остаётся только через операторскую CLI-команду `asset register`); сам enum — только ярлык, поэтому схема дополнительно структурно проверяет, что значение с ярлыком `url` — настоящий http(s) URL, а не просто помечено как таковое (независимое ревью нашло этот пробел, RISK-58 в `docs/TECHNICAL_DEBT.md`, половина `url` закрыта в этом же слайсе; `external_artifact_id` намеренно остаётся непрозрачным без структурной валидации). Новые capability: `content_proposal.register_external_artifact` (DRAFT), `content_proposal.list_proposal_artifacts` (READ); MCP `agent_register_external_artifact`/`agent_list_proposal_artifacts`, CLI `agent register-external-artifact`/`agent list-proposal-artifacts`. `AGENT_API_VERSION` → `0.7.0`. Полный дизайн — `docs/AGENT_OPERATIONS_INTERFACE.md` §4f.
- **Слайс H (полнота MCP/API-поверхности) — проверен по факту.** Сверка кода (не только документации) не нашла расхождений между `AGENT_CAPABILITIES` и реально зарегистрированными MCP-инструментами/CLI-паритетом. Полная 34-секционная owner-спека восстановлена из транскрипта сессии и перепроверена по секциям, ранее не процитированным в этом документе — найдено 2 нигде не отслеженных пробела (BL-088 §10 comparable-content context, BL-089 §16 performance↔asset linkage) и один процессный пробел (§28, нет отдельного acceptance-контракта для Phase 7). Детали — `docs/AGENT_OPERATIONS_INTERFACE.md` §7.
- **Слайс I (Codex operations workspace path surfacing, owner-спека §3/§30) — IMPLEMENTED.** Владелец сузил объём (Telegram 2026-09-24): редакционные/операционные инструкции для агента живут в папке вне репозитория, приложение хранит только путь к ней (настройка `operationsWorkspacePath`, ключ-значение в существующей таблице `appSettings`, без новой миграции) и отдаёт содержимое агенту через MCP/CLI по запросу — никогда не генерирует и не коммитит сам контент (AGENTS.md §B). Новый модуль `src/lib/operations-instructions/`. Путь можно установить **только** через `POST /api/settings` (Settings → AI Agent) — ни один MCP-инструмент или CLI-команда в namespace `agent` не может его задать (та же логика самоавторизации доступа к файловой системе, что уже применена к `local_path` в слайсе G2). Проверка при установке (`validateOperationsWorkspacePath`): путь должен быть абсолютным, существующей директорией, не пересекающейся с собственной app-data директорией приложения (RISK-07 — там лежат OAuth-токены в открытом виде). Та же проверка **повторно выполняется при каждом чтении** (не только при установке) через реальный `realpath`, т.к. директория может быть позже пересимлинкована на что-то небезопасное — подтверждено отдельным тестом. MCP `agent_list_operations_files`/`agent_get_operations_file`, CLI `agent list-operations-files`/`agent get-operations-file` — оба READ, не привязаны к каналу (в отличие от всех остальных capability этого интерфейса), не блокируются device-availability gate. `AGENT_API_VERSION` → `0.8.0`. Проверено вживую в браузере (реальная директория, невалидный относительный путь, путь пересекающийся с app-data — все три сценария отработали корректно) и через CLI (реальное чтение файла из настроенной директории). Полный дизайн — `docs/AGENT_OPERATIONS_INTERFACE.md` §4j.
- **Слайс K (comparable-content context, `find_comparable_videos`, owner-спека §10) — IMPLEMENTED.** Найден и назначен владельцем в фазу 7 тем же 2026-09-24 (Telegram: "Да, такие находки как BL 88 и 89 тоже включай в список тасков текущей 7 фазы"), `BL-088`. Разбит на два коммита. **K0**: новая колонка `videos.durationSeconds` (SCHEMA_MIGRATIONS версия 19, nullable, additive — тот же паттерн, что и view/comment/like-count в версии 4), парсер `parseIso8601DurationToSeconds` в `youtube-read-gateway/data-api.ts` (`videos.list` теперь запрашивает и `contentDetails`); `"P0D"`/`"PT0S"` (плейсхолдер живой трансляции) парсятся в `null`, никогда не в фиктивный `0`; `video-details/adapters/store.ts`'s `refreshVideoFields` дополнен переносом `durationSeconds` вперёд (иначе любое несвязанное редактирование метаданных затирало бы уже синхронизированную длительность обратно в `null` — поймано `advisor()` до появления теста). **K1**: новый модуль `src/lib/comparable-content/` (contracts/schemas/services/index — паттерн §6.2), anchor-based запрос: `{channelId, anchorVideoId, credentialRef?, publicationWindowDays?, durationToleranceSeconds?, performanceMetric?, performanceThreshold?, sort, limit?}` → `{anchorVideoId, candidates[], excludedForMissingData, truncated}`. Переиспользует без дублирования (AGENTS.md §D): `createChangeSetChannelStoreAdapter().listVideosByChannel`, `createAnalyticsCore().listMetrics` (вызывается только когда реально запрошен `performanceMetric`) и `computeComparableAgeSeries`/`diffCalendarDays`/`toPacificCalendarDate` из `@/lib/analytics/comparable-age.ts` для сравнения производительности с выравниванием по возрасту видео (капа 365 дней) — никогда живой вызов YouTube, никогда вторая реализация выравнивания по возрасту. **Явно не поддерживает** "тот же тип контента"/"похожая аудитория"/"похожий паттерн метаданных" — источника данных для этого в приложении нет, и capability прямо заявляет это в своём описании, а не молча аппроксимирует. `sharedTitleTokens` — буквальное пересечение слов в нижнем регистре (после короткого английского стоп-листа), никогда не "смысловая похожесть" и никогда не эмбеддинги (owner-спека §10 явно исключает эмбеддинги для первой реализации). MCP `agent_find_comparable_videos`, CLI `agent find-comparable-videos` — оба READ, channel-scoped (`assertActiveChannel`, как у слайса D), не блокируются device-availability gate. `AGENT_API_VERSION` → `0.9.0`, новый `AGENT_CAPABILITY_DOMAINS` элемент `comparable_content` (без нового `AGENT_DATA_DOMAINS` — переиспользует `video_metadata`/`video_analytics`). Полный дизайн — `docs/AGENT_OPERATIONS_INTERFACE.md` §4g.
- **Слайс L (performance ↔ asset linkage, owner-спека §16) — IMPLEMENTED, независимый цикл ревью закрыт (4 раунда, находки 5/1/1/0).** `BL-089`. Новый модуль `src/lib/asset-performance/` (contracts/schemas/services/index, паттерн §6.2) — join существующего каталога ассетов (`creative_assets.linkedVideoId`) с уже собранными данными по видео. Переиспользует без дублирования (AGENTS.md §D): `assetCatalogCore.listAssets`, тот же `listVideosByChannel`-адаптер, что и слайс K, и вынесенную из слайса K функцию `getCumulativeValueAtDayOffset` (`analytics/comparable-age.ts`, отдельным коммитом до начала слайса L, 26 тестов слайса K прошли без изменений). Всегда отдаёт lifetime-счётчики видео (viewCount/likeCount/commentCount/durationSeconds + lifetimeCountersAsOf из videos.lastSyncedAt) и опционально age-aligned значение метрики на явно заданный `performanceDayOffset` (никогда не из `now()` — урок раунда 1 слайса K применён с самого начала). `excludedForMissingLink` — всего 2 причины (`unlinked`/`linkedVideoNotOnChannel`), а не 3: channel-scoped чтение видео структурно не может отличить "никогда не синхронизировано" от "принадлежит другому каналу" (найдено advisor()-ревью до начала цикла независимого ревью). Явно не поддерживает: CTR/impressions миниатюр (данные не собираются), metadata/version-привязку (нет временного диапазона у linkedVideoId), experiment/outcome (Phase 10), content_proposal_artifacts-связи (другая семантика). MCP `agent_list_asset_performance`, CLI `agent list-asset-performance`. `AGENT_API_VERSION` → `0.10.0`. Полный дизайн — `docs/AGENT_OPERATIONS_INTERFACE.md` §4h.
- **Слайс J (финальный независимый обзор безопасности/интеграции всей фазы, owner-спека §28) — CLOSED.** Ретроактивный acceptance-контракт (`docs/acceptance/PHASE_7_ACCEPTANCE.md` §8-§11) и свежие сквозные проверки (channel isolation, capability parity, secrets, DB access, dev-repo dependency, approval separation). `RISK-59` разрешён эмпирически (рендеринг MCP `tools/list` подтверждён корректным, фикс не требуется). Вся фаза 7 (слайсы A-L + J) смержена в `dev` (`cc6a431`, 2026-09-25) с явным одобрением владельца. Детали — `docs/AGENT_OPERATIONS_INTERFACE.md` §7.

### 2.9s Зоны ответственности агентов (BL-091, `docs/roadmap/plans/AGENT_ZONES_PLAN.md`) — **слайсы 1-3 IMPLEMENTED, не в `dev`, ждёт независимого ревью**

- **Статус:** owner instruction, Telegram 2026-09-25 ("составь план выполнения на эту фичу с несколькими агентами и приступай к его выполнению... чтобы это было отдельным модулем"). Ветка `feature/agent-connections`, не смержена. Полный план и дизайн — `docs/roadmap/plans/AGENT_ZONES_PLAN.md` (не дублируется здесь).
- **Слайс 1 (реестр + модель данных) — IMPLEMENTED.** Новый модуль `src/lib/agent-connections/` (contracts/schemas/services/adapters/index, паттерн §6.2). Таблицы `agent_connections`/`agent_capability_zones` (SCHEMA_MIGRATIONS версия 20, аддитивно), не в `SNAPSHOT_TRANSFERRED_TABLES` (owner: "оставим локально"). Зона привязана к конкретной capability (не к домену) — позволяет при желании разделить действия внутри одного домена между разными агентами. Список из 6 зонируемых capability id вынесен в общие константы/`ZONED_CAPABILITIES` (`contracts.ts`), а не продублирован в MCP/CLI/UI по отдельности.
- **Слайс 2 (enforcement) — IMPLEMENTED, только для 6 явно одобренных владельцем действий** (Telegram: "1. Согласен" на расширение зон за пределы Phase 7 agent-operations DRAFT-уровня): `channel_sync`, `changeset_create_from_import`, `ai_localization_generate`, `ai_localization_create_change_set`, `agent_create_content_proposal`, `agent_register_external_artifact`. Единая функция `assertAgentAllowedForCapability`, политика keyed на ВКЛЮЧЁННЫЕ подключения (не просто зарегистрированные — отключение всех возвращает поведение к сегодняшнему single-agent состоянию):
  - 0 включённых подключений — no-op;
  - 1+ включённых — нужна распознаваемая, включённая, зарегистрированная идентичность вызывающего;
  - зона без явного назначения открыта, только пока включено РОВНО одно подключение — при 2+ включённых **не назначенная зона отклоняется для ВСЕХ**, а не делится (owner: "нельзя одну и ту же зону ответственности дать обоим") — исправлено после первого прохода независимого ревью, который изначально оставлял не назначенную зону открытой для любого включённого подключения даже при 2+ активных, что нарушало это требование;
  - явно назначенная зона отклоняет всех кроме назначенного, независимо от количества включённых.

  Идентичность — `AGENT_CONNECTION_ID` (env) для MCP, `--agentConnectionId`/тот же env для CLI. Проверено сквозным тестом на реальном модуле (`src/mcp/server.zone-enforcement.e2e.test.ts`, `...-disable-reverts.e2e.test.ts`), не только фейками. **Не является полной механической инвентаризацией всех ~46 MCP-инструментов** (ранний черновик плана ошибочно заявлял это и связь с `RISK-32` — исправлено; `RISK-32` про другое, про согласованность device-availability gate, и остаётся отдельным открытым риском). Найден и задокументирован новый `RISK-60` (гонка по глобальному активному каналу при нескольких одновременных подключениях) — не исправлен без отдельного подтверждения владельца. `npm test` 1373 → 1415, `tsc`/`lint`/`build` чисты.
- **Слайс 3 (Web UI) — IMPLEMENTED.** Новый компонент `src/components/agent-connections-manager.tsx` (Settings → AI Agent, после `McpConnectionSettings`): регистрация подключений (id + label, без секрета), `ToggleSwitch` enable/disable, назначение зон по каждой из 6 подключённых capability (сгруппировано по домену), явная маркировка "заблокировано для всех" когда включено 2+ подключений и зона не назначена. Новые API-маршруты `GET/POST /api/agent-connections`, `PUT /api/agent-connections/[connectionId]`, `GET/PUT /api/agent-connections/zones` — подтверждено, что без сессии возвращают 401. Импортирует общий список зон из `@/lib/agent-connections/contracts` напрямую (не из barrel `index.ts`, который тянет за собой серверный `db.ts` и ломает клиентскую сборку — реальная ошибка сборки, найденная и исправленная до подачи фичи как готовой). **Полная проверка в авторизованном браузере не проводилась** (реальный Google OAuth login вне рамок автоматической работы, `AGENTS.md` §G — та же принятая ранее в этом репозитории оговорка). `tsc --noEmit`/`npm run lint`/`npm run build` чисты, все три новых маршрута видны в выводе сборки.
- **Остаётся:** независимый цикл ревью (в процессе, несколько раундов уже прошло, реальных багов в логике exclusivity не найдено — только устаревшие комментарии/доки, цикл ещё не закрыт), затем финальный мердж в `dev` (требует явного "да" владельца, т.к. это substantive feature, `AGENTS.md` §K.2). Открытый вопрос для владельца: операторская CLI-команда `asset register` остаётся вне системы зон (не gated) даже когда весь домен "assets" отдан конкретному агенту — не решено самостоятельно, вынесено на решение владельца.

### 2.10 Web UI — **IMPLEMENTED**

- **Ответственность:** дашборд оператора (`/dashboard`) с вкладками **Home** (с 2026-09-20, editorial-profile панель; с 2026-09-23 также карточки последнего видео/списка видео/канальной аналитики — §2.9n) / **Content** (с 2026-09-20, ex-Sync, Slice S2) / **Analytics** (с 2026-09-20 заглушка, с 2026-09-22 реальный сбор — Phase 8, §2.9i; с 2026-09-23 также живая вкладка "Overview" — §2.9n) / **Languages** (с 2026-09-20, объединяет прежние отдельные вкладки Localizations и AI Localization — `docs/roadmap/plans/LANGUAGES_TAB_MERGE_PLAN.md`) / Batches / **Settings** (Phase 6 "AI Connections") / **Device**. Хром вокруг вкладок — постоянный левый sidebar (иконка + label на каждую вкладку, активная подсвечена акцентным цветом) и верхний бар (активный канал, переключение канала, sign out) в стилистике YouTube Studio (`src/components/app-shell.tsx`), заменивший прежний горизонтальный pill-tab-bar. **Manual и Rules удалены 2026-09-20** (решение владельца проекта: «давай удалим их, т.к. пока не вижу им применения») — обе были унаследованы от upstream TubeMaster baseline (Phase 0/1) и не связаны с локализацией/Change Sets/Batches этого проекта; Home теперь дефолтная вкладка вместо Manual.
- **Файлы:** `src/app/page.tsx` (страница входа), `src/app/dashboard/page.tsx`, `src/components/app-shell.tsx` (sidebar+topbar шелл), `src/components/icons.tsx` (inline SVG-иконки sidebar), `src/components/{session-provider,content-manager,languages-manager,change-set-review,batch-manager,ai-connections-manager,editorial-profile-panel,device-handoff-panel}.tsx`.
- **Точки входа:** браузер → `http://localhost:3000` → NextAuth Google sign-in → `/dashboard`.
- **Зависимости:** API routes (`src/app/api/**`), сессия NextAuth.
- **Read/Write:** вкладка **Content** (ex-Sync) и «читающая» часть **Languages** (ex-Localizations) читают YouTube только через уже синхронизированные данные для таблицы видео/локализаций; XLSX-импорт/экспорт и approve/reject внутри Languages (Phase 4) — только локальная запись в БД, без вызовов YouTube API. **С 2026-09-21 у Languages появились два прямых вызова YouTube API** (устарело: «Languages вообще не вызывает YouTube API напрямую» — исправлено по AGENTS.md §H): кнопка «Sync now» (тот же `channel_sync`, что и в Content — реальный read с YouTube, запись только в локальную БД) и подсказки в «Add language column» (`GET /api/youtube/languages`, реальный read-only `i18nLanguages.list`, см. §2.9 выше). Ни один из них не пишет в YouTube. (Удалённая 2026-09-20 вкладка **Manual** была единственным местом в Web UI, писавшим в YouTube напрямую — add/remove из плейлиста, создание плейлиста; сам `playlist-management`-модуль и его API-роуты сохранены для MCP/CLI, см. §2.12/§2.13.) Секция «Generate with AI» внутри **Languages** (ex-AI Localization tab, объединено 2026-09-20) — генерирует предложения через mock-провайдер (ноль сетевых вызовов по умолчанию; реальный вызов возможен только если явно выбран настоящий AI Connection, см. §2.9c), позволяет отредактировать текст и создать Change Set; approve/reject и Batch/dry-run происходят в той же вкладке **Languages** / в **Batches** без изменений. Вкладка **Batches** — выбор одобренных изменений → создание Batch (по умолчанию принудительно dry-run на уровне API-роута) → просмотр неизменяемого состава, статусов ledger-строк, отчёта об ошибках и audit-трейла → кнопка запуска dry-run превью. **С 2026-09-21 (owner instruction, BL-045):** если в Settings включён тумблер «Live writes» (по умолчанию выключен каждую сессию), появляется чекбокс «Create as a real, live batch» при создании и кнопка «Execute (real write)» на уже созданном live-batch — обе за `ConfirmDialog` с явным предупреждением (канал, число видео, число полей). См. §2.9f ниже для полной технической модели этого тумблера.
- **Важные ограничения безопасности:** каждый API route, к которому обращается UI, сам проверяет сессию (`getServerSession`) и (для write) guardrail канала — UI не является границей безопасности. Для Batches-роутов channel-scoping обеспечивает новая `requireBatchForChannel` (AGENTS.md §F). Вкладка **Settings** — управление AI Connections; credential никогда не возвращается обратно в браузер (только `hasCredential: boolean`); «Test connection» — явное действие с предупреждением о возможной стоимости перед подтверждением. **С 2026-09-20:** вкладка Settings также показывает карточку "App version" (версия из `package.json`, git-коммит, время сборки) — `src/components/app-version-info.tsx`, читает статический `/build-info.json`, который генерируется заново перед каждым `npm run build`/`npm run dev` скриптом `scripts/write-build-info.mjs` (npm-хуки `prebuild`/`predev`). Добавлено для диагностики: при авто-обновлении start.sh/start.bat (`docs/FIRST_LOCAL_TEST_BUILD.md` §3/§4) не всегда очевидно, какая версия реально собрана и запущена.
- **Settings разбит на 4 суб-вкладки (2026-09-23, owner instruction: "давай в настройках сделаем 4 категории закладок").** Простой pill-переключатель внутри `dashboard/page.tsx` (тот же визуальный паттерн, что и суб-вкладки Languages, `src/components/languages-manager.tsx`), состояние `settingsSubTab` не персистится между заходами. **API** — Live writes (`live-writes-settings.tsx`), Data API/Analytics reads тумблеры (`read-gateway-settings.tsx`), Google Cloud connection (`cloud-connection-settings.tsx`), время/таймзона автосбора аналитики (переименовано `analytics-sync-settings.tsx` → `analytics-collection-settings.tsx`/`AnalyticsCollectionSettings`, чтобы не путать со словом "Sync" в новой суб-вкладке — сама настройка про расписание сбора, а не про межустройственную синхронизацию). **AI Agent** — тумблер MCP-подключения, выделен из `LiveWritesSettings` в новый `mcp-connection-settings.tsx`/`McpConnectionSettings` (оба независимо читают/пишут `/api/settings`, который уже применяет только присланные в POST поля — ничего общего между компонентами не сломалось), плюс AI provider connections (`ai-connections-manager.tsx`). **Важно:** MCP-подключение (как внешний AI-агент вроде Codex/Claude подключается К этому приложению) и AI Connections (как это приложение подключается К внешним AI-провайдерам для AI Localization) — два никак не связанных механизма; объединены на одной суб-вкладке по явному решению владельца, отмечено прямо в коде во избежание путаницы у будущего разработчика. **Sync** — путь до Syncthing-папки, новый `sync-folder-settings.tsx`/`SyncFolderSettings`, вынесен из `device-handoff-panel.tsx` (вкладка Merge сохранила только сами действия синка — Sync now, export/import снэпшотов, список конфликтов; текст с упоминаниями "folder above" на Merge исправлен на явную ссылку "Settings → Sync"). **About** — `AppVersionInfo` без изменений.

### 2.11 API routes — **IMPLEMENTED**

- **Ответственность:** HTTP-граница между Web UI (и потенциально внешними интеграциями) и доменными сервисами.
- **Файлы:**
  - Существующие: `src/app/api/video-metadata/{apply,preview,transcript}/route.ts`, `src/app/api/youtube/{videos,playlists,create-playlist,add-to-playlist,remove-from-playlist,channel-info}/route.ts`, `src/app/api/auth/[...nextauth]/route.ts`. (`src/app/api/rules/route.ts` и `src/app/api/run/route.ts` — вместе с самой Rules-фичей — удалены 2026-09-20.)
  - **Новые (Phase 2):** `GET /api/channels` (`route.ts`), `POST /api/channels/sync` (`sync/route.ts`), `GET /api/channels/[channelId]/videos` (`[channelId]/videos/route.ts`).
  - **Новые (Phase 3):** `GET /api/channels/[channelId]/localizations` (обзорная таблица), `GET /api/channels/[channelId]/localizations/[videoId]` (детали видео), `GET /api/channels/[channelId]/localizations/export` (XLSX-файл, `?videoIds=a,b,c` опционально).
  - **Новые (Phase 4):** `POST /api/channels/[channelId]/localizations/import/preview` (multipart, без персистентности), `POST /api/channels/[channelId]/localizations/import` (multipart, создаёт Change Set), `GET /api/channels/[channelId]/change-sets` (список), `GET /api/channels/[channelId]/change-sets/[changeSetId]` (детали + ревалидация конфликтов, фильтры/пагинация), `POST .../change-sets/[changeSetId]/changes/[changeId]/approve`, `POST .../changes/[changeId]/reject`, `POST .../change-sets/[changeSetId]/approve-all`, `POST .../change-sets/[changeSetId]/reject-all`.
  - **Новые (Phase 5 completion task, 2026-09-18):** `GET/POST /api/channels/[channelId]/batches` (список/создание — по умолчанию `dryRun: true` принудительно, см. ниже), `GET /api/channels/[channelId]/batches/[batchId]` (batch + ledger rows, channel-scoped через `requireBatchForChannel`), `POST .../batches/[batchId]/prepare` (запуск dry-run пайплайна, `prepareBatchExecution`), `GET .../batches/[batchId]/errors` (`getBatchErrorReport`), `GET .../batches/[batchId]/audit` (`createAuditCore().listForBatch`).
  - **Новые (2026-09-21, BL-045/BL-047 — Gate B + MCP connection toggles, см. §2.9f):** `GET/POST /api/settings` (`liveWritesEnabled`/`mcpConnectionEnabled` флаги), `POST .../batches/[batchId]/execute` — единственный роут, которому разрешено ссылаться на `executeBatch`/`WriteExecutor` (явное, единственное исключение в `write-path-inventory.test.ts`; см. §2.9f). `POST .../batches` теперь честно создаёт `dryRun: false` batch, но только пока настройка live-writes включена в момент вызова — иначе всё ещё принудительно `dryRun: true`, как раньше. **Все остальные роуты этой секции по-прежнему не вызывают и не ссылаются на live-execution функции/типы** — проверено автоматическим тестом `src/lib/batches/write-path-inventory.test.ts`, который ищет эти символы по содержимому файлов (включая комментарии), а не только по импортам.
  - **Новые (Phase 6, Slice 1):** `POST /api/channels/[channelId]/ai-localization/generate` (превью-генерация, ничего не персистит), `POST /api/channels/[channelId]/ai-localization/change-sets` (создаёт Change Set из отредактированных предложений через `changesets.createChangeSetFromProposals`). Оба роута только вызывают `createAiLocalizationCore()` и не содержат отдельной approve/batch-логики — она полностью переиспользуется из уже существующих Change Set/Batches роутов выше.
  - **Новые (Phase 6, Channel Editorial Profiles, 2026-09-19):** `GET/PUT /api/channels/[channelId]/ai-localization/profile` (просмотр/сохранение профиля), `GET /api/channels/[channelId]/ai-localization/change-sets/[changeSetId]/provenance` (read-only, channel-scoped: возвращает `null`, если запись принадлежит другому каналу).
  - **Новые (Phase 6, AI Connections, 2026-09-19):** `GET/POST /api/ai-connections` (список/создание — глобально, не channel-scoped, см. §2.9c), `GET/PUT/DELETE /api/ai-connections/[connectionId]`, `POST /api/ai-connections/[connectionId]/test` (единственный вызывающий `testConnection`, всегда явное действие пользователя).
  - **Новые (Cloud connection, 2026-09-22, см. §2.9j, не в `dev`):** `GET /api/cloud-connection/start` (редирект на Google consent, `state` в httpOnly cookie), `GET /api/cloud-connection/callback` (обмен кода на токены, редирект обратно на дашборд), `GET /api/cloud-connection/status` (публичный статус, без токена), `POST /api/cloud-connection/disconnect`. Полностью независимы от `[...nextauth]` — отдельный OAuth-флоу, не завязанный на активный канал.
- **Точки входа:** HTTP-запросы, каждый начинается с `getServerSession(authOptions)`.
- **Зависимости:** соответствующий доменный core (`createVideoMetadataCore()`, `createPlaylistManagementCore()`, `createChannelSyncCore()`, `createLocalizationCore()`, `createChangeSetCore()`, `createBatchCore()`, `createAuditCore()`), общие `error-status.ts`/`parse-json-body.ts` из `video-metadata` (переиспользуются, не дублируются).
- **Read/Write:** зависит от роута; все новые роуты Phase 2–5 не пишут в YouTube (export/import/approve/batches — либо чтение, либо только локальная запись в БД; batches-роуты структурно не могут достичь `videos.update`).
- **Важные ограничения безопасности:** без валидной сессии — `401` до вызова доменной логики; каждый change-set/batch роут проверяет, что вложенный ресурс принадлежит указанному `channelId` (`not_found`/`batch_not_found` иначе), чтобы нельзя было прочитать/изменить чужой ресурс через чужой канал.

### 2.12 CLI — **IMPLEMENTED** (metadata/auth/playlist/changeset/batch/channel/analytics/ai-localization); только apply-класс (approve/execute) для Change Sets/Batches остаётся **PLANNED** (см. `docs/TECHNICAL_DEBT.md` RISK-04, PARTIALLY RESOLVED)

- **Ответственность:** локальный терминальный интерфейс для metadata/auth/playlist/changeset/batch/channel/analytics операций.
- **Файлы:** `src/cli/video-metadata.ts` (namespaces: `metadata` (без явного префикса), `auth`, `playlist`, `changeset`, `batch`, `channel`, `analytics`).
- **Точки входа:** `npm run cli:video-metadata -- <namespace> <command>`.
- **Зависимости:** `createVideoMetadataCore()`, `createPlaylistManagementCore()`, `createCliAuthService()`, `createChangeSetCore()`, `createBatchCore()`, `createChannelSyncCore()`, `createAnalyticsCore()` — те же фабрики, что MCP и Web UI, без параллельной реализации (AGENTS.md §D).
- **Read/Write:** `apply`, `playlist create/update/delete/add/remove`, `channel sync`, `changeset import`, `ai-localization create-change-set` — write (`channel sync`/`changeset import`/`ai-localization create-change-set` пишут только в локальную БД, никогда в YouTube, но всё равно гейтятся); `changeset list/get/preview`, `batch list/get`, `channel list/video-list`, `analytics list/overview/data-quality/comparable-age/weekly-reports/weekly-report-get`, `ai-localization generate` — read-only, не гейтятся (`analytics overview`/`ai-localization generate` с реальным `--connectionId` — живые сетевые вызовы, но по той же логике §6.7 классифицируются как read, не мутация; реальный `--connectionId` гейтится своей собственной внутренней проверкой, RISK-30).
- **Важные ограничения безопасности:** JSON-конверты на stdout (`{ ok: true|false, ... }`), ненулевой exit code при ошибке; `READ_ONLY_CLI_COMMANDS`/`AUTH_SESSION_EXEMPT_CLI_COMMANDS` — единственные два явных списка исключений из гейта, всё остальное гейтится по умолчанию (fail-safe: забыть добавить команду в whitelist делает её излишне гейтированной, а не негейтированной); `--file` читается напрямую с диска (в отличие от MCP, где нет бинарного поля и нужен base64); `batch get` проверяет принадлежность батча каналу через `requireBatchForChannel` (AGENTS.md §F).

### 2.13 MCP — **IMPLEMENTED** (metadata/auth/playlist tools; read/propose/create Change Set tools; read-only Batch tools; channel-sync tools; analytics read tools; AI Localization generate/create-Change-Set tools); только apply-класс (approve/execute) для Change Sets/Batches остаётся **PLANNED** (см. `docs/TECHNICAL_DEBT.md` RISK-04, PARTIALLY RESOLVED)

- **Ответственность:** stdio MCP-сервер для AI-агентов.
- **Файлы:** `src/mcp/server.ts` (`createMcpServer`, `createMcpToolHandlers`).
- **Точки входа:** `npm run mcp:video-metadata`; инструменты: `write_context`, `write_channel_list`, `write_channel_select`, `whoami`, `auth_user_select`, `list`, `transcript`, `preview`, `apply`, `playlist_list`, `playlist_create`, `playlist_update`, `playlist_delete`, `playlist_add_videos`, `playlist_remove_videos`, `changeset_list`, `changeset_get`, `localization_import_preview`, `batch_list`, `batch_get` (Phase 7 slice 1, `docs/roadmap/plans/PHASE_7_PLAN.md`), `channel_sync`, `channel_list`, `channel_video_list` (`BL-008`, `docs/roadmap/BACKLOG.md`), `changeset_create_from_import` (реальное создание Change Set, не только превью), `analytics_list`, `analytics_overview`, `analytics_data_quality`, `analytics_comparable_age`, `analytics_weekly_reports_list`, `analytics_weekly_report_get` (2026-09-23, докладка Phase 8 -- "machine-readable analytics for operational agents" / "data-quality diagnostics" / "comparing videos at comparable ages" / "analytical reports and weekly channel reviews", `docs/roadmap/FUTURE_PHASES.md` §4, `docs/ARCHITECTURE.md` §14.9/§14.10/§14.11), `ai_localization_generate`, `ai_localization_create_change_set` (2026-09-23, BL-075/BL-078, см. §2.9b).
- **Зависимости:** те же core-фабрики, что и CLI/Web UI (`createChangeSetCore()`, `createBatchCore()`, `createChannelSyncCore()`, `createAnalyticsCore()` — те же, что использует API этих доменов, без параллельной реализации, AGENTS.md §D); `resolveEffectiveCredentialRef` из `cli-auth`.
- **Read/Write:** `apply`, `playlist_*`, `channel_sync`, `changeset_create_from_import`, `ai_localization_create_change_set` — write (с `dryRun`-поддержкой для `apply`; `channel_sync`/`changeset_create_from_import`/`ai_localization_create_change_set` пишут только в локальную БД, никогда в YouTube, но всё равно гейтятся `assertMcpDeviceAvailable` как локально-мутирующие); `changeset_list`/`changeset_get`/`localization_import_preview`/`batch_list`/`batch_get`/`channel_list`/`channel_video_list`/`analytics_list`/`analytics_overview`/`analytics_data_quality`/`analytics_comparable_age`/`analytics_weekly_reports_list`/`analytics_weekly_report_get`/`ai_localization_generate` — исключительно read/propose-only, не гейтятся (не мутируют ничего; `analytics_overview` — живой запрос к Analytics API, `ai_localization_generate` с реальным `connectionId` — реальный сетевой вызов к AI-провайдеру, но оба классифицируются как read, т.к. ничего не мутируют локально, см. `docs/DEVELOPMENT_PLAYBOOK.md` §6.7; реальный `connectionId`-вызов гейтится своей собственной внутренней проверкой, RISK-30). Сознательно НЕ добавлены `collectMetrics`/`runAutoCollectionIfStale` — это реальные локальные мутации, расходующие Analytics API квоту; запуск нового сбора остаётся только через Web UI/суточный автотриггер.
- **Важные ограничения безопасности:** все входы — строгие Zod-схемы; все ошибки — структурированный JSON, никогда голый текст; `batch_get` проверяет принадлежность батча каналу через `requireBatchForChannel`, а не голый `getBatch(batchId)` (AGENTS.md §F).
- **MCP connection** (тумблер в Settings, второй аргумент `createMcpServer(core, {connectionEnabled: true})`, см. §2.9f) — по умолчанию выключен: сервер не регистрирует НИ ОДНОГО инструмента из списка выше, пока владелец явно не включит тумблер. Реализует "operation-specific permissions and read-only access to application data" из плана Phase 7 (`docs/roadmap/FUTURE_PHASES.md` §3) в default-deny виде: отсутствие доступа — состояние по умолчанию, а не отдельный «ограниченный» режим.
- **Ограничение: apply-класса для Change Sets/Batches через MCP до сих пор нет.** CLI-паритет для read/propose/create Change Set/Batch/channel-sync/analytics инструментов **уже есть** (`src/cli/video-metadata.ts`, namespaces `changeset`/`batch`/`channel`/`analytics`) — это утверждение исправлено 2026-09-23, было устаревшим (паритет добавлен ещё 2026-09-20, см. `docs/ROADMAP_STATUS.md`'s "Phase 7 — in-repository scope" row, эта строка тогда не была обновлена). Approve/reject/create/execute для Change Set/Batch по-прежнему доступны только через Web UI.

### 2.14 Tests — **IMPLEMENTED**

- **Ответственность:** unit/integration-тесты на моках (без реальных сетевых вызовов к YouTube).
- **Файлы:** `*.test.ts` рядом с тестируемым модулем; раннер — `node --import tsx --test "src/**/*.test.ts"` (встроенный `node:test`, без Jest/Vitest).
- **Новые файлы Phase 2:** `src/lib/youtube.test.ts` (батчинг/пагинация против моков реальной формы `googleapis`-клиента), `src/lib/channel-sync/services.test.ts` (sync/list на фейковом store).
- **Новые файлы Phase 3:** `src/lib/localization/services.test.ts` (overview/detail/export на фейковом store), `src/lib/localization/adapters/xlsx.test.ts` (реальная сборка workbook через `exceljs` + чтение обратно, без моков библиотеки).
- **Новые файлы Phase 4:** `src/lib/changesets/diff.test.ts` (чистая классификация/статусы/инвалидация approval), `src/lib/changesets/import.test.ts` (парсинг/валидация реальных XLSX-книг через `exceljs`: blank=no-change, дубликаты, невалидный язык, конфликт, channel mismatch, лимиты размера), `src/lib/changesets/services.test.ts` (сквозные сценарии: создание change set, approve/reject, ре-синхронизация инвалидирует approval, bulk approve не трогает невалидные/конфликтующие строки, изоляция между каналами).
- **Новые файлы Phase 6 (Slice 1):** `src/lib/ai-localization/{services,provider-registry,integration.acceptance,write-path-inventory}.test.ts` — генерация/валидация/классификация (add/modify/unchanged), изоляция провайдерских сбоев, дедупликация целей, невалидные языки/videoId, интеграция AI → Change Set → approve → Batch dry-run с сохранением непонятых локалей (AC-PRESERVE-01), инвентарный тест на отсутствие сетевых AI-SDK/live-write ссылок.
- **Новые файлы Phase 6 (Channel Editorial Profiles, 2026-09-19):** `src/lib/ai-localization/profiles.test.ts` — channel isolation, missing/empty profile, версия/reproducibility после правки профиля, per-field merge с per-request editorialBrief, malformed data, отсутствие персистентных побочных эффектов.
- **Новые файлы Phase 6 (AI Connections, 2026-09-19):** `src/lib/ai-connections/{crypto,endpoint-security,services,integration.acceptance,write-path-inventory}.test.ts`, `adapters/openai-compatible.test.ts` — 37 тестов: CRUD, шифрование/расшифровка credential, fail-closed без ключа шифрования, SSRF-блокировка (приватные/loopback/metadata адреса, DNS rebinding, IPv6), unsupported-capability до сетевого вызова, malformed JSON/timeout от провайдера изолированы, usage репортится как `null`, а не 0, когда неизвестен, полный pipeline connection → generate → Change Set → approve → Batch dry-run с сохранением непонятых локалей.
- **Текущее состояние:** 396 тестов, все проходят (`npm test`).
- **Важное ограничение:** ни один тест не пишет в реальный YouTube-канал (`docs/PROJECT_SPEC.md` §42); Phase 4 тесты дополнительно доказывают, что import/approve не может вызвать ни одного YouTube write-метода (нет такого метода в зависимостях сервисов вообще); Phase 6 тесты аналогично доказывают отсутствие любого реального AI-provider вызова и реального DNS/сетевого запроса (инжектируемый `fetchImpl`/`dnsLookup` в каждом тесте).

### 2.15 Configuration / local state — **IMPLEMENTED**

- **Ответственность:** переменные окружения и локальные файлы состояния.
- **Файлы:** `.env.local` (не в репозитории; обязательные `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `NEXTAUTH_URL`, `NEXTAUTH_SECRET`; опциональные `CLI_OAUTH_CALLBACK_PORT`, `YOUTUBE_TRANSCRIPT_PROVIDER`, `METADATA_GENERATOR_MODE`, `METADATA_GENERATOR_RAW_OUTPUT`, и (Phase 6) `AI_CONNECTIONS_ENCRYPTION_KEY` — base64-encoded 32-byte AES-256-GCM key; без неё AI Connections с credential сохранить нельзя, plaintext-фолбэка нет; и (см. §2.9j, не в `dev`) `CLOUD_CONNECTION_ENCRYPTION_KEY` — та же схема, намеренно отдельный ключ от `AI_CONNECTIONS_ENCRYPTION_KEY`, плюс требует добавления `http://localhost:3000/api/cloud-connection/callback` в Authorized redirect URIs OAuth-клиента в Google Cloud Console). **Обновлено (Pre-Release Cross-Platform Persistence, см. §2.16):** `playlist-manager.db`/`auth-context.json`/`backups/` больше не живут в `<repo>/data/` — они находятся в платформо-зависимой app-data директории (`src/lib/platform-paths`, `resolveAppPaths`): Windows — `%APPDATA%\YouTubeOperationsManager\`, macOS — `~/Library/Application Support/YouTubeOperationsManager/`. `<repo>/data/playlist-manager.db` остаётся действительным только как одноразовый источник миграции при первом запуске на новой машине (никогда не изменяется, никогда не удаляется).
- **Важные ограничения безопасности:** `data/*.db`, `data/auth-context.json`, `data/oauth/`, `data/tokens/`, `credentials/`, `.env*` — всё в `.gitignore` (актуально и для legacy-расположения, и для новой app-data директории, которая физически вне репозитория). Секреты никогда не попадают в логи или к AI-провайдеру. AI-connection credential шифруется отдельно от этого — см. §2.9c.

---

### 2.16 Cross-Platform Persistence & Device Handoff (Pre-Release, Variant A) — **IMPLEMENTED for the scope below**

- **Ответственность:** платформо-зависимое расположение данных приложения; явное версионирование схемы поверх аддитивного boot-паттерна (`docs/decisions/0002-additive-schema-versioning.md`); экспорт/импорт scrub-and-checksum снапшотов БД для переноса состояния между устройствами через Syncthing как внешний транспорт (Syncthing никогда не рассматривается как база данных); явный workflow передачи устройства (`Finish work / Export` — `Continue work / Import`) с restricted read-only recovery mode при незавершённых YouTube-операциях. Полный контракт: `docs/acceptance/PRE_RELEASE_CROSS_PLATFORM_ACCEPTANCE.md`.
- **Файлы:**
  - `src/lib/platform-paths/{contracts,services,runtime,index}.ts` — `resolveAppPaths`, `getProductionAppPaths` (с безопасным redirect на изолированную temp-директорию под `npm test`, ключ — `NODE_TEST_CONTEXT`, устанавливаемый самим `node --test`).
  - `src/lib/bootstrap-config/` — девайс-локальный `bootstrap-config.json` (`deviceId`, `syncthingRootPath`).
  - `src/lib/schema-versioning/` — `schema_meta`, `assertSupportedSchemaVersion` (reject-newer строго до любой мутации схемы), `runSchemaMigrations`. Подключено в `src/lib/db.ts`'s `initializeDatabaseSchema` (существующий аддитивный baseline не тронут).
  - `src/lib/db-backup/` — `copyDatabaseConsistently` поверх `VACUUM INTO`: единая реализация консистентного снимка БД, общая для pre-migration-бэкапа и шага копирования в снапшот-пайплайне (без дублирования, `AGENTS.md` §D).
  - `src/lib/operation-lock/` — `app_operation_locks` (миграция schema version 2): атомарный девайс-локальный лок на export/import/migration через `INSERT` с фиксированным id (реальная SQLite-гарантия, не advisory-флаг сам по себе), diagnostics по "stale"-локу без авто-освобождения.
  - `src/lib/snapshot/` — `SNAPSHOT_TRANSFERRED_TABLES` (явный allowlist, fail-safe по умолчанию), `exportSnapshot`/`verifySnapshotForImport`/`applySnapshotToDatabase`/`migrateStagedCopy`/`scanForUnresolvedExecutionState`. Формат снапшота: `manifest.json` (`complete: true`, записанный последним) + `data.db` + SHA-256 по каждому файлу; staging-директория атомарно переименовывается в финальный `snapshotId` только после полной записи.
  - `src/lib/device-handoff/` — `exportHandoff`/`importHandoff`/`acknowledgeRecoveryDiagnostics`/`assertDeviceAvailableForMutation`/`isDeviceInRecoveryMode`. Recovery mode **никогда не кэшируется** — пересчитывается заново по `batch_ledger_rows` (`APPLYING`/`UNKNOWN`) при каждой проверке; acknowledgement — строго информационная запись, не влияющая на сам gate.
  - `src/proxy.ts` (Next.js 16 переименовал `middleware.ts` → `proxy.ts`, по умолчанию Node.js runtime) + choke-points в `src/cli/video-metadata.ts`'s `runCliCommand` и `src/mcp/server.ts`'s `createMcpToolHandlers` — единая реализация гейта (`assertDeviceAvailableForMutation`), три точки вызова.
  - Новые API-роуты: `src/app/api/device-handoff/{status,export,import,snapshots,acknowledge,bootstrap-config}/route.ts`; UI: `src/components/device-handoff-panel.tsx`, вкладка **Device** в дашборде.
- **Точки входа:** `POST /api/device-handoff/export`, `POST /api/device-handoff/import`, `GET /api/device-handoff/status`, `GET /api/device-handoff/snapshots`, `POST /api/device-handoff/acknowledge`, `GET/PUT /api/device-handoff/bootstrap-config`.
- **Read/Write:** ноль вызовов YouTube API и ноль вызовов AI-провайдера в любом коде этого раздела (не структурная гарантия отдельным инвентарным тестом, как у Phase 5/6, но ни один файл этого раздела не импортирует `googleapis` или AI-connections адаптеры — проверено при разработке). Экспорт/импорт пишут только в локальные файлы снапшота и в собственную БД (той же машины).
- **Важные ограничения безопасности:** снапшот никогда не содержит `users` (OAuth-токены) и `ai_connection_credentials` — не просто исключены из экспорта, а вообще не упоминаются в коде импорта (`SNAPSHOT_REPLACE_ON_IMPORT_TABLES` их не содержит). `users.id` подтверждён инспекцией как стабильный Google OAuth `sub` (не локальный артефакт) — импорт никогда не создаёт/не меняет строку `users`, поэтому здесь структурно невозможна путаница идентичности между устройствами. Divergent lineage (форк истории снапшотов) блокируется явно, никогда не разрешается по `createdAt`. `acknowledgeRecoveryDiagnostics` не может изменить статус ни одной строки `batch_ledger_rows` и не может снять recovery mode сама по себе — это отдельно протестировано (AC-HANDOFF-05). **M6, 2026-09-23** (`docs/decisions/0009-defer-write-pipeline-sync-gateway-migration.md`, владелец выбрал вариант (б) после исследования индустриальных паттернов): `SNAPSHOT_TRANSFERRED_TABLES` сужен до `schema_meta` + четырёх таблиц Category D (`batches`/`batch_ledger_rows`/`batch_attempts`/`audit_events`) — `change_sets`/`changes`/`channel_editorial_profiles`/`ai_localization_generation_provenance`/`ai_connections` больше не переезжают этим механизмом (все пять теперь непрерывно синхронизируются через `src/lib/sync-gateway/`); упомянутый выше upsert-по-`id` для `ai_connections` удалён вместе с ним — этот механизм остаётся живым, но только как явная, атомарная передача владения именно этими четырьмя таблицами (аналог LiteFS/Litestream-style primary handoff), не общей синхронизацией.
- **Чего нет:** installer/auto-updater для standalone-среза `published/<version>/` (без `.git`) — осознанно вне рамок этой задачи (`docs/RELEASE_LAYOUT.md`). `scripts/{macos,windows}/start.{sh,bat}` больше НЕ делают `git pull` сами (убрано 2026-09-21 по прямому запросу владельца — «за актуальностью гита я буду следить сам»; см. BL-048) — они вообще не трогают git/сеть/рабочее дерево. Вместо этого при запуске из git-чекаута сверяют текущий закоммиченный HEAD с маркер-файлом, в котором записано, из какого коммита был собран `.next`, и пересобирают при расхождении (закрывает реальный баг: раньше `.next`, уже существующий на диске, считался «актуальным» независимо от того, менялся ли исходный код) — см. `docs/FIRST_LOCAL_TEST_BUILD.md` §3/§4. Также чего нет: OS-keychain для credential; application-managed синхронизация (Syncthing остаётся внешним транспортом); одновременное редактирование с нескольких устройств; реальная macOS-валидация (только Windows, см. `docs/TECHNICAL_DEBT.md` RISK-17); in-app способ выйти из recovery mode (нужны CLI/MCP-инструменты для Batches, RISK-04/RISK-16).

---

## 3. Где вносить изменения

| Задача | Где |
|---|---|
| Добавить новую read-операцию YouTube | Низкоуровневая функция в подходящем дочернем модуле `src/lib/youtube-read-gateway/` (`data-api.ts` для Data API v3), реэкспортированная через барьер `index.ts`, затем обёртка в `adapters/youtube-api.ts` нужного доменного модуля, импортирующая **только** из `@/lib/youtube-read-gateway` (**никогда** напрямую из `data-api.ts`). Зафиксировано `read-gateway-inventory.test.ts` и `docs/decisions/0007-youtube-read-gateway.md` |
| Добавить новую write-операцию YouTube (что угодно, что реально может изменить данные на YouTube) | Низкоуровневая функция в `src/lib/youtube-write-gateway/` (**никогда** в `youtube-read-gateway/` и **никогда** напрямую в адаптере доменного модуля) + вызов `assertLiveWritesAuthorized()` из шлюза непосредственно перед вызовом в самом адаптере, вызывающем шлюз. Единственный разрешённый путь, зафиксировано `gateway-inventory.test.ts` и `docs/decisions/0005-youtube-write-gateway.md` (см. §2.9g) |
| Добавить новый доменный модуль | Новая директория `src/lib/<module>/` с `contracts.ts` / `schemas.ts` / `services.ts` / `adapters/` / `index.ts` — по образцу `channel-sync/` или `playlist-management/` |
| Добавить новый API route | `src/app/api/<path>/route.ts`, переиспользуя `getVideoMetadataErrorStatus` и `parseVideoMetadataJsonBody` из `src/app/api/video-metadata/` |
| Добавить новую CLI-команду | Новый namespace/command в `src/cli/video-metadata.ts`, по образцу существующих `metadata`/`auth`/`playlist` |
| Добавить новый MCP-инструмент | `server.registerTool(...)` в `src/mcp/server.ts` + обработчик в `createMcpToolHandlers` |
| Добавить новую персистентную сущность | Новая `sqliteTable` + `CREATE TABLE IF NOT EXISTS` в `initializeDatabase()` внутри `src/lib/db.ts` (пока схема аддитивна — см. `docs/ARCHITECTURE.md` §6.2 про решение по миграциям) |
| Добавить новый safe write workflow | Переиспользовать `write-context.assertWriteChannel` (не копировать проверку); придерживаться модели identity check → validation → backup → diff → approval → dry-run → audit → verification из `docs/PROJECT_SPEC.md` |

---

## 4. Статус компонентов

### 4.0 Ключевые ограничения (не путать с «не реализовано» — это активные инварианты, которые должен знать любой агент, прежде чем предлагать write-функциональность)

- **Approval ≠ применено к YouTube.** `Change.approvalStatus === "approved"` — это только локальная запись в SQLite (`docs/ARCHITECTURE.md` §6.9). Ни один код-путь в `src/lib/changesets/` не вызывает `googleapis`.
- **Conflict detection в Phase 4 сверяется с последним синхронизированным SQLite-снимком, а не с живым состоянием YouTube.** Свежая проверка remote-состояния непосредственно перед записью обязательна для Phase 5 (`docs/TECHNICAL_DEBT.md` RISK-03).
- **Приложение работает по модели single-operator.** Полноценной per-user/multi-tenant модели (ролей, разделения данных между людьми) по-прежнему нет — это осознанное допущение для локального инструмента. Но с 2026-09-20 каждое канало-зависимое чтение (Web/MCP/CLI) фильтруется по активному каналу вызывающего (`docs/decisions/0004-active-channel-read-scoping.md`), что закрывает конкретную утечку "любая сессия видит все локально известные каналы" — `docs/TECHNICAL_DEBT.md` RISK-02 теперь FIXED; RISK-39 (явный `channelId` в syncChannel без проверки владения) остаётся отдельным открытым пунктом.
- **CLI-интерфейсов для Change Set/Batch/channel-sync не существует отдельно от той же MCP-версии; и MCP, и CLI покрывают весь read/propose/create слой** (`changeset_list`/`changeset_get`/`localization_import_preview`/`changeset_create_from_import`/`batch_list`/`batch_get`/`channel_sync`/`channel_list`/`channel_video_list`/`ai_localization_generate`/`ai_localization_create_change_set`, см. §2.12/§2.13; `docs/TECHNICAL_DEBT.md` RISK-04, PARTIALLY RESOLVED) — approve/reject/execute для Change Set/Batch по-прежнему доступны только через Web UI/API (создание Change Set — из XLSX и теперь также из AI-генерации — через MCP/CLI уже есть).
- **Живая browser/OAuth-проверка не выполнена независимо** (`docs/TECHNICAL_DEBT.md` RISK-05) — автоматические тесты и один сквозной прогон на реальной БД/реальном экспорте существуют, но реального клика в браузере с настоящей Google-сессией не было.

**Реализовано и работает (проверено тестами):**
Auth (Web + CLI PKCE/device flow), credential resolution, write-context guardrail, YouTube client layer (read + write методы для metadata/playlist), persistence (`users`, `rules`, `channels`, `videos`, `change_sets`, `changes`, `batches`, `batch_ledger_rows`, `batch_attempts`, `video_execution_locks`, `audit_events`), channel-sync (канал + видео, батчинг), localization read model + XLSX export (Phase 3), **XLSX import + draft state + change sets + field-level diff + local approve/reject + conflict detection (Phase 4)**, **Batch/ledger/attempt data model, concurrency locks, safety preparation (identity/approval/fresh-fetch/conflict/backup/merge/dry-run), retry+reconciliation+verification, durable audit, crash recovery, item-level/systemic isolation (Phase 5, Slices 1-3 — см. 2.9a; реального `videos.update` по-прежнему нигде нет)**, Web UI (Manual/Rules/Sync/Localizations, включая Import и Change Set Review), API routes (metadata/playlist/youtube/rules/run/channels/localizations/change-sets), CLI (metadata/auth/playlist), MCP (metadata/playlist tools).

**Примечание:** этот абзац не обновлялся при добавлении Phase 6 (AI Localization/Connections, см. §2.9b/§2.9c) и не входит в рамки настоящей задачи для полной ревизии — актуальный, полный статус см. §2 целиком и `docs/ROADMAP_STATUS.md`. **Добавлено (Pre-Release Cross-Platform Persistence, см. §2.16):** платформо-зависимые пути (`src/lib/platform-paths`), версионирование схемы (`src/lib/schema-versioning`, `docs/decisions/0002-*.md`), device-локальный lock (`src/lib/operation-lock`), scrub-and-checksum снапшот-пайплайн (`src/lib/snapshot`), device handoff с restricted recovery mode (`src/lib/device-handoff`), единый choke-point для Web (`src/proxy.ts`)/CLI/MCP. 448 тестов на момент завершения этой задачи (`npm test`).

**Запланировано, но не реализовано (Phase 5, Slice 4 — единственное оставшееся):**
- Реальный YouTube-адаптер за портом `WriteExecutor` — Slices 1-3 построили весь pipeline (ledger/attempt/retry/reconciliation/verification/audit/recovery/isolation) поверх абстрактного порта; ни один approved change по-прежнему не отправляется в YouTube ни при каких обстоятельствах.
- CLI-команды для синхронизации каналов/видео, localization и change sets/batches (`channel_sync`, `channel_list`, `video_list`, `localization_list`, `localization_export`, `changeset_approve` и т.п.) — MCP read/propose-инструменты для всего этого уже есть (§2.13, включая `channel_sync`), CLI-паритета и MCP apply-класса пока нет.
- Конфигурация целевых языков канала (сейчас язык — это только то, что уже есть в существующих локализациях; см. `docs/ARCHITECTURE.md` §5.4).
- Явная модель deletion-предложений (осознанно отложена в Phase 4, см. `docs/ARCHITECTURE.md` §6.14).
- AI-генерация локализаций (провайдер-агностичный интерфейс, драфты через тот же approval pipeline).

**Осознанно отложено (не входит в текущий MVP, см. `docs/PROJECT_SPEC.md` §58):**
AI-генерация метаданных, publishing/upload видео, thumbnail-модуль, multi-user SaaS/RBAC, миграция БД-инструментария (Drizzle Kit) — до первого неаддитивного изменения схемы. **YouTube Analytics больше не в этом списке** — реализовано (Phase 8, §2.9i; Studio-Parity S6b Overview, §2.9n); этот абзац не обновлялся с момента Phase 6 (см. предупреждение выше), актуальный статус — §2 целиком.

---

## 5. Замечание про безопасность записи (сквозной инвариант)

Единственный существующий write-путь для метаданных — `video-metadata/services.ts` → `applyMetadata` (identity check + guardrail + diff + опциональный dry-run, без backup/audit/verification — это уже задокументированный пробел в `docs/UPSTREAM_ANALYSIS.md` §7). Любой новый write-путь **обязан** проходить через `write-context.assertWriteChannel`, а не через собственную проверку канала.
