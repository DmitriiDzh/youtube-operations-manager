# ROADMAP_STATUS.md

**Execution log**, not a requirements document. `docs/PROJECT_SPEC.md` remains the unmodified source of truth for *what* the product must do and in what order; this file records *what has actually happened* — which phase is complete, when, in which commit, and what is blocking the next one. When the two would ever seem to disagree on sequencing, `docs/PROJECT_SPEC.md` wins; this file is corrected to match reality, never the other way around.

Update this file whenever a phase completes — see `AGENTS.md` §C. **Completing a phase and recording it here does not authorize starting the next phase.** The next phase begins only when the project owner explicitly assigns it.

For the strategic backlog of *not-yet-assigned* future phases (Phase 7 onward), see
`docs/roadmap/FUTURE_PHASES.md` — that file is planning context only; this file remains the sole
record of what has actually been implemented.

---

## Phase status

| Phase | Status | Completion date | Commit(s) | Summary |
|---|---|---|---|---|
| Phase 0 — Analyze upstream | COMPLETED | 2026-09-15 | `bab2432` | `docs/UPSTREAM_ANALYSIS.md` — architecture inventory of the TubeMaster-derived baseline. |
| Phase 1 — Independent baseline | COMPLETED | 2026-09-15 | `bab2432` | `docs/UPSTREAM_BASELINE.md` — verified install/test/lint/build against baseline commit `e8f5bae`; documented as "READY FOR PHASE 2". |
| Phase 2 — Channel/video synchronization | COMPLETED | 2026-09-15 | `259d9a1`, `17880a4` | Read-only `channel-sync` module, `channels`/`videos` tables, uploads-playlist enumeration, batched `videos.list`. |
| Phase 3 — Localization Manager (read-only) + XLSX export | COMPLETED | 2026-09-15 | `137c9ab` | `localization/` read model, XLSX export (Videos + Localizations sheets). |
| Phase 4 — XLSX import, draft state, Change Sets, diff/approval | COMPLETED | 2026-09-16 | `6a561ea` | `changesets/` module, `change_sets`/`changes` tables, import/validate/diff/approve-reject, conflict detection against synced state, Web UI + API. No YouTube writes anywhere in this phase. |
| Phase 4.5 — Development autonomy, documentation & security gates | COMPLETED | 2026-09-16 | `19df1fb`, `f54ffbf` | `docs/DEVELOPMENT_PLAYBOOK.md`, `docs/TECHNICAL_DEBT.md`, `docs/decisions/` (ADR policy), `AGENTS.md` rewritten as the primary persistent instruction file, specification-driven/independent testing standards (`AGENTS.md` §L, `docs/DEVELOPMENT_PLAYBOOK.md` §6.14). Documentation-only — no application code changed. |
| Phase 5 — Safe YouTube localization writes | **CODE-COMPLETE against mocked adapters** (Gate B — live validation — explicitly NOT satisfied) | Slices 1-3: 2026-09-17; Slice 4: 2026-09-18; Slice 5 + completion task: 2026-09-18; RISK-12/13 follow-up: 2026-09-18 | `d38704e` (RISK-12/13 fix + Phase 5 completion task; Slices 1-3 in `74403cf`) | `src/lib/batches/`, `src/lib/backup/`, `src/lib/audit/` — batch entity, per-video execution ledger, durable attempt-intent model, safety preparation, dry-run, crash recovery, durable audit trail, post-write verification, a real `WriteExecutor` (`adapters/write-executor.youtube.ts`) behind a two-layer, unconditional live-write barrier, configurable bounded concurrency, a batched preliminary fetch, the full 100-video resume scenario, `AC-CONCURRENCY-03`'s two-batch race, `AC-E2E-01`'s integrated scenario, RISK-11 resolved for both write paths, and a minimal dry-run-only Web UI/API. **Still no production code path can reach a real `videos.update` call** — confirmed by an automated repository-wide inventory test, independently re-verified after this round's new API routes were added, and again after Phase 6 Slice 1 added its own new API routes (see that row below). See `docs/SYSTEM_MAP.md` §2.9a and `docs/TECHNICAL_DEBT.md` RISK-09/12/13 for full detail. RISK-12 (unsafe `dryRun` default, including a second site found in the CLI) and RISK-13 (AC-QUOTA-01 wording) are both **closed/resolved** as of 2026-09-18 with explicit project-owner approval; the only remaining Gate B blocker is now the `LIVE VALIDATION` track. **Unchanged and not reopened by Phase 6 work below** — still code-complete, live validation still pending, Gate B still pending. |
| Phase 6, Slice 1 — AI Localization (mock provider) | **CODE-COMPLETE for the mock-provider vertical slice** (real-provider integration explicitly out of scope, pending a separate future decision) | 2026-09-19 | uncommitted as of this entry | `src/lib/ai-localization/` — `LocalizationProvider` extension point (`docs/PROJECT_SPEC.md` §32), deterministic mock provider, proposal generation + validation (same field rules as XLSX import), human inspect/edit step, and Change Set creation (`source: "ai_localization"`) via a newly-generalized, shared persistence path in `src/lib/changesets/services.ts` (`createChangeSetFromProposals`, also now used by XLSX import — no parallel implementation, `AGENTS.md` §D). Approval, conflict revalidation, and Batch/dry-run are the exact same Phase 4/5 code, unmodified and unaware this feature exists (`ChangeSet.source` is never branched on in `src/lib/batches/**`). Two new dry-run-safe API routes (`.../ai-localization/generate`, `.../ai-localization/change-sets`) and one new Web UI tab. Acceptance contract: `docs/acceptance/PHASE_6_ACCEPTANCE.md`. Zero real AI-provider calls and zero real YouTube calls are structural properties, verified by an automated inventory test (`src/lib/ai-localization/write-path-inventory.test.ts`) mirroring Phase 5's own write-path inventory pattern. Independently reviewed twice (2026-09-19): one real isolation bug found and fixed (a provider that throws, not just returns an error outcome, could abort the whole generation call — fixed with a per-target try/catch, regression test added). |
| Phase 6, Channel Editorial Profiles (Part B) | **CODE-COMPLETE** | 2026-09-19 | uncommitted as of this entry | Persistent, per-channel, versioned editorial profile (`channelEditorialProfiles` + immutable `aiLocalizationGenerationProvenance` tables, `src/lib/db.ts`, purely additive per ADR 0001), merged per-field with an optional per-request `editorialBrief` override (`mergeEditorialContext`, `src/lib/ai-localization/services.ts`), automatically applied during generation, and recorded against the resulting Change Set so it survives later profile edits (reproducibility). New API routes (`GET/PUT .../ai-localization/profile`, `GET .../ai-localization/change-sets/[changeSetId]/provenance`) and a minimal profile editor added to the existing AI Localization tab. 10 new tests (`docs/acceptance/PHASE_6_ACCEPTANCE.md` AC-PROFILE-01..10): channel isolation, missing/empty profile, edit/persist semantics (omit vs. explicit null), version/snapshot survival across edits, per-field merge, malformed data rejection, no auto-approval side effect. Supports an arbitrary number of channels; no channel-specific content is hardcoded anywhere. |
| Phase 6, Provider-Agnostic AI Connections | **CODE-COMPLETE for the OpenAI-compatible protocol adapter** | 2026-09-19 | uncommitted as of this entry | `src/lib/ai-connections/` — a provider-agnostic connection entity (id, name, adapter type, base URL, model id, encrypted-at-rest optional credential, enabled/status, capability metadata, task assignment), a small adapter registry (`mock` reusing the existing deterministic provider; `openai_compatible`, the one real protocol adapter, requiring an explicitly-declared structured-output capability). Credential storage: AES-256-GCM, key from `AI_CONNECTIONS_ENCRYPTION_KEY` (never committed), fail-closed with no plaintext fallback if unset — chosen over OS-keychain (deferred, native-dependency risk) per the recorded architectural decision in `docs/acceptance/PHASE_6_AI_CONNECTIONS_ACCEPTANCE.md` §2. SSRF protection (`validateEndpointUrl`): HTTPS-only for remote endpoints, IP-literal and DNS-resolved-address blocking against private/loopback/link-local/metadata/reserved ranges, a separate explicit local-inference-mode flag, re-validated immediately before every real call (documented residual TOCTOU limitation, RISK-14). `src/lib/ai-localization/services.ts` gained one optional dependency (`resolveConnectionProvider`) and one optional input field (`connectionId`) — no other change to that domain's logic (AGENTS.md §D). New Settings UI tab (`ai-connections-manager.tsx`) and API routes (`/api/ai-connections`, `/api/ai-connections/[connectionId]`, `/api/ai-connections/[connectionId]/test` — the only caller of `testConnection`, always an explicit user action). Cost controls: a much lower per-call target cap for real connections than the mock path, bounded timeout+retry, token usage surfaced when the provider reports it, unknown usage/pricing reported as `null` never `0`. 34 new tests (`docs/acceptance/PHASE_6_AI_CONNECTIONS_ACCEPTANCE.md` AC-CONN-01..17): CRUD, credential non-exposure/encryption/fail-closed, SSRF blocking (IP literals, DNS-resolved private addresses, IPv6), capability rejection, malformed-JSON/timeout isolation, and a full connection-backed generation → Change Set → approve → Batch dry-run pipeline proving the mock-provider guarantees hold identically for a real (fake-HTTP-backed) connection. Independently reviewed for requirements conformance and, separately, for security (credential handling + SSRF specifically) — see that review's findings incorporated into this entry and into RISK-14/15. |
| Pre-Release — Cross-Platform Persistence & Syncthing Handoff (Variant A) | **CODE-COMPLETE, Windows-validated only** (not a numbered phase — does not authorize Phase 7) | 2026-09-19 | Merged to `dev` in `25917b5` ("Интегрировать feature/cross-platform-persistence в dev") | Platform-aware app-data location (`src/lib/platform-paths`, Windows/macOS), explicit schema versioning layered on the existing additive boot pattern (`src/lib/schema-versioning`, `docs/decisions/0002-additive-schema-versioning.md`), a real device-local operation lock (`src/lib/operation-lock`, `app_operation_locks`), a scrub-then-checksum snapshot export/import pipeline with an explicit transfer allowlist (`src/lib/snapshot`), device-handoff orchestration with a live-recomputed (never cached) restricted recovery mode for imported unresolved execution state (`src/lib/device-handoff`), and one shared mutation gate enforced at three independent choke points (`src/proxy.ts` — Next.js 16's renamed `middleware.ts`; the CLI's `runCliCommand`; MCP's `createMcpToolHandlers`). New minimal Web UI (Device tab) and API routes (`/api/device-handoff/**`). Full detail: `docs/ARCHITECTURE.md` §13, `docs/SYSTEM_MAP.md` §2.16, `docs/RELEASE_LAYOUT.md` (first-run/device-switching procedure), `docs/acceptance/PRE_RELEASE_CROSS_PLATFORM_ACCEPTANCE.md` (acceptance contract, ~45 scenarios). Three follow-up corrections from the project owner during design review were incorporated before implementation: (1) `users.id` confirmed to be the stable Google OAuth `sub`, so import never touches `users`/credentials at all rather than merely excluding them from the snapshot; (2) the operation lock's enforcement boundary made concrete (a real SQLite-level `INSERT`-based exclusivity plus three interface choke points, not an advisory flag); (3) operator acknowledgement of recovery-mode diagnostics made strictly informational — it can never itself lift the gate or change a row's status. `npm test`: 448/448 (up from 419 before this task); `npm run lint`/`npm run build` clean throughout. **Explicitly not validated on real macOS hardware** (`docs/TECHNICAL_DEBT.md` RISK-17) and no real Google OAuth login was performed in this environment (`docs/TECHNICAL_DEBT.md` RISK-05, unchanged) — both stated plainly, not implied as done. New residual risks recorded: RISK-16 (recovery mode has no in-app resolution path until RISK-04 is addressed), RISK-17 (macOS unvalidated). |
| Pre-Release — First Local Test Build | **Windows launcher written, not run on real Windows; macOS launcher and build/runtime checks actually executed** (not a numbered phase — does not authorize Phase 7) | 2026-09-19 | Merged to `dev` in `a6bbb0c` | `docs/FIRST_LOCAL_TEST_BUILD.md` (operator guide), `scripts/windows/{start,stop,update}.bat`, `scripts/macos/{start,stop,update}.sh`, `.env.example`. On real macOS hardware: production build actually run (not just `npm run build`) — server start, HTTP 200 on `/` and `/dashboard`, `401` on unauthenticated API routes, database created at the real platform-aware app-data path with the full expected schema, byte-identical database across a restart, and a disposable-data update-verification (seeded rows survive a rebuild+restart under an overridden `HOME`, never touching the operator's real location). The three macOS launcher scripts were executed for real, not just reviewed. **No UI-level (signed-in browser) checks were performed** — every such check requires a real Google OAuth login, explicitly out of scope for an automated agent (`AGENTS.md` §G) — see `docs/FIRST_LOCAL_TEST_BUILD.md` §7 for the resulting manual checklist. **Windows launcher scripts were written and reasoned about but not executed on a real Windows machine** — none was available in the implementing environment; this is the largest remaining gap given the task's stated Windows priority. Full session detail: `docs/reports/FIRST_LOCAL_TEST_BUILD_SESSION.md`. Two stale-documentation discrepancies found and corrected as part of this task: `docs/ROADMAP_STATUS.md`'s own Cross-Platform Persistence row said "pending `dev` merge" when it was already merged (see the row above, now corrected), and `docs/getting-started.md` §6 still pointed at the legacy `data/playlist-manager.db` path superseded by the immediately preceding task. `npm test`: 456/456; `npm run lint`/`npm run build` clean. |
| Pre-Release — Publish snapshots + independent-review risk remediation | **Both done** (not a numbered phase — does not authorize Phase 7) | 2026-09-19 | 5 feature branches merged to `dev`, tip `b299287` | Two project-owner-assigned follow-ups after the First Local Test Build: (1) `published/<version>/` in-repo release-snapshot mechanism (`docs/decisions/0003-published-release-snapshots.md`, `scripts/publish-snapshot.mjs`) — allowlisted, immutable-once-created, generated and end-to-end smoke-tested outside the repository, never populated inside it (no numbered version has actually been released yet); (2) independent review (`/code-review high`) run twice against this work, both times mis-scoping its diff against `main` instead of the actual feature-branch base (a reproducible tool bug, reported upstream) and surfacing 17 real findings in already-merged prior work instead — all logged as RISK-18..34 in `docs/TECHNICAL_DEBT.md`, then explicitly assigned by the project owner ("Начни с 1. Отработай найденные риски") and fixed across 5 sequential batches: RISK-18/24 (device-handoff path traversal; app-data dir `chmod 0700`), RISK-19/21/33 (fail-open bare-catch error handling, consolidated into a new shared `isMissingTableError`), RISK-28/31 (write-channel guardrail skipped on batch resume — the single most safety-relevant finding; ledger-status-drift on a concurrent race), RISK-27/29 (leaked pre-import backup on failed migration; positional snapshot merge broken by `ALTER TABLE`-added columns), RISK-20/22/30/34 (boot-time migration never lock-protected; no Windows rename retry in `writeJsonFileAtomic`; AI Localization's real-connection path bypassing the device-availability gate; the two "recovery-gate" test suites never actually exercising recovery mode). RISK-26 (snippet-field whitelist completeness) and RISK-32 (a shared mutating-operation registry across `proxy.ts`/CLI/MCP) were deliberately left `OPEN` with an explicit disposition rather than force-fit into this round — the first needs external API-docs research before Gate B, the second is its own cross-cutting refactor. `npm test`: 456 → 479 (+23 new regression tests, one per fix). `npm run lint`/`npm run build` clean throughout; every batch independently verified before merging into the next. |
| Phase 7 planning — Codex Operations Interface plan document | **Plan produced, not implemented** (does not authorize starting Phase 7's implementation) | 2026-09-20 | Merged to `dev` in feature/phase-7-plan | `docs/roadmap/plans/PHASE_7_PLAN.md` — produced per the project owner's explicit Telegram instruction to work through `docs/roadmap/BACKLOG.md`'s assigned items (`BL-001`/`BL-002`). Inspects the actual current MCP surface (`src/mcp/server.ts`), confirms `docs/TECHNICAL_DEBT.md` RISK-04 as the concrete gap, proposes a read/propose-only first slice (`changeset_*`/`batch_*`/`localization_*` MCP tools, no apply-class tool — deliberately excluded pending Gate B), explicit non-goals, and acceptance criteria drafted per `AGENTS.md` §L. No MCP tool code, API route, or workspace was written — this is planning only. |
| Phase 8 planning — Intelligence Foundation plan document | **Plan produced, not implemented** (does not authorize starting Phase 8's implementation) | 2026-09-20 | Merged to `dev` in feature/phase-8-plan | `docs/roadmap/plans/PHASE_8_PLAN.md` — confirms Phase 8 is greenfield (no analytics code, table, or OAuth scope exists today), proposes an additive `video_metrics_daily`-style table and a manual-trigger `views`-only collection slice as the smallest first step, explicitly deferring scheduling, additional metrics, and any hypothesis/report logic. Flags the new Analytics OAuth scope as its own required owner decision. No code written. |
| Phase 9 planning — Market Discovery & Trend Intelligence plan document | **Plan produced, not implemented** (does not authorize starting Phase 9's implementation) | 2026-09-20 | Merged to `dev` in feature/phase-9-plan | `docs/roadmap/plans/PHASE_9_PLAN.md` — proposes `research_channels`/`research_evidence` tables, structurally separate from `channels`/`videos` (the owned/not-owned boundary stays a distinct entity, never a flag), with a manually-seeded first slice (no auto-discovery, no scoring). Explicitly excludes any private-analytics-shaped field and any profitability conclusion per `FUTURE_PHASES.md` §5's own constraint. No code written. |
| Phase 10 planning — Decision & Experiment Engine plan document | **Plan produced, not implemented** (does not authorize starting Phase 10's implementation) | 2026-09-20 | Merged to `dev` in feature/phase-10-plan | `docs/roadmap/plans/PHASE_10_PLAN.md` — proposes `hypotheses`/`experiments`/`decisions` tables, structurally separate from `change_sets`/`changes`, with a manual-entry-only first slice (no AI-generated hypotheses, no automatic execution). Explicitly notes dependency on Phase 8 (metrics) and Phase 9 (candidates) for real evidence, and that the record-keeping shape works even without them at first. No code written. |
| Future directions — feasibility research (multi-device sync, automated media production) | **Research done; neither advanced to a plan document** (does not authorize starting either) | 2026-09-20 | Merged to `dev` in feature/future-directions-research | `docs/roadmap/plans/FUTURE_DIRECTIONS_RESEARCH.md` — for multi-device sync, concludes a foundational architecture decision (conflict resolution strategy vs. moving off local SQLite) must happen before any vertical slice exists to plan; for automated media production, concludes it is four largely unrelated sub-capabilities needing an owner choice of which one (if any) to pursue first. Neither produced a `PHASE_N_PLAN.md`-style document, since `FUTURE_PHASES.md` §7 itself frames both as opportunities only, not phases. No code written. |
| Studio-parity planning — Home/Content/Analytics/Languages tabs | **Plan produced, not implemented** (does not authorize starting any slice) | 2026-09-20 | Merged to `dev` in feature/studio-parity-plan | `docs/roadmap/plans/STUDIO_PARITY_PLAN.md` — produced per the project owner's Telegram request (msg 122) after manually testing the app against real YouTube Studio. Live-researched (via `claude-in-chrome`, against the real "Tropico Jazz" Studio session) the exact structure of Studio's Home/Content/Analytics/Languages sections, built a per-tab dependency table (what this app already has vs. is missing), and proposed slices `BL-012`..`BL-017` (`docs/roadmap/BACKLOG.md`) in a recommended dependency order — S1 (sync view/comment/like stats), S2 (Content tab), S3 (Languages tab, later split into its own merge plan — see next row), S4 (Home tab, Data-API-v3 portion; comments/subscribers feeds flagged as needing separate feasibility research first), S6-stub (Analytics placeholder tab). The Analytics tab's full-data slice (S6) is deliberately not a new plan — it is `docs/roadmap/plans/PHASE_8_PLAN.md`'s existing Phase 8 scope, requested here as a full-parity UI target rather than Phase 8's own smaller first vertical slice; `FUTURE_PHASES.md` §7 gained one new bullet recording the idea's existence and this cross-reference. Owner follow-up (Telegram msg 125) resolved 3 of the 4 open questions the plan originally raised: visual fidelity = maximally close to real Studio (not "our own design language"); Languages = restyle the existing tab using Studio's menu as the base (see next row for the resulting sub-plan); Analytics = placeholder stub now, revisit at Phase 8's own stage. No UI, schema, or API code written. |
| Languages tab merge planning — fold AI Localization into a Studio-styled Languages tab | **Plan produced, not implemented** (does not authorize starting any slice) | 2026-09-20 | Merged to `dev` in feature/languages-tab-merge-plan | `docs/roadmap/plans/LANGUAGES_TAB_MERGE_PLAN.md` — produced per the project owner's explicit follow-up request (Telegram msg 125: "Это переделка требует отдельного планирования и проработки") to fold the separate "AI Localization" tab into a Studio-styled "Languages" tab (renamed from "Localizations"). Confirms the Change Set review UI (`change-set-review.tsx`) is already fully shared/unified across both XLSX-import and AI-generated sources — no duplicate approve/reject logic to reconcile — and finds an existing display bug (a Change Set's `source` field is never shown; every AI-generated set is mislabeled "XLSX import" since `importedFilename` is null for it). Proposes a Studio-shaped video table + re-mapped sub-tabs (explicitly avoiding "Опубликовано"/Published wording, since Phase 5 live YouTube writes remain barrier-disabled — RISK-09 — and that label would misleadingly imply a real write happened), a per-video detail view with both "Import from XLSX" and "Generate with AI" entry points feeding the same review UI, and a 5-slice breakdown (L1..L5) where L1 (drop now-redundant per-tab channel selectors post-RISK-02, fix the source-label bug) has no open questions and is separately assignable now. Flags 4 open questions (sub-tab semantics, bulk-vs-per-video AI generation UI placement, editorial-profile placement, nav-item removal) requiring owner sign-off before the rest can be assigned. No code written. **Follow-up (Telegram msg 128) resolved 2 of the 4 open questions same-day:** AI generation is the primary per-video path (manual edit = review/correct the agent's output, not independent authoring), and the "AI Localization" nav item is removed outright, immediately, no transition period. Plan updated in place; sub-tab semantics and editorial-profile placement remain open. **Second follow-up (Telegram msg 131) resolved sub-tab semantics** ("Все/В процессе/Одобрено", confirmed). **Third follow-up (Telegram msg 135) resolved editorial-profile placement — it moves to the Home tab instead, not Languages** (owner: "звучит как что-то фундаментальное... давай вынесем это пока в закладку Home, как доп меню"); `STUDIO_PARITY_PLAN.md` Slice S4 updated accordingly, tracked as `BL-030`. All 4 of this plan's original open questions are now resolved; only an implementation-time visual-prominence judgment call (XLSX-import vs. AI-generate on the landing view) remains, and it does not block assignment. |
| Tab auto-refresh & channel-dropdown-removal planning | **Plan produced, not implemented** (does not authorize starting any slice) | 2026-09-20 | Merged to `dev` in feature/tab-refresh-and-device-sync-plans | `docs/roadmap/plans/TAB_REFRESH_AND_CHANNEL_UI_PLAN.md` — produced per the project owner's Telegram request (msg 128) to remove now-redundant channel-selection dropdowns and make tabs refresh their data automatically on switch. Finds that most tabs (Localizations, Batches, Settings, Device) already refresh on every switch for free, via this app's existing conditional-rendering tab pattern (mount/unmount on tab change) — the real gaps are the Rules tab (state owned by the parent component, fetched once at dashboard-mount) and the Sync tab's live YouTube re-sync, which costs real API quota and needs an explicit trigger-policy decision rather than blind per-click automation. Proposes 3 slices (T1 dropdown removal, T2 Rules fix — both immediately assignable with no open questions — and T3 the Sync policy, blocked on an owner decision between fully-automatic/staleness-gated/manual-only). No code written. |
| Device-handoff auto-sync planning — auto-export, auto-import, update notification | **Plan produced, not implemented** (does not authorize starting any slice) | 2026-09-20 | Merged to `dev` in feature/tab-refresh-and-device-sync-plans | `docs/roadmap/plans/DEVICE_HANDOFF_AUTO_SYNC_PLAN.md` — produced per the project owner's Telegram request (msg 128) to auto-save handoff snapshots on session close/on every change, auto-load the latest on startup, and notify when a newer snapshot appears while running. Grounded in the actual code: export acquires the app-wide operation lock and runs a full-database `VACUUM` (real cost — "export on every single mutation" is deliberately not the literal design), import's divergent-lineage rejection is an intentional human-decision point that must never be auto-resolved, and **no graceful-shutdown hook exists anywhere in this codebase today** (a `SIGTERM`/`SIGINT` handler is new infrastructure, not a config flag). Proposes 5 slices (D1 shutdown-export hook, D2/D3 newer-snapshot detection + a new notification-bell UI, D4 safe-case-only auto-import at startup, D5 periodic auto-export pending an owner decision on interval/cost) and explicitly states this does **not** make concurrent multi-device editing safe — that remains the separate, harder problem already tracked as future-direction "multi-device application-managed synchronization" (`BL-006`). No code written. |
| Phase 7, Slice 1 — read/propose-only Change Set/Batch MCP tools | **CODE-COMPLETE for this slice** (Phase 7 is not fully done — channel_sync MCP tool and any apply-class tool remain unimplemented; Gate B/live-write validation unaffected) | 2026-09-20 | Merged to `dev` in feature/phase-7-slice-1-mcp-tools | Per the project owner's explicit "Приступай к реализации этих планов по порядку" (Telegram, 2026-09-19), implementing `docs/roadmap/plans/PHASE_7_PLAN.md` §6 slices 1-4: `changeset_list`, `changeset_get`, `localization_import_preview`, `batch_list`, `batch_get` MCP tools in `src/mcp/server.ts`, wired to the same `createChangeSetCore()`/`createBatchCore()` factories the Web UI's own API routes already use (no parallel implementation). `batch_get` reuses `requireBatchForChannel`, not a bare `getBatch`, for channel-ownership verification (`AGENTS.md` §F). None of the five tools are wrapped by the device-availability mutation gate, since none mutates anything. 12 new tests (`src/mcp/server.test.ts`): success shapes, validation-error shapes, `DomainError` passthrough, base64 decode fidelity, and the channel-ownership check specifically. `docs/TECHNICAL_DEBT.md` RISK-04 moved from OPEN to PARTIALLY RESOLVED; `docs/interfaces.md` and `docs/SYSTEM_MAP.md` §2.13/§4.0 updated to match. `npm test`: 497 → 506. `npm run lint`/`npm run build` clean. Deliberately excluded from this slice, per the plan's own §4 non-goals: any apply-class Change Set/Batch tool (blocked on Gate B) and the isolated Codex workspace/compatibility mechanism (blocked on a published version existing). |
| BL-008 — `channel_sync`/`channel_list`/`channel_video_list` MCP tools | **CODE-COMPLETE** (closes the MCP portion of RISK-04; CLI parity and any apply-class tool remain unimplemented) | 2026-09-20 | Merged to `dev` in feature/backlog-bl008-channel-sync-mcp | Self-initiated backlog item (not part of `PHASE_7_PLAN.md`'s own slice list) found while auditing RISK-04's actual state after Phase 7 slice 1, per the project owner's explicit "если по результатам анализа создавай новые задачи в бэклог и реализуй их" (Telegram msg 67). `channel_sync` (mutating — writes to local `channels`/`videos`, gated by `assertMcpDeviceAvailable` exactly like `apply`), `channel_list`, `channel_video_list` (both read-only, ungated) in `src/mcp/server.ts`, reusing `createChannelSyncCore()` unchanged. 5 new tests including one proving the mutation gate is wired correctly (`channel_sync` blocked under the operation lock, `channel_list` not). `npm test`: 506 → 511. `docs/TECHNICAL_DEBT.md` RISK-04, `docs/interfaces.md`, `docs/SYSTEM_MAP.md` §2.13/§4.0 updated. `npm run lint`/`npm run build` clean. |
| Phase 7 — in-repository scope | **CODE-COMPLETE for everything buildable inside this repository** (Phase 7 as a whole is NOT fully done — see the two non-delegable remainders below) | 2026-09-20 | Merged to `dev` across feature/phase-7-slice-1-mcp-tools, feature/backlog-bl008-channel-sync-mcp, feature/phase-7-changeset-create-mcp, feature/phase-7-mcp-restricted-mode, feature/phase-7-cli-parity | Per the project owner's explicit "Создай список задач чтобы полностью реализовать объем фазы 7 и довести ее до релиза... Автоматическая работа" (Telegram, 2026-09-20), completed a 5-item task list covering every part of Phase 7 that can actually be built from inside this repository: (1) `changeset_create_from_import` MCP tool — real Change Set creation, correcting an earlier wrong claim that this was Gate-B-blocked (it is local-only persistence, not a YouTube write); (2) MCP restricted mode (`MCP_RESTRICTED_MODE`) — every YouTube-write-capable or identity-switching tool is never even registered, the concrete implementation of "operation-specific permissions and read-only access to application data" (`docs/roadmap/FUTURE_PHASES.md` §3); (3) CLI parity — `changeset`/`batch`/`channel` namespaces mirroring every MCP read/propose/create tool; (4) verified (not assumed) that the CLI's device-availability gate correctly covers the new mutating commands, finding and fixing a real whitelist gap along the way; (5) this consolidating status entry. Combined with the earlier Phase 7 slice-1/BL-008 rows above, `docs/TECHNICAL_DEBT.md` RISK-04 is now PARTIALLY RESOLVED with the full read/propose/create surface on both MCP and CLI. `npm test`: 506 → 525 across this task's four commits; `npm run lint`/`npm run build` clean throughout. **Two remainders are permanently outside this repository's ability to close, regardless of instruction or working mode:** the isolated Codex operations workspace (`AGENTS.md` §B places it entirely outside this repository); and publishing/tagging/releasing a version for that workspace to target (`AGENTS.md` §K.2 requires separate, explicit project-owner approval every time, never inferred from an autonomous-work instruction). The only remaining in-repo Phase 7 work is an apply-class Change Set/Batch tool, itself gated on Gate B's live-write validation track (unrelated to this task, unaffected by it). |
| Pre-Release — Local test build re-validation (post review series + Phase 7) | **Done** (not a numbered phase — does not authorize a release) | 2026-09-20 | HEAD of `dev` at the time, `4941b3c` | Per the project owner's "собери тестовый релиз" (clarified via question to mean a local test build, not a `dev→main` release — `AGENTS.md` §K.2/§K.4 remain untouched by this task). Repeated the automated portion of `docs/FIRST_LOCAL_TEST_BUILD.md` §7 against the current `dev` tip, after the UI-shell redesign, roadmap-backlog skill, all of Phase 7 (MCP/CLI read-propose-create surface + restricted mode), and the 5-cycle review series: clean `npm run build`; server start under an isolated, disposable `HOME` (never the operator's real app-data path); `GET /` and `GET /dashboard` → 200; unauthenticated `GET /api/channels` and `GET /api/rules` → 401; database created at the correct platform-aware path with the expected schema (`schema_version = 3`, all Phase 4-6/cross-platform/Phase-7 tables present); a genuine stop/restart with the database byte-identical (same MD5) across it; clean shutdown, port freed, temp directory removed afterward. No real Google OAuth login was performed (`AGENTS.md` §G) — the manual, signed-in-browser portion of §7's checklist remains the operator's own work, unchanged from the original First Local Test Build task. |
| Pre-Release — YouTube Studio UI shell redesign (layer 1) | **Layer 1 (chrome/navigation only) done** (not a numbered phase — does not authorize Phase 7) | 2026-09-20 | Merged to `dev` in `4d0338c` ("Интегрировать feature/youtube-studio-ui-shell в dev") | Project-owner-assigned after "После 6 цикла проверки переходи к формированию плану по дизайну UI в стилистике YouTube studio... приступай к его выполнению" — a persistent left sidebar (`src/components/app-shell.tsx`, icon nav via new `src/components/icons.tsx`, red active indicator) and a top bar (active channel, switch-channel, sign-out) replace the previous single horizontal pill-tab-bar; a YT-Studio-style always-dark palette on CSS variables (`src/app/globals.css`); branding renamed "YouTube Playlist Manager" -> "YouTube Operations Manager". Explicitly scoped as chrome/navigation only — every tab's own content component (`ManualMode`, `RuleForm`, `AiLocalizationPanel`, etc.) is unchanged internally and still uses its pre-existing raw `zinc-*` classes; migrating those to the shell's design tokens is a separate, not-yet-assigned follow-up. Independent review (`/code-review high`) found 7 issues; 5 fixed on the feature branch before merge (dropped content max-width causing edge-to-edge stretch on wide viewports; `Tab`/`NAV_ITEMS` duplicated with no compiler-enforced completeness, now `Tab` is derived from `NAV_ITEMS` via `as const satisfies`; `AppShell` duplicated the channel type instead of importing `ChannelInfo`; `IconBase`'s default `className` was unreachable behind `{...props}`, now merged instead of overridden; unused `ChevronDownIcon` removed). Deliberately not fixed: no responsive collapse for the fixed 240px sidebar on narrow viewports (a real but larger UX gap, candidate for a future backlog item); the generic `AppShell<T>` having only one instantiation (not a defect). `npm test`: 497/497 both before and after the merge; `npm run lint`/`npm run build` clean. **No real, authenticated-browser visual verification was performed** — blocked by `AGENTS.md` §G's ban on automatic Google OAuth login — left for manual operator review. |

---

## Next assignment

**Pre-Release — Cross-Platform Persistence & Syncthing Handoff is CODE-COMPLETE as of 2026-09-19**
(see its own row above) — the project owner's explicit assignment for this specific task.

**Pre-Release — First Local Test Build is done as of 2026-09-19** (see its own row above) — a real
macOS runtime validation (build/start/DB-init/restart/update-persistence, and the macOS launcher
scripts themselves) was actually performed, partially addressing RISK-17; RISK-17 is not fully
closed, since it covered launcher validation and real macOS hardware existed to test that, but a
full manual browser smoke test (`docs/FIRST_LOCAL_TEST_BUILD.md` §7) has not. The Windows launcher
remains entirely unvalidated on real Windows hardware.

**Pre-Release — Publish snapshots + independent-review risk remediation is done as of 2026-09-19**
(see its own row above) — the `published/<version>/` mechanism and 15 of the 17 RISK-18..34
findings are fixed; RISK-26/RISK-32 are open by deliberate disposition (external research /
separately-scoped refactor, not forgotten).

**None of the above authorizes Phase 7 or any other phase** — the next phase still requires its
own explicit assignment per `AGENTS.md` §C. Before this build is fully operator-ready: the manual
browser smoke-test checklist (`docs/FIRST_LOCAL_TEST_BUILD.md` §7) and a real Windows run of
`scripts/windows/start.bat` — and, unrelated to this task, pre-existing — Phase 5's own
`LIVE VALIDATION` track described immediately below.

**Pre-Release — YouTube Studio UI shell redesign, layer 1, is done as of 2026-09-20** (see its own
row above) — assigned by the project owner ("После 6 цикла проверки переходи к формированию плану
по дизайну UI в стилистике YouTube studio... приступай к его выполнению"), explicitly preceding
Phase 7 in the owner's own instruction. This closes layer 1 (persistent sidebar + top bar chrome);
migrating each tab's own internal styling to the shell's design tokens was not part of this
assignment and would need its own. Per the owner's own sequencing, only after this work is
finished may backlog/roadmap items (Phase 7 onward) be started autonomously — see
`docs/roadmap/FUTURE_PHASES.md` §8-9.

**Phase 7's in-repository scope is CODE-COMPLETE as of 2026-09-20** (see its own row above) —
assigned directly by the project owner across two Telegram instructions ("приступай к реализации
этих планов по порядку", then "Создай список задач чтобы полностью реализовать объем фазы 7 и
довести ее до релиза... Автоматическая работа"). Every part of Phase 7 buildable inside this
repository now exists: read/propose/create MCP tools for Change Sets/Batches/channel-sync, an MCP
restricted mode, and full CLI parity. **Phase 7 as a whole remains not fully done** — the isolated
Codex operations workspace lives entirely outside this repository by design (`AGENTS.md` §B), and
publishing a version for it to target requires its own separate, explicit project-owner approval
every time (`AGENTS.md` §K.2) — neither is something any autonomous-work instruction can close.
The only remaining in-repo Phase 7 item, an apply-class Change Set/Batch tool, waits on Gate B
(below), unrelated to and unaffected by this task.

**Phase 5 is CODE-COMPLETE against mocked/fake adapters as of 2026-09-18 (completion task, following Slice 5 and its independent review)** — every automated `AC-*` acceptance scenario now has a passing test (`AC-CONCURRENCY-03` and `AC-E2E-01`, the two gaps Slice 5 itself left open, are closed), a minimal dry-run-only Web UI/API exists for Batches, and RISK-11 (read-only snippet echo) is resolved for both write paths. **Gate B (`docs/TECHNICAL_DEBT.md`) is explicitly NOT satisfied and was not closed by this work** — real, non-dry-run YouTube writes remain disabled by an unconditional, two-layer barrier (independently re-verified this round) and require their own separate future authorization per `AGENTS.md` §K, plus every `LIVE VALIDATION` scenario `docs/acceptance/PHASE_5_ACCEPTANCE.md` §4 requires before Gate B (AC-MERGE-01/05, AC-GUARD-01, AC-CONFLICT-01, AC-RESUME-01, AC-E2E-01) — none of which were run.

**Both project-owner decisions from the prior round are now resolved (2026-09-18):**
- **RISK-12 — CLOSED.** The project owner approved flipping `applyMetadata`'s `dryRun` default to `true` as a deliberate, safety-first BREAKING BEHAVIORAL CHANGE. Applied: `applyMetadataInputSchema`'s default, the MCP `apply` tool's description, and a second, independently-found unsafe-default site in the CLI's `apply` command (which sent an explicit `dryRun: false` on omission, bypassing any schema default) are all fixed. All four entry points (Web UI — still zero callers, `/api/video-metadata/apply`, MCP `apply`, CLI `apply`) re-verified fail-closed: omitting `dryRun` anywhere is now a preview, never a write.
- **RISK-13 — RESOLVED.** The project owner approved the proposed narrow split. `docs/acceptance/PHASE_5_ACCEPTANCE.md`'s `AC-QUOTA-01` is now `AC-QUOTA-01a` (batched preliminary overview) and `AC-QUOTA-01b` (mandatory unbatched per-video fetch, restated) — a wording clarification only; no code or test changed, no safety requirement weakened.

**Phase 6, Slice 1 (AI Localization, mock provider only) is CODE-COMPLETE as of 2026-09-19**, per the project owner's own "Overnight Autonomous Development — Phase 6" assignment message (which is the explicit phase authorization `AGENTS.md` §C requires — no other document names a "Phase 6"). This did not touch, reopen, or weaken Phase 5 in any way: Phase 5 remains code-complete with live validation and Gate B both still pending, exactly as recorded above. See `docs/reports/OVERNIGHT_SESSION.md` for the full session report and `docs/acceptance/PHASE_6_ACCEPTANCE.md` for the acceptance contract.

**Phase 6, Channel Editorial Profiles (Part B of `docs/ai-localization/CHANNEL_CONTEXT_PROPOSAL.md`) is CODE-COMPLETE as of 2026-09-19**, per the project owner's explicit "Phase 6 — Channel Editorial Profiles" authorization. Persistent per-channel profiles, per-field merge with per-request `editorialBrief`, and reproducibility (provenance survives later profile edits) are all implemented and tested — see `docs/acceptance/PHASE_6_ACCEPTANCE.md` §4a (AC-PROFILE-01..10) and `docs/reports/OVERNIGHT_SESSION.md` for detail. This did not touch Phase 5.

**Remaining open question for the project owner:** which real `LocalizationProvider` (if any) to select and fund (OpenAI/Anthropic/DeepL/Google Translation/custom) — deliberately left unresolved per the original assignment's instructions (no paid provider selection, no external AI cost). `src/lib/ai-localization/provider-registry.ts` is the single place that decision would be wired in; `docs/ai-localization/PROVIDER_INTEGRATION_PLAN.md` documents the technical plan for when it is made.

**Next assignment, when the project owner is ready to pick one:** (a) the live-validation pass itself (a real test channel, real OAuth, project-owner or designated person only — never bundled into `npm test`) — this is now the single largest remaining item before Gate B; (b) CLI/MCP interfaces for Change Sets and Batches (`RISK-04`, deferred to before operations handoff, `DEC-OQ-5`); (c) the live-write activation procedure itself, a distinct and separately-gated future task, never to be started incidentally as part of another task; (d) the 20 remaining non-critical `npm audit` findings (`RISK-06`); (e) a real AI-provider integration for Phase 6 (a separate, explicit, cost-bearing decision, see `docs/ai-localization/PROVIDER_INTEGRATION_PLAN.md`).

`docs/TECHNICAL_DEBT.md`'s **Gate A** (before implementing Phase 5) is satisfied as of Phase 4.5 — see that document for the checklist.

**Acceptance contract:** `docs/acceptance/PHASE_5_ACCEPTANCE.md` — status **APPROVED** (fifth review round, 2026-09-17). OQ-1..OQ-6 answered; this approval authorizes Phase 5 preparation/implementation work (including mocked tests) but is not itself authorization for any real, non-dry-run YouTube write (`AGENTS.md` §K).

## Open blockers before Phase 5 writes may go live (Gate B)

Per `docs/TECHNICAL_DEBT.md`'s Gate B, none of the following may be waived by risk acceptance — each must be actually implemented AND tested AND, where the mechanism specifically requires it, live-validated:

- **RISK-03** — fresh remote conflict detection: **implemented and tested against mocks** for the batch preparation/send-time pipeline; `AC-CONFLICT-01` (= official test §56) still requires a live-validation run before Gate B.
- **RISK-09** — immutable backup, durable audit log, per-item execution ledger with idempotent resume, post-write verification: **all implemented and tested against mocks/fakes**, including the full 100-video resume scenario and the two-batch concurrency race; the real `WriteExecutor` exists but is barrier-disabled. **The only remaining blocker for Gate B is now the `LIVE VALIDATION` track itself** — every scenario in `docs/acceptance/PHASE_5_ACCEPTANCE.md` §4 (AC-MERGE-01/05, AC-GUARD-01, AC-CONFLICT-01, AC-RESUME-01, AC-E2E-01) still requires a real test channel and real OAuth, never bundled into `npm test`. RISK-12 and RISK-13 are both closed (see below) and no longer block anything.

Also tracked, not blocking Phase 5 *development* but blocking later gates (see `docs/TECHNICAL_DEBT.md` for full detail): RISK-04 (no CLI/MCP Change Set/Batch interfaces — blocks Gate C, operations handoff), RISK-05 (no live browser/OAuth verification — blocks Gate C), RISK-06 (20 remaining, non-critical dependency advisories — blocks Gates C and D; the two critical ones are already patched), RISK-01/02/07 (deferred, accepted for the current single-operator local model, blocking only Gate D / network deployment). RISK-12 (legacy `applyMetadata`/MCP `apply` unsafe `dryRun` default) is **CLOSED**, no longer blocking Gate C.

---

## How to keep this file current

1. When a phase's Definition of Done (`docs/DEVELOPMENT_PLAYBOOK.md` §6.13) is satisfied and the project owner has accepted the work, add or update its row in the table above with the actual completion date and commit hash(es) — do this as part of the phase's own final commit or immediately after, not as a separately-forgotten follow-up.
2. Update "Next assignment" to name whatever the project owner assigns next; do not guess ahead or pre-announce a phase that has not been explicitly assigned.
3. Re-check "Open blockers" against the current state of `docs/TECHNICAL_DEBT.md` — a risk closed there should be reflected here, and vice versa.
4. This file is part of the documentation-maintenance table in `docs/DEVELOPMENT_PLAYBOOK.md` §6.12 ("a development phase is completed" row) — keep both in sync.
