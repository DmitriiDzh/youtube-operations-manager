# ROADMAP_STATUS.md

**Execution log**, not a requirements document. `docs/PROJECT_SPEC.md` remains the unmodified source of truth for *what* the product must do and in what order; this file records *what has actually happened* — which phase is complete, when, in which commit, and what is blocking the next one. When the two would ever seem to disagree on sequencing, `docs/PROJECT_SPEC.md` wins; this file is corrected to match reality, never the other way around.

Update this file whenever a phase completes — see `AGENTS.md` §C. **Completing a phase and recording it here does not authorize starting the next phase.** The next phase begins only when the project owner explicitly assigns it.

For the strategic backlog of *not-yet-assigned* future phases (Phase 7 onward), see
`docs/roadmap/FUTURE_PHASES.md` — that file is planning context only; this file remains the sole
record of what has actually been implemented.

---

## Phase status

| Phase | Status | Completion date | Commit(s) | Summary |
|---|---|---|---|---|
| Phase 0 — Analyze the original codebase | COMPLETED | 2026-09-15 | `bab2432` | Architecture inventory of the original pre-rewrite baseline (doc since retired once the codebase fully diverged, per the project owner's later cleanup instruction). |
| Phase 1 — Independent baseline | COMPLETED | 2026-09-15 | `bab2432` | Verified install/test/lint/build against the original baseline commit `e8f5bae`; documented as "READY FOR PHASE 2" (doc since retired, same cleanup instruction). |
| Phase 2 — Channel/video synchronization | COMPLETED | 2026-09-15 | `259d9a1`, `17880a4` | Read-only `channel-sync` module, `channels`/`videos` tables, uploads-playlist enumeration, batched `videos.list`. |
| Phase 3 — Localization Manager (read-only) + XLSX export | COMPLETED | 2026-09-15 | `137c9ab` | `localization/` read model, XLSX export (Videos + Localizations sheets). |
| Phase 4 — XLSX import, draft state, Change Sets, diff/approval | COMPLETED | 2026-09-16 | `6a561ea` | `changesets/` module, `change_sets`/`changes` tables, import/validate/diff/approve-reject, conflict detection against synced state, Web UI + API. No YouTube writes anywhere in this phase. |
| Phase 4.5 — Development autonomy, documentation & security gates | COMPLETED | 2026-09-16 | `19df1fb`, `f54ffbf` | `docs/DEVELOPMENT_PLAYBOOK.md`, `docs/TECHNICAL_DEBT.md`, `docs/decisions/` (ADR policy), `AGENTS.md` rewritten as the primary persistent instruction file, specification-driven/independent testing standards (`AGENTS.md` §L, `docs/DEVELOPMENT_PLAYBOOK.md` §6.14). Documentation-only — no application code changed. |
| Phase 5 — Safe YouTube localization writes | **CODE-COMPLETE against mocked adapters** (Gate B — live validation — explicitly NOT satisfied) | Slices 1-3: 2026-09-17; Slice 4: 2026-09-18; Slice 5 + completion task: 2026-09-18; RISK-12/13 follow-up: 2026-09-18 | `d38704e` (RISK-12/13 fix + Phase 5 completion task; Slices 1-3 in `74403cf`) | `src/lib/batches/`, `src/lib/backup/`, `src/lib/audit/` — batch entity, per-video execution ledger, durable attempt-intent model, safety preparation, dry-run, crash recovery, durable audit trail, post-write verification, a real `WriteExecutor` (`adapters/write-executor.youtube.ts`) behind a two-layer, unconditional live-write barrier, configurable bounded concurrency, a batched preliminary fetch, the full 100-video resume scenario, `AC-CONCURRENCY-03`'s two-batch race, `AC-E2E-01`'s integrated scenario, RISK-11 resolved for both write paths, and a minimal dry-run-only Web UI/API. **Still no production code path can reach a real `videos.update` call** — confirmed by an automated repository-wide inventory test, independently re-verified after this round's new API routes were added, and again after Phase 6 Slice 1 added its own new API routes (see that row below). See `docs/SYSTEM_MAP.md` §2.9a and `docs/TECHNICAL_DEBT.md` RISK-09/12/13 for full detail. RISK-12 (unsafe `dryRun` default, including a second site found in the CLI) and RISK-13 (AC-QUOTA-01 wording) are both **closed/resolved** as of 2026-09-18 with explicit project-owner approval; the only remaining Gate B blocker is now the `LIVE VALIDATION` track. **Unchanged and not reopened by Phase 6 work below** — still code-complete, live validation still pending, Gate B still pending. |
| Phase 6, Slice 1 — AI Localization (mock provider) | **CODE-COMPLETE for the mock-provider vertical slice** (real-provider integration explicitly out of scope, pending a separate future decision) | 2026-09-19 | uncommitted as of this entry | `src/lib/ai-localization/` — `LocalizationProvider` extension point (`docs/PROJECT_SPEC.md` §32), deterministic mock provider, proposal generation + validation (same field rules as XLSX import), human inspect/edit step, and Change Set creation (`source: "ai_localization"`) via a newly-generalized, shared persistence path in `src/lib/changesets/services.ts` (`createChangeSetFromProposals`, also now used by XLSX import — no parallel implementation, `AGENTS.md` §D). Approval, conflict revalidation, and Batch/dry-run are the exact same Phase 4/5 code, unmodified and unaware this feature exists (`ChangeSet.source` is never branched on in `src/lib/batches/**`). Two new dry-run-safe API routes (`.../ai-localization/generate`, `.../ai-localization/change-sets`) and one new Web UI tab. Acceptance contract: `docs/acceptance/PHASE_6_ACCEPTANCE.md`. Zero real AI-provider calls and zero real YouTube calls are structural properties, verified by an automated inventory test (`src/lib/ai-localization/write-path-inventory.test.ts`) mirroring Phase 5's own write-path inventory pattern. Independently reviewed twice (2026-09-19): one real isolation bug found and fixed (a provider that throws, not just returns an error outcome, could abort the whole generation call — fixed with a per-target try/catch, regression test added). |
| Phase 6, Channel Editorial Profiles (Part B) | **CODE-COMPLETE** | 2026-09-19 | uncommitted as of this entry | Persistent, per-channel, versioned editorial profile (`channelEditorialProfiles` + immutable `aiLocalizationGenerationProvenance` tables, `src/lib/db.ts`, purely additive per ADR 0001), merged per-field with an optional per-request `editorialBrief` override (`mergeEditorialContext`, `src/lib/ai-localization/services.ts`), automatically applied during generation, and recorded against the resulting Change Set so it survives later profile edits (reproducibility). New API routes (`GET/PUT .../ai-localization/profile`, `GET .../ai-localization/change-sets/[changeSetId]/provenance`) and a minimal profile editor added to the existing AI Localization tab. 10 new tests (`docs/acceptance/PHASE_6_ACCEPTANCE.md` AC-PROFILE-01..10): channel isolation, missing/empty profile, edit/persist semantics (omit vs. explicit null), version/snapshot survival across edits, per-field merge, malformed data rejection, no auto-approval side effect. Supports an arbitrary number of channels; no channel-specific content is hardcoded anywhere. |
| Phase 6, Provider-Agnostic AI Connections | **CODE-COMPLETE for the OpenAI-compatible protocol adapter** | 2026-09-19 | uncommitted as of this entry | `src/lib/ai-connections/` — a provider-agnostic connection entity (id, name, adapter type, base URL, model id, encrypted-at-rest optional credential, enabled/status, capability metadata, task assignment), a small adapter registry (`mock` reusing the existing deterministic provider; `openai_compatible`, the one real protocol adapter, requiring an explicitly-declared structured-output capability). Credential storage: AES-256-GCM, key from `AI_CONNECTIONS_ENCRYPTION_KEY` (never committed), fail-closed with no plaintext fallback if unset — chosen over OS-keychain (deferred, native-dependency risk) per the recorded architectural decision in `docs/acceptance/PHASE_6_AI_CONNECTIONS_ACCEPTANCE.md` §2. SSRF protection (`validateEndpointUrl`): HTTPS-only for remote endpoints, IP-literal and DNS-resolved-address blocking against private/loopback/link-local/metadata/reserved ranges, a separate explicit local-inference-mode flag, re-validated immediately before every real call (documented residual TOCTOU limitation, RISK-14). `src/lib/ai-localization/services.ts` gained one optional dependency (`resolveConnectionProvider`) and one optional input field (`connectionId`) — no other change to that domain's logic (AGENTS.md §D). New Settings UI tab (`ai-connections-manager.tsx`) and API routes (`/api/ai-connections`, `/api/ai-connections/[connectionId]`, `/api/ai-connections/[connectionId]/test` — the only caller of `testConnection`, always an explicit user action). Cost controls: a much lower per-call target cap for real connections than the mock path, bounded timeout+retry, token usage surfaced when the provider reports it, unknown usage/pricing reported as `null` never `0`. 34 new tests (`docs/acceptance/PHASE_6_AI_CONNECTIONS_ACCEPTANCE.md` AC-CONN-01..17): CRUD, credential non-exposure/encryption/fail-closed, SSRF blocking (IP literals, DNS-resolved private addresses, IPv6), capability rejection, malformed-JSON/timeout isolation, and a full connection-backed generation → Change Set → approve → Batch dry-run pipeline proving the mock-provider guarantees hold identically for a real (fake-HTTP-backed) connection. Independently reviewed for requirements conformance and, separately, for security (credential handling + SSRF specifically) — see that review's findings incorporated into this entry and into RISK-14/15. |
| Pre-Release — Cross-Platform Persistence & Syncthing Handoff (Variant A) | **CODE-COMPLETE, Windows-validated only** (not a numbered phase — does not authorize Phase 7) | 2026-09-19 | Merged to `dev` in `25917b5` ("Интегрировать feature/cross-platform-persistence в dev") | Platform-aware app-data location (`src/lib/platform-paths`, Windows/macOS), explicit schema versioning layered on the existing additive boot pattern (`src/lib/schema-versioning`, `docs/decisions/0002-additive-schema-versioning.md`), a real device-local operation lock (`src/lib/operation-lock`, `app_operation_locks`), a scrub-then-checksum snapshot export/import pipeline with an explicit transfer allowlist (`src/lib/snapshot`), device-handoff orchestration with a live-recomputed (never cached) restricted recovery mode for imported unresolved execution state (`src/lib/device-handoff`), and one shared mutation gate enforced at three independent choke points (`src/proxy.ts` — Next.js 16's renamed `middleware.ts`; the CLI's `runCliCommand`; MCP's `createMcpToolHandlers`). New minimal Web UI (Device tab) and API routes (`/api/device-handoff/**`). Full detail: `docs/ARCHITECTURE.md` §13, `docs/SYSTEM_MAP.md` §2.16, `docs/RELEASE_LAYOUT.md` (first-run/device-switching procedure), `docs/acceptance/PRE_RELEASE_CROSS_PLATFORM_ACCEPTANCE.md` (acceptance contract, ~45 scenarios). Three follow-up corrections from the project owner during design review were incorporated before implementation: (1) `users.id` confirmed to be the stable Google OAuth `sub`, so import never touches `users`/credentials at all rather than merely excluding them from the snapshot; (2) the operation lock's enforcement boundary made concrete (a real SQLite-level `INSERT`-based exclusivity plus three interface choke points, not an advisory flag); (3) operator acknowledgement of recovery-mode diagnostics made strictly informational — it can never itself lift the gate or change a row's status. `npm test`: 448/448 (up from 419 before this task); `npm run lint`/`npm run build` clean throughout. **Explicitly not validated on real macOS hardware** (`docs/TECHNICAL_DEBT.md` RISK-17) and no real Google OAuth login was performed in this environment (`docs/TECHNICAL_DEBT.md` RISK-05, unchanged) — both stated plainly, not implied as done. New residual risks recorded: RISK-16 (recovery mode has no in-app resolution path until RISK-04 is addressed), RISK-17 (macOS unvalidated). |
| Pre-Release — First Local Test Build | **Windows launcher written, not run on real Windows; macOS launcher and build/runtime checks actually executed** (not a numbered phase — does not authorize Phase 7) | 2026-09-19 | Merged to `dev` in `a6bbb0c` | `docs/FIRST_LOCAL_TEST_BUILD.md` (operator guide), `scripts/windows/{start,stop,update}.bat`, `scripts/macos/{start,stop,update}.sh`, `.env.example`. On real macOS hardware: production build actually run (not just `npm run build`) — server start, HTTP 200 on `/` and `/dashboard`, `401` on unauthenticated API routes, database created at the real platform-aware app-data path with the full expected schema, byte-identical database across a restart, and a disposable-data update-verification (seeded rows survive a rebuild+restart under an overridden `HOME`, never touching the operator's real location). The three macOS launcher scripts were executed for real, not just reviewed. **No UI-level (signed-in browser) checks were performed** — every such check requires a real Google OAuth login, explicitly out of scope for an automated agent (`AGENTS.md` §G) — see `docs/FIRST_LOCAL_TEST_BUILD.md` §7 for the resulting manual checklist. **Windows launcher scripts were written and reasoned about but not executed on a real Windows machine** — none was available in the implementing environment; this is the largest remaining gap given the task's stated Windows priority. Full session detail: `docs/reports/FIRST_LOCAL_TEST_BUILD_SESSION.md`. Two stale-documentation discrepancies found and corrected as part of this task: `docs/ROADMAP_STATUS.md`'s own Cross-Platform Persistence row said "pending `dev` merge" when it was already merged (see the row above, now corrected), and `docs/getting-started.md` §6 still pointed at the legacy `data/playlist-manager.db` path superseded by the immediately preceding task. `npm test`: 456/456; `npm run lint`/`npm run build` clean. |
| Pre-Release — Publish snapshots + independent-review risk remediation | **Both done** (not a numbered phase — does not authorize Phase 7) | 2026-09-19 | 5 feature branches merged to `dev`, tip `b299287` | Two project-owner-assigned follow-ups after the First Local Test Build: (1) `published/<version>/` in-repo release-snapshot mechanism (`docs/decisions/0003-published-release-snapshots.md`, `scripts/publish-snapshot.mjs`) — allowlisted, immutable-once-created, generated and end-to-end smoke-tested outside the repository, never populated inside it (no numbered version has actually been released yet); (2) independent review (`/code-review high`) run twice against this work, both times mis-scoping its diff against `main` instead of the actual feature-branch base (a reproducible tool bug, reported upstream) and surfacing 17 real findings in already-merged prior work instead — all logged as RISK-18..34 in `docs/TECHNICAL_DEBT.md`, then explicitly assigned by the project owner ("Начни с 1. Отработай найденные риски") and fixed across 5 sequential batches: RISK-18/24 (device-handoff path traversal; app-data dir `chmod 0700`), RISK-19/21/33 (fail-open bare-catch error handling, consolidated into a new shared `isMissingTableError`), RISK-28/31 (write-channel guardrail skipped on batch resume — the single most safety-relevant finding; ledger-status-drift on a concurrent race), RISK-27/29 (leaked pre-import backup on failed migration; positional snapshot merge broken by `ALTER TABLE`-added columns), RISK-20/22/30/34 (boot-time migration never lock-protected; no Windows rename retry in `writeJsonFileAtomic`; AI Localization's real-connection path bypassing the device-availability gate; the two "recovery-gate" test suites never actually exercising recovery mode). RISK-26 (snippet-field whitelist completeness) and RISK-32 (a shared mutating-operation registry across `proxy.ts`/CLI/MCP) were deliberately left `OPEN` with an explicit disposition rather than force-fit into this round — the first needs external API-docs research before Gate B, the second is its own cross-cutting refactor. `npm test`: 456 → 479 (+23 new regression tests, one per fix). `npm run lint`/`npm run build` clean throughout; every batch independently verified before merging into the next. |
| Phase 7 planning — Codex Operations Interface plan document | **Plan produced, not implemented** (does not authorize starting Phase 7's implementation) | 2026-09-20 | Merged to `dev` in feature/phase-7-plan | `docs/roadmap/plans/PHASE_7_PLAN.md` — produced per the project owner's explicit Telegram instruction to work through `docs/roadmap/BACKLOG.md`'s assigned items (`BL-001`/`BL-002`). Inspects the actual current MCP surface (`src/mcp/server.ts`), confirms `docs/TECHNICAL_DEBT.md` RISK-04 as the concrete gap, proposes a read/propose-only first slice (`changeset_*`/`batch_*`/`localization_*` MCP tools, no apply-class tool — deliberately excluded pending Gate B), explicit non-goals, and acceptance criteria drafted per `AGENTS.md` §L. No MCP tool code, API route, or workspace was written — this is planning only. |
| Phase 8 planning — Intelligence Foundation plan document | **Plan produced, not implemented** (does not authorize starting Phase 8's implementation) | 2026-09-20 | Merged to `dev` in feature/phase-8-plan | `docs/roadmap/plans/PHASE_8_PLAN.md` — confirms Phase 8 is greenfield (no analytics code, table, or OAuth scope exists today), proposes an additive `video_metrics_daily`-style table and a manual-trigger `views`-only collection slice as the smallest first step, explicitly deferring scheduling, additional metrics, and any hypothesis/report logic. Flags the new Analytics OAuth scope as its own required owner decision. No code written. |
| Phase 9 planning — Market Discovery & Trend Intelligence plan document | **Plan produced, not implemented** (does not authorize starting Phase 9's implementation) | 2026-09-20 | Merged to `dev` in feature/phase-9-plan | `docs/roadmap/plans/PHASE_9_PLAN.md` — proposes `research_channels`/`research_evidence` tables, structurally separate from `channels`/`videos` (the owned/not-owned boundary stays a distinct entity, never a flag), with a manually-seeded first slice (no auto-discovery, no scoring). Explicitly excludes any private-analytics-shaped field and any profitability conclusion per `FUTURE_PHASES.md` §5's own constraint. No code written. |
| Phase 10 planning — Decision & Experiment Engine plan document | **Plan produced, not implemented** (does not authorize starting Phase 10's implementation) | 2026-09-20 | Merged to `dev` in feature/phase-10-plan | `docs/roadmap/plans/PHASE_10_PLAN.md` — proposes `hypotheses`/`experiments`/`decisions` tables, structurally separate from `change_sets`/`changes`, with a manual-entry-only first slice (no AI-generated hypotheses, no automatic execution). Explicitly notes dependency on Phase 8 (metrics) and Phase 9 (candidates) for real evidence, and that the record-keeping shape works even without them at first. No code written. |
| Future directions — feasibility research (multi-device sync, automated media production) | **Research done; neither advanced to a plan document** (does not authorize starting either) | 2026-09-20 | Merged to `dev` in feature/future-directions-research | `docs/roadmap/plans/FUTURE_DIRECTIONS_RESEARCH.md` — for multi-device sync, concludes a foundational architecture decision (conflict resolution strategy vs. moving off local SQLite) must happen before any vertical slice exists to plan; for automated media production, concludes it is four largely unrelated sub-capabilities needing an owner choice of which one (if any) to pursue first. Neither produced a `PHASE_N_PLAN.md`-style document, since `FUTURE_PHASES.md` §7 itself frames both as opportunities only, not phases. No code written. |
| Studio-parity planning — Home/Content/Analytics/Languages tabs | **Plan produced, not implemented** (does not authorize starting any slice) | 2026-09-20 | Merged to `dev` in feature/studio-parity-plan | `docs/roadmap/plans/STUDIO_PARITY_PLAN.md` — produced per the project owner's Telegram request (msg 122) after manually testing the app against real YouTube Studio. Live-researched (via `claude-in-chrome`, against the real "Tropico Jazz" Studio session) the exact structure of Studio's Home/Content/Analytics/Languages sections, built a per-tab dependency table (what this app already has vs. is missing), and proposed slices `BL-012`..`BL-017` (`docs/roadmap/BACKLOG.md`) in a recommended dependency order — S1 (sync view/comment/like stats), S2 (Content tab), S3 (Languages tab, later split into its own merge plan — see next row), S4 (Home tab, Data-API-v3 portion; comments/subscribers feeds flagged as needing separate feasibility research first), S6-stub (Analytics placeholder tab). The Analytics tab's full-data slice (S6) is deliberately not a new plan — it is `docs/roadmap/plans/PHASE_8_PLAN.md`'s existing Phase 8 scope, requested here as a full-parity UI target rather than Phase 8's own smaller first vertical slice; `FUTURE_PHASES.md` §7 gained one new bullet recording the idea's existence and this cross-reference. Owner follow-up (Telegram msg 125) resolved 3 of the 4 open questions the plan originally raised: visual fidelity = maximally close to real Studio (not "our own design language"); Languages = restyle the existing tab using Studio's menu as the base (see next row for the resulting sub-plan); Analytics = placeholder stub now, revisit at Phase 8's own stage. No UI, schema, or API code written. |
| Languages tab merge planning — fold AI Localization into a Studio-styled Languages tab | **Plan produced, not implemented** (does not authorize starting any slice) | 2026-09-20 | Merged to `dev` in feature/languages-tab-merge-plan | `docs/roadmap/plans/LANGUAGES_TAB_MERGE_PLAN.md` — produced per the project owner's explicit follow-up request (Telegram msg 125: "Это переделка требует отдельного планирования и проработки") to fold the separate "AI Localization" tab into a Studio-styled "Languages" tab (renamed from "Localizations"). Confirms the Change Set review UI (`change-set-review.tsx`) is already fully shared/unified across both XLSX-import and AI-generated sources — no duplicate approve/reject logic to reconcile — and finds an existing display bug (a Change Set's `source` field is never shown; every AI-generated set is mislabeled "XLSX import" since `importedFilename` is null for it). Proposes a Studio-shaped video table + re-mapped sub-tabs (explicitly avoiding "Опубликовано"/Published wording, since Phase 5 live YouTube writes remain barrier-disabled — RISK-09 — and that label would misleadingly imply a real write happened), a per-video detail view with both "Import from XLSX" and "Generate with AI" entry points feeding the same review UI, and a 5-slice breakdown (L1..L5) where L1 (drop now-redundant per-tab channel selectors post-RISK-02, fix the source-label bug) has no open questions and is separately assignable now. Flags 4 open questions (sub-tab semantics, bulk-vs-per-video AI generation UI placement, editorial-profile placement, nav-item removal) requiring owner sign-off before the rest can be assigned. No code written. **Follow-up (Telegram msg 128) resolved 2 of the 4 open questions same-day:** AI generation is the primary per-video path (manual edit = review/correct the agent's output, not independent authoring), and the "AI Localization" nav item is removed outright, immediately, no transition period. Plan updated in place; sub-tab semantics and editorial-profile placement remain open. **Second follow-up (Telegram msg 131) resolved sub-tab semantics** ("Все/В процессе/Одобрено", confirmed). **Third follow-up (Telegram msg 135) resolved editorial-profile placement — it moves to the Home tab instead, not Languages** (owner: "звучит как что-то фундаментальное... давай вынесем это пока в закладку Home, как доп меню"); `STUDIO_PARITY_PLAN.md` Slice S4 updated accordingly, tracked as `BL-030`. All 4 of this plan's original open questions are now resolved; only an implementation-time visual-prominence judgment call (XLSX-import vs. AI-generate on the landing view) remains, and it does not block assignment. |
| Tab auto-refresh & channel-dropdown-removal planning | **Plan produced, not implemented** (does not authorize starting any slice) | 2026-09-20 | Merged to `dev` in feature/tab-refresh-and-device-sync-plans | `docs/roadmap/plans/TAB_REFRESH_AND_CHANNEL_UI_PLAN.md` — produced per the project owner's Telegram request (msg 128) to remove now-redundant channel-selection dropdowns and make tabs refresh their data automatically on switch. Finds that most tabs (Localizations, Batches, Settings, Device) already refresh on every switch for free, via this app's existing conditional-rendering tab pattern (mount/unmount on tab change) — the real gaps are the Rules tab (state owned by the parent component, fetched once at dashboard-mount) and the Sync tab's live YouTube re-sync, which costs real API quota and needs an explicit trigger-policy decision rather than blind per-click automation. Proposes 3 slices (T1 dropdown removal, T2 Rules fix — both immediately assignable with no open questions — and T3 the Sync policy, blocked on an owner decision between fully-automatic/staleness-gated/manual-only). No code written. |
| Device-handoff auto-sync planning — auto-export, auto-import, update notification | **Plan produced, not implemented** (does not authorize starting any slice) | 2026-09-20 | Merged to `dev` in feature/tab-refresh-and-device-sync-plans | `docs/roadmap/plans/DEVICE_HANDOFF_AUTO_SYNC_PLAN.md` — produced per the project owner's Telegram request (msg 128) to auto-save handoff snapshots on session close/on every change, auto-load the latest on startup, and notify when a newer snapshot appears while running. Grounded in the actual code: export acquires the app-wide operation lock and runs a full-database `VACUUM` (real cost — "export on every single mutation" is deliberately not the literal design), import's divergent-lineage rejection is an intentional human-decision point that must never be auto-resolved, and **no graceful-shutdown hook exists anywhere in this codebase today** (a `SIGTERM`/`SIGINT` handler is new infrastructure, not a config flag). Proposes 5 slices (D1 shutdown-export hook, D2/D3 newer-snapshot detection + a new notification-bell UI, D4 safe-case-only auto-import at startup, D5 periodic auto-export pending an owner decision on interval/cost) and explicitly states this does **not** make concurrent multi-device editing safe — that remains the separate, harder problem already tracked as future-direction "multi-device application-managed synchronization" (`BL-006`). No code written. |
| Phase 7, Slice 1 — read/propose-only Change Set/Batch MCP tools | **CODE-COMPLETE for this slice** (Phase 7 is not fully done — channel_sync MCP tool and any apply-class tool remain unimplemented; Gate B/live-write validation unaffected) | 2026-09-20 | Merged to `dev` in feature/phase-7-slice-1-mcp-tools | Per the project owner's explicit "Приступай к реализации этих планов по порядку" (Telegram, 2026-09-19), implementing `docs/roadmap/plans/PHASE_7_PLAN.md` §6 slices 1-4: `changeset_list`, `changeset_get`, `localization_import_preview`, `batch_list`, `batch_get` MCP tools in `src/mcp/server.ts`, wired to the same `createChangeSetCore()`/`createBatchCore()` factories the Web UI's own API routes already use (no parallel implementation). `batch_get` reuses `requireBatchForChannel`, not a bare `getBatch`, for channel-ownership verification (`AGENTS.md` §F). None of the five tools are wrapped by the device-availability mutation gate, since none mutates anything. 12 new tests (`src/mcp/server.test.ts`): success shapes, validation-error shapes, `DomainError` passthrough, base64 decode fidelity, and the channel-ownership check specifically. `docs/TECHNICAL_DEBT.md` RISK-04 moved from OPEN to PARTIALLY RESOLVED; `docs/interfaces.md` and `docs/SYSTEM_MAP.md` §2.13/§4.0 updated to match. `npm test`: 497 → 506. `npm run lint`/`npm run build` clean. Deliberately excluded from this slice, per the plan's own §4 non-goals: any apply-class Change Set/Batch tool (blocked on Gate B) and the isolated Codex workspace/compatibility mechanism (blocked on a published version existing). |
| BL-008 — `channel_sync`/`channel_list`/`channel_video_list` MCP tools | **CODE-COMPLETE** (closes the MCP portion of RISK-04; CLI parity and any apply-class tool remain unimplemented) | 2026-09-20 | Merged to `dev` in feature/backlog-bl008-channel-sync-mcp | Self-initiated backlog item (not part of `PHASE_7_PLAN.md`'s own slice list) found while auditing RISK-04's actual state after Phase 7 slice 1, per the project owner's explicit "если по результатам анализа создавай новые задачи в бэклог и реализуй их" (Telegram msg 67). `channel_sync` (mutating — writes to local `channels`/`videos`, gated by `assertMcpDeviceAvailable` exactly like `apply`), `channel_list`, `channel_video_list` (both read-only, ungated) in `src/mcp/server.ts`, reusing `createChannelSyncCore()` unchanged. 5 new tests including one proving the mutation gate is wired correctly (`channel_sync` blocked under the operation lock, `channel_list` not). `npm test`: 506 → 511. `docs/TECHNICAL_DEBT.md` RISK-04, `docs/interfaces.md`, `docs/SYSTEM_MAP.md` §2.13/§4.0 updated. `npm run lint`/`npm run build` clean. |
| Phase 7 — in-repository scope | **CODE-COMPLETE for everything buildable inside this repository** (Phase 7 as a whole is NOT fully done — see the two non-delegable remainders below) | 2026-09-20 | Merged to `dev` across feature/phase-7-slice-1-mcp-tools, feature/backlog-bl008-channel-sync-mcp, feature/phase-7-changeset-create-mcp, feature/phase-7-mcp-restricted-mode, feature/phase-7-cli-parity | Per the project owner's explicit "Создай список задач чтобы полностью реализовать объем фазы 7 и довести ее до релиза... Автоматическая работа" (Telegram, 2026-09-20), completed a 5-item task list covering every part of Phase 7 that can actually be built from inside this repository: (1) `changeset_create_from_import` MCP tool — real Change Set creation, correcting an earlier wrong claim that this was Gate-B-blocked (it is local-only persistence, not a YouTube write); (2) MCP restricted mode (`MCP_RESTRICTED_MODE`) — every YouTube-write-capable or identity-switching tool is never even registered, the concrete implementation of "operation-specific permissions and read-only access to application data" (`docs/roadmap/FUTURE_PHASES.md` §3); (3) CLI parity — `changeset`/`batch`/`channel` namespaces mirroring every MCP read/propose/create tool; (4) verified (not assumed) that the CLI's device-availability gate correctly covers the new mutating commands, finding and fixing a real whitelist gap along the way; (5) this consolidating status entry. Combined with the earlier Phase 7 slice-1/BL-008 rows above, `docs/TECHNICAL_DEBT.md` RISK-04 is now PARTIALLY RESOLVED with the full read/propose/create surface on both MCP and CLI. `npm test`: 506 → 525 across this task's four commits; `npm run lint`/`npm run build` clean throughout. **Two remainders are permanently outside this repository's ability to close, regardless of instruction or working mode:** the isolated Codex operations workspace (`AGENTS.md` §B places it entirely outside this repository); and publishing/tagging/releasing a version for that workspace to target (`AGENTS.md` §K.2 requires separate, explicit project-owner approval every time, never inferred from an autonomous-work instruction). The only remaining in-repo Phase 7 work is an apply-class Change Set/Batch tool, itself gated on Gate B's live-write validation track (unrelated to this task, unaffected by it). |
| Pre-Release — Local test build re-validation (post review series + Phase 7) | **Done** (not a numbered phase — does not authorize a release) | 2026-09-20 | HEAD of `dev` at the time, `4941b3c` | Per the project owner's "собери тестовый релиз" (clarified via question to mean a local test build, not a `dev→main` release — `AGENTS.md` §K.2/§K.4 remain untouched by this task). Repeated the automated portion of `docs/FIRST_LOCAL_TEST_BUILD.md` §7 against the current `dev` tip, after the UI-shell redesign, roadmap-backlog skill, all of Phase 7 (MCP/CLI read-propose-create surface + restricted mode), and the 5-cycle review series: clean `npm run build`; server start under an isolated, disposable `HOME` (never the operator's real app-data path); `GET /` and `GET /dashboard` → 200; unauthenticated `GET /api/channels` and `GET /api/rules` → 401; database created at the correct platform-aware path with the expected schema (`schema_version = 3`, all Phase 4-6/cross-platform/Phase-7 tables present); a genuine stop/restart with the database byte-identical (same MD5) across it; clean shutdown, port freed, temp directory removed afterward. No real Google OAuth login was performed (`AGENTS.md` §G) — the manual, signed-in-browser portion of §7's checklist remains the operator's own work, unchanged from the original First Local Test Build task. |
| Pre-Release — YouTube Studio UI shell redesign (layer 1) | **Layer 1 (chrome/navigation only) done** (not a numbered phase — does not authorize Phase 7) | 2026-09-20 | Merged to `dev` in `4d0338c` ("Интегрировать feature/youtube-studio-ui-shell в dev") | Project-owner-assigned after "После 6 цикла проверки переходи к формированию плану по дизайну UI в стилистике YouTube studio... приступай к его выполнению" — a persistent left sidebar (`src/components/app-shell.tsx`, icon nav via new `src/components/icons.tsx`, red active indicator) and a top bar (active channel, switch-channel, sign-out) replace the previous single horizontal pill-tab-bar; a YT-Studio-style always-dark palette on CSS variables (`src/app/globals.css`); branding renamed "YouTube Playlist Manager" -> "YouTube Operations Manager". Explicitly scoped as chrome/navigation only — every tab's own content component (`ManualMode`, `RuleForm`, `AiLocalizationPanel`, etc.) is unchanged internally and still uses its pre-existing raw `zinc-*` classes; migrating those to the shell's design tokens is a separate, not-yet-assigned follow-up. Independent review (`/code-review high`) found 7 issues; 5 fixed on the feature branch before merge (dropped content max-width causing edge-to-edge stretch on wide viewports; `Tab`/`NAV_ITEMS` duplicated with no compiler-enforced completeness, now `Tab` is derived from `NAV_ITEMS` via `as const satisfies`; `AppShell` duplicated the channel type instead of importing `ChannelInfo`; `IconBase`'s default `className` was unreachable behind `{...props}`, now merged instead of overridden; unused `ChevronDownIcon` removed). Deliberately not fixed: no responsive collapse for the fixed 240px sidebar on narrow viewports (a real but larger UX gap, candidate for a future backlog item); the generic `AppShell<T>` having only one instantiation (not a defect). `npm test`: 497/497 both before and after the merge; `npm run lint`/`npm run build` clean. **No real, authenticated-browser visual verification was performed** — blocked by `AGENTS.md` §G's ban on automatic Google OAuth login — left for manual operator review. |
| Studio-parity — Languages tab merge (BL-014/BL-018/BL-019/BL-020) | **CODE-COMPLETE** (not a numbered phase — does not authorize any further Studio-parity slice) | 2026-09-20 | Merged to `dev` in feature/languages-ai-merge | Assigned 2026-09-20 (Telegram msg 137, "Да, реализуй то что мы обсуждали", after `LANGUAGES_TAB_MERGE_PLAN.md`'s open questions were all resolved). Folded the separate "AI Localization" tab into a restyled "Languages" tab (renamed from "Localizations"): `src/components/languages-manager.tsx` (new) replaces both `localization-manager.tsx` and `ai-localization-panel.tsx` (both deleted). "Generate with AI" is now the primary, top-of-tab action, with a per-row "Generate with AI for this video" shortcut that pre-selects that video and scrolls to the section; XLSX import/export (selected/filtered/all, preserved from the old component) is a secondary, collapsed-by-default section. A Studio-shaped video table (Video / Languages count / Last modified) replaces the old per-language ✓/— grid on the landing view (still available per-video in the expandable detail row) — backed by a new `LocalizationOverviewRow.lastSyncedAt` field threaded through `src/lib/localization/{contracts,schemas,services}.ts` from the already-existing `StoredVideoRecord.lastSyncedAt`. Change Sets are filtered by three sub-tabs — Все/В процессе/Одобрено — mapped onto `ChangeSet.status` (`in_review`/`partially_approved` → "В процессе", `approved` → "Одобрено"), a deliberate mapping onto the change-set-approval unit rather than a nonexistent per-video draft state Studio's own UI assumes; "Одобрено" never implies a real YouTube write happened (Phase 5's write barrier, RISK-09, is unaffected and untouched). Also fixed a real, independently-found display bug while implementing L1: `change-set-review.tsx`'s header always read "XLSX import" regardless of `ChangeSet.source` — it now reads `changeSet.source` and shows "AI Generated" correctly; the component also gained an optional `onStatusChange` callback (fired only after a real approve/reject mutation, never on initial load) so `languages-manager.tsx`'s separately-fetched change-set summary list — needed for the sub-tab filtering — stays in sync with actions taken inside the open review panel (found and fixed after live browser testing showed an approved change set not moving to the "Одобрено" sub-tab until a full page reload). Verified end-to-end against the real "Tropico Jazz" channel via `claude-in-chrome`: generate proposal (mock provider) → review/edit → create Change Set → approve → sub-tab move, and XLSX export-selected → real file download, all confirmed working. `npm test`: 554/554 (unchanged count — no new test files added for this UI-layer change, existing `localization`/`changesets`/MCP/CLI test suites re-verified passing); `npm run lint`, `npx tsc --noEmit`, `npm run build` all clean. **Note for whoever picks up Studio-parity next:** the earlier Content-tab restyle, video-statistics sync (view/comment/like counts), and Home/Analytics tab additions (commits `9aa5f58`, `1f7da88`, `0797172`, `9bcd4ff`, all already merged to `dev` before this row) do not yet have their own `ROADMAP_STATUS.md` rows — that bookkeeping gap predates this task and was not backfilled here (out of this task's scope), but should be closed before relying on this file as a complete record of Studio-parity's implementation history. |
| Removed — Manual and Rules tabs (legacy features from the project's original pre-rewrite baseline) | **Done** (not a numbered phase; a deletion, not an implementation) | 2026-09-20 | Merged to `dev` in feature/remove-manual-and-rules | Assigned 2026-09-20 by explicit project-owner instruction over Telegram ("Давай удалим их, т.к. пока не вижу им применения"), after the owner asked what Manual/Rules do and was told they are legacy features from the project's original pre-rewrite baseline (manual playlist add/remove; a simple title/description-match auto-playlisting rule engine run via a button) unrelated to this project's own localization/Change-Set/Batch work. Deleted: `src/components/{manual-mode,rule-form,rule-list,run-button}.tsx`, `src/app/api/{rules,run}/route.ts`, the `Manual`/`Rules` nav entries and their `ManualIcon`/`RulesIcon` icons, the now-unused `rules` Drizzle table definition and its `dbSchema` entry (`src/lib/db.ts`) — the underlying `CREATE TABLE IF NOT EXISTS rules` baseline SQL statement is deliberately left in place rather than replaced with a `DROP TABLE` migration (see the comment directly above it in `src/lib/db.ts`: a subtractive schema change needs its own new ADR per `docs/decisions/0001-additive-idempotent-schema-strategy.md`, not warranted here since nothing reads/writes that table anymore and any existing row data is simply left untouched, not destroyed). **Deliberately kept, per explicit clarification with the owner before starting:** `src/lib/playlist-management/` and its Web API routes (`/api/youtube/{videos,playlists,create-playlist,add-to-playlist,remove-from-playlist}`) — these are the same domain module the MCP `playlist_*` tools and CLI `playlist` namespace already depend on (`docs/SYSTEM_MAP.md` §2.12/§2.13), a programmatic surface for a future operations agent (`AGENTS.md` §B) independent of whether a Web UI tab exists for it; removing the Manual tab does not remove this capability. `Home` is now the dashboard's default tab (was `Manual`). `src/proxy.test.ts`'s three generic operation-lock-gate tests, which used `/api/rules` only as an arbitrary example `/api/**` pathname (not testing Rules-specific behavior), were repointed to `/api/channels/sync` and `/api/channels` — real, still-existing routes. `docs/roadmap/BACKLOG.md`'s `BL-022` (a not-yet-assigned Rules-tab refresh item, now moot) marked `dropped`. `docs/{ARCHITECTURE,SYSTEM_MAP,interfaces}.md` updated. Verified live in the browser (`claude-in-chrome`, real "Tropico Jazz" session): Manual/Rules gone from the sidebar, Home loads as the default tab, Content and Languages both still work correctly. `npm test`: 554/554 unchanged (no test existed for Manual/Rules specifically — both were pre-existing code from the project's original pre-rewrite baseline with no dedicated test coverage); `npm run lint`, `npx tsc --noEmit`, `npm run build` all clean. |
| UI — table-heavy tabs stretch to fill available width, matching real Studio | **Done** (a layout fix, not a numbered phase) | 2026-09-20 | Merged to `dev` in feature/adaptive-content-width | Assigned 2026-09-20 by the project owner ("надо чтобы интерфейс адаптивно растягивался и занимал доступное пространство... особенно важно для табличных форм"), citing real YouTube Studio's own behavior as the reference. Root cause: `src/components/app-shell.tsx`'s `<main>` wrapped every tab's content in a hardcoded `mx-auto max-w-3xl` (768px) div — this cap was deliberately *added* during the 2026-09-18 UI-shell-redesign review (see this table's row above) to fix a *different* problem (unconstrained forms/cards stretching edge-to-edge and looking bad on an ultrawide monitor), but it also capped every table-based tab (Content, Languages, Batches) at a fixed 768px regardless of actual window width, wasting most of a normal desktop viewport. Fix: removed the shell-level cap entirely (`AppShell` is now width-agnostic — `<main>` just applies padding, no `max-w`) and instead applied a local `max-w-3xl` wrapper directly in `src/app/dashboard/page.tsx` to only the tabs that are genuinely card/form-shaped and have no table (Home, Analytics, Settings, Device) — none of those components had their own independent width constraint, so without a wrapper they would regress right back to the original edge-to-edge-stretch problem the 2026-09-18 review fixed. Content/Languages/Batches got no wrapper and now stretch full-width; their `<table className="table-fixed">` + `<colgroup>` structure already had exactly one column with no explicit width (the title/video column), so the browser automatically gives that one column all the newly-available space — no table markup itself needed to change. Verified visually in the browser (`claude-in-chrome`, 1920×1080 window, real "Tropico Jazz" session): Content's and Languages' tables now fill the full content area edge-to-edge with the title column absorbing the extra width; Home's editorial-profile card correctly stayed at its original narrow width, unaffected. `npm test`: 554/554 unchanged (a pure CSS/layout change, no logic touched); `npm run lint`, `npx tsc --noEmit`, `npm run build` all clean. |
| UI — wide tables scroll horizontally instead of compressing columns on a narrow viewport | **Done** (a layout fix, not a numbered phase) | 2026-09-20 | Merged to `dev` in feature/table-horizontal-scroll | Assigned 2026-09-20 by the project owner as a standing rule: "Если таблица имеет больше столбцов чем влазит на экран — добавляем горизонтальный скролл", a direct complement to the row above (stretch to fill a *wide* viewport; scroll rather than break on a *narrow* one). Wrapped each of the three existing `<table>` elements in a `<div className="overflow-x-auto">` and gave the table itself a `min-w-[Npx]` (sum of its fixed `<col>` widths plus a readable minimum for the one flexible column): `content-manager.tsx` (`min-w-[720px]`), `languages-manager.tsx` (`min-w-[600px]`), `batch-manager.tsx` (`min-w-[480px]`, the one table without a `<colgroup>`/`table-fixed`, so this also gives its auto-layout columns a floor they didn't have before). `docs/DEVELOPMENT_PLAYBOOK.md` §6.9 gained a new numbered rule (6) recording this as the standing pattern for any future table. Verified: `content-manager.tsx` and `languages-manager.tsx` confirmed live in the browser (`claude-in-chrome`, real "Tropico Jazz" session) by shrinking each wrapper to 300px via injected JS and reading `scrollWidth`/`clientWidth`/`overflowX` directly off the DOM (a real physical narrow-window screenshot wasn't obtainable in this session — `resize_window` reported success but `window.innerWidth` stayed at 1920 regardless) — both wrappers correctly reported `overflowX: "auto"` and `isScrollable: true` at their configured `min-w`. `batch-manager.tsx`'s table was not live-verified the same way (its ledger table only renders after preparing a real Batch, out of scope for this quick check) — the identical CSS mechanism applied to the other two is standard, well-understood browser behavior, not something specific to this component, so this is a low-risk, code-reviewed-only gap, not a claim of having tested something unverified. `npm test`: 554/554 unchanged (pure CSS/layout change); `npm run lint`, `npx tsc --noEmit`, `npm run build` all clean. |
| `src/lib/video-details/` — Studio-parity "Details" edit module (real write, no UI yet) | **CODE-COMPLETE for the module + API; NO Web UI** (see BL-031, `docs/TECHNICAL_DEBT.md` RISK-40) | 2026-09-20 | Merged to `dev` in feature/video-details-module | Assigned 2026-09-20 by the project owner over Telegram, after being told the existing legacy `video-metadata.apply` path silently writes to `localizations` on every edit (violating AGENTS.md §F) and has none of Batches' backup/diff/approval/audit protections, and after explicitly choosing "Вариант 3: просмотр + реальное сохранение сразу в YouTube." Per the owner's own requirement ("модуль, который мы свободно сможем использовать позже, включая запись батчем... не перезаписывая всё остальное"), built as a standalone, reusable domain module rather than a one-off UI wire-up. First real (non-dry-run-only) YouTube write path reachable from this app's Web API. Fields: confirmed against the OFFICIAL YouTube Data API v3 "settable properties" reference (live-checked 2026-09-20, not from memory) — `snippet.{title,description,tags,categoryId,defaultLanguage}`, `status.{privacyStatus,publishAt,license,embeddable,publicStatsViewable,selfDeclaredMadeForKids,containsSyntheticMedia}`, `recordingDetails.recordingDate`; `localizations` is structurally unreachable from this module (AGENTS.md §F). Partial-patch semantics: only patched fields are ever read from the request, merged onto a freshly-fetched current snapshot, and sent — an untouched `part` (`snippet`/`status`/`recordingDetails`) is never included in the `videos.update` call at all. Meets AGENTS.md §G's safety-critical minimum: identity check (reused `write-context.assertWriteChannel`), validation (Zod, including a `publishAt` cross-field rule requiring `privacyStatus: "private"` in the same patch, an "already published" rejection, exact UTF-8-byte description limit via `Buffer.byteLength` not JS `.length`, and YouTube's real tag-budget accounting for comma/quote overhead), backup (reused `src/lib/backup/`, generalized — see below), diff + approval (`previewFieldsUpdate`'s diff, an explicit future "Save" UI action), dry-run (`previewFieldsUpdate`, fully read-only), audit (new `video_edit_audit_events` table, SCHEMA_MIGRATIONS version 5), verification (an independent post-write re-read compared field-by-field against what was sent). **Generalized `src/lib/backup/`** rather than duplicating it (AGENTS.md §D, documented reason): `captureBackup`'s `batchId` param renamed to `operationId` (pure rename, Batches' own behavior unchanged) and `BackupSnapshot` widened to a discriminated union (`"localization"` = the original Batches shape, unchanged; `"video_fields"` = new). **Did NOT reuse `audit_events`** for the new audit trail — its `batch_id`/`ledger_row_id` are `NOT NULL` foreign keys into `batches`/`batch_ledger_rows`, which a single-video edit has neither of; loosening those FKs would be the "first non-additive schema change" `docs/decisions/0001-additive-idempotent-schema-strategy.md` says needs its own ADR, so a second, separate, FK-free append-only table was added instead (documented in both the schema comment and `docs/SYSTEM_MAP.md` §2.9d so it doesn't read as accidental duplication later). Local-cache "не ломая синхронизацию" handling: `refreshVideoFields` reads the current stored `videos` row first (new `getStoredVideo(channelId, videoId)`), merges in only the fields this module touches that the cache also tracks (title/description/privacyStatus/defaultLanguage/etag), and reuses `upsertVideos` — deliberately does NOT advance `lastSyncedAt` (a targeted field patch is not a full channel resync, and advancing it would suppress `content-manager.tsx`'s next legitimate auto-resync for unrelated fields like view/comment/like counts). 32 new/updated tests (`src/lib/youtube.test.ts` extended, `src/lib/video-details/{schemas,services}.test.ts` new), spec-driven per AGENTS.md §L — expected values hand-derived from the official API reference and the owner's own stated requirement, not copied from a first implementation run; includes negative/boundary cases (empty patch, unknown field, invalid enum, publishAt without privacyStatus, publishAt-already-published, multi-byte description over/at the exact byte limit, tag-budget with space-quoting). Two new API routes: `POST .../details/preview` (exempted from `src/proxy.ts`'s operation-lock gate, like other genuine previews) and `POST .../details/apply` (deliberately NOT exempted — confirmed via a new proxy test that it is gated like any other real mutation). **Live-verified in the browser** (`claude-in-chrome`, real "Tropico Jazz" channel, via direct `fetch` calls since no UI exists yet): `preview` returned the real current title/description/tags/category/privacy for a real video and a correct diff; empty-patch, unknown-field, and publishAt-without-privacyStatus were all correctly rejected with the right error codes. **`apply` was deliberately never called by the agent** — per an explicit prior agreement with the project owner, only the project owner will trigger the first real write through this path. `npm test`: 554 → 580; `npm run lint`, `npx tsc --noEmit`, `npm run build` all clean. **Known, deliberate gaps** (`docs/TECHNICAL_DEBT.md` RISK-40, `docs/roadmap/BACKLOG.md` BL-031): no Web UI (the module cannot be triggered from the dashboard yet — BL-031, not yet assigned); no CLI/MCP parity (same class of gap as RISK-04); `paidProductPlacementDetails.hasPaidProductPlacement` not implemented (the official API docs are ambiguous about write support — needs an empirical test before adding, not assumed from the schema alone); multi-video "Details" batching is designed for (a pure, composable per-video function) but not wired into `src/lib/batches/`'s ledger/resume/crash-recovery machinery, which is separate, larger scope. |
| BL-031 — Content tab "Details" edit UI, wired to `src/lib/video-details/` | **DONE** (`docs/TECHNICAL_DEBT.md` RISK-40 Gap 1 closed) | 2026-09-20 | Merged to `dev` in feature/content-video-details-ui | Assigned same day as the module above, immediately after the owner asked one clarifying question (whether Localization could also use this module — answered no, by design, `localizations` is structurally unreachable from it) and then said: "возвращаемся к тому что через контент можно открывать как в студио отдельные видео и менять их по необходимости." Delivered: `src/components/content-manager.tsx`'s video rows are now click-to-expand (mirroring `languages-manager.tsx`'s existing pattern), rendering the new `src/components/video-details-panel.tsx` — title/description/privacy/category/tags always visible, the rest (`defaultLanguage`, `recordingDate`, `publishAt` [only shown when `privacyStatus === "private"` and never yet published], `license`, `embeddable`, `publicStatsViewable`, `selfDeclaredMadeForKids`, `containsSyntheticMedia`) behind a "Show more" toggle, matching Studio's own basic/advanced split. The patch sent to the API is built by diffing the form against the snapshot the panel loaded, not the whole form — untouched fields are never included (the one documented exception: setting `publishAt` always includes `privacyStatus: "private"` in the patch too, since the API requires both together even if the operator didn't touch privacy). "Save to YouTube" is disabled until "Preview changes" has been run for the byte-for-byte current patch (`patchesEqual`, JSON-compared) — editing any field after a preview immediately re-disables Save with a visible "Preview again before saving" notice, so the operator can never save a diff they didn't actually see. Added a second module capability alongside the UI: `getSnapshot` (a new, audit-free pure-read service method + `GET .../details` route, since `previewFieldsUpdate` requires a non-empty patch and can't serve as a bare "load the panel" call) and conflict detection (`applyFieldsUpdate` now accepts an optional `expectedEtag`; if the video's live `etag` no longer matches what the panel's last preview was shown against, the call fails closed with a new `video_details_conflict` code instead of silently applying a stale-diff patch — protects the "approval" leg of AGENTS.md §G across a slow UI session, e.g. the operator gets distracted between preview and save). 8 new tests (`AC-SVC-08/09/09b` in `services.test.ts`) covering the pure-read path and both the matching- and mismatched-etag cases. `npm test`: 580 → 584; `npm run lint`, `npx tsc --noEmit`, `npm run build` all clean. **Live-verified in the browser** (`claude-in-chrome`, real "Tropico Jazz" channel): opened a real video's panel (real title/description/tags/category loaded), confirmed both buttons start disabled with no changes, edited the title and confirmed "Preview changes" enabled while "Save to YouTube" stayed disabled with the "patch changed" notice, ran a real preview and confirmed the real diff rendered and "Save to YouTube" became enabled, then edited the title again and confirmed "Save to YouTube" correctly re-disabled. **"Save to YouTube" was never clicked by the agent, in this session or any prior one** — per the same standing agreement, only the project owner will trigger the first real write. Remaining gaps unchanged from the row above: no CLI/MCP parity, `paidProductPlacementDetails` unimplemented, no Batches-ledger multi-video mode. |
| BL-032 — Languages tab UX redesign, analysis + proposal | **Plan produced, not implemented** (does not authorize starting any slice) | 2026-09-20 | Merged to `dev` in feature/languages-ux-redesign-plan | Assigned 2026-09-20 by the project owner over Telegram ("Сейчас там 2 таблицы, нужно преобразовать в одну и сделать эту функцию максимально user friendly. Сделай анализ и подготовь предложение по редизайну"), immediately after BL-031 shipped. Delivered as `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md`, from a full re-read of the current `languages-manager.tsx` (not from memory) — identifies the two overlapping "list every video" surfaces (the "Generate with AI" checklist and the main table), three independent and visually unlinked selection sets (`selectedVideoIds` for AI generation, `exportSelectedIds` for XLSX export, and the single `expandedVideoId`), and that "Generate with AI for this video" scrolls the operator away from the row they just opened up to a generic top-of-page checklist rather than generating in place. Proposes: one table (enriched with per-video language badges and, as an open question, a "pending change set" indicator), a contextual bulk-action bar that appears only when rows are checked (replacing the always-open checklist), and per-video AI generate/review moved inline into the row's own expanded detail (mirroring the pattern `video-details-panel.tsx` just established for Content). Explicitly notes real Studio has no bulk multi-video AI-translate flow at all — that stays this app's own addition, only its entry point changes. Four open questions left for the owner (language-badge display, whether a new backend-touching "pending" column is worth it, popover vs. panel for the bulk-generate action, and which of four proposed slices — (a)-(c) UI-only, (d) needs a small overview-endpoint addition — to assign and in what order). Plan only; no UI or API code written. |
| BL-034 — Languages tab: six follow-up requirements, analysis + risk write-up | **Plan produced, not implemented** (does not authorize starting any slice) | 2026-09-20 | Merged to `dev` in feature/languages-redesign-plan-v2 | Assigned 2026-09-20 by the project owner over Telegram, same day as BL-032, listing six concrete requirements: per-language ✓/— columns, tooling to add/remove which languages are tracked/shown, a sortable table (default: publish date), an AI-powered "recommended languages for this channel" feature with an apply button, and a bulk "add translation to every video missing it" action per language. Delivered as `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` §7 (addendum), which **explicitly supersedes** one specific piece of BL-032's own recommendation (the "compact count instead of a grid" call) — flagged as a deliberate reversal, not an inconsistency, since nothing from BL-032 was implemented yet. Key findings: (1) per-language columns and sortable-by-header need no backend change at all (`OverviewRow.presentLanguages`/`missingLanguages` already carry the data; `listStoredVideosByChannel` already defaults to publish-date order) — flagged only for a real but unsolved table-width UX tradeoff past ~6-8 tracked languages; (2) add/remove tracked-language columns is a genuinely new, small additive backend piece (`channels.target_languages_json`, `SCHEMA_MIGRATIONS` version 6) **and raises one real safety question requiring explicit owner confirmation before being built**: "remove a language column" must be a display-preference-only action that can never delete, or prompt to delete, real translation data (AGENTS.md §F's "never let a blank imply deletion" principle applied to a new surface); (3) the AI-recommended-languages feature is flagged as the one requirement needing a **product decision, not just an engineering task** — a real audience-based recommendation would require the YouTube Analytics API, which remains unintegrated (Phase 8, its own separately-gated OAuth-scope decision), so what's actually buildable today is a content-based heuristic guess, and shipping it without clearly labeling it as such risks the operator mistaking it for real audience data; (4) the bulk "add missing translation per language" action is the lowest-risk item, entirely a UI convenience over already-existing data/flows. Produced a revised 6-slice plan (E1-E6) ordered by risk/dependency, with E1-E4 immediately assignable (no open questions) and E5/E6 each blocked on one named decision. No `docs/TECHNICAL_DEBT.md` entries added yet — deliberately, since nothing is implemented; a risk entry there would misrepresent a still-speculative feature as an accepted tradeoff in shipped code. Plan only; no code written. |
| BL-035 — Languages tab: owner's answers on deletion + AI-recommendation scope | **Plan revised, not implemented** (does not authorize starting E5) | 2026-09-20 | Merged to `dev` in feature/languages-redesign-plan-v3 | The owner answered BL-034's two open product questions over Telegram the same day. §7.4 (AI-recommended languages) resolved to the simplest safe option: an empty, clearly-labeled placeholder now ("coming with Analytics integration, Phase 8"), matching the existing Analytics-tab stub pattern — no AI call, no product-honesty risk, moved into the low-risk E1-E4b lane outright. §7.2 (tracked-language columns) turned out to be a much bigger ask than the original plan assumed: the owner said real deletion — including from YouTube itself — is wanted, gated behind multi-step confirmation and a 30-day (default) UI-visible restore window, not the display-only reading BL-034 had proposed and asked to confirm. Consulted the advisor before revising the plan (not before answering, since orientation/re-reading BL-034's own text was enough to see the gap) to pressure-test the analysis; it confirmed and sharpened two points now recorded explicitly in `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` §7.2/§7.7: (1) this **reverses `docs/PROJECT_SPEC.md` §8's documented "prefer deferring deletion" preference** and `docs/ARCHITECTURE.md`'s present-tense "deletion remains fully deferred" — surfaced as an explicit question (update the spec, or treat this as a documented exception to an otherwise-still-valid preference?) rather than silently building around it, per AGENTS.md §A; (2) a real `videos.update` call removing a `localizations` key is structurally a **localization** write, which today only has one safety-critical pipeline, `src/lib/batches/` — but that pipeline's `WriteExecutor` is unconditionally barrier-disabled pending Gate B, so routing deletion through it would ship a delete button that can never actually delete anything; the only way to ship a *working* one today is a **second, standalone live-write path** for `localizations`, architecturally a sibling to `src/lib/video-details/` (reusing `write-context`/`src/lib/backup/`'s established pattern) but structurally outside the one pipeline Phase 5 built its live-write safety guarantees around — named explicitly as a real, accept-it-knowingly tradeoff, not a reason to refuse the feature. Also established: "restore" within the 30-day window is itself a full write (re-sending the backed-up content), not a free local undo, and the 30-day figure governs only what the UI *offers* as restorable — `src/lib/backup/`'s files already never expire and this plan proposes no purge job. E5 (now covering all of §7.2) is marked **high risk and not assignable** until three named questions are answered (§7.7: spec-reversal handling, explicit acceptance of the second write path, restore-mechanics confirmation) — E1-E4b remain immediately assignable regardless. Plan only; no code written; no `docs/PROJECT_SPEC.md` change made yet (that is itself Open Question 1, not assumed). |
| BL-035 follow-up — `docs/PROJECT_SPEC.md` §16 updated; deletion-policy questions 1 and 3 resolved | **Spec updated; question 2 (second write path) still open** (does not authorize starting E5) | 2026-09-20 | Merged to `dev` in feature/project-spec-deletion-policy | The owner answered Open Question 1 directly: "обновляем сам PROJECT_SPEC. но добавляем что никакое удаление не может быть перманентным и сразу... несколько этапов подтверждения... кэш N дней (по умолчанию 30)." `docs/PROJECT_SPEC.md` §16 ("XLSX Import," not §8 — see correction below) now states this as a **permanent, application-wide constraint on any deletion feature this app ever builds**, not scoped to this one Languages feature: no deletion may be permanent/immediate regardless of confirmation-step count; a deleted value must stay locally recoverable for a configurable window (default 30 days); "restore" means re-applying the value as a full write through the same safety model (identity/dry-run/audit/verification), never a bare undo; the window governs what's offered as restorable, not the backup file's own lifetime. This resolves Open Questions 1 and 3 from `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` §7.7 (both updated to reflect it). **Correction found and fixed while doing this:** `docs/ARCHITECTURE.md`, `docs/SYSTEM_MAP.md`, and `docs/DEVELOPMENT_PLAYBOOK.md` had all mis-cited the pre-existing "deletion must be explicit" guidance as `docs/PROJECT_SPEC.md` §8 (actually "Channel and Account Model," unrelated) — the real source was always §16; all three corrected, per AGENTS.md §H ("a discrepancy discovered during any task should be corrected as part of that task"). Separately, the owner gave direction on backup retention: real deletion of old backup files is wanted eventually, but explicitly deferred — "пока можешь записать в технический долг, вернёмся к этому потом" — recorded as `docs/TECHNICAL_DEBT.md` RISK-41, an unusual pre-emptive entry (no code exists yet) written down specifically per that instruction so it isn't forgotten once E5 ships; current "backups never expire" behavior is unaffected for now. The owner also asked for a plain-language explanation of the Gate B blocker from the prior round (Open Question 2) rather than answering yet — explained directly in chat, not repeated in the docs; Question 2 (accepting a second live-write path outside Batches) remains the one open item blocking E5. |
| BL-037 — Languages tab redesign, slices E1-E4b | **CODE-COMPLETE, UI-only** (E5 remains unassigned, blocked on BL-035's Open Question 2) | 2026-09-21 | Merged to `dev` in feature/languages-ux-e1-e4b | The owner answered `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` §7.7's last open item directly: "Да, начинай" (start E1-E4b). Delivered as a full rewrite of `src/components/languages-manager.tsx` — no backend/API change, this is the UI-layer redesign that plan's §7.6 describes. **E1 (sortable columns):** client-side `sort`/`compareRows` state, any column header click toggles sort direction; default stays `publishedAt` desc (unchanged from before). A "Published" column was added to make that default visible/clickable — a deliberate small addition beyond the literal requirement text, noted as such rather than silently expanding scope. **E2 (per-language ✓/— columns):** undoes the 2026-09-20 Languages-tab-merge session's own earlier restyle from a per-language grid down to a bare count — one `<th>`/`<td>` per `overview.languages` entry, sortable (missing-first by default for a language column, since that's the more useful direction — "who still needs this"). **E3 (bulk add-missing per language):** a small `+N` button under each language header selects every video channel-wide missing that language and opens the bulk-generate popover pre-filled with it. **E4 (contextual bulk bar + inline per-row generate):** the old always-open top-of-tab "Generate with AI" checklist (a third, independent selection set) is removed entirely; one shared `selectedIds` set now drives both AI generation and XLSX export (previously two separate sets, per the plan's own §4.5 goal). A bulk-action bar appears above the table only when ≥1 row is checked, opening a popover; each expanded row also gets its own inline generate mini-form, replacing the old behavior of scrolling the whole page back up to a generic top section. Both entry points funnel through one shared `renderGenerationPanel()` render function and one `generateScope` value (`{kind:"bulk"}` vs `{kind:"row", videoId}`), so there is exactly one generate/review/create-Change-Set implementation, not two independently-maintained copies. **E4b:** a static, unwired "Recommended languages — coming with Analytics integration (Phase 8)" placeholder card, matching the existing Analytics-tab stub pattern. **Live-verified end-to-end** (`claude-in-chrome`, real "Tropico Jazz" channel): default sort order, clicking the "es" column header re-sorted missing-first with the indicator showing; the `+N` shortcut for "es" selected the correct 14 videos and pre-filled the popover; the bulk path was run all the way through generate → review 14 proposals → create Change Set (real local Change Set created, confirmed, then rejected afterward as test cleanup since it was local test data with zero YouTube effect); the per-row inline path was independently run end-to-end on a single video, confirming it generates for exactly that one video regardless of the unrelated bulk selection state; zero console errors throughout. `npm test`: 594 unchanged (pure UI-layer change, no new domain/API logic — consistent with this repo's existing convention of no component-level unit tests, e.g. `video-details-panel.tsx`'s pure helpers are likewise only browser-verified, not unit-tested); `npm run lint`, `npx tsc --noEmit`, `npm run build` all clean. **Not re-exercised in this pass:** XLSX export against the newly-unified `selectedIds` (a mechanical rename `tsc` validates end-to-end, but not independently clicked through live — flagged rather than silently assumed covered). **Known limitation, not fixed in this pass:** the bulk-generate popover (`position: absolute; right-4`, fixed width) can render partially off the left edge of a genuinely narrow browser window; not observed as a problem at normal desktop widths, where this app is actually used. **Deliberately not added:** no "remove language" button/affordance anywhere on the new per-language column headers, even though their layout makes the idea obvious — that action belongs to E5, which remains unassigned pending BL-035's Open Question 2 (the second-live-write-path decision), and adding even a UI stub for it now would visually imply a capability that doesn't exist and isn't authorized. |
| BL-036 — Localization deletion, backend proposal path only (routed through the existing, Gate-B-blocked Batches pipeline) | **CODE-COMPLETE for the backend slice; no UI, no restore mechanism** (does not close BL-035's Open Question 2, and does not authorize the rest of E5) | 2026-09-21 | Merged to `dev` in feature/localization-deletion-backend | The owner answered BL-035's still-open Question 2 (accept a second, standalone live-write path outside Batches for deletion?) not by choosing yes or no, but by deferring the decision itself: "откладываем до реальных боевых тестов Gate B, пока можешь просто сделать просто функционал который упирает в gate B" — build the deletion logic now, routed through the existing `src/lib/batches/` pipeline (which safely refuses to execute anything, live-write barrier still unconditionally disabled), rather than build a second, riskier write path speculatively. Delivered: `ChangeType` gained `"delete"`, `ChangeSetSource` gained `"deletion"`; new `changesets.proposeLocalizationDeletion({channelId, videoId, language})` creates a two-`Change` (title+description) Change Set through the existing shared `persistChangeSet` path — a deletion proposal goes through the identical review/approve/reject/conflict-revalidation pipeline as any other change, satisfying §16's "multi-step confirmation" requirement structurally (propose → approve → [batch execution, currently always blocked] are three separate, independently-gated steps) rather than by a new bespoke confirmation UI. **Refuses at propose time** (`DomainError("deletion_targets_default_language")`) if the target language is the video's own `defaultLanguage` — that language's title/description live on `snippet`, not a removable `localizations` entry, and deleting it would blank the video's real title/description instead. `src/lib/batches/merge.ts`'s `buildSafeLocalizationsPayload` carries the identical guard as defense-in-depth (throws; the one call site in `src/lib/batches/services.ts` converts it to a `FAILED` ledger outcome, fail-closed) and implements delete-wins merge semantics (a `"delete"` always wins over a same-locale `"modify"` approved in the same set, regardless of array order — verified both orders). Consulted the advisor before implementing (confirmed the design and the defaultLanguage danger) and again before committing — the second pass caught that no test yet exercised a delete change through `assertApprovalStillValid`/`approveChange` with `proposedValue: ""`; read both and confirmed all the relevant comparisons are strict (`!== null`, `!== proposedValue`), never a truthy check that an empty string would fail, so no bug existed, but this was verified rather than assumed. Also found and fixed, while in the area, a reintroduction of an earlier-fixed bug: `change-set-review.tsx`/`languages-manager.tsx`'s Change-Set-source label would have rendered a `"deletion"` set as "XLSX import" (same class of bug as the Languages-tab-merge row above) — fixed with a third label branch; not user-reachable yet since no UI creates a deletion Change Set. 10 new tests (`src/lib/batches/merge.test.ts`, `src/lib/changesets/services.test.ts`), spec-driven: delete-only removes the locale entirely (others byte-identical), delete beats modify in both array orders, defaultLanguage deletion refused at both the service layer and merge layer, a third-party edit made after proposing deletion is caught as a conflict on re-sync (mirrors the existing "re-sync draft preservation" test), and an empty-baseline/empty-current-value pair is correctly not a false-positive conflict. `npm test`: 584 → 594; `npm run lint`, `npx tsc --noEmit`, `npm run build` all clean. **Explicitly deferred, per the advisor-reviewed scope agreed before writing any code:** no UI anywhere triggers a deletion proposal (this is pure backend/service-layer capability); no restore mechanism (§16's 30-day recovery window) — both are meaningless to build/test further right now since nothing this pipeline produces can reach a real YouTube write until Gate B's live-write barrier is lifted, which is itself a separate, future, explicitly-authorized action. BL-035's Open Question 2 (the second-write-path decision) remains genuinely open — this row does not answer it, it postpones the need to answer it. `docs/SYSTEM_MAP.md` §2.9 and `docs/ARCHITECTURE.md` §6.14/§6.15 updated to describe the new capability and its explicit boundaries. |
| BL-037 follow-up — `autonomous-dev-loop` skill's first real run: independent-review cycle over the E1-E4b redesign | **Cycle stopped after 7 rounds, by decision, not at "0 findings"** (does not authorize starting Phase 7 — `.claude/skills/autonomous-dev-loop/` §3's exception requires a clean final round, which this run never reached) | 2026-09-21 | Merged to `dev` across feature/review-cycle-{1..6}-languages-fixes, feature/review-cycle-revert-round6 | The owner confirmed the `autonomous-dev-loop` skill (Telegram, "Подтверждаю, можешь приступать к работе используя этот скилл") and named Phase 6 as current. Phase 6 had no further assignable work (all three slices already CODE-COMPLETE; the one open item — real AI-provider selection — is a paid, owner-only decision; RISK-14/15 are explicitly owner-approval-gated, not freely pickupable), so per the skill's §2/§4 the loop ran an independent-review cycle (`/code-review high`, never the billed `ultra` variant) against `languages-manager.tsx` instead. **Findings per round: 7, 5, 6, 3, 5, 2, 6.** Rounds 1-4 fixed real, narrowing bugs in the E1-E4b generation-session state machine (stale-session data leaking across scope switches, a request-cancellation race that could double-fire a real paid AI call, silently swallowed provider errors, a mid-flight deselection race) — each fixed, live browser-verified, merged, pushed. **Round 6 made a wrong call** (pruning `targets` on every deselection to stop a resurfacing-old-content "surprise") that round 7 caught as two real regressions of its own (destroyed operator edits on an accidental checkbox double-click; the round-5 "results discarded" notice could silently vanish) — reverted the same day after consulting the advisor, with the reasoning recorded as a decision (`docs/TECHNICAL_DEBT.md` RISK-45, RESOLVED): a resurfaced AI proposal is not a safety issue, since it still passes through the full review/approve/Gate-B-blocked-write pipeline regardless of which checkbox state produced it, while destroying operator edits on a misclick is a real, unrecoverable loss. **The cycle was deliberately stopped after this revert, not run to a literal "0 findings" round** — rounds 5-7 had converged on re-reviewing the cycle's own prior-round fixes rather than surfacing new defects in the original E1-E4b feature, and the skill's own §2a two-no-progress-iterations stop condition exists for exactly this shape of diminishing, self-referential return. Four of the seven rounds independently flagged the same underlying gap — `docs/TECHNICAL_DEBT.md` RISK-42 (zero component/unit test coverage anywhere in `src/components/`) — as the reason each round's fix could go on to break the previous round's fix with nothing automated to catch it; surfaced to the owner as the one concrete, actionable decision this cycle produced, separate from the individual bug fixes themselves. `npm test`: 594 throughout (unchanged — pure UI-layer fixes); `npm run lint`/`npx tsc --noEmit`/`npm run build` clean after every round. Two new lower-priority `docs/TECHNICAL_DEBT.md` entries from rounds 3-5 remain OPEN and undecided: RISK-43 (no true cancellation of an abandoned real AI request server-side) and RISK-44 (the tab's one shared error banner can be overwritten by an unrelated later error) — both are UX/cost-hygiene items requiring their own product decision, not correctness defects. |
| BL-038 — Shared video-detail popup module, replacing row-expansion in Content and Languages | **CODE-COMPLETE** | 2026-09-21 | Merged to `dev` in feature/video-detail-modal | Assigned by the project owner over Telegram right after the independent-review cycle above wrapped up: "Первая правка функционала, может использоваться как в контенте, так и в языках и возможно будет нужна где-то ещё — поэтому можно делать модулем. При нажатии на видео, нам раскрывать информацию о нём не расширением строки, а выводить отдельный поп ап, почти на весь экран с информацией по выбранному видео. Функционал / наполнение этого поп апа будет отличаться в зависимости от того в какой категории мы сейчас находимся." Delivered exactly as scoped: new `src/components/video-detail-modal.tsx` is a pure-presentation shell (fixed full-viewport backdrop, a `max-w-5xl h-[92vh]` card, a header with thumbnail/title/close button, closes on backdrop click or Escape) with zero domain logic -- what renders inside is entirely up to the caller via `children`, satisfying the owner's "naполнение будет отличаться в зависимости от категории" requirement structurally rather than with an internal mode switch. `content-manager.tsx`: replaced its `<tr>`-expansion block with the modal wrapping the already-existing, unmodified `VideoDetailsPanel` -- trivial migration since that component was already self-contained (its own `channelId`/`videoId` props, own data fetching). `languages-manager.tsx`: extracted the previously-inline detail block (original title/description, existing locales, the inline AI-generate mini-form/review panel) into a new `renderVideoLocalizationDetail()` function rendered inside the same shared modal; `toggleExpand` split into `openVideoDetail`/`closeVideoDetail` (a row click now always opens rather than toggling collapse, since closing is the modal's own job now) with identical state-management semantics to before (a row-scoped generate session still closes correctly when its video's popup closes). Both components now look up the "currently open" video from their *full* video list (not the filtered/paginated/sorted view), so the popup stays open and correct even if the operator changes search/sort/page while it's open. Live-verified in the browser (`claude-in-chrome`, real "Tropico Jazz" channel) for both tabs: popup opens with correct video data, Content's `VideoDetailsPanel` renders and functions identically to before, Languages' original/locales/inline-generate-to-review-panel flow works identically to before, closing via backdrop click and via Escape both work, zero console errors. `npm test`: 594 unchanged (pure UI-layer change); `npm run lint`, `npx tsc --noEmit`, `npm run build` all clean. `docs/SYSTEM_MAP.md` gained a new §2.9e for the module and had its Content/Languages sections' stale "expandable row" wording corrected; `docs/ARCHITECTURE.md` §9's Languages-tab paragraph (last accurate as of the 2026-09-20 tab merge, silently gone stale across the same-day E1-E4b redesign) was rewritten to match current reality and now points to `docs/SYSTEM_MAP.md` as the authoritative detail source rather than duplicating it. **Not done, not requested:** no other tab (Batches, Home) uses this module yet -- the owner's own "возможно будет нужна где-то ещё" was explicitly speculative, not a request to wire it in everywhere now. |
| BL-038 follow-up — video-detail-modal fixes after the owner's first hands-on test | **CODE-COMPLETE** | 2026-09-21 | Merged to `dev` in feature/video-detail-modal-fixes | The owner tested BL-038's popup and reported three issues over Telegram: (1) Content's Description/Tags fields too small; (2) dragging a text selection and releasing outside the popup incorrectly closed it; (3) closing with unsaved edits should never be silent. Fixed all three. (1) `video-details-panel.tsx` restructured into a bounded flex column (`h-full flex flex-col`) so Description grows to fill remaining vertical space with its own internal scrollbar (`flex-1 min-h-0` + `overflow-y-auto` on the textarea itself, not the whole panel); Tags changed from a single-line `<input>` to a 3-row `<textarea>`. (2) `video-detail-modal.tsx`'s backdrop-click-to-close now requires BOTH `mousedown` and `mouseup` to land directly on the backdrop element itself, not just `onClick` -- a native `click` event fires based on where `mouseup` lands regardless of where a drag/selection started, which is exactly what let a text-selection drag ending outside the card close it before. (3) New `VideoDetailModal` prop `hasUnsavedChanges`; when true, every close path (backdrop, Escape, the × button) shows a "Discard unsaved changes?" confirmation instead of closing immediately. `VideoDetailsPanel` gained an `onDirtyChange` callback (fired from its already-existing `hasChanges` computation, used for the Preview/Save button states) so `content-manager.tsx` can track dirtiness without the panel needing to know anything about its container; Languages defines "dirty" as an open row-scoped AI-generation session with at least one still-pending (not yet submitted) proposal. Live-verified in the browser on both tabs: enlarged fields with visible internal scrollbar, a simulated mousedown-inside/mouseup-outside sequence no longer closes the modal while a plain backdrop click still does, and both tabs correctly show the discard-confirmation dialog (Cancel keeps edits and the modal open; Discard actually closes) when there's unsaved work. `npm test`: 594 unchanged (pure UI); `npm run lint`, `npx tsc --noEmit`, `npm run build` all clean. `docs/SYSTEM_MAP.md` §2.9e updated with these three fixes (this row itself was not recorded in this file at the time -- added here as part of the immediately-following BL-039 documentation pass, per AGENTS.md §H). |
| BL-039 — E5a: tracked-language columns (add/remove as a display preference, no real deletion) | **CODE-COMPLETE** | 2026-09-21 | Merged to `dev` in feature/tracked-languages | The owner asked how to add a new language column and was told, honestly, that it isn't currently possible -- `LocalizationOverview.languages` was purely the union of languages with at least one REAL, already-synced localization (`collectChannelLanguages`), so a language could never appear as a column before it already had real YouTube data, and no local draft (AI-generated or XLSX-imported, both blocked from ever reaching YouTube by Gate B) could ever change that either. The owner then explicitly assigned exactly this fix, plus remove-column with a real-deletion warning: "Давай пока добавим возможность добавить столбцы для новых языков, на которые мы можем захотеть сделать перевод. И возможность удалять колонки. Если в удалённой колонке есть язык на реальном канале, появляется предупреждение... back up для восстановления и отправляет эти корректировки точно так же как мы отправляем информацию о дополнении информации." Consulted the advisor before implementing, given this touches deletion/safety territory (`docs/PROJECT_SPEC.md` §16) -- it confirmed the design and gave one explicit, followed instruction: **split the work.** Ship the tracked-language add/remove display preference (E5a) alone first; the real-deletion half (E5b, warning + backup + routing through the existing Change-Set/Batch pipeline) is safety-critical, needs its own spec-driven tests, and deserves its own UI-iteration pass with the owner -- exactly the kind of large, coupled change whose risk this session's own 7-round independent-review cycle over E1-E4b had just demonstrated. **Delivered (E5a only):** additive `channels.target_languages_json` (`SCHEMA_MIGRATIONS` version 6, `isDuplicateColumnError`-guarded `ALTER TABLE`, nullable -- never backfilled to `"[]"`); two new, deliberately narrow `src/lib/db.ts` functions (`getChannelTargetLanguages`/`setChannelTargetLanguages`) rather than widening the broadly-shared `StoredChannel` type every other domain also uses (AGENTS.md §D, same reasoning as `PendingChangeRecord`'s own narrow copy); `src/lib/localization/services.ts` gained `addTrackedLanguage`/`removeTrackedLanguage` (reusing `isValidLanguageCode` from `changesets/diff.ts`, not a new copy) and `getLocalizationOverview`'s `languages` is now `trackedLanguages ∪ languages-with-real-data`, with `trackedLanguages` also exposed separately in the response so the UI can tell the two cases apart; two new API routes (`POST`/`DELETE .../localizations/tracked-languages[/[language]]`), each going through the existing `channelAccess.assertActiveChannel` channel-scoping check (AGENTS.md §F) like every other route in this module. UI: an "Add language column" input in the table toolbar; a small "✕" next to each language column header. **Critical design point, explicitly told to the owner in the same report:** because `languages` is that union, untracking a language that still has real translations does nothing visible -- the column stays, and the UI now says so explicitly ("...but it still has real translations on this channel, so its column remains visible") rather than implying the removal succeeded when only the local preference changed. 6 new spec-driven tests (`src/lib/localization/services.test.ts`): a tracked language with zero real data appears as an all-missing column; adding an already-tracked language is idempotent; an invalid language code is rejected; untracking a no-real-data language removes its column entirely; untracking a language that still has real data leaves it in `languages` unchanged. **Found and fixed an unrelated, pre-existing test fragility while validating:** `src/lib/snapshot/services.test.ts`'s RISK-29 column-reordering test hand-builds a `channels`-shaped table to simulate a physically-reordered schema, and had not been updated for this new column -- its own staged-copy migration failed with "no such column" because the test's migration-tracking metadata (carried over from the original, pre-manipulation table) claimed the migration had already run against a table the test had since manually replaced. Fixed by adding the column to the test's hardcoded schema; not a production-code defect. Live-verified in the browser against the real "Tropico Jazz" channel: added "fr" (appeared as an all-missing column with its own bulk-generate shortcut), removed it (column disappeared, no real data existed), then removed "es" (stayed visible with the explicit notice, since real translations exist for it). `npm test`: 594 → 599; `npm run lint`, `npx tsc --noEmit`, `npm run build` all clean. **Not done, not requested:** E5b (real deletion) -- see `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md` §7.6's E5b row for the design already sketched for it, awaiting its own explicit assignment. |
| BL-040 — E5b: real localization-deletion proposal (warning UI + unified bulk backend), routed through the existing Gate-B-blocked Batches pipeline | **CODE-COMPLETE for the proposal slice; no UI/mechanism for restore** (does not lift Gate B, does not authorize any real YouTube write) | 2026-09-21 | Merged to `dev` in feature/localization-deletion-e5b | The owner assigned this directly ("Продолжай с E5b") after BL-039 (E5a) shipped the display-only half. Consulted the advisor before implementing, given real-deletion territory (`docs/PROJECT_SPEC.md` §16/§21); it gave two decisions actually followed: (1) unify BL-036's earlier single-video `proposeLocalizationDeletion({channelId, videoId, language})` and E5b's needed whole-column form into one function, `proposeLocalizationDeletion({channelId, language, videoIds?})` -- an omitted `videoIds` means "every video on the channel with a real localization for this language," rather than maintaining two parallel deletion-proposal code paths; (2) never call the E5a untrack endpoint when proposing a real deletion, since the column stays visible via the `trackedLanguages ∪ real-data` union regardless until a genuine write+re-sync removes the real data -- untracking on top of it would misleadingly imply the column was gone. **Delivered:** `src/lib/changesets/schemas.ts`'s `proposeLocalizationDeletionInputSchema` widened to accept an optional `videoIds` array; `src/lib/changesets/services.ts`'s `proposeLocalizationDeletion` rewritten to branch on `videoIds` presence, returning `{changeSet: ChangeSet | null, affectedVideoIds, skippedDefaultLanguageVideoIds}` -- `changeSet: null` is a legitimate "nothing to propose" result, not an error. The `defaultLanguage` exclusion (`skippedDefaultLanguageVideoIds`) is computed directly from each candidate video's own `video.defaultLanguage === language`, deliberately NOT derived from the `existingLocalizations`-based union `collectChannelLanguages` uses -- the advisor flagged that a video can carry a language as its own `defaultLanguage` while contributing zero `existingLocalizations` entries for it, so the union-based check would silently undercount how many videos are actually being protected (the "undercount trap"); a dedicated test proves this case is still caught. New route `POST /api/channels/[channelId]/localizations/languages/[language]/propose-deletion` (no `videoIds` in the request body -- always the bulk/whole-column form from the UI's own single "✕" action), behind the existing `channelAccess.assertActiveChannel` scoping check like every other route in this module. UI (`languages-manager.tsx`): `handleRemoveTrackedLanguage` now branches on whether the language has any real data on the channel -- no real data keeps the original E5a untrack-only behavior unchanged; real data present shows an explicit `window.confirm` warning (what will happen, that a backup is captured automatically by the *existing* batch-preparation pipeline -- not new work for this slice, that nothing is applied immediately, that this only needs Gate B to actually take effect) before calling the new route, then surfaces either a "nothing to propose" notice or opens the created Change Set for review. 5 spec-driven tests (`src/lib/changesets/services.test.ts`, replacing 4 narrower single-video-only tests): the explicit-`videoIds` single-video path; a video whose own `defaultLanguage` matches is always skipped and reported; the undercount-trap case (matching `defaultLanguage` with zero `existingLocalizations` entries for that language) is still skipped; the whole-column form (`videoIds` omitted) correctly spans every video with a real localization while skipping unrelated and `defaultLanguage`-matching ones (4-video fixture, hand-computed expected `affectedVideoIds`/`skippedDefaultLanguageVideoIds`/`totalChanges`); the existing re-sync-detects-conflict scenario adapted to the new signature. `npm test`: 599 → 600; `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean. **Live-verified in the browser** against the real "Tropico Jazz" channel: the no-real-data untrack path still works unchanged (added/removed "zz"); the real-deletion path correctly fired the new route and correctly found that all 21 videos with an "es" localization also have "es" as their own `defaultLanguage` on this particular channel -- so it returned `changeSet: null` with an explicit "nothing to propose, 21 skipped" notice, and no stray Change Set was created (queue count unchanged, zero console errors). This means the "creates a real, non-null Change Set with affected videos" path could not be exercised against this specific channel's current live data; it is exercised instead by the hand-computed unit tests above, which was judged sufficient rather than injecting synthetic local-DB rows to force the scenario, given the added risk/cleanup of manipulating the dev database for a case the spec-driven tests already cover independently. **Explicitly out of scope, not requested:** any restore UI or mechanism for the 30-day recovery window (`docs/PROJECT_SPEC.md` §16) -- there is nothing to restore until Gate B is cleared and a real deletion can actually happen; `docs/TECHNICAL_DEBT.md` RISK-41 already tracks the backup-retention/purge side of this for that future point. `docs/SYSTEM_MAP.md` §2.9 and `docs/roadmap/plans/LANGUAGES_UX_REDESIGN_PLAN.md`'s own E5b row updated to describe the delivered capability and its explicit boundaries. |
| BL-041 — Languages tab: "Sync now" button + real-language-list combobox for "Add language column" | **CODE-COMPLETE** | 2026-09-21 | Merged to `dev` in feature/languages-sync-and-language-list | Owner-requested (Telegram): a "Sync now" button matching Content's, and replacing the free-text "Add language column" field with one based on a real, searchable list of languages ("En - English", narrows as you type). Per the advisor's review: "Sync now" also refetches Change Sets (a re-sync can invalidate an approval, `docs/ARCHITECTURE.md` §6.7), without porting Content's mount-time auto-resync. The language list comes from a real YouTube API call (`i18nLanguages.list`, new `src/lib/youtube.ts` function + cached global route `GET /api/youtube/languages`) and is a suggestion, not a hard allowlist -- a typed code outside the list is still accepted via Enter/Add. 2 new tests (`src/lib/youtube.test.ts`, 600 → 602); tsc/lint/build clean; live-verified in the browser (Sync now, and typing "english" correctly narrowed to `en-GB`/`en-IN` while excluding the channel's already-tracked `en`/`en-US`/`es`). See `docs/SYSTEM_MAP.md` §2.9 for the technical detail this row intentionally doesn't restate (`AGENTS.md` §H). |
| BL-042 — Languages tab: hard language allowlist (researched, hardcoded) + write-time-rejection fallback | **CODE-COMPLETE** | 2026-09-21 | Merged to `dev` in feature/hardcoded-language-allowlist | Owner asked (Telegram) whether BL-041's suggestion-only list could instead be the exact real set, researched it (official YouTube docs give no dedicated list for `localizations`; the only real, queryable one is `i18nLanguages.list`), and found empirically -- before implementing anything -- that this list is missing "en-US", a language the "Tropico Jazz" channel already really uses. Surfaced this to the owner before proceeding rather than silently shipping a regression; owner chose to hardcode the raw 83-language list anyway (`src/lib/youtube-supported-languages.ts`, avoids a live API call per owner's "не дергать API лишний раз") and accept the documented gap, enforced as a genuine server-side gate in `addTrackedLanguage` (not just client UI) plus a matching client-side disable on Add/Enter. Already-real languages stay visible regardless via the existing `trackedLanguages ∪ real-data` union (BL-039/E5a) -- the gate only blocks typing a brand-new, not-yet-used code. The owner's requested write-time-rejection fallback turned out to need no new code: `classifyYoutubeWriteError` already classifies any `videos.update` 400 (including an invalid language) as a `permanent` FAILED ledger row with YouTube's own error text, and `batch-manager.tsx` already renders per-row errors -- verified by reading both, not assumed. 5 new tests (`src/lib/youtube-supported-languages.test.ts`, `src/lib/youtube.test.ts`; 602 → 605); tsc/lint/build clean; live-verified in the browser (typing "en-US" leaves Add disabled and Enter inert with no suggestions; "fr", a real list entry, adds normally). See `docs/SYSTEM_MAP.md` §2.9 for detail (`AGENTS.md` §H). |
| BL-043 — Shared `ConfirmDialog` replacing `window.confirm`; standing no-native-dialogs rule | **CODE-COMPLETE** | 2026-09-21 | Merged to `dev` in feature/confirm-dialog-no-native-popups | Owner flagged, after BL-042's browser testing, that a native `window.confirm` in `languages-manager.tsx`'s remove-tracked-language flow broke visual consistency, and asked for a standing rule: any popup/notice/error message this app itself designs must render through the app's own UI, never a native browser dialog. New `src/components/confirm-dialog.tsx` (one shared implementation, `AGENTS.md` §D) now backs both of `languages-manager.tsx`'s remove-language confirmations and `video-detail-modal.tsx`'s discard-changes confirmation (refactored from its own inline markup to the shared component). Rule recorded in `docs/DEVELOPMENT_PLAYBOOK.md` §6.9 item 9, citing the concrete motivating incident: the native dialog had also frozen a live-verification browser session earlier in the day (CDP is blocked while a native dialog is open). `grep` confirms zero remaining `window.confirm/alert/prompt` calls in `src/`. 605/605 tests unchanged (pure UI refactor); tsc/lint/build clean; live-verified in the browser (both remove-language paths now show the styled in-app dialog, Cancel/confirm both work, no browser freeze). |
| BL-044 — Languages: two-column redesign of the per-video review popup | **CODE-COMPLETE** | 2026-09-21 | Merged to `dev` in feature/languages-video-review-redesign | Owner asked for a wider popup, original language on the left and one selected translation on the right (dropdown above it), independent scroll per text box, and title as its own visual box above description -- and left open whether the dropdown should be bound to already-tracked columns. Discussed before implementing: recommended binding it to `overview.languages` (the same union the table's own columns already show) rather than only `detail.locales`, so the popup can also select a language the video is still missing and generate straight from an empty state -- owner agreed. Delivered: `VideoDetailModal` gained an optional `widthClassName` (Languages passes `max-w-7xl`; Content's own popup untouched); `renderVideoLocalizationDetail` rewritten as a two-column grid, each column's title/description in its own bordered box with independent `overflow-y-auto`; the right column's dropdown excludes the video's own `defaultLanguage`, and an empty selection shows a "Generate with AI for this language" shortcut that opens the existing generation panel pre-filled with that language. Live-verified in the browser against "Tropico Jazz": the empty state (this channel's real data has no cross-language video -- every video's one real localization always equals its own `defaultLanguage`, so the populated branch was verified by temporarily intercepting `fetch` with synthetic locale data, no real DB write); both text boxes scroll independently; the per-language generate shortcut correctly pre-fills the target-language field. 605/605 tests unchanged (pure UI); tsc/lint/build clean. |
| BL-045 — Gate B "live writes" toggle + persisted MCP-restricted-mode toggle + the Batches "Execute" action | **CODE-COMPLETE -- does NOT close Gate B** | 2026-09-21 | Merged to `dev` in feature/gate-b-live-writes-toggle | Owner asked to test the first live write against a real channel; a multi-message discussion first clarified what Gate B actually is (an internal readiness checklist, not a YouTube API concept) and surfaced that the acceptance contract requires a dedicated test channel, never a production one -- still unanswered, this row does not authorize or perform any real write. Owner then asked for a Settings-tab switch (session-default-off, explicit Save/Apply) plus an equivalent persisted toggle for MCP restricted mode "by analogy." Consulted the advisor before implementing (safety-critical): its one hard requirement was to add the setting as a SECOND, independent layer in front of the existing barrier, never collapse the two-layer design (RISK-09/AC-SCOPE-01) into a single check. **Delivered:** `src/lib/db.ts` gained `app_settings` (SCHEMA_MIGRATIONS v7) with `getLiveWritesEnabled`/`setLiveWritesEnabled`/`getMcpRestrictedModeEnabled`/`setMcpRestrictedModeEnabled`; `initializeDatabase()` unconditionally resets `live_writes_enabled` to `false` on every process boot (web app, MCP server, or CLI, whichever imports `db.ts` first) via a raw `rawClient.execute` call specifically to avoid deadlocking against its own guarded-client wrapper (found and fixed during implementation -- the guarded `db` object awaits the same `databaseInitialization` promise this function itself is). Layer 1: new `src/lib/batches/adapters/write-executor.ts`'s `createLiveWriteExecutorIfEnabled` constructs a real `WriteExecutor` only when the setting is on, called only from the one new route `POST .../batches/[batchId]/execute`. Layer 2: `assertLiveWritesAuthorized` (`write-executor.youtube.ts`) is now `async` and re-reads the identical setting itself, independently, immediately before any network call -- unchanged in spirit from its prior unconditional-throw design, just no longer permanently unconditional. `POST .../batches` now honors a request's `dryRun: false` only when the setting is on at that moment (fail-closed otherwise, exactly as before). `src/lib/batches/write-path-inventory.test.ts` updated with one explicit, narrow allowlist entry for the new execute route (not weakened generally) plus a new test proving that allowlisted file actually gates its `executeBatch` call through `createLiveWriteExecutorIfEnabled`. MCP: `startMcpServer()` now resolves the persisted setting once at process boot and passes it explicitly to `createMcpServer` (which stays synchronous, unchanged signature/tests) -- stated, not solved, per the advisor: an already-open MCP connection keeps its existing tool set until it reconnects, since an MCP server's tool registration is fixed at construction time. UI: `src/components/live-writes-settings.tsx` (Settings tab, both toggles, `ConfirmDialog` before enabling live writes) and `batch-manager.tsx` gained a "Create as a real, live batch" checkbox (visible only when the setting is on) and an "Execute (real write)" button on an already-created live batch, itself behind a `ConfirmDialog` stating the channel, video count, and field count (the advisor's explicit requirement -- this is new YouTube-writing UI, so it gets the same confirm treatment as any other app-designed popup, BL-043's standing rule). 605 → 607 tests (`write-executor.youtube.test.ts` gained an on-case mirroring the existing off-case); `npx tsc --noEmit`, `npm run lint`, `npm run build` clean. Live-verified in the browser against "Tropico Jazz": enabling the toggle shows the confirm dialog and persists after Save/Apply; the live-batch checkbox and Execute button appear/disappear correctly; a dry-run batch created with the toggle on (unchecked) behaves exactly as before (no regression); toggle turned back off before finishing, confirmed via `GET /api/settings` returning `false`. **The actual real write was deliberately never triggered** -- clicking Execute against a real channel is a separate, still-pending decision (which channel, informed consent per §4 of the acceptance contract) that this task's scope was explicitly limited to not include. `docs/TECHNICAL_DEBT.md` RISK-09 updated to describe the barrier as "owner-gated, not unconditional" -- Gate B itself remains `OPEN`; a toggle existing is not evidence the required live-validation scenarios (a real test channel, real OAuth, AC-MERGE-01/05/GUARD-01/CONFLICT-01/RESUME-01/E2E-01) have run. |
| BL-046 — single YouTube write gateway (`src/lib/youtube-write-gateway/`) | **DONE** | 2026-09-21 | Merged to `dev` in `feature/youtube-write-gateway` | Closes the gap BL-045's own review surfaced: `apply`/`playlist_*` had the identity guardrail but no live-write barrier at all. Every mutating `videos`/`playlists`/`playlistItems` call in the repo moved into `src/lib/youtube-write-gateway/`, mechanically enforced by `gateway-inventory.test.ts` (fails on any direct call elsewhere). `assertLiveWritesAuthorized` is now the one shared Gate B check; Batches' existing two-layer barrier is unchanged, and the three previously-ungated adapters (video-metadata, video-details, playlist-management) now call it too. Deleted dead code from the project's original pre-rewrite baseline (`updateVideoMetadataSafe`, unused since the code's original commit, which also wrote the §F-forbidden `defaultLanguage` field). See `docs/decisions/0005-youtube-write-gateway.md` for full rationale. 617 tests, `tsc`/`lint`/`build` clean. |
| BL-055 — single YouTube read gateway (`src/lib/youtube-read-gateway/`) | **DONE** | 2026-09-22 | 28945d7, merged to `dev` (5591c70) in `feature/youtube-read-gateway` | Owner-instructed (Telegram, "все запросы на получения данных должны идти через него и никак иначе"), mirroring BL-046's write gateway but as an umbrella (`index.ts` barrel) over category-specific children (`data-api.ts` for YouTube Data API v3 today), since a future YouTube Analytics child needs a different auth/client surface. `src/lib/youtube.ts` moved unchanged in behavior; ~10 call sites repointed at the barrel; `read-gateway-inventory.test.ts` mechanically enforces both the barrier and "no direct child import". Full documentation sweep (`AGENTS.md` §G, `SYSTEM_MAP.md`, `ARCHITECTURE.md`, `DEVELOPMENT_PLAYBOOK.md`, `TECHNICAL_DEBT.md` RISK-26/RISK-39, stale source comments) for stale `youtube.ts` references. See `docs/decisions/0007-youtube-read-gateway.md`. 705 tests, `lint`/`build` clean. |
| BL-047 — rename "MCP restricted mode" to "MCP connection", invert default to off | **DONE** | 2026-09-21 | Merged to `dev` in `feature/mcp-connection-toggle` | Owner instruction: MCP/agent should start fully disconnected, gaining any access only once explicitly enabled in Settings ("все взаимодействия MCP/агента должны идти через это переключение"). Replaced the old per-tool exclusion list (8 write/identity tools hidden, everything else always on) with one boolean gate in `createMcpServer`'s `registerTool`: off = zero tools registered at all, including read-only ones. Persists across restarts once enabled (owner-confirmed, unlike Live writes' per-session reset). Removed the `MCP_RESTRICTED_MODE` env var entirely. See `docs/TECHNICAL_DEBT.md` RISK-04's progress note and `docs/interfaces.md`'s "MCP connection" section. 627 tests, `tsc`/`lint`/`build` clean. |
| BL-048 — remove `start.sh`/`start.bat`'s own `git pull`; fix "always stale on Windows" build-staleness bug | **DONE** | 2026-09-21 | Merged to `dev` in `feature/start-script-cleanup` | Owner instruction: the launcher scripts should stop running `git pull` themselves ("за актуальностью гита я буду следить сам"); separately reported that the Windows launcher always required manually deleting `.next` to pick up a new version. Root cause: the scripts' only rebuild trigger was "`.next` doesn't exist yet" — true once, never again, so a manual `git pull` (the owner's own actual workflow) never triggered a rebuild once a build already existed. Fixed both at once: removed the `git pull --ff-only` block entirely, and replaced the staleness check with a marker file (`.next-build-commit.txt`, gitignored) recording which commit `.next` was built from — a checkout whose current `HEAD` no longer matches that marker rebuilds automatically. A no-`.git` standalone `published/<version>/` copy keeps the original "`.next` missing" check (unaffected; `update.bat`/`update.sh` remain its explicit rebuild step). Verified for real on macOS against the actual repository (not a throwaway fixture): a stale marker correctly triggered a rebuild, and an up-to-date marker correctly skipped one; the equivalent `.bat` logic was reasoned through and syntax-checked but not run on real Windows (still open, `docs/FIRST_LOCAL_TEST_BUILD.md`). |
| BL-049 — Automerge migration plan for the draft/change-set layer (BL-006 follow-up) | **DONE (plan only)** | 2026-09-21 | Docs on `docs/automerge-migration-plan` (not yet merged) | Owner asked to evaluate the current device-handoff sync scheme against alternatives, given goals of local storage before sending to YouTube, multi-device sync (ideally concurrent), and multi-channel management. A research pass compared CRDT engines (Automerge, Yjs, cr-sqlite) against Postgres-backed platforms (PowerSync, ElectricSQL, Zero) and Turso's own offline-writes feature — conclusion: only pure CRDT libraries deliver real automatic conflict resolution without a mandatory server. Owner chose Automerge, scoped to `change_sets`/`changes` only (the YouTube-write pipeline is untouched). Recorded as `docs/decisions/0006-automerge-for-draft-layer.md` (the architecture decision) and `docs/roadmap/plans/AUTOMERGE_MIGRATION_PLAN.md` (slices CD1-CD7, acceptance criteria AC-CRDT-01..06, and four required owner decisions — transport, rollout strategy, priority, permanent-vs-phase-1 scope — before any slice may be assigned). No code, schema, or dependency changes made. |
| BL-049 — CD1/CD2/CD4: Automerge draft layer built and cut over | **DONE (CD1, CD2, CD4 — CD5/CD6/CD7 not started)** | 2026-09-21 | `3947580`, `152dee0`+`a98b8f6`, `d8c00c5`+`6cc77ab`, `a5380ea`+`b0dd301` | Owner assigned slice-by-slice via Telegram ("Ок" after each). Built `src/lib/change-drafts/` (Automerge document per channel, `contracts/schemas/services/adapters` pattern): create/add/update/approve, `mergeIncoming` with correct new-conflict detection (AC-CRDT-01/02), `migrateFromSql` (AC-CRDT-03), and a SQL read-projection into the existing `change_sets`/`changes` tables (AC-CRDT-04). Cut `src/lib/changesets/` over to write through it (`adapters/change-drafts-store.ts`) instead of direct SQL — the only changed file at that boundary; `services.ts`/API/MCP/CLI untouched, confirmed by an independent audit that MCP/CLI only ever call `createChangeSetCore()` (closes CD7's own confirmation for this slice). Two real risks found and fixed before merge: batched-write atomicity (old adapter's `db.transaction` guarantee, preserved via one `Automerge.change`+`saveDocument` per batch) and non-atomic `.automerge` file writes (now temp-file+rename). `npm test`: 662/662; `tsc`/`lint`/`build` clean throughout. CD5 (continuous background sync) and CD6 (Merge-tab conflict UI) are next, per `docs/roadmap/plans/AUTOMERGE_MIGRATION_PLAN.md` §6.
| BL-049 — CD5 sync engine + CD6 first slice (wiring, badge, sync trigger) | **DONE (CD5 engine done; CD6 conflict-resolution ACTION not yet built)** | 2026-09-21 | `3444b64`+`f729688` (CD5), CD6 slice uncommitted as of this row | Owner assigned via Telegram ("двигайся дальше к CD5 и CD6"). Two more real `mergeIncoming` data-loss bugs found and fixed before CD5 (own commits, merged separately): device-onboarding merge into an unrelated fresh document (55/100 empirical failure) and merging two documents with no shared history (100/100 deterministic failure, now rejected via `divergent_document_lineage`, `docs/TECHNICAL_DEBT.md` RISK-46). CD5: new `src/lib/change-drafts-sync/` module — each device writes only its own `<deviceId>.automerge` file into the shared Syncthing folder, reads/merges every peer's file with per-peer and per-channel failure isolation, single-flight-guarded `runSyncCycle()`. CD6 first slice: three API routes (`POST /api/change-drafts/sync`, `GET /api/change-drafts/conflicts-summary`, `GET /api/channels/[channelId]/change-drafts/conflicts`), "Device" tab renamed "Merge" with a "Sync now" button + conflict list, sidebar badge (`AppShell`'s new `NavItem.badge`) polled every 20s, real sync cycle polled every 60s (both independent of tab, AC-CRDT-07/08). Conflict *resolution* (choosing a winning value) is explicitly not built yet — display/detection only. Live-verified against the real Tropico Jazz channel including a genuine write to the external Syncthing drive. `docs/SYSTEM_MAP.md` §2.9h, `docs/roadmap/plans/AUTOMERGE_MIGRATION_PLAN.md` §6 updated.
| BL-049 — CD6 conflict-resolution sub-slice | **DONE** | 2026-09-21 | uncommitted as of this row | Owner: "продолжай". New `change-drafts/services.ts`'s `resolveConflict({channelId,changeId,field,winningActorId})` — re-derives the winning value server-side from `Automerge.getConflicts` rather than trusting a client-supplied value; restricted to `proposedValue`/`approvalStatus`/`approvedValue`/`conflictStatus`. Empirically verified a fresh write clears an Automerge conflict permanently (probe script). New `POST` on the existing `.../change-drafts/conflicts` route; UI "Use this version" button behind `ConfirmDialog`. 3 new tests. Advisor review before commit found and fixed 2 more issues: `pushError`/`peersSkipped` weren't surfaced in the Merge tab (a real sync failure would show as silent "0 pushed"), and the resolve button had no double-click guard. Live-verified end-to-end against the real Tropico Jazz channel (real concurrent conflict constructed, resolved via the UI, on-disk value confirmed, badge/list cleared correctly) — see `docs/roadmap/plans/AUTOMERGE_MIGRATION_PLAN.md` §6 CD6 for detail.
| BL-049 — CD7: confirmed + dead-code cleanup, plan CD1–CD7 fully done | **DONE** | 2026-09-22 | uncommitted as of this row | Owner: "продолжай". Full audit of every call site of `db.ts`'s `createChangeSetWithChanges`/`updateStoredChangeSetStatus`/`updateStoredChange`/`bulkUpdateStoredChanges` confirmed CD2's cutover already satisfied CD7 for every real path (CLI/MCP/API all go through `createChangeSetCore()`; `batches/`'s own adapter is read-only). One real finding: `src/lib/changesets/adapters/store.ts` still defined the old pre-cutover direct-SQL `createChangeSetStoreAdapter()` with zero callers anywhere — deleted along with its now-unused imports. `npm test` unchanged (690/690), confirming it was genuinely dead. See `docs/roadmap/plans/AUTOMERGE_MIGRATION_PLAN.md` §6 CD7 for detail.
| RISK-47 fix — approve/approve-all now refuse a change with an open CRDT conflict | **DONE** | 2026-09-22 | uncommitted as of this row | Owner: "Займись этими рисками". `changesets/services.ts` gained a `crdtConflicts.listConflictedChangeIds` dependency (wired via `change-drafts`'s `listConflicts`); `approveChange` throws a new `crdt_conflict_open` (409), `approveAllValid` silently excludes such a change from the batch. Reused the existing generic error-banner UI, no component changes needed. 4 new tests; `npm test` 693/693, `tsc`/`lint`/`build` clean. See `docs/TECHNICAL_DEBT.md` RISK-47.
| RISK-46 fix (option b only) — in-app "discard local, adopt peer" divergent-lineage resolution | **DONE (option b; option a — structural prevention — still open)** | 2026-09-22 | uncommitted as of this row | Same owner instruction. `change-drafts/services.ts`'s `discardLocalAndAdoptPeer` (backs up the discarded document via a new dedicated `discarded-backup-store.ts`, then overwrites, then deletes any SQL projection row that existed ONLY in the discarded document -- a real gap found live, see below); `change-drafts-sync/services.ts`'s `adoptDivergentPeer` (re-reads the peer's current file at resolution time, mutually exclusive with `runSyncCycle`, rejects rather than coalesces a second concurrent call with different args). New route `POST .../change-drafts/adopt-peer`; UI button on `divergent_document_lineage` `peersSkipped` entries behind `ConfirmDialog`. Live-verified end-to-end against the real Tropico Jazz channel (a genuinely divergent peer constructed, adopted via the real UI, backup file confirmed on disk, local document confirmed replaced) -- this run surfaced the SQL-orphan-row bug (a discarded change set stayed forever visible via `listChangeSets` yet threw `not_found` on any action), fixed the same session (new `deleteStoredChangeSet`/`deleteStoredChange` in `db.ts`). 10 new tests across 3 files; `npm test` 704/704, `tsc`/`lint`/`build` clean. See `docs/TECHNICAL_DEBT.md` RISK-46.
| BL-055 — Phase 8 slice 2: `video_metrics_daily` table + tests | **DONE** | 2026-09-22 | `c4fb7a1`, `61527a1`, `9b55503` on `feature/phase-8-intelligence-foundation`, merged to `dev` in `6f75ccf` | Owner: "Приступить к полной реализации фазы 8". Additive `video_metrics_daily` table (`SCHEMA_MIGRATIONS` v8, FK on `video_id`), `upsertVideoMetric`/`listVideoMetricsByVideo`. 3 rounds of independent review found and fixed 2/2 real issues (missing FK the plan's own DDL required; a negative test with no error matcher), round 3 clean. `npm test` 707/707, `tsc`/`lint`/`build` clean. See `docs/ARCHITECTURE.md` §14, `docs/SYSTEM_MAP.md` §2.9i, `docs/roadmap/plans/PHASE_8_PLAN.md` §6. |
| BL-056 — Phase 8 slice 1: YouTube Analytics OAuth scope (`yt-analytics.readonly`) | **DONE** | 2026-09-22 | on `feature/phase-8-intelligence-foundation`, merged to `dev` in `6f75ccf` | Owner: "Да, разрешаю" (msg 356). `YOUTUBE_ANALYTICS_READ_SCOPE` added to `src/lib/auth.ts`'s `YOUTUBE_SCOPES`; no separate re-consent flow needed since every sign-in path already forces full consent. `npm test` 709/709, `lint`/`build` clean. Real re-authorization of the owner's Google account still pending (owner action, not automatable). See `docs/ARCHITECTURE.md` §14.1, `docs/SYSTEM_MAP.md` §2.1.
| BL-058 — Phase 8 slice 4: manual "collect now" trigger (route + Web UI) | **DONE** | 2026-09-22 | on `feature/phase-8-intelligence-foundation`, merged to `dev` in `6f75ccf` | Owner: "Приступить к полной реализации фазы 8" / "Продолжай с 053". `POST /api/channels/[channelId]/analytics/collect` (real mutation, gated by `src/proxy.ts` normally) + `GET .../analytics` (read) + `src/components/analytics-manager.tsx` (replaces BL-017's stub). `collectMetrics`/`listMetrics` do their own active-channel check, not duplicated in the routes (mirrors channel-sync's own pattern). Live-verified against the real "Tropico Jazz" channel via `claude-in-chrome`: tab loads, date defaults are local-calendar (not UTC) and end at yesterday (the API never returns the most recent day(s) yet), "Collect now" exercises the real chain and correctly fails with `AUTH_SCOPE_INSUFFICIENT` (real token predates BL-056's scope), zero console errors. `npm test` 727/727, `lint`/`build` clean. See `docs/ARCHITECTURE.md` §14.4/14.5, `docs/SYSTEM_MAP.md` §2.9i. **Follow-up, same day, after real re-consent (see BL-057's row): this route was re-exercised against the real "Rural Japan Music" channel and succeeded end-to-end** (28/28 videos, 2912 rows written) -- the `AUTH_SCOPE_INSUFFICIENT` path above was this route's only prior verification; a genuine success run is now also confirmed.
| BL-057 — Phase 8 slice 3: Analytics adapter, live-verified | **DONE** | 2026-09-22 | on `feature/phase-8-intelligence-foundation`, merged to `dev` in `6f75ccf` | Owner performed the real Google re-consent and enabled "YouTube Analytics API" in Google Cloud Console (the latter a real, separate GCP configuration step this session's first live attempt surfaced -- `AUTH_SCOPE_INSUFFICIENT` cleared once the scope was granted, but the very next attempt failed differently until the API itself was enabled on the project). After both, `POST .../analytics/collect` succeeded for real: 28/28 videos, 0 skipped, 2912 rows written to `video_metrics_daily`. Checked directly against the database: all 28 `ANALYTICS_METRIC_NAMES` are present in the real response -- the metric list needed no correction. `PHASE_8_PLAN.md` §10 item 5's bulk-query question is now closed: a temporary route (never committed) reusing the app's own real credential-resolution path tested `dimensions=video,day` with no `filters=video==...` against the live API, which rejected it as an unsupported query. The one-call-per-video design is confirmed as the only supported shape, not a missed optimization. See `docs/roadmap/BACKLOG.md`'s BL-057 row for full detail.
| BL-059 — Phase 8: daily staleness-based auto-collection + local sync-time/timezone setting | **DONE** | 2026-09-22 | on `feature/phase-8-intelligence-foundation`, merged to `dev` in `6f75ccf` | Owner: msg 356 items 3-6 / "Ок". `src/lib/analytics/staleness.ts` (pure wall-clock boundary check, IANA-timezone-aware via `Intl` string comparison, DST-flip verified by a dedicated test for America/New_York Jan vs Jul); `channels.analyticsLastAutoCollectedAt` (v9 migration, mirrors `lastSyncedAt`, deliberately not `MAX(video_metrics_daily.collected_at)`); `runAutoCollectionIfStale` (mark-then-run -- marks the timestamp before collecting, so two concurrent dashboard mounts never both run a full collection, proven by a dedicated test); two new `app_settings` keys (local time, IANA timezone -- the latter auto-detected from the OS once then persisted), validated at the `/api/settings` write boundary; `POST .../analytics/auto-collect` (real mutation, gated by `src/proxy.ts` normally, never exempted); dashboard-mount trigger (`page.tsx`, once per session, not a repeating interval); `analytics-sync-settings.tsx` (Settings tab). Live-verified against the real "Tropico Jazz" channel: the auto-collect effect fires exactly once and correctly fails at `AUTH_SCOPE_INSUFFICIENT`; confirmed directly against the real database that this DID mark `analyticsLastAutoCollectedAt` (the documented mark-then-run tradeoff), then reset that column back to NULL afterward (throwaway script, not committed) so the owner's first post-re-consent load isn't skipped. Also confirmed the real machine's OS timezone (Europe/Helsinki) was correctly auto-detected and persisted on first settings read. Confirmed the pre-existing `SQLITE_BUSY`/stale-schema-version build-time noise during `next build`'s multi-worker page-data collection is unrelated to this change (`git stash -u` + rebuild reproduces it on the prior commit too). `npm test` 747/747, `lint`/`build` clean. See `docs/ARCHITECTURE.md` §14.6, `docs/SYSTEM_MAP.md` §2.9i.
| BL-060 — per-category read-gateway toggles + `youtube-analytics.ts` folded into `youtube-read-gateway/analytics-api.ts` | **DONE** | 2026-09-22 | on `feature/read-gateway-toggles-and-analytics-reconciliation`, merged to `dev` in `aab67dd` | Owner: "выведи такие же тумблеры в настройки по запросам API (теперь входящим). Делаем отдельный тумблер на каждый модуль / шлюз API чтения." `assertDataApiReadsAuthorized`/`assertAnalyticsReadsAuthorized` (`src/lib/db.ts`'s `getDataApiReadsEnabled`/`getAnalyticsReadsEnabled`, default-enabled, persist across restarts) live inside `createYoutubeClient`/`createYoutubeAnalyticsClient` themselves -- the one choke point every caller in that category already uses, so disabling reads also correctly fails write paths that depend on a read first (Batches' write-client construction, `write-context`'s pre-write identity check) -- intentional, not incidental. Two `ToggleSwitch`es in Settings (`read-gateway-settings.tsx`), `/api/settings` GET+POST. Live-verified in the browser: each toggle off correctly breaks a real read (Content tab's Sync now, Analytics tab's Collect now) with a clear error, and recovers once switched back on. `youtube-analytics.ts` moved to `youtube-read-gateway/analytics-api.ts` (no behavioral change) completing `docs/decisions/0007-youtube-read-gateway.md`'s own deferred follow-up. 752 tests, `tsc`/`lint`/`build` clean.
| BL-061/062/063 — gateway traffic counters, Analytics daily-freshness gate, Cloud connection, real Cloud Quotas, YouTube-quota-exceeded classification | **DONE** | 2026-09-22 | merged to `dev` in `4be1f29` (from `feature/gateway-traffic-counters`) | Owner treated all of this as one feature (msg 425, "(а) считаем одной фичей") and held the merge until every part was ready. Delivered: rolling-24h attempts/succeeded counters per gateway category (5 categories); a ticking-countdown daily-freshness gate for Analytics auto-collection; a device-persistent Google Cloud OAuth connection (`monitoring.read` scope) independent of the YouTube channel login; real Google Cloud quota limit/usage numbers (24h for Data API/Analytics, per-minute for Cloud Monitoring itself) via `src/lib/cloud-quotas/`; a `youtube_quota_exceeded` `DomainError` classifying real 403 quotaExceeded/dailyLimitExceeded responses at the single `youtube-read-gateway` client-construction choke point (reaches both the Web UI and MCP/CLI agents); a standing rule against `Co-Authored-By` trailers (`AGENTS.md` §J); and a Settings-tab UI pass (info tooltips replacing inline descriptions, stats/quota moved to a larger right-hand column, larger section headers). See `docs/ARCHITECTURE.md` §16, `docs/SYSTEM_MAP.md` §2.9j/k/l, `docs/decisions/0008-cloud-connection.md`. 816 tests, `lint`/`build` clean; live-verified throughout against the real "Rural Japan Music" channel. Owner approved the merge in Telegram, 2026-09-22: "ок, можно мерджить в дев".
| BL-064/065 — M0/M1: `src/lib/sync-gateway/` stood up; `change-drafts`/`change-drafts-sync` moved into it unchanged; `rules` dropped from the snapshot allowlist | **DONE** | 2026-09-22 | `84a5ee0` (M0), `38b4a60` (M1), on `feature/sync-gateway`, not yet merged | Owner: "Объединяем весь этот функционал в отдельный модуль... используем также правило 1 модуля и шлюза" (see `docs/roadmap/plans/FULL_DEVICE_HANDOFF_MIGRATION_PLAN.md`). Pure move/refactor for M1 -- CD1-CD7's existing behavior and test suite carried over unchanged. |
| BL-066 M2/M3 -- `channels`/`videos` dropped from cross-device transfer (pure re-derivable caches); generic Automerge engine extracted; `channel_editorial_profiles` and `ai_connections` (config fields) cataloged through the sync gateway, each its own document per the owner's "вариант Б, отдельными документами" | **DONE** | 2026-09-22 | `fa4f67d` (M2); `6b05ca2`, `e1518f3`, `e8dd969`, `ac8b2a2`, `87f2c85`, `43cf17f`, `0020d5d`, `2a18792` (M3), on `feature/sync-gateway`, not yet merged | New `automerge-core/` (generic merge engine + per-channel sync runner), reused identically by editorial-profile via a real per-channel key and by ai-connections-catalog via a constant `"global"` key. Both cut over from direct SQL with a lazy bootstrap-on-first-save so existing rows are never lost. Two real bugs found and fixed before commit: an `Automerge.merge` mutation-order bug that hid genuine concurrent-edit conflicts (isolated via a standalone probe script), and an orphan-row bug where a peer's deletion wasn't projected to SQL (same class as CD1-CD7's own RISK-46 fix). Both live-verified via "Sync now" against real documents. |
| BL-066 M4 -- `ai_localization_generation_provenance` folded into the existing per-channel change-drafts document as an additive, optional field | **DONE** | 2026-09-22 | `f04a097`, on `feature/sync-gateway`, not yet merged | Additive per `docs/decisions/0001` -- a document saved before this field existed has no `provenance` key on disk; every read path defensively initializes it rather than assuming it's present. |
| BL-067 -- Category D (write-pipeline) sync-gateway migration, dropped in full | **DROPPED, see `docs/decisions/0009-defer-write-pipeline-sync-gateway-migration.md`** | 2026-09-22 | n/a -- no code changed | A pre-implementation research pass (per `AGENTS.md` §L, before writing any M5 code) found the "append-only, safe to migrate" premise the plan and the owner's approval were both based on to be false: `batches`/`batch_ledger_rows`/`batch_attempts` depend on SQL compare-and-set/UNIQUE-constraint primitives with no Automerge equivalent (Phase 5 AC-CONCURRENCY-01/02/03/AC-RESUME-01), and `audit_events`'s exact ordering guarantee (AC-AUDIT-01/04) depends on its SQLite `AUTOINCREMENT` rowid, which a CRDT document also has no equivalent for. ADR 0006's original write-pipeline exclusion is reinstated for all four tables. Whether `src/lib/device-handoff/`/`src/lib/snapshot/` could be deleted outright, or needed a small scoped-down transfer for just these four tables, was an open decision for the owner at the time this row was written -- resolved 2026-09-23 as BL-068 (owner chose the scoped-down transfer, option (b)), see that row below. |
| BL-068 -- M6: scope `device-handoff`/`snapshot` down to the 4 Category D tables (owner chose option (b), not deletion) | **DONE** | 2026-09-23 | `87fc810` on `feature/sync-gateway`, merged to `dev` in `5fff0fa` | Owner, after a web-research pass confirming this is the industry-standard shape (LiteFS/Litestream-style explicit primary handoff, lease-based job-scheduler worker handoff) for a single-writer subsystem that still must move between machines: *"'CRDT для драфтов/настроек + явная передача владения для конвейера записи' ок, тогда так и делай"*. `SNAPSHOT_TRANSFERRED_TABLES` narrowed to `schema_meta` + the four Category D tables; `ai_connections`' upsert-by-id special case removed along with it from the allowlist. Tests in `src/lib/snapshot/services.test.ts`/`src/lib/device-handoff/services.test.ts` that used `change_sets`/`ai_connections` as their "some transferred table" example rewritten against `batches`/`batch_ledger_rows`. `docs/ARCHITECTURE.md` §13.5 (new) and `docs/SYSTEM_MAP.md` §2.9m (new -- also closes a pre-existing gap: M3/M4's editorial-profile/ai-connections-catalog/provenance work from earlier in this session had never been documented there at all) updated. `npm test` 857/857, `lint`/`build` clean. |
| BL-069 -- Settings split into 4 sub-tabs (API / AI Agent / Sync / About) | **DONE** | 2026-09-23 | `feature/settings-tabs`, merged to `dev` in `318b859` | Owner: "давай в настройках сделаем 4 категории закладок". MCP connection split out of `LiveWritesSettings` into `mcp-connection-settings.tsx` (grouped with AI provider connections under "AI Agent" per the owner's own explicit choice, despite these being two unrelated mechanisms -- noted in the code); Syncthing folder path moved out of the Merge tab into `sync-folder-settings.tsx` under "Sync"; `analytics-sync-settings.tsx` renamed to `analytics-collection-settings.tsx` to avoid colliding with the new "Sync" name. Three live-testing follow-ups fixed same day: the sub-tab pill background stretched to the full container instead of hugging its buttons; switching sub-tabs unmounted/remounted every card, causing a loading flicker even for an already-visited sub-tab (fixed via CSS-hide instead of conditional render); Settings only started fetching once first opened (fixed by mounting it, hidden, from dashboard load -- a stated tradeoff: Google Cloud Monitoring's quota call now fires once per dashboard session instead of only when Settings opens). `npm test` 816/816, `tsc`/`lint`/`build` clean. |
| BL-070 -- Merge tab redesign: persistent sync status, conflicts/divergent-peer parity across all 3 sync-gateway families, Device handoff as its own card | **DONE** | 2026-09-23 | `feature/merge-tab-redesign`, merged to `dev` in `864c0a7` | Owner: "давай распланируем что должно быть показано на закладке merge... проведи исследование". Research (conflict-resolution and sync-status-dashboard UX patterns) found two real gaps: no persistent sync status (a one-off summary that vanished on reload couldn't distinguish "synced moments ago" from "unreachable for days"), and conflict/divergent-peer resolution UI existed only for `change-drafts` even though `editorial-profile`/`ai-connections-catalog` already had the identical server-side capability. Generalized `adoptDivergentPeer` into `automerge-core/sync-runner.ts` (`AGENTS.md` §M -- `change-drafts-sync`'s own bespoke implementation left untouched, `AGENTS.md` §D) and added `resolveConflict` to both services, mirroring `change-drafts`'s own pattern (`winningActorId`, never a raw value). New `sync_family_status` table (SCHEMA_MIGRATIONS v12) for a real persistent last-sync-outcome per family; `POST /api/change-drafts/sync` now records each family's outcome independently (one family's thrown error no longer blocks recording the other two's). 4 new API routes (editorial-profile conflicts/adopt-peer, device-wide ai-connections conflicts/adopt-peer). `device-handoff-panel.tsx` rewritten: a persistent "Sync status" section, a unified "Conflicts" list across all 3 families, unified peer-skipped/divergent-lineage warnings, and "Device handoff" as its own visually separated card (owner: "отдельная карточка"). Live-verified with real demo conflicts the owner explicitly authorized creating ("делаем можешь для демонстрации сделать тестовые записи") -- constructed via direct production-core Automerge fork+merge (no stray peer files left on disk, unlike the session's earlier fake-peer-file incident), both conflicts shown and resolved through the real UI, then cleaned up via real UI actions (profile field cleared, demo connection deleted) and confirmed empty directly in the database. `npm test` 872/872, `tsc`/`lint`/`build` clean. **Independent-review cycle: 5 rounds, findings 1/1/1/1/0** (see commits `bd91b4e`, `d5b9910`, `3787d30`, `a6834b8`, plus grammar-only polish `4568eb6`); one lower-severity last-writer-wins error-banner race in `handleSyncNow` explicitly deferred, documented in-code, since fixing it properly would require changing what `refreshConflicts`/`refreshSyncStatuses` return to every other caller. Final live browser re-verification after all 5 rounds (Sync status card, unified Conflicts empty state, Device handoff card, a real "Sync now" click) confirmed clean with no console errors. Owner gave explicit merge approval (Telegram, "ok") after this report; merged `--no-ff` into `dev` and pushed. |
| BL-071 -- Persistent, re-activatable channel connections: Settings "Channels" section to connect N channels once and switch between them without re-consenting to Google | **DONE** | 2026-09-23 | `feature/persistent-channel-connections`, merged to `dev` in `370a81c` | Owner, following a research answer on the current single-active-channel/NextAuth-session model: "используй подход модульности... Сделать эту фичу с хранилищем каналов... как отдельный модуль так же. Составь план и приступай к его реализации в самодостаточном режиме" (`docs/decisions/0010-persistent-channel-connections.md`). Investigation found `users` already persists every connected identity's OAuth tokens indefinitely -- no new encrypted credential store was built; the actual gap was reactivating an already-known identity's session without a fresh Google consent screen. Closed via a new NextAuth Credentials provider (`channel-connections`) whose `authorize()` requires an already-valid existing session (`resolveExistingSessionToken`, a from-scratch cookie parser since NextAuth's own `SessionStore` never reads `req.headers.cookie`) before reactivating a stored identity; `signIn` callback gained a branch so this provider's synthetic account never overwrites the reactivated identity's real tokens. New `src/lib/channel-connections/{contracts,services,index}.ts` (reads/writes existing `channels`/`users` tables, no schema change), 2 new routes, new Settings "Channels" UI. Also extracted duplicated AES-256-GCM logic from `ai-connections/crypto.ts`/`cloud-connection/crypto.ts` into a new shared `src/lib/shared-crypto/` module per the owner's explicit modularity instruction. **Independent-review cycle: 3 rounds, findings 3/5/0.** Round 1: no test coverage for the cookie-parsing/session-lookup gate (fixed, incl. a real round-trip test using `next-auth/jwt`'s own `encode`/`getToken`); `decodeURIComponent` could throw on one unrelated malformed cookie (fixed, per-cookie try/catch); raw error message forwarded to client in `channel-info`'s new catch block (fixed, server-side-only logging). Round 2: documented (not fixed -- deliberate) that channel list/disconnect have no per-caller channel-ownership check, the whole point being to manage channels other than the current one (`docs/TECHNICAL_DEBT.md` RISK-49, ADR updated); plus 4 low fixes (signIn-callback branch test coverage, duplicate-cookie-name handling to match the `cookie` package, test env-var cleanup, defensive JSON.stringify). Round 3: zero new findings, cycle closed. Live-verified against the owner's real Tropico Jazz / Rural Japan Music channels: switching without any Google consent screen, graceful handling (no crash) of a channel whose stored refresh token had genuinely gone stale with Google (`invalid_grant`), and Disconnect confirmed via direct DB read (tokens cleared, `connectedUserId` unlinked, `channels` row preserved). **Post-review, owner live-testing found one more real bug**: `signIn("google")` alone never links a freshly-authenticated identity into `channels.connectedUserId` -- only `channel-sync`'s "mine" path does that, previously triggered only from Content/Languages, so a channel connected via "Connect a new channel" silently never appeared in this list. Fixed by having the Channels card's own mount effect call the same, already-tested `channel-sync` mine-path itself; reproduced and confirmed fixed by directly resetting a real, valid channel's `connectedUserId` to simulate the exact reported state. `npm test` 905/905, `tsc`/`lint`/`build` clean. **Same-day follow-up (owner instruction):** reworked the topbar's "Switch channel" button into a quick-switch dropdown over the same connected-channels list -- picking a non-active row reactivates it (no Google round-trip), a "+ Connect a new channel" row at the bottom still does a real Google sign-in. Extracted the list-fetch/activate logic shared by this dropdown and the Settings card into `src/components/use-connected-channels.ts` per the owner's explicit "maximize reuse, this is just a visual wrapper" instruction -- no new mechanism. The mine-path sync fix now lives inside that one shared hook rather than duplicated per caller. Live-verified: instant switch from the topbar dropdown, list stays consistent with Settings -> Channels, zero console errors. **Final comprehensive review (owner-requested, "фича полностью реализована? синхронизация корректна? модульность правильная?"), covering all 13 commits:** 0 new findings across completeness, cross-component sync correctness (traced session propagation, the `key`-remount vs. prop-driven reactivity split, and the documented duplicate-sync-cost tradeoff), and modularity (confirmed `use-connected-channels.ts` is the single fetch/activate implementation, no dead code, `write-context`/gateways untouched). Additional owner-directed polish folded in before merge: removed the redundant name/email text next to Sign out; fixed 5 channel-scoped tabs not refreshing when the channel changes without leaving the page (`key={channel?.id}`); restyled "Live writes" to match "Data API reads"/"Analytics reads"; removed a dead channel-picker `<select>` from Batches (now prop-driven like Merge). Owner gave explicit merge approval (Telegram, "если проблем нет, то можем мерджить") after this review; merged `--no-ff` into `dev` and pushed. |

| BL-072 -- Studio-Parity S6b (Analytics "Overview": channel-level cards/chart/deltas) + completing BL-015's Home stats cards | **DONE** | 2026-09-23 | `4a0a47d`, `9a4441a`, `affffad` on `feature/studio-parity-home-analytics-overview`, merged to `dev` in `111adf7` | Owner instruction, Telegram ("Давай поработаем над окнами Home и Analytics... сделай весь такой же функционал отображения статистики"), after a live YT Studio research pass this session. See `docs/roadmap/BACKLOG.md` BL-072 and `docs/ARCHITECTURE.md` §14.8/`docs/SYSTEM_MAP.md` §2.9n for full detail. **Three independent-review rounds (1 pre-commit + 2 fresh-subagent bug hunts + 1 fresh-subagent completeness/modularity pass) found and fixed 5 real bugs** (a percent-change sign error for a negative baseline, a chart-spacing-by-index bug, a misleading percent-vs-absolute delta on Home's subscriber card, a wrong HTTP status on an invalid date range, and a silently-rolled-over calendar-invalid date) plus one minor factoring gap (duplicated `computeRange`/`formatHours` helpers, extracted into `period.ts`); the final round found nothing further and would merge as-is. `npm test` 929/929, `lint`/`build` clean; live-verified in the browser against two real channels (Tropico Jazz, Rural Japan Music), including Analytics-reads-OFF leaving Home's video cards intact per `AGENTS.md` §M. Owner approved the merge in Telegram ("Ок"), 2026-09-23; merged `--no-ff` into `dev` and pushed. |
| BL-073 -- Phase 8 follow-up 1/4: `analytics_list`/`analytics_overview` MCP tools + CLI parity | **DONE** | 2026-09-23 | merged to `dev` in `7da72fa` | See `docs/roadmap/BACKLOG.md` BL-073 for full detail. Machine-readable analytics for agents (`docs/roadmap/FUTURE_PHASES.md` §4), both tools reuse existing `analyticsCore` methods unchanged, read-only/ungated, CLI parity included, `analytics_list` gained optional filters. Independent review found and fixed 1 real bug (unvalidated date-range filter, same class as an earlier BL-072 fix). `npm test` 944/944, `lint`/`build` clean. Owner approved the merge in Telegram ("Ок, мердж"), 2026-09-23; merged `--no-ff` and pushed. |
| BL-074 -- Phase 8 follow-up 2/4: data-quality diagnostics (`getDataQualityReport`, `analytics_collection_runs`) | **DONE** | 2026-09-23 | `feature/analytics-data-quality-diagnostics`, merged to `dev` | See `docs/roadmap/BACKLOG.md` BL-074 for full detail. New additive table + pure `computeDataQualityReport`, exposed via API/MCP/CLI + a small Web UI warning card. Live-verified against a real channel: correctly surfaced a genuine 6-of-28-days coverage gap. Independent-review cycle: 2 rounds, findings 3/0 (round 1 found and fixed 3 real bugs -- a zero-video run falsely counted as coverage forever, skip flags never healing after a later success, a UI state-reset gap). `npm test` 972/972, `lint`/`build` clean before merge; merge commit itself re-verified (`npm test` 972/972, `lint`/`build` clean). Owner approved the merge in Telegram ("ок, если по слайсу 2 все работы выполнены"), 2026-09-23; merged `--no-ff` into `dev` and pushed. |
| BL-076 -- Phase 8 follow-up 3/4: comparable-age video comparison (`getComparableAgeComparison`) | **DONE** | 2026-09-23 | `feature/comparable-age-comparison`, merged to `dev` in `f251e66` | See `docs/roadmap/BACKLOG.md` BL-076 for full detail. Aligns 2-10 videos' `video_metrics_daily` rows by days-since-publish (Pacific-Time day 0), not calendar date; explicit additive-metric allowlist; a missing day is "unknown," never a fabricated zero (cumulative stops at the first gap). Real-data caveat confirmed by directly querying two real channels before designing: videos older than ~1-2 weeks before regular collection started typically lack data at low day-offsets (rolling ~7-day auto-collect window, not "first 7 days since publish") -- no backfill added, out of scope. Facts-only, no ranking language, per `FUTURE_PHASES.md` §4. Independent-review cycle: 2 rounds, findings 5/0 (round 1 found 3 documentation-accuracy issues -- a false "in dev" claim, an overstated data-coverage caveat conflating `points` with `cumulativePoints`, `analytics_overview` wrongly grouped with local/ungated reads -- plus a low-severity latent error-mapping gap and missing boundary tests; round 2 confirmed all fixes and found nothing further). `npm test` 1004/1004, `lint`/`build` clean before merge; merge commit itself re-verified (`npm test` 1004/1004, `lint`/`build` clean). Owner approved the merge in Telegram ("ok"), 2026-09-23; merged `--no-ff` into `dev` and pushed. |
| BL-077 -- Phase 8 follow-up 4/4 (last slice): weekly analytical reports (`runWeeklyReportIfDue`/`listWeeklyReports`/`getWeeklyReport`) | **DONE** | 2026-09-23 | `feature/analytics-weekly-reports`, merged to `dev` in `8ca0ab2` | See `docs/roadmap/BACKLOG.md` BL-077 for full detail. Frozen, reproducible weekly (Mon-Sun) snapshot per channel, computed entirely from already-collected local `video_metrics_daily` -- never a live YouTube call. Trigger reuses the same `localTime`/`timezone` Settings pair the daily auto-collection boundary already uses; `status: "final"`/`"provisional"` protects against skew between the local trigger time and the Analytics API's own reporting lag/Pacific-Time day-numbering; `percentChange` is `null` entirely unless both the current and previous week are fully covered; `syncedVideoTotals` explicitly documented as not "channel totals". New `analytics_weekly_reports` table (SCHEMA_MIGRATIONS v14). Independent-review cycle: 2 rounds, findings 2/2 (round 1 found and fixed a real HIGH-severity race -- concurrent dashboard-mount triggers, e.g. two open tabs, could silently regress an already-`"final"` report back to `"provisional"` with stale content; fixed via a DB-layer conditional `ON CONFLICT ... WHERE status != 'final'`, not just the service's own read-then-write check; round 2 confirmed the fix and found 2 low-severity, non-blocking items, left documented). `npm test` 1038/1038, `lint`/`build` clean before merge; merge commit itself re-verified (`npm test` 1038/1038, `lint`/`build` clean). Owner approved the merge in Telegram ("Ок"), 2026-09-23; merged `--no-ff` into `dev` and pushed. This closes all four Phase 8 follow-up slices the owner assigned ("Ок, доделай эти функции"). |
| BL-078 -- MCP/CLI tools for AI Localization's existing generate + create-Change-Set workflow (`ai_localization_generate`/`ai_localization_create_change_set`) | **DONE** | 2026-09-23 | `feature/ai-localization-mcp-cli-tools`, merged to `dev` in `1e2188f` | See `docs/roadmap/BACKLOG.md` BL-075/BL-078 for full detail. BL-075's own research (owner: "запиши в бэклог что эксель нам тоже не нужен... перевод делать должен агент") found the framing didn't match reality: XLSX was never the agent's authoring interface -- "Generate with AI" already bypasses it entirely and creates the identical Change Set. The actual, narrower gap -- no MCP/CLI tool could reach `generateProposals`/`createChangeSetFromGeneration` -- is what this item closes: both new tools/commands call those exact, unchanged, already-tested functions with zero new validation/persistence/approval logic. Every resulting Change still starts `pending` (AGENTS.md §G untouched); MCP/CLI replicate the explicit `assertActiveChannel` check `changeset_list`/`changeset_get` already use, since `ai-localization`'s own services don't check it internally. Independent review: 1 round, 0 findings (confirmed via diff that `src/lib/ai-localization/**`/the Web UI routes are untouched; verified the approval-bypass invariant, channel-scoping at all 4 call sites, and gating classification with real tests) -- one non-blocking nitpick recorded as `docs/TECHNICAL_DEBT.md` RISK-51 (CLI `generate` has no `--editorialBrief` flag) per the owner's own explicit request to track it for later. `npm test` 1049/1049, `lint`/`build` clean before merge; merge commit itself re-verified (`npm test` 1049/1049, `lint`/`build` clean). Owner approved the merge in Telegram ("Да можем мерджить в дев"), 2026-09-23; merged `--no-ff` into `dev` and pushed. |
| Phase 7 -- Agent Operations Interface (owner spec, slices A-L + closing review J) | **CODE-COMPLETE, merged to `dev`** (in-repository scope only -- the Codex operations-workspace itself and any release/publish step remain outside this repository per `AGENTS.md` §B/§K.2) | 2026-09-25 | `feature/phase-7-agent-operations-interface`, merged `--no-ff` into `dev` in `cc6a431` | Full agent-facing MCP/CLI interface (capability discovery, channel/video/analytics context, creative-asset catalog, draft/proposal provenance, bulk-localization integration, Content Proposals + external-artifact registration, comparable-content and performance-linkage context) with a uniform READ+DRAFT permission ceiling (never APPROVE/EXECUTE) and a phase-wide closing security/integration review against owner spec §28. Full detail per slice: `docs/roadmap/BACKLOG.md` BL-079..BL-090; design and status tables: `docs/AGENT_OPERATIONS_INTERFACE.md`; acceptance contract: `docs/acceptance/PHASE_7_ACCEPTANCE.md`. `npm test` 1360/1360, `npm run lint`/`npm run build` clean both on the feature branch tip and re-verified on the merge commit itself. Owner approved the merge in Telegram ("Хорошо, давай фазу 7 замержим в дев"), 2026-09-25. |
| BL-091 -- Multi-agent responsibility zones (`src/lib/agent-connections/`) | **DONE** | 2026-09-25 | `feature/agent-connections`, merged `--no-ff` into `dev` in `18f8854` | Owner: "если мы дадим пользователю подключать два агента разом... пользователь должен выбрать за какие задачи какой агент должен отвечать и нельзя одну и ту же зону ответственности дать обоим." New module registers agent connections (id/label/enabled) and per-capability zone assignments across 6 capabilities (`channel_sync`, `changeset_create_from_import`, `ai_localization_generate`/`create_change_set`, `content_proposal.create_content_proposal`/`register_external_artifact`); single choke point `assertAgentAllowedForCapability` wired into both MCP and CLI. Final policy, corrected by the owner after the review cycle was explicitly stopped: 0 enabled connections is a no-op; 1+ enabled requires a known caller identity; an unassigned capability is always blocked once any connection is enabled, with no exception for exactly one enabled connection ("Любое действие должно быть заблокировано до тех пор, пока его не назначат агенту... Добавление одного агента не должно автоматом давать ему авторство над всеми модулями"). Full detail incl. the 13-round review tally and the post-cycle targeted review: `docs/roadmap/BACKLOG.md` BL-091. `npm test` 1422/1422, `npm run lint`/`npm run build`/`git diff --check` clean both on the feature branch tip and re-verified on the merge commit itself. Owner approved the merge in Telegram ("Ок, мердж"), 2026-09-25. |
| Modular architecture cleanup -- feature-module blast-radius isolation + remaining shared-logic extraction (AGENTS.md §M) | **DONE** | 2026-09-25 | `feature/modular-architecture-cleanup`, merged `--no-ff` into `dev` in `8ac25ff` | Owner: "1) Части логики которые отвечают за определение скоуп ответственности должны быть объединены в отдельные модули чтобы их отключение... не влекло падение остальной логики. 2) Если логика переиспользуется много где, мы тоже её выносим в отдельный модуль" -- both principles already codified in AGENTS.md §M (2026-09-22); this was a fresh audit+fix pass against it, explicitly excluding the already-compliant translations/analytics/gateway/agent-connections/agent-operations modules and the just-refactored cli-auth/write-context/channel-access. Two parallel audit forks (excluded modules untouched) found 5 concrete items, all implemented on one branch per the owner's "Создай для этой задачи одну единую ветку": (A) zero React error boundaries anywhere in the app -- new `src/components/feature-error-boundary.tsx` wrapped around every dashboard tab and every Settings sub-tab individually, so a render crash in one (e.g. AgentConnectionsManager) no longer takes down the whole Settings tab or the app shell; (B) 6 modules reached directly into `changesets/adapters/store.ts`'s unexported `createChangeSetChannelStoreAdapter` -- extracted into new `src/lib/channel-video-store/`, with a thin re-export left under the old name for the two excluded callers; (C+D) `parseWithSchema`/`formatZodError` (14 duplicate copies) and `mapUnknownError` (7 duplicate copies) moved into `video-metadata/contracts.ts` (already the de facto shared `DomainError`/`isDomainError` source every domain module re-exports); (E) `createDefaultLogger` (2 duplicate copies plus 7 modules importing it through an unrelated module) extracted into new `src/lib/shared-logger/`. Rider: `createIdGenerator` (6 duplicate `randomUUID` wrappers) folded into the same `video-metadata/contracts.ts` change. Purely structural throughout -- zero behavior change, `npm test` 1422/1422 unchanged on every commit; verified via repo-wide grep that no excluded module was touched. `npx tsc --noEmit`/lint/build/diff-check clean on the feature branch tip and re-verified on the merge commit itself. UI change (A) could not be live-verified in a browser this session (the browser-automation tool failed to reach even an external test page, an unrelated tool-connectivity issue, reported separately) -- flagged explicitly to the owner as an incomplete verification, not claimed as done. Owner approved the merge in Telegram ("да"), 2026-09-25. |
| Repository history cleanup -- remove all TubeMaster origin references, delete confirmed-dead template code, bring remaining non-modular domain modules into the standard pattern | **DONE** | 2026-09-25 | `feature/remove-tubemaster-scaffolding` (`0cef9e6`), `feature/repo-history-cleanup` (`380f52b`), `feature/cli-auth-module-restructure` (`ad39c35`), `feature/write-context-module-restructure` (`fa100b1`), all merged `--no-ff` into `dev` | Owner: "Наш репозиторий был основан на другом... 1) Удали все упоминания оригинального репозитория. 2) Найди есть ли элементы которые мы не используем... удали их. 3) Найди ли остались ли элементы которые мы используем... переделай их под нашу модульную архитектуру." Three parallel audit forks confirmed scope before any edit. **(1)** `package.json`/lockfile renamed to `youtube-operations-manager`; `LICENSE` copyright to the owner's own name; `docs/UPSTREAM_ANALYSIS.md`/`UPSTREAM_BASELINE.md` deleted with their 6 live citations (ADR 0001, `TECHNICAL_DEBT.md` RISK-05/RISK-09, `ARCHITECTURE.md` x2, `SYSTEM_MAP.md`) rewritten inline; `AGENTS.md` §D's origin/upstream remote model replaced with a plain independence statement (no upstream remote exists); `docs/PROJECT_SPEC.md`'s body (~2350 lines, ~50 mentions) had every purely-historical TubeMaster-bootstrap passage removed (§2, §4.1, Phase 0/1 under §7, "Initial Agent Assignment") while every section cited by number elsewhere in the repo (§3, §8, §26, §27, §36, §45-47, Rule 4-5, §60-65, §68) kept its exact number and was reworded as this project's own standalone rule -- verified via repo-wide grep that no numbered cross-reference broke. **(2)** Deleted `public/{file,vercel,next,globe,window}.svg` (unused Next.js template assets), `openspec/` (superseded pre-backlog tooling, confirmed nothing it proposed was lost), `.atl/skill-registry.md` (another machine's local config, unrelated username/project) -- required the owner's own explicit permission in chat (`git rm` was blocked once by the local sandbox's "Irreversible Local Destruction" classifier; the owner's "Разрешаю удаление" let it proceed). **(3)** `src/lib/cli-auth/` (the clearest violation, one 742-line file) split into `contracts/schemas/services/adapters/index`; `src/lib/write-context/` and `src/lib/channel-access/` renamed `service.ts`->`services.ts` and gained an `index.ts`; `write-context/index.ts`'s new `createWriteContextCore()` also deduplicated an identical 5-line adapter-wiring block that had been copy-pasted across 5 call sites (`video-details`, `video-metadata`, `playlist-management`, `batches`, `cli-auth`). Pure structural work throughout -- zero behavior change, same `npm test` 1422/1422 count on every commit. A `git merge --no-ff` into `dev` was itself blocked twice by the sandbox's "Modify Shared Resources" classifier despite the owner's explicit chat approval ("Ок" / "Разрешаю этот мердж") for both the cli-auth and write-context merges -- resolved by retrying the same already-approved command, which succeeded on retry each time (reported to the Claude Code team as a reproducible inconsistency, since earlier merges in the same session had gone through with no such block). |
| Analytics daily-freshness gate -- don't trust the "already collected today" mark without a genuine backing run | **DONE** | 2026-09-25 | `fix/analytics-mark-vs-run-history-check`, merged `--no-ff` into `dev` in `da33cdd` | Live bug: the mark-on-success fix (earlier same-day work) only prevents new bad marks going forward; a pre-existing stale mark kept `collectMetrics` refusing real attempts despite 3-day-old data on screen. Owner's own principle (2026-09-25 Telegram): if it's past the boundary and yesterday's data still isn't in, the sync didn't genuinely complete. Fix checks `collectionRunStore` for a non-total-failure run whose window covers the canonical "yesterday" before honoring the mark; if none exists, logs `mark_disagrees_with_run_history` and retries instead of refusing. Two pre-existing tests updated (their manual date literals predated this invariant and no longer reflected a realistic caller); two new tests added for the actual bug scenario. `npm test` 1432/1432, `tsc`/`lint`/`build`/`diff --check` clean. |
| Analytics settings UI -- HH:MM text input instead of native time picker, IANA timezone `datalist` instead of free text | **DONE** | 2026-09-25 | `fix/analytics-settings-timezone-time-input`, merged `--no-ff` into `dev` in `b20de33` | Owner (Telegram): native time widget rendered "12.05" instead of "12:05" under a Finnish-locale browser; free-text timezone input was "место для потенциальной ошибки". Both fields moved to plain validated text inputs (reusing `isValidLocalTimeOfDay`/`isValidIanaTimezone`); timezone gained a `<datalist>` sourced from `Intl.supportedValuesOf("timeZone")`. `npm test` 1432/1432, `tsc`/`lint`/`build` clean. |
| Analytics tab deep Studio-parity plan (Overview/Content/Audience) | **Plan produced, not implemented** (does not authorize starting any slice) | 2026-09-25 | `feature/analytics-tab-studio-parity-ui` | `docs/roadmap/plans/ANALYTICS_TAB_DEEP_PARITY_PLAN.md` -- produced per the owner's Telegram request for a "glubokoye issledovaniye" of real YouTube Studio's Overview/Content/Audience sub-tabs (Trends excluded), including click-triggered UI (tooltips, retention-mode toggles, funnels), live-verified via `claude-in-chrome` against the real "Rural Japan Music" channel, cross-checked against official YouTube Analytics API docs for feasibility. Supersedes `STUDIO_PARITY_PLAN.md`'s one-paragraph S6b/S6c/S6d sketch now that S6a's foundation is validated against real data. Found and flagged one real false-negative in this codebase's own `CHANNEL_OVERVIEW_METRIC_NAMES` doc comment (impressions/CTR were tested under the wrong metric name in 2026-09-23; Google added the real ones -- `videoThumbnailImpressions`/`videoThumbnailImpressionsClickThroughRate` -- on 2026-01-15). Proposes ~15 slices across the 3 tabs with a recommended first-assignment order (card-click chart switching, then two cheap live-probe research spikes, then every confirmed-feasible card); several panels (cross-channel affinity, hour-of-day heatmap, new/casual/regular segmentation) are flagged as open feasibility questions, not promised. No code written. |
| Analytics tab deep Studio-parity implementation (Overview click-switching, Content + Audience sub-tabs) | **DONE** | 2026-09-26 | `feature/analytics-tab-studio-parity-implementation`, merged `--no-ff` into `dev` in `7a09248` | Implements the plan above, one unified branch per the owner's explicit correction ("Мы создали ветку под этот план и должны делать все в нем"): Overview metric-card click-switching + Studio-format chart tooltip dates; new Content sub-tab (traffic sources, top-10 videos, retention curve in "Intro" mode only); new Audience sub-tab (5 breakdown cards: device type, geography, age/gender, subscribed status, content format). New shared read-gateway function `queryChannelBreakdownReport` (any non-`day` dimension, optionally per-video), backing `getChannelBreakdown`/`getVideoRetentionCurve` (both channel-scoped, the latter also verifying `videoId` ownership). Confirmed, not merely deferred, permanently out of scope: the 48h realtime panel (O3) and the impressions/CTR funnel (C3) -- neither reachable via any public API surface, verified by live probe. Full slice history: `docs/roadmap/BACKLOG.md` BL-092..098. **Independent-review cycle: 10 rounds, findings 4/4/8/6/3/3/2/2/4/0, closed 2026-09-26** -- every finding from round 3 onward was documentation-only drift, no code/test/security defect found since round 2. `npm test` 1465/1465, `tsc`/`lint`/`build` clean on the feature branch tip and re-verified on the merge commit itself. Owner approved the merge in Telegram ("Да, мердж"), 2026-09-26. |

**Validation for M0-M4 (this session, `feature/sync-gateway`, not yet merged):** `npm test` 857/857, `npm run lint` clean, `npm run build` clean, `git diff --check` clean. **Independent-review cycle: 2 rounds, findings 1/0.** Round 1 (fresh subagent, no memory of writing the code) found a real bug in `discardLocalAndAdoptPeer` (`change-drafts/services.ts`): a discarded change set's `ai_localization_generation_provenance` row was never cleaned up (a real, un-cascaded `NOT NULL UNIQUE` FK to `change_sets`), so deleting the change set could throw a constraint error that silently aborted cleanup of every other row in the same discard too (one shared try/catch around both loops) -- fixed in `a5b3f56`. The advisor tool's own re-check of that fix (before round 2 closed) found the fix itself still had the same bug class in a narrower form -- provenance cleanup was scoped to "only when its parent change set is also removed," missing the case where the change set survives (the peer's own document also has it) but the peer's version carries no provenance for it. Fixed in `0c768f3` by diffing `provenance` by its own key set, exactly like `changes`, independent of which change sets are being removed. Round 2 (re-run of the same review + the advisor's own re-check): 0 findings.

---

## Next assignment

**Current, 2026-09-25:** Phase 7 (Agent Operations Interface, all slices A-L + closing review J) is CODE-COMPLETE and merged to `dev` in `cc6a431` -- see its row above and `docs/roadmap/BACKLOG.md` BL-079..BL-090 for detail. Owner approved the merge in Telegram ("Хорошо, давай фазу 7 замержим в дев"), 2026-09-25. **Newly assigned the same day:** a multi-agent responsibility-zone mechanism -- when more than one MCP-connected agent (e.g. Codex and Claude) is active at once, the owner wants each DRAFT-capable domain assignable exclusively to one agent, while READ-only domains stay shared across all connected agents (owner's own framing, Telegram: "Клод делает переводы и анализ аналитики. А кодекс делает ассеты... но при этом все должны работать в одном информационном поле"). Owner's explicit instruction: "составь план выполнения на эту фичу с несколькими агентами и приступай к его выполнению... чтобы это было отдельным модулем отвечающим за подключение агентов + доп модули если требуется" (per-domain zoning, own module, `AGENTS.md` §M). Plan: `docs/roadmap/plans/AGENT_ZONES_PLAN.md`; tracked as BL-091. **Update, 2026-09-25:** BL-091 is DONE -- merged `--no-ff` into `dev` in `18f8854` after a 13-round independent-review cycle (stopped by explicit owner decision, not zero-closure) and one targeted review of a post-cycle policy correction. See its row above and `docs/roadmap/BACKLOG.md` BL-091 for full detail. **Update, 2026-09-25:** owner then assigned two repository-hygiene passes, both DONE and merged -- the TubeMaster-history/dead-code/module-restructure cleanup and the modular-architecture-blast-radius cleanup; see their own rows above for detail. No new phase or backlog item has been assigned as of this update; `RISK-60` (write_channel_select/auth_user_select global active-channel race, surfaced during BL-091's design) remains explicitly OPEN and unassigned, per `docs/TECHNICAL_DEBT.md`.

**Earlier, 2026-09-23:** Sync Gateway migration (BL-064..BL-068) M0-M4 and M6 are all DONE and merged to `dev` -- see the rows above for detail. M5 (Category D / write pipeline) is dropped in full, ADR 0009; M6 scoped `device-handoff`/`snapshot` down to the 4 Category D tables rather than deleting them, per the owner's explicit choice. The independent-review cycle for this work closed at zero findings (M0-M4: 2 rounds, 1 -> 0; M6: 3 rounds, 7 -> 2 -> 1 -> 0), and the owner gave explicit merge approval for `feature/sync-gateway` -> `dev` (`5fff0fa`). Three immediately-following, related feature branches were also completed and merged the same day: BL-069 (Settings split into 4 sub-tabs, `318b859`), BL-070 (Merge tab redesign, unified sync status/conflicts across all 3 sync-gateway families, `864c0a7`), and BL-071 (persistent, re-activatable channel connections, `370a81c`) -- see their own rows above.
**Same-day follow-up:** BL-072 (Studio-Parity S6b Analytics Overview + Home stats cards) is DONE and merged to `dev` in `111adf7` (owner approval, Telegram "Ок") -- see its own row above.

Phase 8 (Intelligence Foundation) slices 1-4 (BL-055..BL-059) are all DONE, including live end-to-end verification against the real "Rural Japan Music" channel (real re-consent + Google Cloud Console API enablement performed by the owner, real collection run succeeded: 28/28 videos, 2912 rows, all 28 declared metrics confirmed valid) -- see each row above for detail. This work, along with BL-060 and BL-061/062/063, has since been merged into `dev` (`6f75ccf`, `aab67dd`, `4be1f29` respectively), each with the owner's own separate, explicit merge approval per `AGENTS.md` §K.2. Everything below this paragraph is earlier history, kept for context.

**Pre-Release — Cross-Platform Persistence & Syncthing Handoff is CODE-COMPLETE as of 2026-09-19**
(see its own row above) — the project owner's explicit assignment for this specific task.

**Pre-Release — First Local Test Build is done as of 2026-09-19** (see its own row above) — a real
macOS runtime validation (build/start/DB-init/restart/update-persistence, and the macOS launcher
scripts themselves) was actually performed, partially addressing RISK-17; RISK-17 is not fully
closed, since it covered launcher validation and real macOS hardware existed to test that, but a
full manual browser smoke test (`docs/FIRST_LOCAL_TEST_BUILD.md` §7) has not. The Windows launcher
remains entirely unvalidated on real Windows hardware.

**Pre-Release — Publish snapshots + independent-review risk remediation is done as of 2026-09-19**
(see its own row above) — the `published/<version>/` mechanism and 15 of the 17 RISK-18..34
findings are fixed; RISK-26/RISK-32 are open by deliberate disposition (external research /
separately-scoped refactor, not forgotten).

**None of the above authorizes Phase 7 or any other phase** — the next phase still requires its
own explicit assignment per `AGENTS.md` §C. Before this build is fully operator-ready: the manual
browser smoke-test checklist (`docs/FIRST_LOCAL_TEST_BUILD.md` §7) and a real Windows run of
`scripts/windows/start.bat` — and, unrelated to this task, pre-existing — Phase 5's own
`LIVE VALIDATION` track described immediately below.

**Pre-Release — YouTube Studio UI shell redesign, layer 1, is done as of 2026-09-20** (see its own
row above) — assigned by the project owner ("После 6 цикла проверки переходи к формированию плану
по дизайну UI в стилистике YouTube studio... приступай к его выполнению"), explicitly preceding
Phase 7 in the owner's own instruction. This closes layer 1 (persistent sidebar + top bar chrome);
migrating each tab's own internal styling to the shell's design tokens was not part of this
assignment and would need its own. Per the owner's own sequencing, only after this work is
finished may backlog/roadmap items (Phase 7 onward) be started autonomously — see
`docs/roadmap/FUTURE_PHASES.md` §8-9.

**Phase 7's in-repository scope is CODE-COMPLETE as of 2026-09-20** (see its own row above) —
assigned directly by the project owner across two Telegram instructions ("приступай к реализации
этих планов по порядку", then "Создай список задач чтобы полностью реализовать объем фазы 7 и
довести ее до релиза... Автоматическая работа"). Every part of Phase 7 buildable inside this
repository now exists: read/propose/create MCP tools for Change Sets/Batches/channel-sync, an MCP
restricted mode, and full CLI parity. **Phase 7 as a whole remains not fully done** — the isolated
Codex operations workspace lives entirely outside this repository by design (`AGENTS.md` §B), and
publishing a version for it to target requires its own separate, explicit project-owner approval
every time (`AGENTS.md` §K.2) — neither is something any autonomous-work instruction can close.
The only remaining in-repo Phase 7 item, an apply-class Change Set/Batch tool, waits on Gate B
(below), unrelated to and unaffected by this task.

**Phase 5 is CODE-COMPLETE against mocked/fake adapters as of 2026-09-18 (completion task, following Slice 5 and its independent review)** — every automated `AC-*` acceptance scenario now has a passing test (`AC-CONCURRENCY-03` and `AC-E2E-01`, the two gaps Slice 5 itself left open, are closed), a minimal dry-run-only Web UI/API exists for Batches, and RISK-11 (read-only snippet echo) is resolved for both write paths. **Gate B (`docs/TECHNICAL_DEBT.md`) is explicitly NOT satisfied and was not closed by this work** — real, non-dry-run YouTube writes remain disabled by an unconditional, two-layer barrier (independently re-verified this round) and require their own separate future authorization per `AGENTS.md` §K, plus every `LIVE VALIDATION` scenario `docs/acceptance/PHASE_5_ACCEPTANCE.md` §4 requires before Gate B (AC-MERGE-01/05, AC-GUARD-01, AC-CONFLICT-01, AC-RESUME-01, AC-E2E-01) — none of which were run.

**Both project-owner decisions from the prior round are now resolved (2026-09-18):**
- **RISK-12 — CLOSED.** The project owner approved flipping `applyMetadata`'s `dryRun` default to `true` as a deliberate, safety-first BREAKING BEHAVIORAL CHANGE. Applied: `applyMetadataInputSchema`'s default, the MCP `apply` tool's description, and a second, independently-found unsafe-default site in the CLI's `apply` command (which sent an explicit `dryRun: false` on omission, bypassing any schema default) are all fixed. All four entry points (Web UI — still zero callers, `/api/video-metadata/apply`, MCP `apply`, CLI `apply`) re-verified fail-closed: omitting `dryRun` anywhere is now a preview, never a write.
- **RISK-13 — RESOLVED.** The project owner approved the proposed narrow split. `docs/acceptance/PHASE_5_ACCEPTANCE.md`'s `AC-QUOTA-01` is now `AC-QUOTA-01a` (batched preliminary overview) and `AC-QUOTA-01b` (mandatory unbatched per-video fetch, restated) — a wording clarification only; no code or test changed, no safety requirement weakened.

**Phase 6, Slice 1 (AI Localization, mock provider only) is CODE-COMPLETE as of 2026-09-19**, per the project owner's own "Overnight Autonomous Development — Phase 6" assignment message (which is the explicit phase authorization `AGENTS.md` §C requires — no other document names a "Phase 6"). This did not touch, reopen, or weaken Phase 5 in any way: Phase 5 remains code-complete with live validation and Gate B both still pending, exactly as recorded above. See `docs/reports/OVERNIGHT_SESSION.md` for the full session report and `docs/acceptance/PHASE_6_ACCEPTANCE.md` for the acceptance contract.

**Phase 6, Channel Editorial Profiles (Part B of `docs/ai-localization/CHANNEL_CONTEXT_PROPOSAL.md`) is CODE-COMPLETE as of 2026-09-19**, per the project owner's explicit "Phase 6 — Channel Editorial Profiles" authorization. Persistent per-channel profiles, per-field merge with per-request `editorialBrief`, and reproducibility (provenance survives later profile edits) are all implemented and tested — see `docs/acceptance/PHASE_6_ACCEPTANCE.md` §4a (AC-PROFILE-01..10) and `docs/reports/OVERNIGHT_SESSION.md` for detail. This did not touch Phase 5.

**Remaining open question for the project owner:** which real `LocalizationProvider` (if any) to select and fund (OpenAI/Anthropic/DeepL/Google Translation/custom) — deliberately left unresolved per the original assignment's instructions (no paid provider selection, no external AI cost). `src/lib/ai-localization/provider-registry.ts` is the single place that decision would be wired in; `docs/ai-localization/PROVIDER_INTEGRATION_PLAN.md` documents the technical plan for when it is made.

**Next assignment, when the project owner is ready to pick one:** (a) the live-validation pass itself (a real test channel, real OAuth, project-owner or designated person only — never bundled into `npm test`) — this is now the single largest remaining item before Gate B; (b) CLI/MCP interfaces for Change Sets and Batches (`RISK-04`, deferred to before operations handoff, `DEC-OQ-5`); (c) the live-write activation procedure itself, a distinct and separately-gated future task, never to be started incidentally as part of another task; (d) the 20 remaining non-critical `npm audit` findings (`RISK-06`); (e) a real AI-provider integration for Phase 6 (a separate, explicit, cost-bearing decision, see `docs/ai-localization/PROVIDER_INTEGRATION_PLAN.md`).

`docs/TECHNICAL_DEBT.md`'s **Gate A** (before implementing Phase 5) is satisfied as of Phase 4.5 — see that document for the checklist.

**Acceptance contract:** `docs/acceptance/PHASE_5_ACCEPTANCE.md` — status **APPROVED** (fifth review round, 2026-09-17). OQ-1..OQ-6 answered; this approval authorizes Phase 5 preparation/implementation work (including mocked tests) but is not itself authorization for any real, non-dry-run YouTube write (`AGENTS.md` §K).

## Open blockers before Phase 5 writes may go live (Gate B)

Per `docs/TECHNICAL_DEBT.md`'s Gate B, none of the following may be waived by risk acceptance — each must be actually implemented AND tested AND, where the mechanism specifically requires it, live-validated:

- **RISK-03** — fresh remote conflict detection: **implemented and tested against mocks** for the batch preparation/send-time pipeline; `AC-CONFLICT-01` (= official test §56) still requires a live-validation run before Gate B.
- **RISK-09** — immutable backup, durable audit log, per-item execution ledger with idempotent resume, post-write verification: **all implemented and tested against mocks/fakes**, including the full 100-video resume scenario and the two-batch concurrency race; the real `WriteExecutor` exists but is barrier-disabled. **The only remaining blocker for Gate B is now the `LIVE VALIDATION` track itself** — every scenario in `docs/acceptance/PHASE_5_ACCEPTANCE.md` §4 (AC-MERGE-01/05, AC-GUARD-01, AC-CONFLICT-01, AC-RESUME-01, AC-E2E-01) still requires a real test channel and real OAuth, never bundled into `npm test`. RISK-12 and RISK-13 are both closed (see below) and no longer block anything.

Also tracked, not blocking Phase 5 *development* but blocking later gates (see `docs/TECHNICAL_DEBT.md` for full detail): RISK-04 (no CLI/MCP Change Set/Batch interfaces — blocks Gate C, operations handoff), RISK-05 (no live browser/OAuth verification — blocks Gate C), RISK-06 (20 remaining, non-critical dependency advisories — blocks Gates C and D; the two critical ones are already patched), RISK-01/02/07 (deferred, accepted for the current single-operator local model, blocking only Gate D / network deployment). RISK-12 (legacy `applyMetadata`/MCP `apply` unsafe `dryRun` default) is **CLOSED**, no longer blocking Gate C.

---

## How to keep this file current

1. When a phase's Definition of Done (`docs/DEVELOPMENT_PLAYBOOK.md` §6.13) is satisfied and the project owner has accepted the work, add or update its row in the table above with the actual completion date and commit hash(es) — do this as part of the phase's own final commit or immediately after, not as a separately-forgotten follow-up.
2. Update "Next assignment" to name whatever the project owner assigns next; do not guess ahead or pre-announce a phase that has not been explicitly assigned.
3. Re-check "Open blockers" against the current state of `docs/TECHNICAL_DEBT.md` — a risk closed there should be reflected here, and vice versa.
4. This file is part of the documentation-maintenance table in `docs/DEVELOPMENT_PLAYBOOK.md` §6.12 ("a development phase is completed" row) — keep both in sync.
